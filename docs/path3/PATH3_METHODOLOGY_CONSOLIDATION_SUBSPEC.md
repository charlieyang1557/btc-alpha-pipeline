# PATH 3 SUB-SPEC — Methodology Consolidation Cycle Sub-Spec

**Status:** WORKING DRAFT — session-1 of 3 ((a') three-session pacing); SEAL pending session-3 register-event boundary after session-2 reviewer routing + per-fix adjudication.

**Cycle anchor:** Path 3 sub-spec drafting cycle, entry register-event boundary authorized by Charlie register chain at 2026-05-16. Cycle-class: process/spec deliverable register-class (parallel-class to Path 3 scoping cycle; register-class-distinct from Phase 5 substantive empirical register-class).

**Authorization register:** Charlie register chain "path 1 authorized" + "approve on 3 leg convergence" + "Ratify" — cycle entry (Gate 1) + Q1/Q2/Q3/Q5 disposition adoption + plan refinement adjudication + session-1 drafting fire (Gate 2). Authorization source = Charlie register per `feedback_authorization_routing.md` hard rule; reviewer convergence (ChatGPT structural-overlay + Claude advisor full-prose-access) advisory only.

**ChatGPT precision rule (binding throughout this cycle, per sealed Path 3 scoping decision §5.8):** This sub-spec enumerates per-class target section wording framework + tier disposition framework + execution-boundary constraints. Final codification text + per-candidate ADOPT/DEFER/PUSHBACK disposition + per-item target location within candidate-class eligibility reserved for Path 3 execute cycle.

---

## §0 — Scope + structure

### §0.1 — §6 #9 acknowledgment (per Q2 path 2 + preamble synthesis; Refinement 2 integration)

Sealed Path 3 scoping decision `6750274` §6 reserved an open question (#9) at sub-spec drafting cycle entry register-event boundary: whether sub-spec drafting cycle scope (vs collapse into execute cycle) is justified given methodology consolidation register-class distinct from Phase 5 substantive empirical register-class. **Charlie register chain "path 1 authorized" + "approve on 3 leg convergence" + "Ratify" at this cycle entry substantively resolves §6 #9 by selection of sub-spec path over direct-execute path.** Per sealed §6 #9 timing binding ("reserved at sub-spec drafting cycle entry register-event boundary"), this §0.1 acknowledgment AT entry register honors the timing-binding precisely; no separate session work item dedicated to re-litigating substance per Q2 path 2 anti-meta-recursion design (Leg-C synthesis ratified at Gate 2 + post-self-修正 3-leg convergence on Refinement 1).

### §0.2 — Cycle scope

Path 3 sub-spec drafting cycle = methodology consolidation cycle sub-spec phase. Sub-spec deliverable is the framework binding for how 43 sealed candidates (from sealed Path 3 scoping doc §2.3) will be routed to target section locations + tier dispositions at Path 3 execute cycle.

**Sub-spec drafting cycle IS:**
- Specifying per-class target section wording framework (Class A/B/C/E/F per sealed §2.2 taxonomy; Class D explicitly route-only per sealed §4 Class D treatment + §2.4)
- Specifying per-class tier disposition framework guidance (D1.a Strong EXCLUDED by construction per §20.6 C4; D1.b Medium / D1.c Weak / D1.d §31-style light append eligibility framing; D2 DEFER / D3 PUSHBACK available)
- Specifying execution-boundary constraints for Path 3 execute cycle
- Enumerating V# anchor chain for session-3 pre-SEAL verification

**Sub-spec drafting cycle IS NOT:**
- Deciding per-candidate ADOPT/DEFER/PUSHBACK disposition (reserved for execute cycle per sealed §7)
- Drafting final codification text (reserved for execute cycle per ChatGPT precision rule)
- Promoting any candidate to Strong tier (excluded by construction per §20.6 C4 maturation criterion)
- Adding new candidates outside sealed 43 (per Q5 strict scope binding + sealed §5.1 hard scope binding)
- Modifying sealed scoping decision (invariant per sealed §5.6 sealed-content invariance)
- Pre-authorizing Path 3 execute cycle entry (anti-momentum-binding per sealed §5.2)
- Re-narrating Phase 5 three narration authorities (discharged at `4b9e2dc`; cross-reference only)
- Processing Class D code-hygiene candidates (route-only per sealed §4 Class D treatment)
- Engaging Codex at THIS cycle (sub-spec drafting cycle Codex SKIP per sealed §5.7; Path 3 execute cycle Codex re-engagement possible per §4.6 register-class boundary)

### §0.3 — Sub-spec doc structure overview

§1 Cycle parameters · §2 Per-class target section wording framework · §3 Tier disposition framework · §4 Execution-boundary constraints · §5 V# anchor chain · §6 Reserved decisions.

---

## §1 — Cycle parameters

Q-disposition adopted at cycle entry register-event (Gate 1) per Charlie register "path 1 authorized" + "approve on 3 leg convergence" with 3-leg convergent state across ChatGPT + Claude advisor + Leg-C executing-agent:

| Q | Disposition | Convergence state |
|---|---|---|
| Q1 Pacing | (a') three-session | 3-leg convergent |
| Q2 §6 #9 handling | path 2 + single preamble (Leg-C synthesis; preamble integrated as §0.1 per Refinement 2) | 3-leg convergent (Leg-C synthesis ratified by ChatGPT + Claude advisor lean-reversal) |
| Q3 Reviewer routing | 2-leg default (ChatGPT structural-overlay + Claude advisor full-prose-access; Codex SKIP per sealed §5.7) | 3-leg convergent |
| Q5 New-candidate scope | (a) strict sealed-43 scope | 3-leg convergent |

Plan refinements ratified at Gate 2 per Charlie register "Ratify":

- **Refinement 1**: Gate 4 = EXPLICIT (per-fix adjudication edit-application register-event boundary requires explicit Charlie register; Gate 3 reviewer-routing-fire authorization does NOT pre-authorize edit application per `feedback_authorization_routing.md` strict reading)
- **Refinement 2**: §6 #9 acknowledgment integrated as first sub-element of §0 — Scope + structure (NOT standalone floating preamble; matches Phase 5 sub-spec cycle context anchor precedent)
- **Refinement 3**: Q5 in-scope-refinement vs out-of-scope-new-observation distinction in V6 anchor + risk surface (ADOPT-LIGHT)
- **Forensic acknowledged**: Gate 7 handoff prompt write codification candidate (forward-only carry-forward; no current-cycle modification)

---

## §2 — Per-class target section wording framework

Per ChatGPT precision rule: framework specifies eligible target section locations per candidate-class + cross-reference vs primary codification pattern. Per-item target location within candidate-class eligibility space reserved for Path 3 execute cycle adjudication.

### §2.1 — Class A: Procedural / pacing discipline

**Eligible target locations** (per sealed scoping doc §4 matrix verbatim):

- METHODOLOGY_NOTES `§X` new-§ (standalone-§ register)
- METHODOLOGY_NOTES `§X`-existing extension (e.g., `§9` Path-2 outline-first / `§11` closeout-assembly checklist) — per sealed §5.6 "extension means addition of NEW sub-§ structure, NOT modification of sealed prose"
- §31-style light append (verbatim sealed term "light append" per Path 3 scoping NEW-D adjudication)
- Memory file new (`feedback_<topic>.md`)
- Memory file existing extension

**Cross-reference vs primary codification pattern:**
- Class A **cross-reference candidates** from sealed §2.3: C26, C27, C28, C29, C30, C31, C37, C32 — already codified at memory file or METHODOLOGY_NOTES §31 register; Path 3 execute cycle produces cross-reference at canonical artifact register without re-codifying primary content
- Class A **new candidates** from sealed §2.3: C5, C6, C7, C9, C10, C21, C23 — primary codification at execute cycle; per-item target slot within eligibility space decided per-item at execute per sealed §4 per-item route + sealed §7 reservation

### §2.2 — Class B: Reviewer-routing discipline

**Eligible target locations** (per sealed scoping doc §4 matrix verbatim):

- METHODOLOGY_NOTES `§X` new-§
- METHODOLOGY_NOTES `§14` bidirectional dual-reviewer register-precision extension
- §31-style light append
- Memory file (`feedback_codex_review_scope.md` existing extension / `feedback_reviewer_suggestion_adjudication.md` existing extension)

**Cross-reference vs primary codification pattern:**
- Class B **cross-reference candidates** from sealed §2.3: C20 (primary-Class-E framing per scoping; C20 referenced from Class B due to overlap), C22, C25 — already codified
- Class B **new candidates** from sealed §2.3: C4, C8, C11, C13 — primary codification at execute cycle

### §2.3 — Class C: Documentation / atomicity discipline

**Eligible target locations** (per sealed scoping doc §4 matrix verbatim):

- METHODOLOGY_NOTES `§X` new-§
- METHODOLOGY_NOTES `§6` commit-messages canonical-result-layer extension
- Memory file (`feedback_claude_md_freshness.md` existing extension — Option 1A binding now first-trigger validated empirically at `578df13`; second trigger forecast at this cycle's session-3 SEAL commit 2)
- `docs/discipline/` new file at sub-discipline register

**Cross-reference vs primary codification pattern:**
- Class C **cross-reference candidates** from sealed §2.3: C24, C34 — already codified
- Class C **new candidates** from sealed §2.3: C3, C12, C19 — primary codification at execute cycle

### §2.4 — Class D: Code-hygiene / dependency — **ROUTE-ONLY**

Class D candidates are **NOT Path 3 codification content** per sealed scoping doc §4 Class D treatment + §2.4 out-of-Path-3-codification-scope observations.

This sub-spec produces **no codification-target framework wording for Class D** (Class D section retains routing framework language — route-only / register-event eligibility / register-class distinction — but no methodology codification placement framework).

Class D candidates from sealed §2.3: **C1** (`ingestion/validators.py:391` `pct_change()` `fill_method=None` explicit-mode patch — pandas 2.x deprecation) + **C2** (`bqplot 0.12.36 ↔ pandas 2.x` user-env conflict — user-env vs project-env separation pattern).

Both are route-only at independent code-hygiene register-event eligible-not-named per sealed scoping doc §6; register-class-distinct from Path 3 codification register; eligible at separate Charlie register-event boundary.

V# anchor V2 (per-class target wording framework completeness) verifies Class D explicit route-only treatment + zero framework wording.

### §2.5 — Class E: Adjudication / register-class discipline

**Eligible target locations** (per sealed scoping doc §4 matrix verbatim):

- METHODOLOGY_NOTES `§X` new-§
- METHODOLOGY_NOTES `§17` procedural-confirmation defect class extension
- METHODOLOGY_NOTES `§25` register-class explicit declaration extension
- §31-style light append
- Memory file (`feedback_authorization_routing.md` existing extension)

**Cross-reference vs primary codification pattern:**
- Class E **cross-reference candidates** from sealed §2.3: C20 (primary Class-E framing per scoping), C33, C35, C36 — already codified
- Class E **new candidates**: no dedicated primary-Class-E new-candidate group surfaced in current sealed §2.3 enumeration. If session-2 reviewer routing surfaces possible Class E candidates missed in sealed §2.3, that is out-of-Q5-scope new-observation per Refinement 3 distinction (NOT in-scope refinement); eligible for separate Charlie register-event boundary, NOT for sub-spec scope addition mid-cycle.

### §2.6 — Class F: Sealed-content invariance / anti-pre-emption discipline

**Eligible target locations** (per sealed scoping doc §4 matrix verbatim):

- METHODOLOGY_NOTES `§X` new-§
- METHODOLOGY_NOTES `§10` anti-pre-naming standing discipline extension
- §31-style light append
- Memory file new

**Cross-reference vs primary codification pattern:**
- Class F **cross-reference candidates** from sealed §2.3: C43 — meta-class binding pattern
- Class F **new candidates** from sealed §2.3: C14, C15, C16, C17, C18, C38, C39, C40, C41, C42 — primary codification at execute cycle (largest new-candidate group across classes per §2.3 codification-class breakdown)

---

## §3 — Tier disposition framework

### §3.1 — Strong tier — **EXCLUDED BY CONSTRUCTION**

Per sealed METHODOLOGY_NOTES `§20.6` 4-criterion AND-conjunction Strong-tier promotion binding scope (cited verbatim at sealed Path 3 scoping doc §3.1):

- **Criterion 1**: ≥10 instances accumulated across ≥3 PHASE2C cycles per sealed §20.6 verbatim binding scope (cross-cycle generalization to non-PHASE2C cycles deferred to Path 3 execute cycle adjudication boundary per sealed scoping §3.1 + Charlie register requirement)
- **Criterion 2**: Operating rule articulation (WHEN/WHAT/HOW; observation-only fails C2)
- **Criterion 3**: Cross-cycle register-class consistency
- **Criterion 4**: ≥1 prior consolidation cycle (maturation through tier hierarchy) — **first-codification-cycle candidates fail C4 by construction**

**Path 3 execute cycle CANNOT promote any sealed-43 candidate to Strong tier.** This binding is enforced at execute cycle by three layers:
- Framework binding at this sub-spec §3.1
- V# anchor V3 (tier disposition framework completeness verifies Strong-tier exclusion explicit)
- Execution-boundary constraint §4.2

### §3.2 — Medium tier (applicable conditions framework)

Per sealed scoping doc §3.2 D1.b: "saturation evidence + operating-rule-pending or operating-rule-articulating but missing C4 maturation."

Path 3 execute cycle may promote candidate to Medium tier when:
- Cross-cycle saturation evidence present at register-precision (specific saturation threshold left for Path 3 execute cycle per-candidate evaluation OR for Charlie register articulation at execute cycle entry; need not be cross-PHASE2C-cycle specifically; Path 3 cycle scope generalizes Criterion 1 PHASE2C-specific to cross-cycle per sealed §3.1 deferred adjudication)
- Operating rule articulation exists at register-precision register-class binding OR achievable at execute cycle scope adjudication

### §3.3 — Weak tier (applicable conditions framework)

Per sealed scoping doc §3.2 D1.c: "observation-only with cross-cycle-pending status note; per §19 register-class precedent."

Path 3 execute cycle may codify candidate at Weak tier when:
- Observation register-class established at canonical artifact register
- Cross-cycle status pending (insufficient saturation for Medium)
- Operating rule potential exists but not yet articulated at register-precision

### §3.4 — §31-style light append (applicable conditions framework)

Per sealed scoping doc §3.2 D1.d: "ADOPT at §31-style light append (codified without tier-class promotion; 'captured here, not formalized into a tier register' register-class precedent)."

Path 3 execute cycle may codify candidate at §31-style light append register when:
- Candidate is observation-class with utility but no clear tier-class fit
- "Captured here, not formalized" register-class precedent applies
- Per-class target section (per §2.X above) eligible for §31-style append (most classes are eligible)

§31 sealed verbatim term **"light append"** (per Path 3 scoping NEW-D adjudication; sealed at `6750274`) — Path 3 execute cycle MUST use this verbatim term; "light-touch append" or other phrasings prohibited per anti-drift discipline.

### §3.5 — D2 DEFER + D3 PUSHBACK (available dispositions)

Per sealed scoping doc §3.2:
- **D2 DEFER** — escalate to deeper Path 3.x cycle or future methodology consolidation cycle; reason recorded in execute cycle artifact; not codified at current execute cycle
- **D3 PUSHBACK** — NOT codified; reason recorded. Reasons may include: redundant with existing canonical artifact / memory binding; insufficient observation density; observation-only without operating-rule potential at any tier; not register-class-coherent with METHODOLOGY_NOTES tier framework

---

## §4 — Execution-boundary constraints (what Path 3 execute cycle must honor)

Sub-spec cycle output constrains Path 3 execute cycle work via the following bindings:

### §4.1 — Q5 strict scope binding + in-scope-refinement vs out-of-scope distinction

Path 3 execute cycle works only on sealed 43 candidates from sealed Path 3 scoping doc §2.3. **No new candidate generation** without explicit Charlie register at execute cycle entry per sealed §5.1 hard scope binding.

V# anchor V6 verifies cycle-internal observation classification per Refinement 3 distinction:
- **(a)** Observations about execute cycle's own process (new observations not in sealed 43; e.g., reviewer routing pattern observations, adjudication discipline observations) → **out-of-Q5-scope**, forward-only carry-forward
- **(b)** Sealed 43 candidates' in-cycle refinements (e.g., "C12 should cross-reference §17 not §6" on deeper analysis at execute cycle) → **in-scope**, NOT Q5 violation

### §4.2 — Strong-tier exclusion binding

Per §3.1 above + sealed §20.6 C4: Path 3 execute cycle cannot promote any sealed-43 candidate to Strong tier (C4 fails by construction at first codification cycle). **Maximum tier ceiling at Path 3 execute cycle = Medium**; achievement conditional on §3.2 saturation + operating-rule-articulation criteria + per-candidate evaluation at execute. Future post-Path-3 methodology consolidation cycles may promote candidates to Strong tier upon C4 maturation across subsequent register-events.

### §4.3 — Sealed-content invariance binding

Per sealed scoping doc §5.6: Path 3 execute cycle cannot modify:
- Sealed Path 3 scoping decision `6750274` (and this sub-spec doc once sealed at session-3 SEAL fire)
- Sealed Phase 5 corpus (`697c26b` + `49ae7e3`/`cd82582` + `54ba912`/`4b9e2dc`; tag `phase5-diagnostic-execution-v1`)
- Sealed Phase 4 corpus
- Sealed METHODOLOGY_NOTES §1-§31 prose
- CLAUDE.md Phase Marker prior entries

**Extension means addition of NEW sub-§ structure, NOT modification of sealed prose** (per sealed §5.6 explicit binding).

### §4.4 — ChatGPT precision rule binding

Per sealed scoping doc §5.8: Path 3 execute cycle is authorized to draft final codification text + decide per-candidate ADOPT/DEFER/PUSHBACK + decide per-item target location within candidate-class eligibility space (defined by this sub-spec §2). Sub-spec framework constrains the eligibility space; execute decides per-item within space.

### §4.5 — Atomicity binding (Option 1A)

Path 3 execute cycle Phase Marker advance commits inherit the Option 1A atomicity binding per `feedback_claude_md_freshness.md` (first empirically validated at `578df13`; sealed scoping doc §5.9 cross-references this binding at scoping cycle SEAL register-event boundary).

### §4.6 — Codex routing register-class

Per sealed scoping doc §5.7: Codex SKIPPED at Path 3 process/spec deliverable register-class (scoping cycle + sub-spec drafting cycle). Path 3 execute cycle register-class-distinct: if execute modifies code (METHODOLOGY_NOTES.md is documentation not code; Class D is route-only here per §2.4, so unlikely; but if hypothetical), Codex routing may re-engage per substantive code/work register-class binding scope.

---

## §5 — V# anchor chain (preliminary; finalized at session-3 pre-SEAL fire)

V# anchor chain to be verified at session-3 SEAL pre-fire register-event boundary per Path 3 scoping V#-chain precedent (13 anchors at scoping cycle). Each V# fires CLEAN before SEAL bundle authorization; any V# failure HARD STOPs SEAL fire + requires errata register-event at separate Charlie register-event boundary per Phase 5 sub-spec V# failure precedent.

- **V1 — §0.1 acknowledgment correctness.** §0.1 cites Charlie register chain accurately ("path 1 authorized" + "approve on 3 leg convergence" + "Ratify"); §6 #9 substantive resolution explicit; §6 #9 timing-binding satisfaction explicit; acknowledgment integrated as first sub-element of §0 (NOT standalone floating paragraph per Refinement 2 adjudication).

- **V2 — Per-class target wording framework completeness.** §2.1-§2.6 covers all 6 classes (A/B/C/D/E/F); Class D explicitly route-only with no codification-target placement framework wording (per sealed §4 + §2.4 + Refinement 2 framework integration; Class D section retains routing framework language); each non-D class has eligible target locations enumerated derived from sealed §4 matrix + cross-reference vs primary pattern + sealed §2.3 candidate references (cross-reference + new candidate sets).

- **V3 — Tier disposition framework completeness.** §3.1 Strong tier EXCLUDED BY CONSTRUCTION explicit (per §20.6 C4 maturation criterion); §3.2 Medium / §3.3 Weak / §3.4 §31-style light append applicable conditions enumerated at framework precision (NOT per-item); §3.5 D2 DEFER + D3 PUSHBACK available; **"light append" verbatim** per sealed NEW-D adjudication.

- **V4 — Execution-boundary constraints completeness.** §4.1-§4.6 enumerates 6 constraints: Q5 strict scope + in-scope-refinement distinction / Strong-tier exclusion / sealed-content invariance / ChatGPT precision rule / atomicity / Codex routing register-class. Each constraint binding-source-cited.

- **V5 — Cross-reference integrity.** References to sealed Path 3 scoping doc sections (§1 / §2.2 taxonomy / §2.3 candidates / §2.4 / §3.1 / §3.2 / §4 matrix / §5.1-§5.9 / §6 / §7) + METHODOLOGY_NOTES sections (§6 / §9 / §10 / §11 / §14 / §17 / §19 / §20.6 / §25 / §31) + memory files (`feedback_authorization_routing.md` / `feedback_claude_md_freshness.md` / `feedback_codex_review_scope.md` / `feedback_reviewer_suggestion_adjudication.md`) + sealed corpus citations (`578df13` / `4b9e2dc` / `54ba912` / `6750274` / `697c26b` / `49ae7e3` / `cd82582` / `aba694b` / `ecb2a70` / tag `phase5-diagnostic-execution-v1`) accurate at register-precision; no fabricated section numbers; no stale tag references.

- **V6 — Q5 strict scope adherence + in-scope-refinement vs out-of-scope distinction.** Sub-spec works only on sealed 43 candidates from §2.3; no new candidates surfaced as primary content in §2. Cycle-internal observations classified per §4.1: (a) about cycle's own process → out-of-Q5-scope, forward-only carry-forward; (b) sealed 43 candidates' in-cycle refinements → in-scope, NOT Q5 violation. Three forward-only carry-forward observations accumulated cross sub-spec entry (cross-register-class (a') validation + advisor abstract-vs-Leg-C-concrete lean-reversal at 3 instances + Gate 7 handoff prompt write codification candidate) logged in conversation history, NOT codified in sub-spec.

- **V7 — ChatGPT precision rule adherence.** No per-candidate ADOPT/DEFER/PUSHBACK disposition; no final codification text drafted; no target section wording at per-item precision; framework operates at class-level eligibility specification only. Tier framework at framework precision (when D1.b/D1.c/D1.d applies for which class characteristics), NOT per-candidate tier assignment.

- **V8 — Charlie register citation accuracy.** Charlie register chain "path 1 authorized" + "approve on 3 leg convergence" + "Ratify" cited verbatim at §0.1 + §1; reviewer text (ChatGPT structural-overlay + Claude advisor full-prose-access) NOT paraphrased as Charlie source; reviewer convergence framed as advisory-only per `feedback_authorization_routing.md` hard rule.

- **V9 — Sealed corpus invariance.** No modification of sealed Path 3 scoping decision (`6750274`); no modification of sealed Phase 5 corpora (`697c26b` / `49ae7e3` + `cd82582` / `54ba912` + `4b9e2dc`); no modification of sealed Phase 4 corpora; no modification of sealed METHODOLOGY_NOTES §1-§31 prose; CLAUDE.md Phase Marker prior entries invariant.

- **V10 — Anti-momentum-binding language.** Sub-spec does NOT pre-authorize Path 3 execute cycle entry; reviewer convergence ≠ Charlie register cited at register-precision; §6 below reserves decisions for separate Charlie register-event boundary; eligible-not-named successor cycle framing preserved.

- **V11 — Three Phase 5 narration authorities cross-reference preservation.** Sub-spec does NOT re-narrate Phase 5 three narration authorities (terminal-conclusion + framing-question resolution + admissibility-flag interpretive narration discharged at `4b9e2dc`); cross-reference only at §4.3 sealed-content invariance binding.

- **V12 — Reserved for findings surfaced at session-1 self-check + session-2 reviewer routing.** Additional V# anchors may be added during session-2 per-fix adjudication if reviewer findings surface novel V# scopes.

---

## §6 — Reserved decisions (per anti-pre-emption invariant)

Reserved at sub-spec drafting cycle SEAL register-event boundary; reserved for downstream Path 3 cycles or separate Charlie register-event boundary:

- **Per-candidate ADOPT/DEFER/PUSHBACK disposition** (reserved for execute cycle per sealed §7 + ChatGPT precision rule)
- **Per-item target location within candidate-class eligibility** (reserved for execute cycle per sealed §4 per-item route)
- **Final codification text drafting** (reserved for execute cycle per ChatGPT precision rule)
- **Tier assignment per candidate** (Weak / Medium / §31-style; Strong EXCLUDED by construction per §3.1; reserved for execute cycle per §3 framework + per-candidate evaluation)
- **Cross-reference vs primary codification per candidate** (reserved for execute cycle per §2.X per-class pattern + per-item adjudication)
- **Path 3 execute cycle entry register-event boundary** (reserved for separate Charlie register per sealed §5.2 anti-pre-emption)
- **Phase 5.1+ cycle entry** (out of cycle scope; eligible-not-named per sealed §6)
- **Forward-only carry-forward codification register-event** (sealed scoping post-SEAL accumulated observations: cross-register-class (a') validation + advisor abstract-vs-Leg-C-concrete lean-reversal pattern at 3 instances + Gate 7 handoff prompt write codification candidate — eligible at separate Charlie register per sealed §5.1 hard scope binding EXCEPT clause)
- **Cycle-internal post-sub-spec-SEAL accumulated observations** (if any surface during cycle work — eligible at separate Charlie register)

**Sealed corpus invariance:** This sub-spec, once sealed at session-3 SEAL register-event boundary, is invariant per anti-momentum-binding strict reading; future modifications require fresh Charlie register at separate register-event boundary (errata register-event analogous to Phase 5 sub-spec session-11 + session-15 errata at `aba694b` + `ecb2a70`).

**Three Phase 5 narration authorities (terminal-conclusion invocation + framing-question resolution + admissibility-flag interpretive narration) remain discharged at `4b9e2dc`** — Path 3 sub-spec drafting cycle does NOT re-narrate these; cross-reference only at §4.3 sealed-content invariance binding.

---

**End of PATH 3 SUB-SPEC working draft (session-1 of 3 ((a') three-session pacing); STOP before session-2 reviewer routing + per-fix adjudication + session-3 SEAL fire).**
