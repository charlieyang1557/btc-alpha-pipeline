# PATH 3 SCOPING DECISION — Methodology Consolidation Cycle

**Status:** WORKING DRAFT — session-1 of 3 ((a') three-session pacing); SEAL pending session-3 register-event boundary after session-2 reviewer routing + per-fix adjudication.

**Cycle anchor:** Path 3 entry scoping cycle at fresh-session boundary post-Phase-5 arc-level closeout SEAL. Phase 5 closed at `54ba912` (arc-level closeout SEAL — `docs/closeout/PHASE5_RESULTS.md`) + `4b9e2dc` (Phase Marker advance) + tag `phase5-diagnostic-execution-v1` per arc-level closeout SEAL register-event boundary discipline.

**Authorization register:** Charlie register "Authorize path 3 scope" + "Both lean a', authorized" at this session entry; reviewer convergence (Claude advisor + ChatGPT) on (a') three-session pacing + 7-section outline + 2-leg routing + NO tag + ChatGPT precision rule attached. Authorization source = Charlie register per `feedback_authorization_routing.md` hard rule; reviewer convergence advisory only.

**ChatGPT precision rule (binding throughout this cycle):** Path 3 scoping may enumerate and classify candidates, but must NOT decide final codification text or target section wording. Per-candidate disposition decisions and final codification text are reserved for downstream Path 3 sub-spec drafting cycle and Path 3 execute cycle (eligible-not-named at this scoping SEAL).

---

## §1 Cycle scope

Path 3 = methodology consolidation cycle. This scoping cycle's deliverable is the framework binding for how accumulated discipline observations across the Phase 5 arc and prior cycles will be filtered, classified, and routed to codification target locations at downstream Path 3 cycles.

**Path 3 scoping cycle IS:**

- Enumerating accumulated candidate observations from Phase 5 arc + Path 2 surfaced items + Path β-restricted + historical accumulation (≥40 candidates) with source attribution.
- Classifying candidates by candidate-class (procedural / reviewer-routing / docs-atomicity / code-hygiene / adjudication / sealed-invariance — taxonomy locked in §2).
- Referencing existing bar criteria framework (METHODOLOGY_NOTES §20.6 4-criterion AND-conjunction) + light append register (§31 precedent) as filter dispositions available at Path 3 execute cycle.
- Setting up target location decision matrix per candidate-class (not per-item) — which target locations are eligible for which candidate-class.
- Defining discipline locks binding throughout Path 3 cycles (sub-spec drafting + execute).

**Path 3 scoping cycle IS NOT:**

- Deciding per-candidate ADOPT/DEFER/PUSHBACK disposition (reserved for execute cycle).
- Drafting final codification text or section wording (per ChatGPT precision rule).
- Executing actual codification fires into METHODOLOGY_NOTES.md or memory files (reserved for execute cycle).
- Promoting any candidate to Strong/Medium tier (Strong-tier promotion criterion 4 maturation requirement at register-class match register binding excludes first-codification-cycle candidates by construction per §20.6).
- Pre-authorizing Path 3 sub-spec drafting cycle entry, Path 3 execute cycle entry, Phase 5.1+ successor cycle entry, pre-existing noise cleanup, or any code-hygiene patch.
- Re-litigating Phase 5 verdict, Phase 4 verdict, or any sealed corpus content.

Path 3 = parallel-class register-event to Phase 5 entry scoping cycle (`697c26b`) — process/spec deliverable register-class, NOT substantive empirical work register-class.

---

## §2 Candidate enumeration

Candidates accumulated from four source registers. Each candidate enumerated with source attribution + brief identification (1-3 lines) + preliminary candidate-class. NO per-candidate disposition at this register per ChatGPT precision rule.

### §2.1 Source registers

- **R1 Phase 5 closeout §12 carry-forwards** (sealed at `54ba912`) — 5 items explicitly logged.
- **R2 Phase 5 arc execution accumulated** — observations surfaced across sub-spec drafting cycle 16 sessions + diagnostic execution session-1 + session-2 + (a') 3-session arc-level closeout pacing.
- **R3 Phase 5 closeout-precursor Path 2** (env hygiene register-event preceding arc-level closeout SEAL) + Path β-restricted reviewer-finding application register.
- **R4 Historical accumulation** — observations surfaced cross PHASE2C / PHASE4 arcs not yet at canonical artifact register.

### §2.2 Candidate-class taxonomy (6 classes, locked at this scoping cycle)

- **Class A — Procedural / pacing discipline.** Pacing templates, scope-extension procedural patterns, narrow-exception precedent application, scope-claim narrowing observations.
- **Class B — Reviewer-routing discipline.** Routing decisions across reviewers (ChatGPT / Claude advisor / Codex / python-reviewer), inheritance patterns, post-pass re-route thresholds, cross-leg complementary patterns.
- **Class C — Documentation / atomicity discipline.** File atomicity rules, machine-readable forensic schema, history-file synchronization, cross-document reference invariants.
- **Class D — Code-hygiene / dependency.** Dependency deprecation patches, user-env vs project-env separation, deprecation-mode explicit binding.
- **Class E — Adjudication / register-class discipline.** Reviewer-vs-Charlie-register stress test patterns, authorization-routing edge cases, register-class explicit declaration.
- **Class F — Sealed-content invariance / anti-pre-emption discipline.** SEAL bundle composition patterns, anti-momentum-binding strict reading, anti-pre-naming binding preservation, three-narration-authority discharge patterns, eligible-not-named successor cycle framing.

### §2.3 Candidate enumeration (≥40 items)

#### From R1 (Phase 5 closeout §12 carry-forwards; verbatim cited):

- **C1** `ingestion/validators.py:391` `pct_change()` `fill_method=None` explicit-mode patch (pandas 2.x deprecation; non-blocking; Path 2 surfaced). Class D.
- **C2** `bqplot 0.12.36 ↔ pandas 2.x` user-env conflict (not in `pyproject.toml`; user-env vs project-env separation pattern). Class D.
- **C3** `mechanical_routing_provenance` schema fields binding (substantive_evaluated_case / substantive_case_a_canonical_check_passed / operational_evaluated_case / operational_case_1_silent_or_terminal_false / operational_distinct_assignment_count / operational_case_1_threshold). Class C.
- **C4** Session-2 reviewer routing experiences: post-Leg-A re-route threshold ("no re-trigger if own-recommended + non-substantive"); cross-leg complementary pattern 2nd observed instance; 4-state `binary_detection_status` discipline (Codex HIGH-2). Class B.
- **C5** Path 2 SEAL discipline pattern (env hygiene as independent register-event before arc-level closeout SEAL; Path 2 → Path 1 → Path 3 sequencing pattern). Class A.

#### From R2 (Phase 5 arc execution accumulated):

- **C6** Narrow-exception precedent for reviewer-finding application (Path β-restricted application criteria). Class A.
- **C7** Scope-extension procedural template (meta-procedural pattern for register-event boundary scope expansion). Class A.
- **C8** Advisor self-check vs Leg-D boundary observation (substantive verification register-class distinct from reviewer-recommendation register). Class B.
- **C9** (a') three-session pacing template general adoption (session-1 draft + session-2 reviewer routing + session-3 SEAL). Class A.
- **C10** Pacing-mode (a) vs (a') trade-off framework (single-session vs three-session pacing decision register). Class A.
- **C11** Codex routing skip at process/spec deliverable register-class (inheritance pattern from Phase 5 scoping + sub-spec drafting cycles). Class B.
- **C12** Pre-fire micro-check pattern (V# anchor verification chain at SEAL pre-fire register). Class C.
- **C13** Session-N self-check vs Leg-D boundary discipline (substantive verification at register-precision register). Class B.
- **C14** Three-narration-authority discharge pattern at arc-level closeout SEAL (terminal-conclusion + framing-question resolution + admissibility-flag interpretive narration). Class F.
- **C15** Sealed-evidence-grounding discipline at SEAL author register (closeout consumes sealed inputs, no new evidence generation). Class F.
- **C16** SEAL bundle composition discipline (closeout doc commit + Phase Marker advance commit + atomic history file update + optional tag). Class F.
- **C17** Tag policy at scoping cycle SEAL (NO tag precedent across Phase 5 scoping + PHASE2C_10-15 + PHASE4 scoping cycles). Class F.
- **C18** Forward-only log register-class discipline (Phase 5 §12 carry-forward register-class precedent — observations logged without entering canonical artifact register at current cycle). Class F.
- **C19** Sub-spec drafting cycle V# anchor verification pattern (16-session cumulative V# anchors discipline at sub-spec drafting cycle SEAL pre-fire register). Class C.

#### From R3 (Path 2 + Path β-restricted):

- **C20** Reviewer-convergence-vs-Charlie-register stress test pattern (reviewer directive language "Authorize"/"Approve" disambiguated from Charlie-register authority — already codified at `feedback_authorization_routing.md`; cross-reference candidate). Class E.
- **C21** Path β-restricted reviewer-finding application pattern (criteria for applying reviewer findings under restricted-scope register-event boundary). Class A.
- **C22** Reviewer-suggestion-adjudication discipline at per-fix granularity (already codified at `feedback_reviewer_suggestion_adjudication.md`; cross-reference candidate). Class B.
- **C23** Path 2 → Path 1 → Path 3 sequencing pattern (env-precondition register-event ordered before arc-level closeout SEAL before methodology consolidation). Class A.

#### From R4 (Historical accumulation):

- **C24** CLAUDE.md freshness + history-file atomicity discipline (already codified at `feedback_claude_md_freshness.md` with Option 1A binding; cross-reference candidate). Class C.
- **C25** Codex-review-scope discipline (already codified at `feedback_codex_review_scope.md`; cross-reference candidate). Class B.
- **C26** Bilingual-concept-explanation discipline for difficult concepts (already codified at `feedback_bilingual_concept_explanation.md`; cross-reference candidate). Class A.
- **C27** Planning-skills-for-complex-tasks discipline (already codified at `feedback_use_planning_skills_for_complex_tasks.md`; cross-reference candidate). Class A.
- **C28** Decision-options-as-plain-text discipline at decision-point register (already codified at `feedback_decision_options_plaintext.md`; cross-reference candidate). Class A.
- **C29** Intuitive-Chinese-at-decision-points discipline (already codified at `feedback_decision_point_intuitive_chinese.md`; cross-reference candidate). Class A.
- **C30** Parallel-subagent-dispatch discipline (already codified at `feedback_parallel_subagent_dispatch.md`; cross-reference candidate). Class A.
- **C31** Long-task-pings discipline (already codified at `feedback_long_task_pings.md`; cross-reference candidate). Class A.
- **C32** Convergence-after-divergence as correction mechanism (METHODOLOGY_NOTES §31 #1 light append; cross-reference candidate). Class B.
- **C33** Smoke-vs-measurement register distinction (METHODOLOGY_NOTES §31 #2 light append; cross-reference candidate). Class E.
- **C34** Empirical-before-prose SEAL pre-fire byte-level verification (METHODOLOGY_NOTES §31 #3 light append; cross-reference candidate). Class C.
- **C35** Report-order discipline (effect size before binary pass/fail) (METHODOLOGY_NOTES §31 #4 light append; cross-reference candidate). Class E.
- **C36** Population-rate vs strategy-level alpha distinction (METHODOLOGY_NOTES §31 #5 light append; cross-reference candidate). Class E.
- **C37** Trim-governance-language pattern (METHODOLOGY_NOTES §31 #6 light append; cross-reference candidate). Class A.
- **C38** PHASE4 Task 7 reassessment NO-codification disposition pattern (closed disposition at meta-discipline register). Class F.
- **C39** Path A.2 register-event boundary discipline. Class F.
- **C40** Anti-momentum-binding strict reading (cross-cycle authorization preservation discipline). Class F.
- **C41** Anti-pre-naming binding preservation pattern (Phase 5.1+ scope NOT pre-committed at Phase 5 closeout SEAL; PHASE2C_10-15 + PHASE4 + Phase 5 scoping precedent). Class F.
- **C42** Eligible-not-named successor cycle framing register-class. Class F.
- **C43** Already-codified items NOT candidates for re-codification (memory files + sealed METHODOLOGY_NOTES sections); Path 3 may reference but not re-codify. Class F.

**Enumeration completeness + codification-class breakdown:** 43 candidates surfaced at this scoping register; not claimed exhaustive. Codification-class split:

- **New candidates** (not yet codified at canonical artifact register; Path 3 execute may codify per §3.2 D1.a-d): C1-C19 + C21 + C23 + C38-C42 = approximately 26 items
- **Cross-reference candidates** (already codified at memory file or METHODOLOGY_NOTES §31; Path 3 may reference but not re-codify per §4 cross-reference disposition): C20 + C22 + C24-C37 + C43 = approximately 17 items

**Enumeration completeness limitation acknowledged:** PHASE2C-arc-specific procedural / pacing observations (e.g., from PHASE2C_5 / PHASE2C_6 / PHASE2C_7.1 / PHASE2C_8.1 / PHASE2C_9 / PHASE2C_10 / PHASE2C_11 / PHASE2C_12 / PHASE2C_13 / PHASE2C_15 arc internals) are NOT individually enumerated at this scoping cycle; coverage is via R4 historical-accumulation framing (C38-C42) + R2 Phase 5 arc execution observations (C6-C19) which incorporate some cross-arc inheritance. Path 3 sub-spec drafting cycle may surface additional PHASE2C-arc-specific candidates within bar criteria framework + §5.1 explicit Charlie register requirement.

Anti-pre-emption preserved: this enumeration does NOT commit to per-candidate disposition.

### §2.4 Out-of-Path-3-codification-scope observations

Candidates C1 (`pct_change()` deprecation patch) and C2 (`bqplot 0.12.36` user-env separation) from Phase 5 closeout §12 carry-forwards are enumerated at §2.3 for Phase 5 arc observation completeness but are NOT Path 3 codification content at any candidate-class. They are **route-only at this scoping cycle** — destined for independent code-hygiene register-event (Class D treatment at §4 matrix); register-class-distinct from methodology codification. Enumeration here preserves traceability without expanding Path 3 codification scope.

---

## §3 Bar criteria + filter framework

### §3.1 Existing bar criteria reference

Path 3 execute cycle filter dispositions reference EXISTING bar criteria at METHODOLOGY_NOTES §20.6 (Strong-tier promotion bar criteria 1-4 AND-conjoined; sealed at PHASE2C_13 implementation arc § seal):

- **Criterion 1** — ≥10 instances accumulated across ≥3 **PHASE2C** cycles per sealed §20.6 verbatim binding scope; cross-cycle generalization to non-PHASE2C cycles (e.g., Path 3 candidate consolidation across Phase 5 arc + historical cycles) deferred to Path 3 execute cycle adjudication boundary per explicit Charlie register.
- **Criterion 2** — Operating rule articulation (WHEN/WHAT/HOW; observation-only fails C2).
- **Criterion 3** — Cross-cycle register-class consistency.
- **Criterion 4** — ≥1 prior consolidation cycle (maturation through tier hierarchy; first-codification-cycle candidates fail C4 by construction).

§20.6 binding applies at Strong-tier promotion register. Path 3 execute cycle may codify candidates at lower tiers (Weak / Medium) or at §31-style light append register without invoking §20.6 Strong-tier criteria.

### §3.2 Filter disposition outcome categories (locked at this scoping cycle)

Three Path 3 execute cycle dispositions available per candidate:

- **D1 ADOPT** — codify at one of the eligible target locations per §4 matrix. ADOPT subtypes:
  - D1.a ADOPT at Strong tier (requires §20.6 4-criteria AND-conjunction; constraint by construction: first-codification-cycle candidates fail C4).
  - D1.b ADOPT at Medium tier (saturation evidence + operating-rule-pending or operating-rule-articulating but missing C4 maturation).
  - D1.c ADOPT at Weak tier (observation-only with cross-cycle-pending status note; per §19 register-class precedent).
  - D1.d ADOPT at §31-style light append (codified without tier-class promotion; "captured here, not formalized into a tier register" register-class precedent).
- **D2 DEFER** — escalate to deeper Path 3.x cycle or future methodology consolidation cycle; reason recorded in execute cycle artifact. Not codified at current Path 3 cycle.
- **D3 PUSHBACK** — NOT codified; reason recorded. Reasons may include: redundant with existing canonical artifact / memory binding; insufficient observation density; observation-only without operating-rule potential at any tier; not register-class-coherent with METHODOLOGY_NOTES tier framework.

### §3.3 Filter framework

For each candidate at Path 3 execute cycle, the framework requires:

- (a) Identify candidate-class per §2.2 taxonomy.
- (b) Identify candidate's accumulated instance count + cycle span at register-precision (anchor to canonical artifacts where possible).
- (c) Identify whether operating rule articulation exists at register-precision register (or is achievable at execute cycle scope).
- (d) Identify whether candidate is already codified at canonical artifact register (memory or METHODOLOGY_NOTES section); if yes, candidate may be cross-reference-only at this cycle.
- (e) Apply §3.2 disposition categories per-candidate at execute cycle Charlie register adjudication.

Per-candidate filter execution OUT-OF-SCOPE at this scoping cycle per ChatGPT precision rule + scoping vs execute cycle-class distinction.

---

## §4 Codification target location decision matrix (per-class, not per-item)

Per candidate-class, eligible target locations are enumerated. Path 3 execute cycle decides per-item target within candidate-class eligibility; this scoping cycle does NOT decide per-item target location.

| Candidate-class | Eligible target locations |
|---|---|
| **Class A — Procedural / pacing** | METHODOLOGY_NOTES §X new-§ (standalone-§ register) / METHODOLOGY_NOTES §X-existing extension (e.g., §9 Path-2 outline-first / §11 closeout-assembly checklist) / §31-style light append / memory file new (`feedback_<topic>.md`) / memory file existing extension |
| **Class B — Reviewer-routing** | METHODOLOGY_NOTES §X new-§ / §14 bidirectional dual-reviewer register-precision extension / §31-style light append / memory file (`feedback_codex_review_scope.md` existing extension / `feedback_reviewer_suggestion_adjudication.md` existing extension) |
| **Class C — Documentation / atomicity** | METHODOLOGY_NOTES §X new-§ / §6 commit-messages canonical-result-layer extension / memory file (`feedback_claude_md_freshness.md` existing extension) / `docs/discipline/` new file at sub-discipline register |
| **Class D — Code-hygiene / dependency** | Cross-reference §2.4 out-of-Path-3-codification-scope observations; route-only at independent code-hygiene register-event, NOT Path 3 codification content |
| **Class E — Adjudication / register-class** | METHODOLOGY_NOTES §X new-§ / §17 procedural-confirmation defect class extension / §25 register-class explicit declaration extension / §31-style light append / memory file (`feedback_authorization_routing.md` existing extension) |
| **Class F — Sealed-content invariance / anti-pre-emption** | METHODOLOGY_NOTES §X new-§ / §10 anti-pre-naming standing discipline extension / §31-style light append / memory file new |

**Class D treatment note:** Class D candidates are route-only at this scoping cycle — they enumerate observations from sealed Phase 5 §12 carry-forwards for completeness but are NOT Path 3 codification content. Route to independent code-hygiene register-event per §2.4.

**Cross-reference-only candidates:** Candidates marked "(cross-reference candidate)" in §2.3 (C20, C22, C24-C37) are already codified at memory or METHODOLOGY_NOTES §31. Path 3 execute cycle may produce cross-references at canonical artifact register without re-codifying primary content. Cross-reference disposition register-class-distinct from D1 ADOPT primary codification.

---

## §5 Discipline locks (binding throughout Path 3 scoping + sub-spec drafting + execute cycles)

### §5.1 Hard scope binding

NO new candidate generation outside §2.3 enumeration at Path 3 sub-spec drafting + execute cycles. **ALL new-candidate additions, regardless of source** (sealed Phase 5 corpus, pre-Phase-5 sealed artifacts, session-2 reviewer findings, session-3 V# verification findings, or any other register), **require explicit Charlie register** at sub-spec drafting cycle entry or execute cycle entry per `feedback_authorization_routing.md` hard rule. NO modification of sealed Phase 5 corpus / sealed METHODOLOGY_NOTES sections / Phase 4 sealed corpus / CLAUDE.md Phase Marker prior entries at Path 3 cycles.

### §5.2 Anti-pre-emption

Path 3 scoping cycle SEAL register-event boundary does NOT pre-authorize Path 3 sub-spec drafting cycle entry; sub-spec drafting cycle entry register-event boundary requires explicit Charlie register authorization per `feedback_authorization_routing.md` hard rule. Path 3 sub-spec drafting cycle SEAL does NOT pre-authorize Path 3 execute cycle entry; execute cycle entry register-event boundary requires explicit Charlie register authorization.

### §5.3 Anti-momentum-binding

Path 3 cycle entries are register-class-distinct successor cycle decisions per cycle-class-specific scope binding (parallel to METHODOLOGY_NOTES §20.6 §4.3.4 cross-cycle scope preservation precedent). Scoping cycle SEAL does NOT imply downstream cycle authorization; reviewer convergence does NOT imply authorization. Strict reading: cycle-class-specific register-class boundary at register-class match register binding, NOT cross-cycle authorization sealing.

### §5.4 Per-fix adjudication

Applies at any reviewer cycle within Path 3 per `feedback_reviewer_suggestion_adjudication.md`. NO bulk-accept of reviewer findings; reasoned per-finding ADOPT/PUSHBACK per substantive merit at register-precision register-class binding.

### §5.5 Pacing discipline

Path 3 scoping cycle pacing is (a') three-session per Charlie register at this session entry. Path 3 sub-spec drafting cycle + Path 3 execute cycle pacing register-class-distinct; pacing-mode decisions at each downstream cycle entry per Charlie register adjudication. Pacing-mode (a) single-session vs (a') three-session vs (a'') N-session trade-off framework available per §2.3 C10 candidate (codification disposition reserved at execute cycle).

### §5.6 Sealed-content invariance

Sealed Phase 5 corpus (`PHASE5_SCOPING_DECISION.md` / `PHASE5_DIAGNOSTIC_SUBSPEC.md` / `PHASE5_RESULTS.md` / session-1+session-2 sealed outputs / tag `phase5-diagnostic-execution-v1`) invariant at Path 3 cycles. Sealed METHODOLOGY_NOTES §1-§31 sections invariant at Path 3 cycles: Path 3 execute may add NEW top-level §X sections OR append NEW sub-§ subsections to existing §X sections per §4 matrix; existing §X section prose (and pre-existing sub-§ structure) stays invariant per anti-momentum-binding strict reading. **"Extension" means addition of NEW sub-§ structure, NOT modification of sealed prose.** Phase 4 sealed corpus invariant. CLAUDE.md Phase Marker prior entries invariant (history file additions per Option 1A atomicity binding are NEW content at register-class-distinct register, NOT modifications of prior entries).

### §5.7 Codex routing skip at process/spec deliverable register-class

Path 3 scoping cycle + Path 3 sub-spec drafting cycle = process/spec deliverable register-class per `feedback_codex_review_scope.md` hard rule. Codex SKIPPED at these register-classes; ChatGPT structural-overlay + Claude advisor full-prose-access ADOPT at 2-leg routing. Path 3 execute cycle reviewer routing register-class-distinct; if execute cycle modifies code (e.g., METHODOLOGY_NOTES.md is documentation not code, but if execute touches Python source for Class D), Codex routing may re-engage per substantive code/work register-class.

### §5.8 ChatGPT precision rule

Path 3 scoping may enumerate and classify candidates (§2 + §4 per-class matrix), but must NOT decide final codification text or target section wording. Per-candidate disposition + final codification text + target section wording RESERVED for Path 3 sub-spec drafting cycle (target section wording framework) and Path 3 execute cycle (final codification text + per-candidate ADOPT/DEFER/PUSHBACK + per-item target).

### §5.9 Atomicity binding cross-reference (forward-binding precondition at scoping SEAL commit 2)

Per memory rule `feedback_claude_md_freshness.md` Option 1A binding: this scoping cycle's Phase Marker advance commit MUST atomically include `docs/phase_marker_history.md` update with three rows + verbatim prior-phase blocks:

1. Phase 5 sub-spec drafting cycle SEAL (`49ae7e3` + `cd82582`) — open backlog item per Option 1A binding.
2. Phase 5 arc-level closeout SEAL (`54ba912` + `4b9e2dc`, tag `phase5-diagnostic-execution-v1`) — open backlog item per Option 1A binding.
3. Path 3 methodology consolidation scoping cycle SEAL (this cycle) — current advance row.

§5.9 binding is artifact-atomicity discipline at scoping SEAL register-event boundary; binding scope at commit 2 of this scoping cycle SEAL bundle. Binding does NOT pre-authorize Path 3 sub-spec drafting cycle entry or downstream Path 3 cycle entries.

---

## §6 Eligible-not-named successor cycles

Per anti-pre-naming option (ii) preservation: Path 3 scoping cycle SEAL register-event boundary does NOT pre-commit successor cycle scope or entry. Register-class-eligible successor paths directly implied by §1 + §2 scope:

- **Path 3 sub-spec drafting cycle** — eligible at Charlie register adjudication post-this scoping SEAL. Sub-spec deliverable would refine §3 filter framework + §4 target location matrix at higher precision per cycle scope (e.g., target section wording framework — what § slot + what tier disposition per-class — without yet drafting final codification text).
- **Path 3 execute cycle** — eligible only after Path 3 sub-spec drafting cycle SEAL by default; direct execute path (skip sub-spec drafting) requires explicit Charlie register override at execute cycle entry per `feedback_authorization_routing.md` hard rule.
- **Path 3.x continuation cycles** — eligible if execute cycle disposition surfaces deferred candidates requiring deeper consolidation.
- **Phase 5.1+ successor cycle entry** — eligible per sealed Phase 5 §4a single eligible assignment (cost-model investigation eligible, gross-vs-realistic decomposition); NOT pre-committed at this scoping SEAL.
- **Pre-existing noise cleanup** — eligible at independent register-event (`.DS_Store` gitignore + `git rm --cached`; `docs/d7_stage2c/*` content-level decision); register-class-distinct from Path 3 codification register.
- **`pct_change()` deprecation patch + bqplot env separation** — eligible at independent code-hygiene register-event (Class D candidates; register-class-distinct from methodology codification per §4 Class D treatment note).
- **Project pause / methodology-consolidation-deferred close** — eligible at any cycle entry register-event boundary per Charlie register adjudication.
- **Path 3 errata register-event** — eligible at separate Charlie register for sealed scoping decision modifications, analogous to Phase 5 sub-spec session-11 + session-15 errata path at `aba694b` + `ecb2a70`; register-class-distinct from Path 3 sub-spec drafting / execute cycle entry.
- **Other Charlie-specified successor cycles** — eligible.

**Open question (#9 disposition: surface, not resolve at this scoping SEAL)**: Path 3 sub-spec drafting cycle scope (vs collapse into execute cycle) is reserved at Path 3 sub-spec drafting cycle entry register-event boundary. Default-recommended posture is sub-spec → execute split per Phase 5 precedent. Methodology consolidation register-class (vs Phase 5 substantive empirical register-class) may justify alternative posture (e.g., scoping → execute direct path with explicit Charlie register override per §6 Path 3 execute cycle eligibility framing) at Charlie register adjudication; this open question is NOT resolved at this scoping SEAL.

Charlie register authorization required at successor cycle entry register-event boundary per `feedback_authorization_routing.md` hard rule.

---

## §7 Anti-pre-emption invariant at scoping SEAL

This scoping decision document sealed at Path 3 methodology consolidation scoping cycle SEAL register-event boundary at session-3 SEAL fire.

**Reserved decisions at this SEAL (NOT discharged at scoping cycle; reserved for downstream Path 3 cycles):**

- Per-candidate ADOPT/DEFER/PUSHBACK disposition (reserved for execute cycle).
- Per-candidate target location decision within candidate-class eligibility (reserved for execute cycle per §4 per-item route).
- Final codification text drafting (reserved for execute cycle per ChatGPT precision rule).
- Target section wording (reserved for sub-spec drafting cycle framework + execute cycle final drafting).
- Tier assignment per candidate (Weak / Medium / Strong / §31-style light append; reserved for execute cycle per §20.6 + §3.2 framework).
- Cross-reference vs primary codification per candidate (reserved for execute cycle).

**Sealed corpus invariance:** This document, once sealed, is invariant per anti-momentum-binding strict reading; future modifications require fresh Charlie register at separate register-event boundary (e.g., errata register-event analogous to Phase 5 sub-spec session-11 + session-15 errata at `aba694b` + `ecb2a70`).

**Three Phase 5 narration authorities (terminal-conclusion invocation + framing-question resolution + admissibility-flag interpretive narration) remain discharged at `4b9e2dc`** — Path 3 scoping cycle does NOT re-narrate these; cross-reference only at §5.6 sealed-content invariance binding.

---

## §8 V# self-checklist anchors (session-3 pre-SEAL verification chain)

V# anchor chain to be verified at session-3 SEAL pre-fire register-event boundary per Phase 5 sub-spec V#-chain precedent. Each V# fires CLEAN before SEAL bundle authorization.

- **V1 — 7-section outline completeness.** §1 / §2 / §3 / §4 / §5 / §6 / §7 all present; §8 V# checklist present; no orphan section; no §X out-of-order.
- **V2 — Candidate enumeration density + source attribution + codification-class breakdown.** ≥40 candidates enumerated (target satisfied at 43 = ~26 new candidates + ~17 cross-reference candidates per §2.3 codification-class breakdown); each candidate has source register attribution (R1/R2/R3/R4 per §2.1); each candidate has preliminary candidate-class assignment per §2.2 taxonomy + codification-class flag (new vs cross-reference) per §2.3 enumeration completeness limitation acknowledged for PHASE2C-arc-specific procedural items.
- **V3 — Bar criteria framework reference integrity + key-claim spot-check.** §3.1 references METHODOLOGY_NOTES §20.6 4-criterion AND-conjunction verbatim source (with PHASE2C cycle-class specifier per §3.1 Criterion 1 + cross-cycle generalization framing); §3.2 D1.d references §31 light append register precedent (verbatim sealed term per NEW-D adjudication ADOPT); references accurate at register-precision. **Key-claim spot-check at session-3 pre-SEAL V# fire**: verify §3.1 Criterion 1 "PHASE2C cycles" specifier against sealed METHODOLOGY_NOTES §20.6 lines 2931-2937 verbatim; verify §31 sealed title `(light append)` against draft references.
- **V4 — Target location matrix per-class coverage.** §4 matrix covers all 6 candidate-classes (A-F); each row enumerates eligible target locations; Class D treatment note explicit; cross-reference-only candidates explicit.
- **V5 — Discipline locks count + completeness.** §5 contains 9 locks (§5.1-§5.9); all 9 locks at register-precision register-class binding; §5.9 atomicity binding cross-references Option 1A memory rule + open backlog items.
- **V6 — Eligible-not-named successor cycles enumeration.** §6 enumerates ≥7 eligible successor paths; each path explicitly NOT pre-authorized; Charlie register requirement explicit at register-event boundary discipline.
- **V7 — Anti-pre-emption invariant explicit.** §7 reserves ≥6 decision categories for downstream cycles; sealed corpus invariance explicit; three Phase 5 narration authorities cross-reference preserved (no re-narration).
- **V8 — ChatGPT precision rule honored throughout.** §2 + §4 enumerate + classify only; NO per-candidate disposition; NO final codification text; NO target section wording at this scoping cycle.
- **V9 — Scope precision (scoping vs sub-spec drafting vs execute).** §1 + §6 distinguish three Path 3 cycle classes; downstream cycle entry register-event boundary requirement explicit; default-required sub-spec drafting cycle path explicit at §6.
- **V10 — Cross-reference integrity.** All references to existing canonical artifacts (METHODOLOGY_NOTES §20.6 + §31 + §17 + §18 + §19 + §10 + §14 + §25 / memory files in cross-reference candidate set / sealed Phase 5 corpus / sealed Phase 4 corpus / tag `phase5-diagnostic-execution-v1`) accurate at register-precision; no fabricated section numbers; no stale tag references.
- **V11 — Atomicity binding forward-precondition explicit.** §5.9 + commit 2 SEAL bundle composition explicit on history file inclusion; open backlog 2 items (Phase 5 sub-spec drafting SEAL + Phase 5 arc-level closeout SEAL) + current advance row = 3 rows + verbatim prior-phase blocks.
- **V12 — Anti-momentum-binding language present.** §5.2 + §5.3 + §5.6 + §6 + §7 all enforce strict reading of register-event boundary discipline; reviewer convergence ≠ Charlie register hard rule cited at register-precision register-class binding.
- **V13 — Authorization source citation accurate.** Charlie register "Authorize path 3 scope" + "Both lean a', authorized" verbatim cited at preamble; reviewer convergence framed as advisory-only per `feedback_authorization_routing.md` hard rule; NO reviewer text paraphrased as Charlie-source.

V# verification chain fires sequentially at session-3 SEAL pre-fire register; any V# failure HARD STOPs SEAL fire and requires errata register-event at separate Charlie register-event boundary per Phase 5 sub-spec V# failure precedent.

---

**End of PATH 3 SCOPING DECISION (working draft; session-1 of 3 ((a') three-session pacing); STOP before session-2 reviewer routing + per-fix adjudication + session-3 SEAL fire).**
