# PHASE2C_1_SMOKE_SIGNOFF

## 1. Scope

This signoff covers Phase 2C **smoke acceptance only**. It does not
close full Phase 2C; it does not prove strategy alpha quality; it does
not authorize live trading. The single decision under this signoff is
whether the post-D8.4 pipeline state is safe to scale from a controlled
smoke batch to a real Phase 2C mining batch with budget caps. Alpha
quality, methodology revisions, and live readiness are explicitly out
of scope.

## 2. Evidence Artifact

- **Notebook path:** `docs/test_notebooks/PHASE2C_1_smoke_acceptance.ipynb`
- **Smoke artifact path inspected:** TODO: fill after notebook execution
- **Git commit inspected (HEAD at signoff time):** TODO: fill after notebook execution
- **Date / time (UTC):** TODO: fill after notebook execution
- **Notebook execution kernel / Python version:** TODO: fill after notebook execution

## 3. Preconditions

- D8.4 methodology refinement sub-arc is fully closed (sealed at `767d0e5`).
- Bucket 3 high-severity data write hazard is fixed
  (`bulk_download.py` canonical-overwrite refusal default + `--force`
  override path with archive + atomic-promote, sealed at `1f27fe4`).
- Issue 6 theme-rotation operational boundary is documented across
  `agents/proposer/stage2c_batch.py`, `agents/proposer/stage2d_batch.py`,
  `CLAUDE.md`, and `agents/themes.py` (sealed at `613da3f`).
- Operational theme rotation is the 5-theme rotation
  (`THEME_CYCLE_LEN = 5`); `multi_factor_combination` is canonical but
  excluded from operational rotation pending separate validation.
- Stale `PHASE2_BLUEPRINT_v2.md` references corrected to
  `PHASE2_BLUEPRINT.md` across all `.py` and `.md` sites.
- The signoff process itself does not perform any real API call,
  data refresh, or canonical-data mutation; it only inspects the
  notebook output produced by a separately-executed smoke batch.

## 4. Acceptance Gates

| Gate | Description | Expected | Result | Notes |
|---|---|---|---|---|
| G1 | Environment / git state captured | HEAD SHA + dirty-tree status + Python version recorded in notebook output | TODO | Populate from notebook §1 |
| G2 | Static guardrails pass | No regressions in import paths, theme constants, or CLAUDE.md spec references | TODO | Populate from notebook static-check section |
| G3 | Smoke artifacts discoverable | Smoke batch output directory exists and contains expected files (per-call records, summary JSON, etc.) | TODO | Populate from notebook artifact-discovery section |
| G4 | Lifecycle counts reconcile | `sum(terminal_lifecycle_counts) == hypotheses_attempted` (per CLAUDE.md invariant, checked at batch close) | TODO | Populate from notebook lifecycle reconciliation |
| G5 | Theme rotation matches 5-theme operational boundary | Theme distribution across smoke batch shows only the 5 operational themes (no `multi_factor_combination` calls) | TODO | Populate from notebook theme-distribution section |
| G6 | Cost under configured smoke cap | Actual smoke batch cost ≤ configured smoke cap (well under $20/batch limit) | TODO | Populate from notebook cost section |
| G7 | No unexplained infrastructure errors | No unhandled exceptions, no abort attempts beyond expected retry pattern | TODO | Populate from notebook error-scan section |
| G8 | Output artifacts complete for configured smoke scope | All expected per-call prompts / responses / critic results present; no truncated artifacts | TODO | Populate from notebook artifact-completeness section |
| G9 | No canonical data write path touched | No writes to `data/raw/btcusdt_1h.parquet`; no `bulk_download.py` invocation; smoke batch operated read-only on canonical data | TODO | Populate from notebook write-path audit |
| G10 | Final verdict generated by notebook | Notebook prints explicit PASS / PARTIAL PASS / FAIL with per-gate breakdown | TODO | Populate from notebook final cell |

## 5. Notebook Result Summary

TODO: populate from `PHASE2C_1_smoke_acceptance.ipynb` after execution.

- **Notebook verdict:** TODO
- **Passed gates:** TODO
- **Failed gates:** TODO
- **Warnings:** TODO
- **Inspected artifact path:** TODO
- **Cost (actual / cap):** TODO
- **Candidate count (total):** TODO
- **Usable candidate count (post-validation, pre-mining):** TODO
- **Main failure modes, if any:** TODO

## 6. Decision

**NOT_RUN / DRAFT_PENDING_NOTEBOOK_RESULTS**

Decision rules:
- **PASS:** smoke infrastructure is safe to scale to a real Phase 2C
  mining batch with budget caps.
- **PARTIAL PASS:** smoke mostly worked but needs a small correction
  or a missing-artifact follow-up before scaling.
- **FAIL:** do not scale until the blocking issue is fixed; rerun
  smoke acceptance after fix.
- **NOT_RUN / DRAFT_PENDING_NOTEBOOK_RESULTS:** this signoff is a
  prepared template awaiting notebook execution; no decision yet.

## 7. Next Action

Currently **NOT_RUN**. Required next steps:

1. Run the first controlled Phase 2C smoke batch (small scope, low
   cost, 5-theme operational rotation only).
2. Open and execute `docs/test_notebooks/PHASE2C_1_smoke_acceptance.ipynb`
   against the resulting smoke artifact.
3. Paste the notebook's final verdict and per-gate results into §4 and
   §5 of this document.
4. Reclassify the §6 decision per the rules above.
5. If PASS: proceed to first real Phase 2C mining batch with budget
   caps; keep the 5-theme operational boundary unless a separate
   decision flips it.
6. If PARTIAL PASS: fix listed non-critical issues, then rerun
   acceptance notebook or amend signoff in place.
7. If FAIL: stop scaling; fix blocking issues; rerun smoke acceptance
   from step 1.

## 8. Non-Claims

This signoff explicitly does **not** claim or authorize any of the
following:

- Does not prove strategy profitability or alpha quality.
- Does not validate live trading readiness.
- Does not change the operational theme rotation from 5 to 6 themes.
- Does not modify any D8.4 sub-arc conclusion or recommendation.
- Does not authorize any canonical data overwrite path
  (`bulk_download.py --force` remains a manual operator override gate).
- Does not constitute Phase 2C closeout.

## 9. Final Status

```
Status: NOT_RUN / DRAFT_PENDING_NOTEBOOK_RESULTS
Evidence notebook: docs/test_notebooks/PHASE2C_1_smoke_acceptance.ipynb
Prepared for: Phase 2C smoke batch acceptance
Next required action: run notebook after smoke batch artifact exists
```
