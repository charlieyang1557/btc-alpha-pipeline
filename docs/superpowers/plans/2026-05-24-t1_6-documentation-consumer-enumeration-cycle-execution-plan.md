# T1.6 Documentation + consumer enumeration — sub-plan v7 **RATIFIED v_final 2026-05-24** (Charlie "Authorize sub-plan v_seal ratify" register; sub-plan LOCKED per §8.1 anti-pre-emption)

**Cycle:** T1.6 of B-C-extended Scope-B structural artifact-preservation refactor cycle
**Parent plan v5 (RATIFIED):** [`docs/superpowers/plans/2026-05-22-b_c_extended-scope_b-cycle-execution-plan.md`](2026-05-22-b_c_extended-scope_b-cycle-execution-plan.md)
**Execution order within parent:** T1.2 → T1.3 → T1.1 → T1.4 → T1.5 → **T1.6** → B-C-extended cycle SEAL
**Prior task SEAL commits:** T1.2 + T1.3 + T1.1 bundle at `12dffde` (2026-05-23); T1.4 SEAL at `5a44ec6` (2026-05-23); T1.4 sub-plan v7 amendment at `b647860`; T1.4 v4 PFR LOW cleanup at `56fe413`; T1.5 sub-plan v_final ratify at `79fa4dc` (2026-05-24); T1.5 SEAL at `9d9a40d` (2026-05-24; pushed to origin/main)
**Scope-class:** Path A — full v5 §2.6 scope (7 deliverables)
**Drafting modality (T1.6 execution after sub-plan ratify):** Mixed — M1 orchestrator-direct for (a)-(d) Contract-precision deliverables; γ Hybrid orchestrator skeleton + `general-purpose` subagent fill for (e)-(g) doc-update / grep-heavy deliverables (DS3 lean revised per v1 PFR Advisor F6)
**Sub-plan drafting modality (THIS document):** M1 orchestrator-direct

**Charlie register chain (2026-05-23 to 2026-05-24):**
- T1.6 cycle ENTRY: AUTHORIZED 2026-05-24 (carried forward from T1.5 SEAL pre-authorization 2026-05-23; Charlie register Option 1 "Authorize Option 1" 2026-05-24)
- Scope-class Path A: REGISTERED 2026-05-24 (Charlie "Path A+Mixed")
- Drafting modality Mixed (M1 for (a)-(d) + γ Hybrid for (e)-(g)): REGISTERED 2026-05-24 (Charlie "Path A+Mixed")
- Sub-plan v1 drafting fire: AUTHORIZED 2026-05-24 (bundled in Option 1)
- v1 PFR-rule-Y 2-leg dispatch: AUTHORIZED 2026-05-24 (Charlie "Path α")
- v1 PFR adjudication (16/16 ADOPT): AUTHORIZED 2026-05-24 (Charlie "Authorize v2 sub-plan drafting fire")
- v2 sub-plan drafting fire: AUTHORIZED 2026-05-24 (per "Authorize v2 sub-plan drafting fire")
- v2 PFR-rule-Y 2-leg dispatch: AUTHORIZED 2026-05-24 (Charlie "v2 PFR-rule-Y 2-leg dispatch")
- v2 PFR adjudication (9/9 ADOPT): AUTHORIZED 2026-05-24 (Charlie "Path α''")
- v3 sub-plan drafting fire + v3 PFR-rule-Y 2-leg dispatch: AUTHORIZED 2026-05-24 (bundled in Path α'')
- v3 PFR adjudication (8 ADOPT + 1 PUSHBACK = 9/9 outcomes): AUTHORIZED 2026-05-24 (Charlie "Path α'''")
- v4 sub-plan drafting fire + v4 PFR-rule-Y 2-leg dispatch: AUTHORIZED 2026-05-24 (bundled in Path α''')
- v4 PFR adjudication (5 ADOPT + 1 INFO; cycle saturation empirically reaffirmed): AUTHORIZED 2026-05-24 (Charlie "Path β'''''")
- v5 inline cleanup (5 ADOPT) + skip-v5-PFR-rule-Y + sub-plan SEAL-eve Round 1 2-leg dispatch: AUTHORIZED 2026-05-24 (bundled in Path β'''''; per T1.5 v5→v6 SKIP precedent + R5.2 register #7 cycle saturation criterion + Rule 2 SEAL-eve OPERATIONALLY REQUIRED)
- sub-plan SEAL-eve Round 1 adjudication (4 ADOPT — Codex F1 MEDIUM T_obs documentation gap + Codex F2/F3 LOW + Advisor F1 LOW; Rule 2 SEAL-eve EMPIRICALLY VINDICATED): AUTHORIZED 2026-05-24 (Charlie "Path B'''''")
- v6 inline cleanup (4 ADOPT) + sub-plan SEAL-eve Round 1b 2-leg dispatch (post-Codex F1 substantive T_obs documentation addition): AUTHORIZED 2026-05-24 (bundled in Path B''''')
- sub-plan SEAL-eve Round 1b adjudication (4 ADOPT + 1 PUSHBACK — Codex F1+Advisor F1 CONVERGED LOW line range :802-814 + Codex F4+Advisor F2 CONVERGED LOW §11 stale 1622 + Codex F2/F3 unique LOW + Advisor F3 PUSHBACK §9.8 bounded-claim deferred to B-C-extended SEAL §35 codification; cycle saturation EMPIRICALLY REAFFIRMED at LOW-only floor): AUTHORIZED 2026-05-24 (Charlie "Path A'''''''")
- v7 inline cleanup (4 ADOPT mechanical fixes) + skip-Round-1c per cycle saturation criterion + R5.2 register #7 + T1.5 v5→v6 SKIP precedent + own-cycle Path β''''' skip-v5-PFR precedent + Rule 2 satisfied (Round 1+1b both ran): AUTHORIZED 2026-05-24 (bundled in Path A''''''')
- **Sub-plan v_seal ratify (v7 = v_final): AUTHORIZED 2026-05-24** (Charlie "Authorize sub-plan v_seal ratify"; sub-plan LOCKED; cycle progresses to Implementation work fire pending separate Charlie register-event)

**v3→v4 substantive corrections** (per v3 PFR-rule-Y 8 ADOPT + 1 PUSHBACK; details at §11 v4 revision log):
- **Codex F1 MEDIUM (load-bearing catch):** §2.6.2 item 3 + §2.6.4 PASS criterion + §2.6.3 Phase 2 brief: stale upstream authority paths at parent plan v5 §2.0.2 line 76 + `wf_lineage.py:373-375` docstring contradict T1.2 per-domain split + sealed code gate at `:478-480`; v4 adds PASS criterion to update stale docstring + qualifier in subagent brief (prevents `general-purpose` subagent from re-introducing v1 BLOCKING F1 framing via stale-authority resolution)
- **Codex F2 LOW:** §2.4.2 item 3 split 8 non-cost_anchor_id columns into 3 T1.1 FIX-B1 per-bar fields (`:155-157`) + 5 SYS-fix-1 LineageContext persistence fields (`:166-170`)
- **Advisor F1-F6 LOW:** §2.4.2 (7 sites/6 methods per v4 PFR Advisor F3 arithmetic correction) + §9.7 (BOUNDARY count harmonized scope-qualified to "3 §2.6 extension-protocol BOUNDARYs" per v4 PFR Codex F2+Advisor F2) + §2.1.2 (inner-branch lines symmetric :322-337 + :474-488) + §2.6.2 (canonicalization deferral direction FROM consumer TO producer) + §5.2/§9.5 (γ Hybrid N=4 harmonized with §9.6 N=4 non-collapse reasoning) + §11 (10+ instance cumulative pattern escalation per v4 PFR)
- **Advisor F7 PUSHBACK:** Mode A Layer-3 verified `tests/test_t1_5_registry_integrity.py` has 5 distinct failure tests (`test_failure_1` through `test_failure_5`); v3 §2.3.2 "5 failure cases" count CORRECT; no fix required

**v2→v3 substantive corrections** (per v2 PFR-rule-Y 9/9 ADOPT; details preserved at §11 v3 revision log):
- **CONVERGED MEDIUM (Codex F1 + Advisor F2):** §3.1/§6.4/§10 instance numbering reconciled to 6-instance scheme (now extended to 7-instance at v4 per v4 PFR addition)
- **Codex F2 MEDIUM:** §2.4 future-schema migration note reframed conditionally
- **Advisor F1 MEDIUM:** §2.1.2 item 4 corrected to function `def` lines
- **Advisor F3 MEDIUM (substantive new content):** §2.6.2 extended to 6 §2.6 extension-protocol/code-surface CONTRACT GAPs total (per sub-plan SEAL-eve Round 1 Codex F3 scope-qualification; T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21,95,126,179,379,392` are OUT-OF-SCOPE for §2.6 extension protocol per T1.5 SEAL artifact authoritative)
- 5 LOW corrections (Advisor F4-F8)

---

## §1 Cycle scope statement + Path A scope register provenance

### §1.1 Path A scope decomposition (Charlie register Path A 2026-05-24)

Path A full scope ratified at Charlie register-event 2026-05-24 per `/tmp/post_t1_5_seal_handoff_2026-05-24.md`. **All 7 deliverables IN SCOPE**:

| T1.6 # | Deliverable | Modality | Contract reference |
|---|---|---|---|
| (a) | **Schema spec write-up** | M1 | Contract 2.0.2 (`b_c_extended_v1` schema version) + Contract 2.0.5 (14 header fields) — restructured around 3-tuple architecture per v1 PFR F1 |
| (b) | **γ3/γ4 raw-standardized convention notes** | M1 | Contract 2.0.1 (LOCKED γ4 = `scipy.stats.kurtosis(fisher=False, bias=True, nan_policy='omit')` + 5 PROHIBITED + 1 LOCKED reference) |
| (c) | **Registry linkage spec** | M1 | Contract 2.0.3 (triple linkage `hypothesis_hash + batch_id + run_id`) + Contract 2.0.4 (R3.1d §5.2 6-row `cost_anchor_id` mapping + `canonicalize_execution_config_path()` sealed helper at `artifact_schema.py:91-193`) |
| (d) | **Migration notes** | M1 | T1.x migration from pre-T1.x schema — `MIGRATION_COLUMNS` at `backtest/experiment_registry.py:121-150+` production source + `_T1X_NEW_COLUMNS` test mirror at `tests/test_t1_4_backward_compat.py:51` with `len == 9` invariant |
| (e) | **`data_dictionary.md` updates** | γ Hybrid (`general-purpose` subagent fill) | B-C-extended schema additions per Contract 2.0.x |
| (f) | **`wf_lineage.py` extension pattern documentation** | γ Hybrid (`general-purpose` subagent fill) | 3-tuple + 3-helper extension protocol + CONTRACT BOUNDARY discipline + heavy-private-impl extraction (optional 4th step) + **6 CONTRACT GAPs** (3 in `wf_lineage.py` + 3 in `artifact_schema.py`) |
| (g) | **Consumer enumeration** | γ Hybrid (`general-purpose` subagent fill) | Grep audit on 4 tuples (`ACCEPTED_EVALUATION_SCHEMA_VERSIONS` + `ACCEPTED_WF_SCHEMA_VERSIONS` + `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS` + legacy alias `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS`) + `ARTIFACT_SCHEMA_VERSION_*` constants + `schema_version` field references; per-consumer disposition table |

### §1.2 Documentation coverage scope

T1.6 implements **all 7 deliverables** per Path A register. Aggregate Path A coverage target:
- Documentation for **all sealed T1.x Contract 2.0.x invariants** at canonical declaration boundaries (schema spec + γ3/γ4 convention + registry linkage + migration notes)
- **Consumer-side audit closure** via 4-tuple grep + per-consumer disposition table
- **Extension protocol documentation** so future schema versions follow same per-domain tuple + helper + CONTRACT BOUNDARY pattern (`b_c_extended_v1` becomes reference exemplar)

### §1.3 Aggregate cycle outputs

- **7 documentation deliverables** at canonical paths (per §2 detail)
- **NO new code modules** (T1.6 is documentation-only; no `tests/` or `backtest/` source modifications expected except possibly inline docstring updates within existing modules per §2.6)
- **NO new tests** (T1.5 SEAL closed test-suite scope; T1.6 verifies via documentation review only)
- **Full suite delta target:** zero regression on T1.5 baseline of 2317 tests collected (2315 PASS + 2 xfail DS8 PENDING + 0 FAIL); T1.6 documentation-only changes should not affect test count
- **`data_dictionary.md` line delta target:** locked at T1.6 implementation per §2.5
- **Consumer enumeration disposition table:** **N rows locked empirically at T1.6 §2.7 Phase 1 grep audit; NO a-priori count target** (per v1 PFR Advisor F4 + v2 PFR Advisor F4 anti-pre-emption discipline); audit scope = `backtest/` + `agents/` + `strategies/` + `factors/` + `scripts/` + `tests/` (6 directories per v1 PFR Codex F7 + Advisor F5 reconciliation; `scripts/` added per parent plan v5 line 239 evaluation-gate driver consumer)

---

## §2 Engineering deliverables (per-deliverable documentation design)

### §2.1 (a) Schema spec write-up [M1]

#### 2.1.1 Scope

§2.1 documents Contract 2.0.2 (`b_c_extended_v1` schema version + **3-tuple per-domain split + CONTRACT BOUNDARY** + distinct validation branch) and Contract 2.0.5 (14 header fields + per-bar artifact validation discipline) at the **schema declaration boundary**. Canonical doc location LOCK AT T1.6 IMPLEMENTATION per DS1 (§8.2): candidate paths = `docs/decisions/B_C_EXTENDED_V1_SCHEMA_SPEC.md` (single dedicated doc) OR split per Contract OR section within existing parent doc.

Out-of-scope for §2.1: γ3/γ4 convention (§2.2 scope); registry linkage detail (§2.3 scope); migration story (§2.4 scope).

#### 2.1.2 Content outline (restructured per v1 PFR F1 around 3-tuple + 3-helper + CONTRACT BOUNDARY; corrected per v2 PFR Advisor F1 + F7)

1. **Schema version identifier:** `b_c_extended_v1` — string-literal value + constant name `ARTIFACT_SCHEMA_VERSION_B_C_EXTENDED_V1` + canonical declaration site **`backtest/artifact_schema.py:42`** (NOT `wf_lineage.py`; heavy-private-impl extraction per C1-extract-pre-SEAL Charlie register 2026-05-22) + re-export shim at `backtest/wf_lineage.py:539-548` for consumer backward compat

2. **Per-domain tuple architecture (T1.2 Sub-decision A lock; sealed at bundle `12dffde`):**
   - `ACCEPTED_EVALUATION_SCHEMA_VERSIONS` at `backtest/wf_lineage.py:99-102` — evaluation attestation domain (single-run holdout artifacts); contains `phase2c_7_1` + `phase2c_8_1` only
   - `ACCEPTED_WF_SCHEMA_VERSIONS` at `backtest/wf_lineage.py:107-110` — walk-forward attestation domain (WF summary artifacts); contains `phase2c_7_1` + `phase2c_8_1` only
   - `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS` at `backtest/artifact_schema.py:45-47` — B-C-extended attestation domain (per-bar return series artifacts); contains `b_c_extended_v1` only
   - Backward-compat **legacy alias** (per v2 PFR Advisor F7 reframing — sealed code lines 112-113 explicitly frame as "Backward-compat: existing code... continues to work. New code should use the per-domain tuples above"; currently identical contents to both per-domain tuples since EVALUATION and WF accept same `phase2c_7_1` + `phase2c_8_1`): `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS` at `backtest/wf_lineage.py:117-120` — backward-compat legacy alias holding the pre-domain-split tuple contents; **does NOT include `b_c_extended_v1`** per CONTRACT BOUNDARY; see CONTRACT GAP at `wf_lineage.py:114-116` for the migration trigger

3. **CONTRACT BOUNDARY (sealed at `backtest/wf_lineage.py:47-51` module docstring + reinforced at `:95-96` declaration comment):** "`ACCEPTED_EVALUATION_SCHEMA_VERSIONS` and `ACCEPTED_WF_SCHEMA_VERSIONS` MUST NOT include `b_c_extended_v1`. `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS` MUST NOT include `phase2c_7_1` or `phase2c_8_1`. Each `check_*_semantics_or_raise` helper accepts ONLY its own domain's tuple." Document CONTRACT BOUNDARY verbatim with file:line cite; explain rationale (constant-level domain fence prevents cross-domain validation pollution).

4. **Three distinct validation branch helpers (function `def` lines per v2 PFR Advisor F1 correction; inner-branch lines symmetric per v3 PFR Advisor F3 — both ranges exclude comment headers, span executable schema_version validation block only):**
   - `check_wf_semantics_or_raise()` def at `backtest/wf_lineage.py:250` — accepts ONLY `ACCEPTED_WF_SCHEMA_VERSIONS`; inner schema_version validation branch at lines 322-337 (executable block only; lines 318-321 are comment header preceding); rejects all others including `b_c_extended_v1`
   - `check_evaluation_semantics_or_raise()` def at `backtest/wf_lineage.py:340` — accepts ONLY `ACCEPTED_EVALUATION_SCHEMA_VERSIONS`; inner schema_version validation branch at lines 474-488 (executable block only); rejects all others including `b_c_extended_v1`
   - `check_b_c_extended_semantics_or_raise()` def at `backtest/artifact_schema.py:654` (canonical impl; re-exported via shim at `backtest/wf_lineage.py:544`) — accepts ONLY `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS`; enforces all 14 Contract 2.0.5 fields + per-bar linkage validation

5. **14 header field enumeration** (verbatim cross-reference to Contract 2.0.5 table at parent plan v5 §2.0.5 lines 144-159; do NOT duplicate field list):
   - `artifact_schema_version` / `run_id` / `hypothesis_hash` / `source_batch_id` / `parent_run_id` / `regime_key` / `engine_commit` / `current_git_sha` / `execution_config_path` / `execution_config_sha256` / `parquet_data_sha256` / `cost_anchor_id` / `returns_per_bar_path` / `returns_per_bar_sha256`
   - Per-field semantic + type + Optional-ness + cross-references to other Contracts where applicable

5b. **`T_obs` — required-adjacent 15th field (per sub-plan SEAL-eve Round 1 Codex F1 MEDIUM catch; sealed at `backtest/artifact_schema.py:307-309,815-820`):**
   - **Sealed declaration text at `artifact_schema.py:307-309`** (verbatim): `# T_obs is not in the 14-field header table but is required for artifact validation; included here as field 13-equivalent for moment + T_obs linkage.`
   - **Sealed validation at `artifact_schema.py:815-827`** (per v6 SEAL-eve Round 1b Codex F1 + Advisor F1 CONVERGED LOW extension — range covers full T_obs validation block including positivity branch): presence at `:816-817` (`if "T_obs" not in summary: errors.append("T_obs: missing key (required for b_c_extended_v1)")`) + type at `:819-820` (`t_obs = summary["T_obs"]; if not isinstance(t_obs, int) or isinstance(t_obs, bool):`) + positivity at `:824-827` (`elif t_obs <= 0:` raising `"T_obs: must be positive (> 0)"`)
   - **Type:** `int` (positive integer; `bool` explicitly rejected — `isinstance(t_obs, int)` returns True for `bool` in Python so the negative check `or isinstance(t_obs, bool)` is required)
   - **Semantic:** count of finite per-bar return observations stored in the artifact's `returns_per_bar.parquet`; per-bar artifact validation discipline requires T_obs == finite-row-count of read parquet (T_obs-alignment check per §2.1.2 item 6 step 4)
   - **Why required-adjacent vs in-14-field-table:** T_obs is NOT a header metadata field per Contract 2.0.5 (which enumerates 14 fields covering identity + linkage + path + content-hash); T_obs is a per-bar-artifact-content-shape attribute. Sealed `LineageContext` dataclass at `artifact_schema.py:307-309` includes T_obs as field-13-equivalent (alongside `returns_per_bar_path` field 13 + `returns_per_bar_sha256` field 14) but explicitly comments it as outside the 14-field header table — different semantic class
   - **Registry linkage:** T_obs is one of 3 T1.1 FIX-B1 per-bar artifact linkage fields at `MIGRATION_COLUMNS:155-157` per §2.4.2 item 3 (alongside `returns_per_bar_path` + `returns_per_bar_sha256`); persisted via `engine._write_to_registry()` when lineage_context provided
   - **Validation contract:** consumer-side `check_b_c_extended_semantics_or_raise()` at `artifact_schema.py:654+` enforces T_obs presence + type + positivity at lines 815-820; producer-side `engine._write_to_registry()` populates T_obs from `LineageContext.T_obs` at write-time
   - **Cross-reference:** §2.4.2 item 3 (registry layer) + §2.1.2 item 6 (validation discipline 4-step protocol step 4 T_obs-alignment) + Contract 2.0.5 explicit framing (T_obs NOT in 14-field table — Contract 2.0.5 enumerates header fields only)

6. **Per-bar artifact validation discipline:** 4-step protocol (file-exists + path-confinement + SHA256-recompute + T_obs-alignment) per Contract 2.0.5 + Codex v4 Fv4-4 MEDIUM

7. **Backward compatibility statement:** legacy `phase2c_7_1` + `phase2c_8_1` consumers continue to validate via existing per-domain helpers; `b_c_extended_v1` is additive via new domain + new helper; CONTRACT BOUNDARY prevents cross-domain pollution

8. **Producer/consumer responsibility split:**
   - Producer (`backtest/engine.py` writer chain via `write_per_bar_artifact()`) emits all 14 fields + per-bar parquet + stamps `artifact_schema_version = "b_c_extended_v1"` **[SUB-PLAN-DRAFTING-TIME INTENT; scope refined at Implementation 2026-05-25: T1.6 ships infrastructure-only — registry-write + per-bar parquet (3 columns) only; b_c_extended_v1 JSON header stamping deferred to B-C-narrow successor cycle per CLAUDE.md Phase Marker binding condition; see `docs/decisions/B_C_EXTENDED_V1_SCHEMA_SPEC.md` §1.5 + §1.8 for the corrected forward-looking framing + CONTRACT GAP marker at `backtest/artifact_schema.py:40-46`]**
   - Consumer (`backtest/artifact_schema.py:check_b_c_extended_semantics_or_raise()` via shim) validates all 14 + per-bar SHA256 on read
   - Domain selection: consumer code calls the domain-appropriate helper (NOT the legacy alias); per-domain helper rejects mismatched schema version

#### 2.1.3 PASS criteria

1. All 14 Contract 2.0.5 fields enumerated with type + Optional-ness + semantic in canonical doc
2. Cross-reference to parent plan v5 §2.0.2 + §2.0.5 verbatim text
3. Module docstring at `backtest/wf_lineage.py` references the canonical doc (forward-pointer for future readers)
4. Per-bar artifact 4-step validation discipline documented
5. Backward compatibility statement explicit (per-domain helpers + CONTRACT BOUNDARY + legacy alias framing per v2 PFR Advisor F7)
6. **3-tuple + 3-helper + CONTRACT BOUNDARY architecture documented verbatim with file:line cites** (per v1 PFR F1 BLOCKING fix)
7. **Function `def` lines (250, 340, 654) cited (NOT inner-branch lines)** (per v2 PFR Advisor F1 fix)
8. **Canonical declaration site distinguished from shim re-export site** (`artifact_schema.py:42,45,654` vs `wf_lineage.py:539-548`) (per v1 PFR F1 + Advisor F7)
9. **Legacy alias framing for `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS`** (per v2 PFR Advisor F7; cross-reference CONTRACT GAP at `wf_lineage.py:114-116` migration trigger)
10. No drift from sealed `artifact_schema.py` + `wf_lineage.py` implementation at HEAD (Mode A grep-verified per implementation)

#### 2.1.4 Explicit exclusions

- γ3/γ4 implementation detail — deferred to §2.2
- `cost_anchor_id` mapping table — deferred to §2.3
- `MIGRATION_COLUMNS` registry schema — deferred to §2.4
- Consumer audit — deferred to §2.7
- New schema version design (e.g., `b_c_extended_v2`) — anti-pre-emption preserve

### §2.2 (b) γ3/γ4 raw-standardized convention notes [M1]

#### 2.2.1 Scope

§2.2 documents Contract 2.0.1 (moment estimator convention: γ3 sample skew + γ4 raw standardized kurtosis; LOCKED scipy invocation + 5 PROHIBITED alternatives + 1 LOCKED reference) at the **schema declaration boundary** alongside §2.1 schema spec. Canonical location locked at T1.6 implementation per DS1.

Out-of-scope for §2.2: schema field detail (§2.1 scope); BLdP DSR consumption layer excess-conversion detail (R6.1 V_SEAL artifact authoritative; cross-reference only).

#### 2.2.2 Content outline (per v1 PFR Advisor F8 "5 PROHIBITED + 1 LOCKED reference" disambig)

1. **γ3 (sample skew) formula:** `mean((r - rbar)^3) / mean((r - rbar)^2)^(3/2)` — population formula, NOT bias-corrected; Gaussian limit γ3 = 0
2. **γ4 (raw standardized kurtosis) formula:** `mean((r - rbar)^4) / mean((r - rbar)^2)^2` — population formula, NOT bias-corrected, NOT excess; Gaussian limit γ4 = 3
3. **LOCKED implementations:**
   - γ4 via `scipy.stats.kurtosis(returns_array, fisher=False, bias=True, nan_policy='omit')`
   - γ3 via `scipy.stats.skew(returns_array, bias=True, nan_policy='omit')`
4. **Environment precondition:** scipy ≥ 1.9 required for `nan_policy` keyword; fixture test fails closed if precondition not met (cross-reference T1.5 [`tests/test_t1_5_fixture_moments.py`](../../../tests/test_t1_5_fixture_moments.py) §2.1.5)
5. **5 PROHIBITED alternative implementations** (with empirically-verified divergent values at fixture vector `[-1, 1, 0, 0, 0, 0]`, N=6):
   - pandas `.kurt()` (default; Fisher excess bias-corrected) = 2.5 (excess, NOT raw)
   - pandas `.kurt() + 3` = 5.5 (raw bias-corrected, NOT raw population)
   - scipy `kurtosis(fisher=True, bias=True)` = 0.0 (excess uncorrected; default scipy)
   - scipy `kurtosis(fisher=True, bias=False)` = 2.5 (excess bias-corrected)
   - scipy `kurtosis(fisher=False, bias=False)` = 5.5 (raw bias-corrected)
6. **LOCKED reference value (for comparison; not part of prohibited list):** scipy `kurtosis(fisher=False, bias=True, nan_policy='omit')` = 3.0 (raw uncorrected; Gaussian limit; THIS CONVENTION)
7. **Downstream consumer note:** BLdP DSR consumption layer applies excess conversion (γ4 - 3) if formula expects excess; cross-reference R6.1 V_SEAL artifact [`docs/phase5/R6_1_TIER_6_PROMOTION_CLASS_NOTE.md`](../../phase5/R6_1_TIER_6_PROMOTION_CLASS_NOTE.md) for authoritative DSR consumption layer detail
8. **Test surface:** fixture test [`tests/test_t1_5_fixture_moments.py`](../../../tests/test_t1_5_fixture_moments.py) (9 test methods + γ3 PASS + 5 PROHIBITED lockout + scipy version precondition gate) is the empirical receipt for the LOCKED convention
9. **3-instance orchestrator-adjudication-error pattern cross-reference:** parent plan v5 §0 ll.23-29 (B-C-extended planning arc empirical motivation for fixture test); §2.2 documentation explicitly references the pattern as historical context (does NOT re-narrate it)

#### 2.2.3 PASS criteria

1. γ3 + γ4 formulas documented with explicit `mean((r-rbar)^k) / ...` form
2. LOCKED scipy invocation (with exact kwargs: `fisher=False, bias=True, nan_policy='omit'`) documented
3. scipy ≥ 1.9 precondition documented + cross-reference to T1.5 fixture test
4. All 5 PROHIBITED alternatives enumerated with name + empirical value at fixture vector + reason (excess vs bias-corrected vs default)
5. **LOCKED reference value documented as separate item (NOT inside PROHIBITED list)** (per v1 PFR Advisor F8 disambig)
6. Gaussian limits (γ3=0, γ4=3) stated explicitly
7. Cross-reference to BLdP DSR consumption layer at R6.1 V_SEAL artifact
8. No drift from Contract 2.0.1 verbatim (Mode A cross-check at implementation)

#### 2.2.4 Explicit exclusions

- BLdP DSR formula detail (R6.1 V_SEAL artifact authoritative; cross-reference only)
- Fixture test method enumeration (T1.5 SEAL artifact authoritative; cross-reference only)
- N=1 calibration disclosure for §2.4 threshold 2.0 (R2.0 + R2.1 cycle scope; not T1.6)

### §2.3 (c) Registry linkage spec [M1]

#### 2.3.1 Scope

§2.3 documents Contract 2.0.3 (triple linkage `hypothesis_hash + batch_id + run_id`) and Contract 2.0.4 (`cost_anchor_id` mapping table + canonicalization rule via `canonicalize_execution_config_path()` sealed helper) at the **registry declaration boundary**. Canonical location locked at T1.6 implementation per DS1.

Out-of-scope for §2.3: 14-field schema enumeration (§2.1 scope); γ3/γ4 convention (§2.2 scope); pre-T1.x migration story (§2.4 scope).

#### 2.3.2 Content outline (per v1 PFR Codex F4 + F5 + Advisor F2; v2 PFR Advisor F6 range correction)

**Triple linkage (Contract 2.0.3):**
1. Three keys: `hypothesis_hash` + `batch_id` + `run_id` — all required for unique artifact-to-registry resolution
2. **Artifact-side field aliasing:** `source_batch_id` (artifact) ↔ `runs.batch_id` (registry); `run_id` (artifact) ↔ `runs.run_id` (registry); alias mapping at write-time; consumer rejects mismatched aliases at read-time
3. **Resolution endpoint:** `experiments.db` runs table; SQL query pattern (3-WHERE-clause join) for canonical resolution
4. **5 failure cases (per Contract 2.0.6 (d) authoritative; expanded from v1's 3-case list per v1 PFR Codex F4; T1.5 SEAL artifact authoritative at [`tests/test_t1_5_registry_integrity.py`](../../../tests/test_t1_5_registry_integrity.py)):**
   - Duplicate `run_id`: fail closed
   - Missing `hypothesis_hash`: fail closed
   - Missing `batch_id`: fail closed
   - Mismatched `cost_anchor_id` vs canonicalized `execution_config_path` mapping: fail closed
   - Un-mapped canonicalized `execution_config_path` (non-None, non-default): fail closed
   - Plus 1 happy path: `None` → `config/execution.yaml` → `legacy_perp_inspired_7bps_v0` normalization

**`cost_anchor_id` mapping (Contract 2.0.4):**
1. **Field location:** `runs.cost_anchor_id` column at `backtest/experiment_registry.py:86` (CREATE TABLE column declaration) + `backtest/experiment_registry.py:147` (`MIGRATION_COLUMNS` entry `("cost_anchor_id", "TEXT")`). **CORRECTION** from v1 (which cited `:86,139` propagated from handoff prompt + parent plan v5 §2.0.4 line 91): line 139 is `regime_holdout_passed` migration entry area, NOT cost_anchor_id. Mode A Layer-3 grep verified at v1 PFR per v1 PFR Advisor F2.

2. **Population mechanism:** resolved from canonicalized `execution_config_path` via path-keyed mapping table (R3.1d §5.2 6-row table).

3. **Path canonicalization rule (per v1 PFR Codex F5; v2 PFR Advisor F6 range correction):** sealed implementation at **`canonicalize_execution_config_path()` at `backtest/artifact_schema.py:91-193`** (T1.3 SEAL bundle `12dffde`; Mode A grep-verified at v2 PFR adjudication — function spans lines 91-193 with closing `return rel.replace(os.sep, "/")` at line 193). Rule: relative paths anchored to `repo_root` BEFORE realpath; absolute paths used directly; commonpath-based containment check ensures candidate is genuinely under `repo_root`. Document the sealed `canonicalize_execution_config_path()` rule with code snippet; cross-reference parent plan v5 §2.0.4 commonpath invariant as Contract-level containment requirement.

4. **Case-sensitivity policy (v5 Fv5-1 LOW inline-fix; sealed):** on case-insensitive filesystems (macOS HFS+/APFS default), exact-case match required at mapping lookup; case-only mismatches FAIL CLOSED via mapping miss.

5. **6-row mapping table** (verbatim from parent plan v5 §2.0.4 lines 109-118; do NOT paraphrase column 1+2; column 3 paraphrased role allowed per cross-leg-convergent v5 Fv5-2 LOW; authoritative source at [`docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md`](../../phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md) §5.2 lines 286-293):
   - `config/execution.yaml` → `legacy_perp_inspired_7bps_v0` (default; legacy Phase 1-2 runs)
   - `config/execution_phase4_07bps.yaml` → `phase4_forward_07bps_v1` (Phase 4 cost-grid 07bps supplementary sensitivity)
   - `config/execution_phase4_13bps.yaml` → `phase4_forward_13bps_v1` (Phase 4 cost-grid 13bps supplementary sensitivity)
   - `config/execution_phase4_15bps.yaml` → `phase4_forward_15bps_v1` (Phase 4 forward-holdout primary basis per PHASE4_PLAN §1.5)
   - `config/execution_phase4_17bps.yaml` → `phase4_forward_17bps_v1` (Phase 4 cost-grid 17bps supplementary sensitivity)
   - `config/execution_phaseb_spot_15bps.yaml` → `spot_realistic_15bps_v1` (Phase B Tier 5/6 conservative-anchor gate per R3.1d SEAL output)

6. **Default normalization (Codex v3 Fv3-2 HIGH; Advisor v4 F-v4-6 LOW clarification):** when `lineage_context` object is None OR `execution_config_path` is None, normalize to `config/execution.yaml` → `legacy_perp_inspired_7bps_v0`.

7. **Fail-closed clause:** un-mapped path (non-None, non-default) raises with error message containing (i) canonicalized un-mapped path + (ii) full 6-row mapping table + (iii) guidance "Update R3.1d §5.2 mapping for new anchor or contact human approval before extending mapping".

8. **HARD CONSTRAINT cross-reference:** CLAUDE.md line 272 — `cost_anchor_id` required for new Phase B / Tier 5 / Tier 6 runs.

#### 2.3.3 PASS criteria

1. Triple linkage 3 keys + aliasing rule documented
2. SQL query pattern for canonical resolution documented
3. **5 failure cases enumerated (NOT 3) with cross-reference to T1.5 SEAL** (per v1 PFR Codex F4 fix)
4. **`cost_anchor_id` field location at `experiment_registry.py:86,147` documented** (per v1 PFR Advisor F2 correction; Mode A grep-verified)
5. **`canonicalize_execution_config_path()` sealed helper rule documented with code snippet + cite to `artifact_schema.py:91-193`** (per v1 PFR Codex F5 fix + v2 PFR Advisor F6 range correction)
6. Case-sensitivity policy documented
7. 6-row mapping table verbatim (columns 1+2) + paraphrased role (column 3) + authoritative source pointer
8. Default normalization rule documented
9. Fail-closed error message format documented
10. CLAUDE.md HARD CONSTRAINT line 272 cross-reference

#### 2.3.4 Explicit exclusions

- New mapping table extensions (no new anchors at T1.6; future anchors require fresh Charlie register per R3.1d §5.2 discipline; T1.6 §2.3 documents existing 6-row table only)
- Detail of failure-case test methods (T1.5 SEAL artifact authoritative; cross-reference only)
- DSL compiler / hypothesis hash detail (not in T1.6 scope; Phase 2A discipline)

### §2.4 (d) Migration notes [M1]

#### 2.4.1 Scope

§2.4 documents the **T1.x migration** from pre-T1.x schema to T1.x schema — production source `MIGRATION_COLUMNS` at `backtest/experiment_registry.py:121-150+` + test mirror `_T1X_NEW_COLUMNS` invariant at `tests/test_t1_4_backward_compat.py:51` (`len == 9` asserted at :62) + write-time discipline + backward compatibility statement. Canonical location locked at T1.6 implementation per DS1.

Out-of-scope for §2.4: schema field enumeration (§2.1 scope); γ3/γ4 convention (§2.2 scope); registry linkage detail (§2.3 scope).

#### 2.4.2 Content outline (per v1 PFR Codex F3 + Advisor F10; v2 PFR Codex F2 future-schema migration reframing)

1. **Migration class:** ADDITIVE — new columns added to existing `runs` table; no pre-T1.x columns removed or renamed.

2. **Production source — `MIGRATION_COLUMNS`** at `backtest/experiment_registry.py:121-150+`. Cumulative migration registry covering all post-Phase-1A column additions. T1.x relevant section starts at the Phase B (R3.1d V_SEAL) block (around line 140+) covering `cost_anchor_id` + B-C-extended Scope-B T1.1 FIX-B1 per-bar artifact linkage fields. Full enumeration locked at T1.6 implementation per Mode A grep on `MIGRATION_COLUMNS` constant range.

3. **9 T1.x columns** (test-mirror-asserted invariant; subset of `MIGRATION_COLUMNS` covering B-C-extended-cycle delta; per v3 PFR Codex F2 split into 3 subgroups by sealed comment delimiters):
   - **`cost_anchor_id` (TEXT; Contract 2.0.4 + R3.1d Phase B)** — at `MIGRATION_COLUMNS:147`; populated from canonicalized `execution_config_path`
   - **3 T1.1 FIX-B1 per-bar artifact linkage fields (Contract 2.0.5 persistence obligation; populated by `engine._write_to_registry()` when `lineage_context` provided)** — at `MIGRATION_COLUMNS:155-157`:
     - `returns_per_bar_path` (TEXT) — relative path to per-bar returns parquet artifact
     - `returns_per_bar_sha256` (TEXT) — SHA256 hex digest of that artifact (integrity)
     - `T_obs` (INTEGER) — count of finite per-bar return observations stored
   - **5 SYS-fix-1 LineageContext persistence fields (B3/B4 2026-05-23; populated from LineageContext when lineage_context provided)** — at `MIGRATION_COLUMNS:166-170`:
     - `regime_key` (TEXT) — regime identity key (e.g. "v2.regime_holdout")
     - `current_git_sha` (TEXT) — full-repo git SHA at run time
     - `execution_config_path` (TEXT) — canonicalized repo-relative POSIX path to execution config YAML
     - `execution_config_sha256` (TEXT) — content-addressable hash of execution config file
     - `parquet_data_sha256` (TEXT) — content-addressable hash of source data parquet
   - Test mirror: `_T1X_NEW_COLUMNS` tuple at `tests/test_t1_4_backward_compat.py:51` with `len(_T1X_NEW_COLUMNS) == 9` invariant assertion at `:62` ("§2.5 9 columns count invariant"); 7 reference sites across **6 distinct test methods** (per v3 PFR Advisor F1 disambiguation + v4 PFR Advisor F3 arithmetic correction: sites at lines 700, 727, 1251, 1457, 1607, 1614, 1648; lines 1607+1614 both within `test_pre_t1_3_db_state_migration_adds_columns_preserves_rows` at line 1582; remaining 5 sites in 5 distinct methods at lines 700, 727, 1251, 1457, 1648 (within `test_partial_migration_state_adds_remaining_columns` at line 1621); total 6 distinct test methods)
   - **CORRECTION** from v1 (which referenced `_T1X_NEW_COLUMNS` as if it were a production constant per handoff prompt propagation): `_T1X_NEW_COLUMNS` is **test-only constant**; production source is `MIGRATION_COLUMNS` per v1 PFR Codex F3 + Advisor F10 Mode A Layer-3 verified

4. **Schema migration mechanism:** `ALTER TABLE runs ADD COLUMN ...` per-column via `MIGRATION_COLUMNS` iteration at T1.3 implementation (cross-reference T1.3 SEAL `backtest/experiment_registry.py:197+` where `for col_name, col_def in MIGRATION_COLUMNS:` loop applies migrations; bundle `12dffde`)

5. **Backward compatibility:**
   - Pre-T1.x `runs` rows continue to be readable; new columns are NULL for legacy rows
   - Pre-T1.x consumers continue to work (no required-field changes on existing columns)
   - New T1.x writers MUST populate all 9 new T1.x columns when `lineage_context` is provided
   - **Backfill discipline:** existing Phase 1-2 runs backfilled as `cost_anchor_id = 'legacy_perp_inspired_7bps_v0'`; Phase 4 forward-holdout runs backfilled per `execution_config_path` field per R3.1d §5.2 + `MIGRATION_COLUMNS` inline comment lines 142-146

6. **Write-time discipline:** `_write_to_registry()` populates all 9 new T1.x columns for new runs per Contract 2.0.x; legacy callers without lineage context normalize to `config/execution.yaml` default per Contract 2.0.4 default normalization rule

7. **CLAUDE.md HARD CONSTRAINT cross-reference:** line 272 — `cost_anchor_id` required for new Phase B / Tier 5 / Tier 6 runs

8. **Schema version forward-compatibility note (per v2 PFR Codex F2 conditional reframing):** future schema versions extend the migration pattern conditionally. (a) If the new schema version adds new registry columns, append entries to `MIGRATION_COLUMNS` + update relevant test mirror invariants where the new columns are asserted. (b) If the new schema version adds NO new registry columns (e.g., metadata-only or per-bar-artifact-only changes), `MIGRATION_COLUMNS` and `_T1X_NEW_COLUMNS` need no update. **Do NOT prescribe `_T1X_NEW_COLUMNS` update as the default future-schema action.** Document migration delta in the successor's own decision doc + follow CONTRACT BOUNDARY discipline at extension (different attestation domain → new per-domain tuple + new helper per §2.6).

#### 2.4.3 PASS criteria

1. ADDITIVE migration class statement explicit
2. **`MIGRATION_COLUMNS` documented as production source at `experiment_registry.py:121-150+`** (per v1 PFR Codex F3 + Advisor F10 correction)
3. **9 T1.x columns enumerated with cross-reference to test mirror `_T1X_NEW_COLUMNS` at `tests/test_t1_4_backward_compat.py:51-62`** (Mode A grep-verified at T1.6 implementation)
4. `MIGRATION_COLUMNS` iteration mechanism cross-reference to T1.3 SEAL bundle `12dffde` at `experiment_registry.py:197+`
5. Backward compatibility 4-statement set (legacy rows readable + legacy consumers work + new writers populate + backfill discipline)
6. Write-time discipline cross-reference to `_write_to_registry()` location
7. CLAUDE.md HARD CONSTRAINT line 272 cross-reference
8. **Forward-compatibility note CONDITIONALLY framed (per v2 PFR Codex F2)**: future schema versions update `MIGRATION_COLUMNS` + test mirrors only when registry columns are added; NOT prescribed as default

#### 2.4.4 Explicit exclusions

- Specific migration script (no migration script needed — `MIGRATION_COLUMNS` iteration happens at T1.3 SEAL deployment; T1.6 documents existing migration only)
- Pre-T1.x schema field detail (legacy schema authoritative at git history; not duplicated here)
- Future schema version `b_c_extended_v2` design (not in T1.6 scope; eligible at fresh Charlie register-event)

### §2.5 (e) `data_dictionary.md` updates [γ Hybrid]

#### 2.5.1 Scope

§2.5 updates the canonical `data_dictionary.md` (CLAUDE.md Document Conflict Priority item 5 — human-readable schema reference) with B-C-extended schema additions. Modality: γ Hybrid — orchestrator drafts skeleton + section structure; **`general-purpose` subagent fills per-field detail** (DS3 lean revised per v1 PFR Advisor F6); orchestrator Mode A spot-verifies subagent output.

Out-of-scope for §2.5: Contract spec authoritative text (§2.1-§2.4 scope; data_dictionary references canonical specs); `wf_lineage.py` code-level extension protocol (§2.6 scope).

#### 2.5.2 Content outline

1. **New section:** "B-C-extended schema (`b_c_extended_v1`)" — header under existing data_dictionary structure
2. **14 Contract 2.0.5 fields** enumerated in data_dictionary table format (name + type + Optional + description + cross-reference to canonical spec at §2.1)
3. **9 T1.x columns** in `runs` table — new entries via `MIGRATION_COLUMNS` reference (cross-ref §2.4)
4. **`cost_anchor_id`** column under existing registry schema table (highlight as forensic discriminator per R3.1d)
5. **Per-bar artifact** section — `returns_per_bar.parquet` format + columns + SHA256 invariant
6. **Cross-references** to canonical specs at §2.1-§2.4 (no duplication of Contract text)

#### 2.5.3 γ Hybrid execution plan (per v1 PFR Advisor F6 subagent_type revision)

**Phase 1 — orchestrator skeleton (M1; ~10-15 min):**
- Draft §2.5 outline + identify new sections in `data_dictionary.md`
- Locate exact insertion points via grep on existing `data_dictionary.md` structure
- Specify per-field skeleton (name + type placeholder; subagent fills semantic)

**Phase 2 — subagent fill (γ Hybrid; subagent_type = `general-purpose` per v1 PFR Advisor F6; ~20-30 min):**
- **Rationale for `general-purpose`:** `quant-research-advisor` is calibrated for senior-quant-trader / methodology-review work (Leg-D arbiter for adversarial methodology review per its system prompt); using it as content-fill subagent is a category violation — would force it out of its calibrated review posture. `general-purpose` is the correct default for fill-tasks (also avoids §6.4 cross-leg discipline conflict where γ Hybrid subagent leg could overlap with PFR reviewer leg if both were `quant-research-advisor`).
- Brief: "Fill per-field semantic descriptions for 14 Contract 2.0.5 fields + 9 T1.x `MIGRATION_COLUMNS` entries + per-bar parquet columns; reference canonical specs at §2.1-§2.4 of T1.6 sub-plan; no Contract text duplication; align with existing data_dictionary.md style + tone"
- Subagent reads parent plan v5 §2.0 + T1.6 sub-plan §2.1-§2.4 (this doc) + existing `data_dictionary.md`; produces per-field descriptions
- Mode A spot-verification on 3-5 representative fields post-return (verify type + Optional + cross-reference correctness)

**Phase 3 — orchestrator integration (M1; ~5-10 min):**
- Insert subagent output at identified insertion points
- Apply Mode A corrections if any spot-verification flag triggers
- Final read-through for cross-section consistency

#### 2.5.4 PASS criteria

1. New "B-C-extended schema" section added to `data_dictionary.md`
2. All 14 Contract 2.0.5 fields enumerated with type + Optional + description + cross-reference
3. `cost_anchor_id` + all 9 T1.x columns enumerated in `runs` table section
4. Per-bar artifact `returns_per_bar.parquet` documented
5. No Contract text duplication (cross-references to §2.1-§2.4 instead)
6. Existing data_dictionary.md style + tone preserved
7. **Subagent_type = `general-purpose` per v1 PFR Advisor F6 (NOT `quant-research-advisor`)** (per DS3 lean revision)
8. Mode A spot-verification PASS on 3-5 representative fields

#### 2.5.5 Explicit exclusions

- Authoritative Contract spec text (§2.1-§2.4 own that)
- DSL-level field definitions (Phase 2A scope; not T1.6)
- Per-test-method field detail (T1.5 SEAL artifact authoritative)

### §2.6 (f) `wf_lineage.py` extension pattern documentation [γ Hybrid]

#### 2.6.1 Scope

§2.6 documents the **extension protocol** for adding new schema versions to the **per-domain tuple architecture** (`ACCEPTED_EVALUATION_SCHEMA_VERSIONS` / `ACCEPTED_WF_SCHEMA_VERSIONS` / `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS`) + adding new distinct `check_*_semantics_or_raise` helpers + CONTRACT BOUNDARY discipline. `b_c_extended_v1` is the **reference exemplar** at `backtest/artifact_schema.py` (canonical implementation) + `backtest/wf_lineage.py` re-export shim. Canonical location locked at T1.6 implementation per DS2 (§8.2): candidate paths = module docstring at `backtest/wf_lineage.py` + new `docs/decisions/SCHEMA_VERSION_EXTENSION_PROTOCOL.md` (orchestrator lean: both).

Out-of-scope for §2.6: specific Contract 2.0.x detail (§2.1-§2.4 scope; §2.6 is meta-protocol about extension); consumer enumeration (§2.7 scope).

#### 2.6.2 Content outline (restructured per v1 PFR F1 BLOCKING + Advisor F3 + Advisor F7; v2 PFR Advisor F3 6-GAP expansion)

1. **3-step extension protocol with sub-steps + optional Step 4 (renumbered for unambiguous structure per v4 PFR Advisor F4; Step 3 has integral sub-steps 3a + 3b both mandatory; Step 4 = optional heavy-private-impl extraction):**
   - **Step 1: identify attestation domain** for the new schema version. Three existing domains: evaluation (single-run holdout artifacts) / WF (walk-forward summary artifacts) / B-C-extended (per-bar return series artifacts). If new domain required, document the CONTRACT BOUNDARY discipline for the new tuple.
   - **Step 2: define new schema version constant** at the domain's canonical declaration site:
     - Evaluation + WF domains: `backtest/wf_lineage.py` constants section (e.g., `ARTIFACT_SCHEMA_VERSION_<NEW>`)
     - B-C-extended domain: `backtest/artifact_schema.py` (per heavy-private-impl extraction discipline; re-export shim from `wf_lineage.py`)
   - **Step 3: extend per-domain tuple + helper (integral; Step 3 contains two mandatory sub-steps 3a + 3b that together constitute one logical extension operation):**
     - **Step 3a: append to domain-appropriate accepted-versions tuple:**
       - Evaluation: `ACCEPTED_EVALUATION_SCHEMA_VERSIONS` at `wf_lineage.py:99-102`
       - WF: `ACCEPTED_WF_SCHEMA_VERSIONS` at `wf_lineage.py:107-110`
       - B-C-extended: `ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS` at `artifact_schema.py:45-47`
       - **DO NOT append to legacy alias `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS` at `wf_lineage.py:117-120` — this is the backward-compat legacy alias of pre-domain-split tuple contents; CONTRACT BOUNDARY explicitly forbids adding `b_c_extended_v1` or other new-domain versions to it**
     - **Step 3b: extend (or add) distinct `check_*_semantics_or_raise()` helper** enforcing per-version field discipline at the helper site. Each helper accepts ONLY its own domain's tuple per CONTRACT BOUNDARY.

2. **CONTRACT BOUNDARY (sealed at `backtest/wf_lineage.py:47-51` + reinforced at `:95-96`):** documented verbatim with file:line cite. Explain rationale + why it forbids cross-domain tuple pollution.

3. **6 §2.6 extension-protocol/code-surface CONTRACT GAPs total documented (per v1 PFR Advisor F3 + v2 PFR Advisor F3 6-GAP expansion catching canonical-site GAPs missed at v1 BLOCKING F1 restructure; scope-qualified per v6 SEAL-eve Round 1b Codex F3 LOW — T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21/95/126/179/379/392` are OUT-OF-SCOPE for §2.6 extension protocol per T1.5 SEAL artifact authoritative):**

   **In `backtest/wf_lineage.py` (3 GAPs):**
   - **`backtest/wf_lineage.py:105-106`** — "CONTRACT GAP: if a new WF-specific schema version is introduced, add it here and update `check_wf_semantics_or_raise`'s branching." Forward-trigger for Step 3 (WF tuple-append).
   - **`backtest/wf_lineage.py:114-116`** — "CONTRACT GAP: consumers that imported `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS` and perform per-version branching should migrate to the per-domain tuple that matches their attestation domain. Audit via: `rg "ACCEPTED_ARTIFACT_SCHEMA_VERSIONS"`." Forward-trigger for §2.7 consumer audit at each extension.
   - **`backtest/wf_lineage.py:269`** — "CONTRACT GAP: regime_key validation against REGIME_KEY_LABEL_MAPPING." Distinct from extension-boundary gaps; document for completeness.

   **In `backtest/artifact_schema.py` (3 GAPs — added per v2 PFR Advisor F3 fix-substantive-leak catch; deferral direction clarified per v3 PFR Advisor F4):**
   - **`backtest/artifact_schema.py:684`** — "CONTRACT GAP: regime_key validation against REGIME_KEY_LABEL_MAPPING is deferred..." (in `check_b_c_extended_semantics_or_raise` docstring; B-C-extended domain regime_key validation asymmetry to wf_lineage.py:269 sibling)
   - **`backtest/artifact_schema.py:692`** — "CONTRACT GAP: execution_config_path canonicalization is deferred to T1.3 engine `_write_to_registry()`..." (deferred FROM consumer-side helper `check_b_c_extended_semantics_or_raise` TO producer-side `engine._write_to_registry()` — consumer accepts canonicalized paths; producer canonicalizes before write)
   - **`backtest/artifact_schema.py:972`** — "CONTRACT GAP: execution_config_path canonicalization (T1.3 engine..." (sibling helper canonicalization deferral; same FROM consumer TO producer direction)

   **Stale upstream authority note (per v3 PFR Codex F1 — load-bearing catch):** the docstring at `backtest/wf_lineage.py:373-375` currently reads "Future arcs extend the branching by appending to `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS`" — this is **stale text pre-dating T1.2 Sub-decision A per-domain split**; sealed code at `:478-480` actually gates on `ACCEPTED_EVALUATION_SCHEMA_VERSIONS`, not the legacy union. Parent plan v5 §2.0.2 line 76 contains analogous stale wording. T1.6 §2.6 implementation MUST: (a) update the docstring at `wf_lineage.py:373-375` to per-domain tuple wording (extension via domain-appropriate accepted-versions tuple, not legacy union); (b) qualify parent plan §2.0.2 line 76 cross-reference at canonical doc as "superseded by T1.2 Sub-decision A per-domain split". Without this, γ Hybrid Phase 2 `general-purpose` subagent following the stale-authority text could re-introduce v1 BLOCKING F1 framing through reference resolution.

4. **`b_c_extended_v1` reference exemplar (per v1 PFR Advisor F7):**
   - **Canonical implementation site:** `check_b_c_extended_semantics_or_raise()` def at `backtest/artifact_schema.py:654` — pointer for future implementers
   - **Re-export shim:** `backtest/wf_lineage.py:539-548` (preserves public surface; consumer backward compat per C1-extract-pre-SEAL Charlie register 2026-05-22)
   - **T1.2 SEAL bundle:** `12dffde` (reference implementation commit)
   - **Heavy-private-impl extraction discipline (optional Step 4 for future schema versions):** if new schema version's validation logic is heavy (>200 lines + complex field discipline), consider extraction to dedicated module per `backtest/artifact_schema.py` precedent; preserve public surface via shim re-export from `backtest/wf_lineage.py`. C1-extract-pre-SEAL Charlie register precedent provides governance template. **Note: extracted canonical site has its OWN CONTRACT GAPs (e.g., `artifact_schema.py:684/692/972` for `b_c_extended_v1`) — future implementers MUST enumerate canonical-site GAPs as part of extension protocol; do NOT default to `wf_lineage.py` GAPs alone (per v2 PFR Advisor F3 fix-substantive-leak discipline).**

5. **Validation logic routing:** `artifact_schema_version` field routes validation logic via per-domain helper at consumer call site. Per-domain helper accepts ONLY its own domain's tuple per CONTRACT BOUNDARY. Document the routing structure: consumer determines domain (e.g., "this is a WF artifact" → call `check_wf_semantics_or_raise`; "this is a B-C-extended per-bar artifact" → call `check_b_c_extended_semantics_or_raise`); each helper internally validates `artifact_schema_version` against its domain's tuple + rejects mismatched values.

6. **Forward-compatibility:** future cycles adding new schema versions follow same 4-step protocol; consumer audit obligation per CONTRACT GAP at `wf_lineage.py:114-116`; canonical-site GAP enumeration obligation if heavy-private-impl extraction applied.

7. **Cross-references:** parent plan v5 §2.0.2 (Contract 2.0.2 schema version + validation branch policy); T1.2 SEAL bundle `12dffde` (reference implementation); CLAUDE.md Contract Markers section (CONTRACT GAP + CONTRACT BOUNDARY + DESIGN INVARIANT discipline at L297-300).

#### 2.6.3 γ Hybrid execution plan (per v1 PFR Advisor F6 subagent_type revision)

**Phase 1 — orchestrator skeleton (M1; ~10-15 min):**
- Draft §2.6 outline + identify canonical location options (module docstring + dedicated doc)
- Specify 4-step (3 + optional 4th) protocol structure
- Specify 6 CONTRACT GAPs + CONTRACT BOUNDARY cross-references

**Phase 2 — subagent fill (γ Hybrid; subagent_type = `general-purpose` per v1 PFR Advisor F6; ~15-20 min):**
- Brief: "Fill 4-step extension protocol detail using `b_c_extended_v1` as reference exemplar; reference `backtest/wf_lineage.py` per-domain tuple architecture (3 tuples + 3 helpers + CONTRACT BOUNDARY at lines 47-51 + 95-96) + `backtest/artifact_schema.py` canonical implementation site at line 654; document **6 CONTRACT GAPs** (3 in wf_lineage.py at lines 105-106 + 114-116 + 269; 3 in artifact_schema.py at lines 684 + 692 + 972); produce module docstring section + dedicated doc section as parallel outputs; cite specific file:line where applicable (Mode A re-verifiable). **STALE AUTHORITY DISCLOSURE per v3 PFR Codex F1:** the docstring at `backtest/wf_lineage.py:373-375` says 'Future arcs extend the branching by appending to ACCEPTED_ARTIFACT_SCHEMA_VERSIONS' — this is SUPERSEDED by T1.2 Sub-decision A per-domain split (sealed code at `:478-480` gates on `ACCEPTED_EVALUATION_SCHEMA_VERSIONS`, not legacy union). Parent plan v5 §2.0.2 line 76 contains analogous stale wording. Do NOT recommend appending new-domain schema versions to legacy union `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS`; follow §2.6.2 Step 3 (append to domain-appropriate per-domain tuple) instead. As part of this deliverable, also update the docstring at `wf_lineage.py:373-375` to per-domain tuple wording."
- Subagent reads `backtest/wf_lineage.py` + `backtest/artifact_schema.py` + parent plan v5 §2.0.2 + T1.2 SEAL implementation; produces 4-step protocol text + CONTRACT GAP context + forward-compatibility note + heavy-private-impl extraction discipline section
- Mode A spot-verification on file:line citations + protocol step correctness + CONTRACT BOUNDARY rationale clarity post-return

**Phase 3 — orchestrator integration (M1; ~5-10 min):**
- Insert subagent output at canonical location(s)
- Apply Mode A corrections if any spot-verification flag triggers
- Cross-link `backtest/wf_lineage.py` module docstring with dedicated doc if both used

#### 2.6.4 PASS criteria

1. 4-step extension protocol documented (Step 1 + Step 2 + Step 3 [with integral sub-steps 3a tuple-append + 3b helper-add, both mandatory] + optional Step 4 heavy-private-impl extraction; renumbered per v4 PFR Advisor F4 unambiguous structure)
2. **CONTRACT BOUNDARY at `wf_lineage.py:47-51` + `:95-96` documented verbatim + rationale** (per v1 PFR F1)
3. **6 §2.6 extension-protocol/code-surface CONTRACT GAPs total (per sub-plan SEAL-eve Round 1 Codex F3 scope-qualification; T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21,95,126,179,379,392` are OUT-OF-SCOPE for §2.6 extension protocol per T1.5 SEAL artifact authoritative) documented (per v2 PFR Advisor F3):** 3 in `wf_lineage.py` (`:105-106` WF extension + `:114-116` consumer audit + `:269` regime_key) + 3 in `backtest/artifact_schema.py` (`:684` B-C-extended regime_key + `:692` execution_config_path canonicalization producer-side + `:972` sibling helper canonicalization) with file:line + forward-trigger semantic for each
4. **`b_c_extended_v1` exemplar cross-references both canonical site (`artifact_schema.py:654`) + shim re-export (`wf_lineage.py:539-548`) + T1.2 SEAL bundle `12dffde` + heavy-private-impl extraction discipline as optional 4th step** (per v1 PFR Advisor F7) **+ canonical-site GAP enumeration obligation note (per v2 PFR Advisor F3)**
5. Per-domain validation routing structure documented (consumer determines domain → calls domain-appropriate helper)
6. Forward-compatibility note for future schema versions
7. Cross-references to parent plan v5 §2.0.2 + CLAUDE.md Contract Markers section
8. **Subagent_type = `general-purpose` per v1 PFR Advisor F6** (DS3 lean revision)
9. Mode A spot-verification PASS on file:line citations (all 6 GAPs + 3 helper def lines + CONTRACT BOUNDARY locations)
10. **Stale docstring update at `backtest/wf_lineage.py:373-375` to per-domain tuple wording (per v3 PFR Codex F1)** — replace "appending to ACCEPTED_ARTIFACT_SCHEMA_VERSIONS" with "appending to the domain-appropriate per-domain tuple per T1.2 Sub-decision A; see CONTRACT BOUNDARY at lines 47-51 + 95-96"; documentation-only docstring update within T1.6 scope per [`feedback_claude_md_freshness.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_claude_md_freshness.md)-analog discipline (sealed code docstrings are documentation surface; updates within doc-only scope)
11. **Parent plan v5 §2.0.2 line 76 cross-reference qualified at canonical doc** (per v3 PFR Codex F1) — note "tuple-extension line at parent plan §2.0.2:76 superseded by T1.2 per-domain split per Sub-decision A lock; cite preserved for historical context but DO NOT follow as live instruction"
12. **T_obs required-adjacent 15th field documented per sub-plan SEAL-eve Round 1 Codex F1 MEDIUM (per §2.1.2 item 5b expansion)** — explicit declaration of T_obs as required field outside the Contract 2.0.5 14-field header table; sealed implementation at `artifact_schema.py:307-309` (declaration) + `:815-827` (validation enforcement including positivity branch — full block per v6 SEAL-eve Round 1b Codex F1 + Advisor F1 CONVERGED LOW) cross-referenced; §2.4.2 cross-reference to registry layer where T_obs IS enumerated as 3rd T1.1 FIX-B1 per-bar field at `MIGRATION_COLUMNS:155-157`; semantic + type + positivity + producer/consumer contract documented in canonical doc

#### 2.6.5 Explicit exclusions

- Specific Contract 2.0.x field detail (§2.1-§2.4 own that)
- New schema version design (no new versions at T1.6; future versions follow this protocol)
- Consumer audit results (§2.7 scope)

### §2.7 (g) Consumer enumeration [γ Hybrid]

#### 2.7.1 Scope

§2.7 produces the **per-consumer disposition table** for all consumers of `ACCEPTED_*_SCHEMA_VERSIONS` tuples + `ARTIFACT_SCHEMA_VERSION_*` constants + `schema_version` field references across the codebase. For each consumer: verify it handles `b_c_extended_v1` correctly via the appropriate per-domain helper OR document explicit no-op behavior with rationale. Canonical location locked at T1.6 implementation per DS1.

Out-of-scope for §2.7: extension protocol meta-rules (§2.6 scope); Contract spec detail (§2.1-§2.4 scope).

#### 2.7.2 Content outline (restructured per v1 PFR F1 4-tuple audit + Codex F7 directory list + Advisor F4 drop preliminary count + Advisor F5 add scripts/)

1. **Audit methodology (4-tuple grep architecture per v1 PFR F1):**
   - `rg "ACCEPTED_EVALUATION_SCHEMA_VERSIONS"` — evaluation domain consumers
   - `rg "ACCEPTED_WF_SCHEMA_VERSIONS"` — WF domain consumers
   - `rg "ACCEPTED_B_C_EXTENDED_SCHEMA_VERSIONS"` — B-C-extended domain consumers (note: this symbol is canonically declared at `artifact_schema.py:45-47` + re-exported via shim at `wf_lineage.py:541`)
   - `rg "ACCEPTED_ARTIFACT_SCHEMA_VERSIONS"` — backward-compat legacy alias consumers (audit trigger per CONTRACT GAP at `wf_lineage.py:114-116` — consumers should migrate to per-domain tuple matching attestation domain)
   - Plus: `rg "ARTIFACT_SCHEMA_VERSION_"` — schema version constants
   - Plus: `rg "schema_version"` — schema version field references in artifact headers / SQL queries / docstrings
   - Plus: `rg "b_c_extended_v1"` — literal string-value references

2. **Audit directory scope (per v1 PFR Codex F7 + Advisor F5 reconciliation; 6 directories):**
   - `backtest/`
   - `agents/`
   - `strategies/`
   - `factors/`
   - **`scripts/`** (added per v1 PFR Advisor F5; contains evaluation-gate driver `scripts/run_phase2c_evaluation_gate.py` per parent plan v5 line 239 — known 4th-entry-point lineage context propagation surface)
   - `tests/` (test-side consumer audit per §2.7.2 item 4)

3. **Per-consumer disposition table** — columns:
   - Consumer file:line
   - Reference type (import / function call / docstring / test assertion / internal comment-reference per v1 PFR Advisor F4 disambiguation)
   - Tuple/symbol class (per-domain EVALUATION / WF / B-C-extended / legacy alias / constant / field reference / string-value reference)
   - Disposition (HANDLES: handles `b_c_extended_v1` correctly via domain-appropriate helper | NO-OP: explicit no-op behavior with rationale | NEEDS-EXTENSION: requires code change to handle `b_c_extended_v1`)
   - Cross-reference (link to Contract / sub-plan section / SEAL bundle if applicable)

4. **NEEDS-EXTENSION row handling (per v1 PFR Codex F2 anti-pre-emption fix):**
   - Document in disposition table with explicit "**deferred to successor cycle eligible-not-named per anti-pre-emption**" note
   - Specify exact change required + cross-reference to relevant Contract
   - **NO in-cycle scope expansion path offered**: if NEEDS-EXTENSION work is required, it is FIRMLY DEFERRED to fresh Charlie register-event for separate successor cycle. T1.6 sub-plan does NOT pre-offer Path A → Path A+ in-cycle expansion (which would soft-pre-empt Charlie scope-class decision). The v1 sub-plan's Phase 3 "decision point — defer vs in-cycle expand" framing is REMOVED per v1 PFR Codex F2.

5. **Test-side consumer audit:** test files that assert on `schema_version` field — verify tests cover `b_c_extended_v1` per T1.5 SEAL coverage (cross-reference T1.5 [`tests/test_t1_5_smoke_end_to_end.py`](../../../tests/test_t1_5_smoke_end_to_end.py) + [`tests/test_t1_5_registry_integrity.py`](../../../tests/test_t1_5_registry_integrity.py))

6. **Summary row:** N consumers audited / M HANDLES / K NO-OP / L NEEDS-EXTENSION. **Population count REMOVED from v1's "~40+" preliminary estimate per v1 PFR Advisor F4**; locked at T1.6 §2.7 Phase 1 grep with disambiguated reference-type classification.

#### 2.7.3 γ Hybrid execution plan (per v1 PFR Advisor F6 subagent_type revision + Codex F2 anti-pre-emption fix)

**Phase 1 — orchestrator skeleton + grep (M1; ~15-20 min):**
- Run 4-tuple grep + symbol/string-value grep on 6 directories (per §2.7.2 items 1 + 2)
- Categorize grep hits by reference type (import / function call / docstring / test assertion / internal comment-reference)
- Categorize grep hits by tuple/symbol class (per §2.7.2 item 3 column 3)
- Draft disposition table skeleton with file:line + reference type + tuple/symbol class pre-populated

**Phase 2 — subagent disposition determination (γ Hybrid; subagent_type = `general-purpose` per v1 PFR Advisor F6; ~30-45 min):**
- Brief: "For each grep hit in disposition table skeleton, read the surrounding code (±20 lines) and classify disposition as HANDLES / NO-OP / NEEDS-EXTENSION. For HANDLES, identify which per-domain helper handles `b_c_extended_v1` (evaluation / WF / B-C-extended). For NO-OP, state rationale (e.g., 'this consumer only reads legacy phase2c_7_1 artifacts via per-domain evaluation helper; explicit version filter at line X'). For NEEDS-EXTENSION, specify exact code change required. **Per CLAUDE.md anti-pre-emption + v1 PFR Codex F2: NEEDS-EXTENSION work is FIRMLY DEFERRED to fresh Charlie register-event for separate successor cycle; do NOT offer in-cycle scope expansion path.** Document each NEEDS-EXTENSION row as 'deferred eligible-not-named successor cycle per anti-pre-emption.'"
- Subagent populates disposition column + cross-reference column
- Mode A spot-verification on 5-10 representative rows post-return (verify disposition correctness via independent Read on cited consumer)

**Phase 3 — orchestrator integration + summary (M1; ~10-15 min):**
- Insert disposition table at canonical location
- Compute summary row counts (N / M / K / L)
- Apply Mode A corrections if any spot-verification flag triggers
- **If any NEEDS-EXTENSION rows surface:** all NEEDS-EXTENSION rows documented as deferral to successor cycle (eligible-not-named per §8.2) per anti-pre-emption discipline. T1.6 SEAL boundary preserved as documentation-only. If Charlie wants to fire NEEDS-EXTENSION work, fresh Charlie register-event for separate successor cycle is required (NOT in-cycle scope expansion). Orchestrator surfaces NEEDS-EXTENSION summary to Charlie register at T1.6 SEAL ratify gate.

#### 2.7.4 PASS criteria

1. **4-tuple grep audit run on all 6 directories** (`backtest/`, `agents/`, `strategies/`, `factors/`, `scripts/`, `tests/`) (per v1 PFR Codex F7 + Advisor F5 reconciliation)
2. All grep hits enumerated in disposition table with file:line + reference type + tuple/symbol class
3. Per-consumer disposition classified (HANDLES / NO-OP / NEEDS-EXTENSION) with rationale
4. Cross-references to relevant Contracts / sub-plan sections / SEAL bundles
5. Summary row N / M / K / L counts populated
6. **NEEDS-EXTENSION rows (if any) firmly DEFERRED to successor cycle per anti-pre-emption** (per v1 PFR Codex F2; NO in-cycle scope expansion offered)
7. Mode A spot-verification PASS on 5-10 representative rows
8. Test-side consumer audit cross-reference to T1.5 SEAL artifacts
9. **Subagent_type = `general-purpose` per v1 PFR Advisor F6** (DS3 lean revision)

#### 2.7.5 Explicit exclusions

- Code-level fixes for NEEDS-EXTENSION consumers (T1.6 is documentation-only; code fixes deferred to successor cycle eligible-not-named per §8.2 anti-pre-emption)
- DSL compiler / hypothesis hash consumer audit (Phase 2A scope; not T1.6)
- Per-file authoritative Contract text (§2.1-§2.4 own that; §2.7 disposition table cross-references)
- In-cycle scope expansion path (Path A → Path A+) for NEEDS-EXTENSION work (firmly removed per v1 PFR Codex F2)

---

## §3 Validation approach + success criteria

### §3.1 Pass criteria

T1.6 SEAL gate requires **all 7 deliverables** complete with per-deliverable PASS criteria (§2.1.3 + §2.2.3 + §2.3.3 + §2.4.3 + §2.5.4 + §2.6.4 + §2.7.4) satisfied. Aggregate cycle PASS criteria:

1. All 7 deliverables documented at canonical locations
2. All Contract 2.0.x cross-references verified (Mode A grep-verified)
3. No code-level changes outside docstrings (T1.6 is documentation-only)
4. Full suite delta: zero regression on T1.5 baseline (2317 collected: 2315 PASS + 2 xfail + 0 FAIL)
5. Consumer enumeration disposition table complete (§2.7); any NEEDS-EXTENSION rows have explicit successor-cycle deferral disposition (firm; no in-cycle scope expansion per v1 PFR Codex F2)
6. SEAL-eve adversarial review PASS per Rule 2 (OPERATIONALLY REQUIRED post-PFR convergent APPROVE) at BOTH sub-plan SEAL-eve + T1.6 SEAL-eve (per §6.2 dual-SEAL-eve discipline)
7. **Cross-leg discipline maintained per §6.4 with verifiable instance tracking at §11 revision log — 8-instance scheme expanded across §3.1 + §6.4 + §10 (per v2 PFR Codex F1 + Advisor F2 CONVERGED MEDIUM 6-instance + v4 PFR 7-instance expansion + v6 SEAL-eve Round 1b 8-instance expansion):** [NOTE: this ledger frozen at sub-plan v_final ratify 2026-05-24; **for live #N7+#N8+#N9 outcomes from Implementation cycle 2026-05-25 see §10 task SEAL chain** + §10 phase-entry tracking block; this §3.1 ledger preserves the sub-plan-ratify-gate snapshot]
   - **#N1** — v1 PFR Advisor opus instance (COMPLETED)
   - **#N2** — v2 PFR Advisor opus instance (COMPLETED)
   - **#N3** — v3 PFR Advisor opus instance (COMPLETED)
   - **#N4** — v4 PFR Advisor opus instance (5 findings + 1 INFO; COMPLETED; cycle saturation REAFFIRMED)
   - **#N5** — Sub-plan SEAL-eve Round 1 Advisor opus instance (1 LOW finding; COMPLETED; Rule 2 VINDICATED — Codex caught MEDIUM F1 T_obs gap at same round)
   - **#N6** — Sub-plan SEAL-eve Round 1b Advisor opus instance (current dispatch per Path B''''' authorization; verifies T_obs substantive new content + 3 mechanical fixes)
   - **#N7** — Implementation PFR Advisor opus instance (pending) [now COMPLETED 2026-05-25 per §10]
   - **#N8** — T1.6 SEAL-eve Round 2 Advisor opus instance (pending; per §6.2 dual-SEAL-eve) [now COMPLETED 2026-05-25 per §10]
   - All distinct per §6.4 cross-leg discipline; γ Hybrid `general-purpose` subagent instances (§2.5/§2.6/§2.7) are separate category (do NOT count against Advisor opus instance ledger)
   - Instance separation tracked at §11 revision log per phase entry
   - **8-instance scheme expanded at v6 per sub-plan SEAL-eve Round 1b addition (Path B''''' bundled authorization)**

### §3.2 Failure handling

If any deliverable PASS criteria fails OR documentation drift discovered, cycle does NOT SEAL until resolved. No tolerance relaxation without fresh Charlie register-event. SEAL-eve adversarial finding handling per Path C hybrid invariant-level closure framework where applicable (cf. T1.5 SEAL precedent at §8.2 DS-NEW (f)).

---

## §4 Explicit exclusions (anti-pre-emption discipline)

Per §8.1 anti-pre-emption preserve, the following are EXPLICITLY NOT in T1.6 scope:

| # | Exclusion | Rationale | Provenance |
|---|---|---|---|
| 1 | New schema version design (e.g., `b_c_extended_v2`) | Future versions follow §2.6 extension protocol; design requires fresh Charlie register-event per anti-pre-emption | §2.4.4 + §2.6.5 forward-compatibility note |
| 2 | New `cost_anchor_id` mapping table entries | R3.1d §5.2 6-row table is authoritative; new anchors require fresh Charlie register per R3.1d discipline | §2.3.4 |
| 3 | Code-level fixes for NEEDS-EXTENSION consumers (if any surface at §2.7) | T1.6 is documentation-only; code fixes firmly DEFERRED to successor cycle eligible-not-named per anti-pre-emption + v1 PFR Codex F2 (no in-cycle scope expansion) | §2.7.5 |
| 4 | DSL compiler / hypothesis hash documentation | Phase 2A scope; not T1.6 | §2.5.5 + §2.7.5 |
| 5 | Pre-existing scratch files from pre-T1.5 work (`backtest/engine.py,cover`, `coverage.json`, R5.1/R5.2/R6.1 plan docs that are NOT part of B-C-extended cycle work) — **T1.6 sub-plan + T1.6 deliverable docs ARE in-scope for T1.6 SEAL bundle staging** (per v1 PFR Advisor F11 clarification) | Pre-T1.5 untracked work; do NOT stage in T1.6 SEAL bundle per T1.5 SEAL precedent | T1.5 sub-plan §11 v_seal entry + v1 PFR Advisor F11 |
| 6 | Memory codification of 6 cumulative empirical contributions | DEFERRED to B-C-extended cycle SEAL boundary per Charlie register Option 1 implicit M-defer + R4' option | Charlie register 2026-05-24 |
| 7 | §35 codification text + scope | DECIDED at B-C-extended cycle SEAL boundary per anti-pre-emption; do NOT pre-codify mid-T1.6 | Handoff §"Anti-pre-emption discipline" |
| 8 | DS-NEW (e) production-data-anomaly + DS-NEW (f) artifact-writer + schema-validator chain coverage | Eligible-not-named successor cycles per T1.5 sub-plan §8.2 anti-pre-emption; do NOT pre-name in T1.6 sub-plan or anywhere else without fresh Charlie register-event | T1.5 sub-plan §8.2 |
| 9 | T1.5 followup smoke artifact-writer + schema-validator chain coverage (HIGH-1 deferred at T1.5 SEAL) | Same as exclusion 8 (DS-NEW (f)) | T1.5 sub-plan §11 v_impl_polish_v2 + §8.2 |
| 10 | Production-data anomaly engine→writer smoke (NaN / zero-volume / gap-window axes) | Same as exclusion 8 (DS-NEW (e)) | T1.5 sub-plan §8.2 |
| 11 | R6.1 Tier 6 promotion class evaluation application | Eligible-not-named successor class per R6.1 V_SEAL post-event; not T1.6 scope | CLAUDE.md Phase Marker R6.1 V_SEAL entry |
| 12 | B-C-narrow data-recovery successor cycle + B-C-extended structural artifact-preservation refactor cycle SEAL | Arc-level closeout deferred to post-T1.6 SEAL per Charlie pre-authorization 2026-05-23 | Handoff §"After T1.6 SEAL" |
| 13 | In-cycle scope expansion (Path A → Path A+) for NEEDS-EXTENSION work | Firmly removed per v1 PFR Codex F2 anti-pre-emption; in-cycle expansion would soft-pre-empt Charlie scope-class decision after entry register | v1 PFR Codex F2 |

---

## §5 Risks + dependencies

### §5.1 Dependencies

- **T1.2 + T1.3 + T1.1 SEAL bundle `12dffde`** — schema + registry + writer implementation (T1.6 documents these; no modifications)
- **T1.4 SEAL `5a44ec6`** — backward-compatibility verification (T1.6 references; no modifications)
- **T1.5 SEAL `9d9a40d`** — test suite (T1.6 cross-references for fixture / smoke / registry-integrity test surfaces)
- **Parent plan v5 §2.0** — Contract 2.0.1-2.0.5 authoritative text (T1.6 cross-references; no duplication)
- **`docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md` §5.2** — 6-row mapping table authoritative source (T1.6 §2.3 references)
- **CLAUDE.md HARD CONSTRAINTS** — line 272 `cost_anchor_id` requirement (T1.6 §2.3 + §2.4 cross-references)
- **scipy ≥ 1.9** — precondition for γ4 implementation (T1.6 §2.2 documents; T1.5 fixture test enforces)
- **Sealed CONTRACT BOUNDARY architecture** — `wf_lineage.py:47-51` + `:95-96` + 3 per-domain tuples + 3 helpers (T1.6 §2.1 + §2.6 + §2.7 document this; no modifications)
- **Sealed 6 CONTRACT GAPs** — 3 in `wf_lineage.py` + 3 in `artifact_schema.py` (T1.6 §2.6 documents these; no modifications)

### §5.2 Risks

- **Consumer-extension-mid-cycle risk (Path A surface):** §2.7 4-tuple grep audit on 6 directories may surface NEEDS-EXTENSION consumers that require code-level changes. Mitigation: per §2.7.3 Phase 3 + §2.7.4 PASS criterion 6 + §4 row 13, all NEEDS-EXTENSION rows firmly DEFERRED to successor cycle (anti-pre-emption preserved). **NEEDS-EXTENSION count locked empirically at §2.7 Phase 1 grep audit; NO a-priori target set (per v2 PFR Advisor F4 anti-pre-emption discipline — predicting "count = 0" creates mild pre-emption pressure to re-classify ambiguous rows as HANDLES).** If empirical count > 0, all rows DEFERRED to fresh Charlie register-event for separate successor cycle (no in-cycle expansion).
- **γ Hybrid authorial drift risk** at (e) + (f) + (g) deliverables. Mitigation: Mode A spot-verification on subagent output post-return + orchestrator integration phase. Cross-leg discipline: `general-purpose` subagent instances (DS3 lean revision per v1 PFR Advisor F6) avoid category violation + §6.4 conflict that `quant-research-advisor` would create.
- **Documentation-spec drift risk:** Contract text in T1.6 deliverables drifts from sealed `backtest/wf_lineage.py` + `backtest/artifact_schema.py` + `backtest/experiment_registry.py` implementation. Mitigation: per-deliverable PASS criteria require Mode A grep-verified cross-references (not paraphrase); reviewer rounds verify cross-references. **v1 PFR demonstrated upstream propagation error** (handoff prompt → v1 sub-plan §0/§2.3/§2.4 cited "`experiment_registry.py:86,139`" + "`_T1X_NEW_COLUMNS`" as production constant; Mode A Layer-3 grep at v1 PFR caught both errors — `:147` is correct + `_T1X_NEW_COLUMNS` is test mirror). **v2 PFR Advisor F1 caught additional v1→v2 carryover error** (function def lines 250/340 cited as inner-branch lines 319/474; corrected at v3). T1.6 implementation Mode A discipline preserves this catch capacity.
- **CONTRACT GAP / DESIGN INVARIANT marker preservation risk:** if T1.6 documents existing markers, must not accidentally remove or rephrase trigger conditions. Mitigation: §2.6 documents extension protocol referencing markers at exact file:line (6 CONTRACT GAPs per Advisor F3 v1+v2 cumulative); reviewer rounds verify marker integrity. **v2 PFR Advisor F3 caught fix-substantive-leak from v1 BLOCKING F1 restructure** (moved canonical site to `artifact_schema.py` but did not enumerate canonical-site GAPs at lines 684/692/972; corrected at v3 with 6-GAP total).
- **Cycle-pattern-threshold risk for γ Hybrid + M1 (revised per v1 PFR Advisor F12 + v2 PFR Advisor F8 + v3 PFR Advisor F5 N=4 harmonization):** these modalities have multiple empirical observations. M1 task-class subdivision at B-C-extended cycle SEAL boundary: (a) **implementation class** N=2 (T1.5 implementation + T1.6 (a)-(d) deliverable execution); (b) **sub-plan drafting class** N=4 (T1.6 v1 + v2 + v3 + v4 — each distinct M1 episode with PFR-input integration between events; non-collapse reasoning per v2 PFR Advisor F8). **γ Hybrid empirical (harmonized per v3 PFR Advisor F5):** N=4 (T1.5 sub-plan §2 detail-fill [1 dispatch] + T1.6 (e) data_dictionary fill [1 dispatch] + T1.6 (f) extension protocol fill [1 dispatch] + T1.6 (g) consumer enumeration fill [1 dispatch] = 4 distinct observations; parallel to §9.6 N=4 non-collapse reasoning — each Phase 2 dispatch has distinct subagent brief + distinct fill substrate + distinct M1-skeleton starting point). Mitigation: track per-deliverable + per-task-class modality outcomes at §11 revision log; reassess at B-C-extended cycle SEAL boundary per Charlie R4' caveat with task-class subdivision.
- **Memory codification deferral risk:** 6 contributions deferred to B-C-extended cycle SEAL boundary may accumulate detail drift. Mitigation: T1.5 sub-plan §11 + this T1.6 §10 task SEAL chain + this T1.6 §11 revision log preserve cycle-internal empirical record; cycle SEAL boundary batch codification works from these archived records, not fresh recall.
- **Upstream propagation chain risk (codified per v1 + v2 PFR empirical):** parent plan v5 §2.0.4 line 91 had cite `experiment_registry.py:86,139` which propagated through handoff prompt + v1 sub-plan; v1 cited inner-branch lines 319+/474+ which propagated into v2. Mode A Layer-3 grep at v1 + v2 PFR caught the propagations. Forward discipline: any cite carried from parent plan + handoff prompts at v3 + downstream cycles MUST be Mode A grep-verified at point of use; do NOT trust upstream cite chains without independent verification; function `def` line cites preferred over inner-branch line cites for discoverability.

---

## §6 Reviewer dispatch plan

### §6.1 Sub-plan v_n PFR-rule-Y rounds (post-Charlie-sub-plan-fire)

Per B2 standing rule LOCKED 2026-05-19:
- **Leg 1: Codex cross-model** via `Agent(codex:codex-rescue)` — cross-model diversity per Reading 3 standing rule (B2 LOCKED); auto-notification via `~/.claude/scripts/codex-wait-and-fetch.sh` helper script
- **Leg 2: Claude Advisor opus** via `Agent(quant-research-advisor)` — senior quant trader / researcher lens; Mode A independent re-verification on every Advisor specific-claim before adoption per 3-layer safety architecture Layer 3
- **Both dispatched in single message** for true parallel execution per [`feedback_parallel_subagent_dispatch.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_parallel_subagent_dispatch.md)

PFR-rule-Y scoped post-fix re-review per [`feedback_reviewer_routing_subagent_default.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_routing_subagent_default.md):
- Apply when adjudication introduces meaningful new content beyond literal reviewer adoption
- Skip when all adjudication outcomes are mechanical literal application

**v1→v2 cycle empirical:** v1 PFR Codex + Advisor opus CONVERGED on BLOCKING F1 (3-tuple architecture vs sub-plan's wrong framing). 16/16 ADOPT outcome.
**v2→v3 cycle empirical:** v2 PFR Codex + Advisor opus APPROVE-WITH-FINDINGS both legs (no BLOCKING/HIGH). 9/9 ADOPT outcome including Advisor F3 fix-substantive-leak catch (3 omitted GAPs at canonical site post-v1 BLOCKING F1 restructure).
**v3 PFR-rule-Y operationally required** per Path α'' (Charlie register 2026-05-24) — v3 contains substantive new content (Advisor F3 added 3 GAPs with semantic; Codex F1+Advisor F2 reconciled instance numbering to 6-instance scheme).

### §6.2 Sub-plan SEAL-eve adversarial round + T1.6 SEAL-eve adversarial round (DUAL SEAL-eve discipline per v1 PFR Codex F6)

Per Rule 2 (SEAL-eve adversarial OPERATIONALLY REQUIRED) at T1.5 SEAL empirical confirmation (SEAL-eve v1 + v2 both caught HIGH that PFR convergent APPROVE missed):

**Round 1: Sub-plan SEAL-eve** (after PFR convergent APPROVE on sub-plan v_n; before sub-plan ratify):
- Dispatch SEAL-eve adversarial 2-leg with explicit "assume hidden bugs and find them" framing
- Apply MAX anti-anchoring framing at Advisor brief per [`feedback_advisor_own_anchoring_implementation_review.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_advisor_own_anchoring_implementation_review.md)
- Outcome: sub-plan v_seal ratify OR additional revisions before ratify

**Round 2: T1.6 SEAL-eve** (after Implementation PFR convergent APPROVE on T1.6 deliverable execution; before T1.6 SEAL ratify):
- Dispatch SEAL-eve adversarial 2-leg with explicit "assume hidden bugs and find them" framing
- Apply MAX anti-anchoring framing at Advisor brief
- Outcome: T1.6 SEAL ratify OR additional v_impl_polish iterations before ratify

**Total SEAL-eve rounds per T1.6 cycle: 2** (per v1 PFR Codex F6 dual-SEAL-eve discipline explicit at §10 task SEAL chain).

### §6.3 Implementation review rounds (post-sub-plan-ratify; separate from §6.1-§6.2)

After sub-plan ratify, T1.6 implementation work (writing the 7 deliverables) proceeds per Mixed modality. Implementation PFR + SEAL-eve rounds at T1.6 SEAL ratify gate follow same 2-leg dispatch + Mode A discipline + MAX anti-anchoring framing.

For γ Hybrid deliverables (e)-(g): Mode A spot-verification on `general-purpose` subagent fill output BEFORE Implementation PFR; subagent-fill output is treated as orchestrator-authored for review purposes (subagent provenance disclosed in §11 revision log per cross-leg discipline).

### §6.4 Cross-leg discipline (per v1 PFR Advisor F9 verifiable instance tracking; v2 PFR Codex F1 + Advisor F2 CONVERGED 6-instance reconciliation; v4 PFR expansion to 7-instance; v6 SEAL-eve Round 1b expansion to 8-instance)

Per T1.5 sub-plan §6.4 precedent + B-C-extended cycle empirical + v1 PFR Advisor F9 verifiability requirement + v2 PFR CONVERGED 6-instance scheme + v4 PFR expansion:

- **Sub-plan drafter leg** (orchestrator M1; instance #0) ≠ **Sub-plan reviewer legs**
- **8-instance Advisor opus scheme (EXPANDED at v6 per sub-plan SEAL-eve Round 1b addition; cumulative expansion: v4 added v3 PFR slot; v6 adds Round 1b slot post-Codex F1 MEDIUM substantive new content):**
  - **#N1** — v1 PFR Advisor opus instance (12 findings; v1 PFR-rule-Y; COMPLETED)
  - **#N2** — v2 PFR Advisor opus instance (8 findings; v2 PFR-rule-Y; COMPLETED)
  - **#N3** — v3 PFR Advisor opus instance (7 findings; v3 PFR-rule-Y; COMPLETED)
  - **#N4** — v4 PFR Advisor opus instance (5 findings + 1 INFO; v4 PFR-rule-Y; COMPLETED)
  - **#N5** — Sub-plan SEAL-eve Round 1 Advisor opus instance (1 LOW; per §6.2; COMPLETED; Rule 2 VINDICATED)
  - **#N6** — Sub-plan SEAL-eve Round 1b Advisor opus instance (3 LOW + 1 INFO; v6→v7 cycle saturation reaffirmed; COMPLETED 2026-05-24; outcome at §11 v7 entry lines :1097-1104)
  - **#N7** — Implementation PFR Advisor opus instance (4 findings = 1 MEDIUM + 3 LOW; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; COMPLETED 2026-05-25; Codex parallel leg returned 8 findings = 1 BLOCKING-reframed + 3 MEDIUM + 2 LOW + 2 CONVERGENT; Rule 3 reaffirmed: Codex DIVERGED catches load-bearing — Codex caught LineageContext field-count error + consumer enum scope gap + #N6 status stale that Advisor missed; v_impl_polish ADOPTed 6 fixes per per-fix adjudication)
  - **#N8** — T1.6 SEAL-eve Round 2 adversarial Advisor opus instance (3 findings = 1 MEDIUM + 2 LOW; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; COMPLETED 2026-05-25; Codex parallel leg returned 3 findings; Rule 3 reaffirmed BOTH legs DIVERGED catches — Advisor F1 MEDIUM caught P2 fix doc-vs-code drift; Codex F1/F2 MEDIUM caught LineageContext docstring + sub-plan ledger staleness; v_impl_polish v2 ADOPTed 5 fixes P7-P11)
  - **#N9** — PFR Round 2 Advisor opus instance (5 findings = 4 LOW + 1 MEDIUM-converged; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; COMPLETED 2026-05-25; Codex parallel leg returned 8 findings = 4 actionable + 4 PASS; recursive own-finding-anchoring at v_impl_polish v2 patches caught — orchestrator P10 scope under-swept; v_impl_polish v3 ADOPTed 5 fixes Q1-Q5)
  - **#N10** — PFR Round 3 Advisor opus instance (2 LOW + 1 INFO; verdict APPROVE-FOR-T1.6-SEAL-RATIFY per recursive-pattern severity damping MEDIUM→MEDIUM→LOW; COMPLETED 2026-05-25; Codex parallel returned ADDITIONAL + 3 MEDIUM + 1 LOW DIVERGENT; per Charlie's "both APPROVE" rule → v_impl_polish v4 ADOPTed 3 MEDIUM R1+R2+R3 + R4 DEFER)
  - **#N11** — PFR Round 4 Advisor opus instance (1 MEDIUM-R1-drift + 1 LOW + 1 INFO; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; COMPLETED 2026-05-25; Codex parallel returned ADDITIONAL + 1 MEDIUM R3-ledger-sweep-incomplete DIVERGENT; recursive own-finding-anchoring pattern empirically robust at 4-iteration scale — R1 +13-line shift propagated to 7+ stale T_obs cites; v_impl_polish v5 ADOPTed 3 fixes S1+S2+S3)
  - **#N12** — PFR Round 5 Advisor opus instance (pending per Charlie register; APPROVE required before T1.6 SEAL ratify)
- **γ Hybrid subagent-fill legs**: `general-purpose` instances for §2.5 + §2.6 + §2.7 (3 distinct); per DS3 lean revision, `general-purpose` (NOT `quant-research-advisor`) avoids category violation + §6.4 cross-leg overlap with PFR reviewer leg; γ Hybrid subagent instances are SEPARATE CATEGORY from Advisor opus instance ledger above
- **Codex cross-model leg** is LOAD-BEARING per B2 standing rule + 5+ cycle cumulative empirical (R2.0 + R3.1d + §34 + R2.3 + B2 + T1.1 + T1.4 + T1.5 + T1.6 v1 + v2 + v3 + v4 PFR — extended per v4 PFR Advisor F5 ADOPT)
- **Instance separation tracked at §11 revision log per phase entry** (per v1 PFR Advisor F9 verifiability fix; per v2 PFR convergent 6-instance reconciliation)

---

## §7 SEAL gate criteria

T1.6 SEAL ratify requires:

1. All 7 deliverables PASS criteria satisfied (§2.1.3 + §2.2.3 + §2.3.3 + §2.4.3 + §2.5.4 + §2.6.4 + §2.7.4)
2. §3.1 aggregate cycle PASS criteria satisfied (including verifiable 8-instance cross-leg tracking per F9 + v2 PFR convergent reconciliation + v4 PFR expansion + v6 SEAL-eve Round 1b addition per Codex F2 LOW count-text update)
3. Sub-plan SEAL-eve adversarial 2-leg PASS at sub-plan ratify gate (§6.2 Round 1) — sub-plan v_seal entry locked
4. Implementation PFR 2-leg PASS (§6.3) — implementation work approved
5. T1.6 SEAL-eve adversarial 2-leg PASS at T1.6 SEAL ratify gate (§6.2 Round 2) — implementation work approved post-SEAL-eve
6. Full suite zero-regression preserved (2317 baseline; T1.6 documentation-only)
7. CLAUDE.md HARD CONSTRAINTS satisfied (no test failures pc7; no execution.yaml modification; no environments.yaml modification)
8. Charlie register-event for T1.6 SEAL bundle commit + push authorization

Each gate requires fresh Charlie register-event per anti-pre-emption; reviewer convergence is advisory only per [`feedback_authorization_routing.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_authorization_routing.md).

---

## §8 Anti-pre-emption explicit reminder + named sub-decisions

### §8.1 Anti-pre-emption preserve

Each T1.6 sub-decision below requires fresh Charlie register-event:
- v_n PFR adjudication fix-list ADOPT/PUSHBACK/DEFER per [`feedback_reviewer_suggestion_adjudication.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_suggestion_adjudication.md)
- Sub-plan ratify (v_n → v_final)
- Implementation work fire (after sub-plan ratify)
- γ Hybrid subagent dispatch authorization (per-deliverable; (e) / (f) / (g))
- Implementation PFR adjudication
- Sub-plan SEAL-eve adjudication + T1.6 SEAL-eve adjudication (DUAL per §6.2)
- T1.6 SEAL bundle commit + push
- B-C-extended cycle SEAL bundle drafting fire (post-T1.6 SEAL; arc-level closeout)
- §35 codification scope + text (decided at B-C-extended cycle SEAL boundary)
- Phase Marker advance + atomic `docs/phase_marker_history.md` update (per `feedback_claude_md_freshness.md`)
- Memory codification batch (post-T1.6 SEAL; 6 contributions + γ Hybrid + M1 task-class subdivision reassessment per R4' caveat)
- Successor cycles (DS-NEW (e) + DS-NEW (f) + any NEEDS-EXTENSION cycle from §2.7) — eligible-not-named; require fresh register-event to fire

### §8.2 Named load-bearing sub-decisions

| Sub-decision | Substance | Status | Provenance |
|---|---|---|---|
| **DS1** Canonical doc location for §2.1-§2.4 | Single dedicated doc (`docs/decisions/B_C_EXTENDED_V1_SCHEMA_SPEC.md`) vs split per Contract vs section within parent doc | LOCK AT T1.6 IMPLEMENTATION per Mode A grep on existing `docs/decisions/` directory structure | §2.1.1 + §2.2.1 + §2.3.1 + §2.4.1 |
| **DS2** Canonical doc location for §2.6 | Module docstring at `backtest/wf_lineage.py` + dedicated `docs/decisions/SCHEMA_VERSION_EXTENSION_PROTOCOL.md` (orchestrator lean: both) | LOCK AT T1.6 IMPLEMENTATION per existing docstring conventions | §2.6.1 |
| **DS3** Subagent_type for γ Hybrid (e)/(f)/(g) | **`general-purpose` for ALL γ Hybrid fill subagent calls** per v1 PFR Advisor F6 (lean revised from v1 ambiguous-or-mixed lean); `quant-research-advisor` reserved for review-only per its calibration | REVISED LOCK 2026-05-24 per v1 PFR adjudication; final dispatch confirmation at γ Hybrid Phase 2 fire per anti-pre-emption | §2.5.3 + §2.6.3 + §2.7.3 + §8.2 (this row) |
| **DS-NEW (e)** | Successor cycle "production-data anomaly engine→writer smoke" — DEFERRED per T1.5 sub-plan §8.2; CHARLIE-REGISTER-GATED to fire; scope-class bounded at entry register | NOT pre-committed at T1.6 SEAL; eligible-not-named | T1.5 sub-plan §8.2 |
| **DS-NEW (f)** | Successor cycle "T1.5-followup smoke artifact-writer + schema-validator chain coverage" — DEFERRED per T1.5 sub-plan §8.2; CHARLIE-REGISTER-GATED to fire; scope-class bounded at entry register | NOT pre-committed at T1.6 SEAL; eligible-not-named | T1.5 sub-plan §8.2 |
| **DS-NEW (NEEDS-EXTENSION) eligible-not-named** | Successor cycle for any NEEDS-EXTENSION consumer surfaced at §2.7 grep audit — DEFERRED to fresh Charlie register-event; scope-class bounded at entry register per anti-pre-emption + v1 PFR Codex F2 (no in-cycle scope expansion). **Count locked empirically at §2.7 Phase 1 grep audit; NO a-priori target set (per v2 PFR Advisor F4 anti-pre-emption discipline).** | NOT pre-committed at T1.6 SEAL; eligible-not-named | v1 PFR Codex F2 + v2 PFR Advisor F4 |

**NOTE on v1's DS4 removal:** v1 §8.2 had "DS4 NEEDS-EXTENSION row disposition" with two-path framing (defer vs in-cycle expand). Per v1 PFR Codex F2 anti-pre-emption fix, in-cycle expansion path is REMOVED; DS4 collapses into the eligible-not-named successor cycle framing above. Renamed "DS-NEW (NEEDS-EXTENSION) eligible-not-named" to reflect this discipline. Per v2 PFR Advisor F4 further anti-pre-emption: NO a-priori count target.

---

## §9 Cycle-pattern observations (carry-forward from cumulative empirical)

### §9.1 T1.4 5-instance + T1.5 7+ instance + T1.6 v1+v2+v3+v4 PFR Advisor own-anchoring pattern at implementation-review iteration class

Per [`feedback_advisor_own_anchoring_implementation_review.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_advisor_own_anchoring_implementation_review.md):
- Apply MAX anti-anchoring framing at Advisor briefs at all T1.6 PFR + SEAL-eve rounds
- Treat Advisor APPROVE / LOW-only as advisory signal (not authoritative cycle-final)
- Dispatch Codex 2-leg in parallel per B2 standing rule
- **v1 PFR empirical update**: Advisor opus instance #N1 at v1 PFR returned 12 findings (1 BLOCKING + 4 HIGH + 7 MEDIUM/LOW); did NOT default to APPROVE/LOW-only despite MAX-discipline brief — pattern application worked here
- **v2 PFR empirical update**: Advisor opus instance #N2 at v2 PFR returned 8 findings (0 BLOCKING + 3 MEDIUM + 5 LOW); MAX + own-finding-anchoring warning brief produced fix-substantive-leak catch (F3 at canonical-site GAPs) that own-finding-anchoring filter would otherwise mask. Codify at B-C-extended cycle SEAL boundary as additional instance.

### §9.2 T1.1 9-iteration arc + T1.5 producer-consumer asymmetry recurrence pattern

Per [`feedback_invariant_level_vs_enumeration.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_invariant_level_vs_enumeration.md):
- At any 3rd+ fix iteration on the same boundary class, ask: "Is this producer-consumer asymmetry recurrence pattern? Should we move to invariant-level closure?"
- For T1.6 documentation: less likely to apply (documentation lacks producer/consumer split that test/code modules exhibit) — but watch at §2.7 if NEEDS-EXTENSION rows show recurrence pattern across consumers

### §9.3 Cross-model leg LOAD-BEARING (B2 standing rule LOCKED 2026-05-19)

5+ cycle cumulative empirical (R2.0 + R3.1d + §34 + R2.3 + B2 + T1.1 + T1.4 + T1.5 + T1.6 v1+v2+v3+v4 PFR — extended per v4 PFR Advisor F5 ADOPT):
- Codex cross-model catches that Advisor own-anchoring blocks at SEAL-class artifact discipline
- 2-leg dispatch routine maintained at T1.6 per §6.1-§6.3
- **v1 PFR empirical update**: BOTH legs converged on F1 BLOCKING (sub-plan's wrong tuple-architecture framing vs sealed T1.2 3-tuple + CONTRACT BOUNDARY). Cross-leg diversity load-bearing in catching upstream propagation error originating from parent plan v5 §2.0.4 line 91 + handoff prompt.
- **v2 PFR empirical update**: BOTH legs converged on instance numbering ledger inconsistency (Codex F1 + Advisor F2 CONVERGED MEDIUM). Cross-leg diversity load-bearing in catching internal inconsistency at the very F9 verifiable tracking mechanism v2 ADOPTed.

### §9.4 3-layer safety architecture (Layer 1 + Layer 2 + Layer 3)

Per [`feedback_reviewer_routing_subagent_default.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_routing_subagent_default.md):
- Layer 1: Advisor own-finding-anchoring discount self-declaration (insufficient on its own)
- Layer 2: Codex cross-model leg (no authorship anchor)
- Layer 3: Orchestrator independent verification (Mode A grep/Read on every Advisor specific-claim before adoption)
- All 3 layers load-bearing at SEAL-class artifact discipline
- **v1 PFR empirical update**: Layer 3 caught upstream propagation error chain (handoff "experiment_registry.py:86,139" + `_T1X_NEW_COLUMNS` as production constant; both errors propagated from parent plan v5). Mode A Layer-3 grep at orchestrator independently verified Codex F3 + Advisor F10 substantive claims before adoption.
- **v2 PFR empirical update**: Layer 3 verified all 19+ critical claims (Codex 2/2 + Advisor 8/8 = 10 findings + cross-checks). 0 hallucinations on either leg at v2 PFR; both [VERIFIED] token regimes reliable at this PFR. Layer 2 (Codex cross-model) + Layer 3 (orchestrator independent verification) reinforce each other when Advisor #N2 own-finding-anchoring filter under MAX discipline produces substantive catches not just convergent APPROVE.

### §9.5 γ Hybrid drafting modality empirical (T1.5 sub-plan + T1.6 (e)+(f)+(g) implementation; revised per v1 PFR Advisor F12 + v3 PFR Advisor F5 N=4 harmonization)

- γ Hybrid first observed at T1.5 sub-plan drafting (orchestrator skeleton + `quant-research-advisor` subagent §2 detail-fill; T1.5 used `quant-research-advisor` which created cross-leg overlap risk; T1.6 corrects to `general-purpose` per DS3 lean revision)
- **γ Hybrid empirical N=4 (parallel to §9.6 sub-plan drafting N=4 non-collapse reasoning per v3 PFR Advisor F5):**
  - T1.5 sub-plan §2 detail-fill (1 dispatch; `quant-research-advisor` subagent; T1.5 cycle)
  - T1.6 §2.5 data_dictionary fill (1 dispatch; `general-purpose` subagent; T1.6 cycle)
  - T1.6 §2.6 extension protocol fill (1 dispatch; `general-purpose` subagent; T1.6 cycle)
  - T1.6 §2.7 consumer enumeration fill (1 dispatch; `general-purpose` subagent; T1.6 cycle)
  - Each Phase 2 dispatch has distinct subagent brief + distinct fill substrate + distinct M1-skeleton starting point — 4 distinct observations
- Codification to standing rule deferred to B-C-extended cycle SEAL boundary per Charlie R4' caveat (cycle-pattern-threshold reassessment with T1.6 empirical + DS3 lean revision data)

### §9.6 M1 orchestrator-direct modality empirical (T1.5 implementation + T1.6 deliverable (a)-(d) execution + T1.6 sub-plan v1+v2+v3+v4 drafting class; revised per v1 PFR Advisor F12 + v2 PFR Advisor F8 + v3 PFR Advisor F5 N=4 task-class subdivision)

**M1 task-class subdivision at B-C-extended cycle SEAL boundary (per v2 PFR Advisor F8 + v3 PFR Advisor F5 explicit reasoning for non-collapse):**
- **(a) Implementation class:** N=2 (T1.5 implementation work [test code precision] + T1.6 (a)-(d) deliverable execution [Contract-precision documentation])
- **(b) Sub-plan drafting class:** N=4 (T1.6 v1 initial drafting + T1.6 v2 post-v1-PFR re-drafting + T1.6 v3 post-v2-PFR re-drafting + T1.6 v4 post-v3-PFR re-drafting — each distinct M1 episode with PFR-input integration between events; treating as 4 independent observations because each event has distinct PFR-input substrate and produces distinct M1 outcome empirical)

Codification to standing rule deferred to B-C-extended cycle SEAL boundary per Charlie R4' caveat with explicit task-class subdivision per v1 PFR Advisor F12 + v2 PFR Advisor F8 + v3 PFR Advisor F5 non-collapse reasoning.

### §9.7 CONTRACT GAP / CONTRACT BOUNDARY / DESIGN INVARIANT marker discipline (CLAUDE.md L297-300; v3 PFR Advisor F2 BOUNDARY count harmonization)

- T1.5 SEAL applied CONTRACT GAP marker 2x (§2.2.3 warmup convention divergence + v_impl_polish_v2 T_obs=10 placeholder)
- T1.6 §2.6 documents extension protocol referencing **6 §2.6 extension-protocol/code-surface CONTRACT GAPs total (per sub-plan SEAL-eve Round 1 Codex F3 scope-qualification; T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21,95,126,179,379,392` are OUT-OF-SCOPE for §2.6 extension protocol per T1.5 SEAL artifact authoritative)** at exact file:line:
  - **In `backtest/wf_lineage.py` (3 GAPs):** lines 105-106 + 114-116 + 269
  - **In `backtest/artifact_schema.py` (3 GAPs):** lines 684 + 692 + 972 (added per v2 PFR Advisor F3 fix-substantive-leak catch)
- T1.6 must NOT accidentally remove or rephrase trigger conditions (sealed text is authoritative)
- **CONTRACT BOUNDARY enumeration — §2.6 EXTENSION-PROTOCOL SCOPED (3 BOUNDARYs, distinguished by category per v3 PFR Advisor F2 harmonization + scope-qualified per v4 PFR Codex F2 + Advisor F2 CONVERGED LOW):**
  - **2 cross-domain CONTRACT BOUNDARYs** at `backtest/wf_lineage.py:47-51` (module docstring) + `:95-96` (declaration comment) — forbid cross-domain tuple pollution between EVALUATION + WF + B-C-extended domains
  - **1 heavy-private-impl extraction CONTRACT BOUNDARY** (shim side) at `backtest/wf_lineage.py:535-537` — separates `backtest.artifact_schema` (B-C-extended canonical implementation) from `backtest.wf_lineage` (public surface re-export shim); distinct category from cross-domain BOUNDARYs above
- **Out-of-scope BOUNDARYs (NOT covered by §2.6 extension protocol; documented for completeness per v4 PFR Codex F2 + Advisor F2 fix-substantive-leak BOUNDARY-layer recurrence catch):**
  - `backtest/artifact_schema.py:25` — module-side sibling of the `wf_lineage.py:535-537` extraction BOUNDARY (same architectural separation, viewed from the extracted-module side)
  - `backtest/artifact_schema.py:241` — `LineageContext` co-location with `COST_ANCHOR_ID_MAPPING` (data-co-location BOUNDARY category; distinct from extension protocol)
  - `backtest/artifact_schema.py:624` — `_INDIRECT_STRICT_FIELDS` SSOT discipline (SSOT BOUNDARY category; distinct from extension protocol)
  - **Rationale for out-of-scope framing:** §2.6 documents the **schema-version extension protocol** specifically; LineageContext co-location and SSOT discipline are architectural BOUNDARYs at adjacent concerns, governed by separate documentation if needed. Future cycles may codify these in a dedicated "architectural BOUNDARY catalog" doc; not T1.6 scope.
- Watch at T1.6 for new CONTRACT GAP candidates (if any documentation references future-trigger conditions external to T1.6 scope)
- **v2 PFR cycle-pattern observation**: when heavy-private-impl extraction discipline is applied (e.g., B-C-extended block extracted from `wf_lineage.py` to `artifact_schema.py`), the extracted canonical site has its OWN CONTRACT GAPs that future implementers MUST enumerate. v1 BLOCKING F1 restructure moved the canonical site correctly but did not enumerate the canonical-site GAPs — v2 PFR Advisor F3 caught this fix-substantive-leak. Forward discipline for future cycles: heavy-private-impl extraction MUST be accompanied by canonical-site CONTRACT GAP enumeration.
- **v3 PFR cycle-pattern observation**: when canonical site is documented but **upstream-authority paths** (e.g., parent plan + sealed code docstrings) still reference legacy/superseded structure, γ Hybrid Phase 2 subagent following the stale-authority text could re-introduce the obsolete framing. v3 PFR Codex F1 caught stale docstring at `wf_lineage.py:373-375` saying "append to ACCEPTED_ARTIFACT_SCHEMA_VERSIONS" + parent plan v5 §2.0.2 line 76 analogous. Forward discipline: when sealed implementation undergoes architectural restructure (e.g., per-domain split), ALL upstream-authority references (parent plan + sealed code docstrings + cross-reference comments) MUST be audited + updated to new architecture; otherwise subagent execution can re-introduce obsolete framing through stale-authority resolution.
- **v4 PFR cycle-pattern observation (Codex F2 + Advisor F2 CONVERGED — fix-substantive-leak pattern recurrence at BOUNDARY-enumeration layer)**: when a fix (e.g., v3 PFR Advisor F2 BOUNDARY count harmonization at §9.7) enumerates markers within a single category-scope (e.g., 2 cross-domain BOUNDARYs in `wf_lineage.py` + 1 extraction BOUNDARY shim-side), but the **categorically-relevant cross-file enumeration** is incomplete (e.g., `artifact_schema.py` has module-side extraction-sibling at `:25` + 2 other-category BOUNDARYs at `:241` + `:624`), the original fix solves the immediate count problem but leaves a recurring fix-substantive-leak at the BOUNDARY-enumeration layer. Pattern empirically isomorphic to v2 PFR Advisor F3 catch at GAP-enumeration layer (which caught 3 omitted GAPs at `artifact_schema.py:684/692/972` post-v1 BLOCKING F1 restructure). Forward discipline for future cycles: when enumeration fixes happen (GAP, BOUNDARY, or other categorical markers), the fix MUST audit ALL categorically-relevant markers across ALL files in the architectural scope, NOT just the first-discovered category in the first-discovered file. The v5 §9.7 scope-qualification ("§2.6 EXTENSION-PROTOCOL SCOPED") is the v4 PFR resolution — preserves the original 3-BOUNDARY count claim by explicitly bounding scope rather than expanding enumeration.

### §9.8 §35 codification adjacency (B-C-extended cycle SEAL deferred)

- "Anti-recurrence requires invariant-level constraints, not enumeration of named cases" has 2-cycle empirical foundation (T1.1 9-iteration arc + T1.5 SEAL-eve cycle)
- "Data-preservation requirements for analytical methodology must be specified at the artifact-design boundary, not discovered at the consumption boundary" candidate per R6.1 V_SEAL post-event
- "Upstream cite chains must be Mode A grep-verified at point of use; do NOT trust without independent verification" candidate per v1 + v2 PFR empirical (propagation chain from parent plan v5 §2.0.4 line 91 → handoff prompt → v1 sub-plan; v1→v2 propagation of inner-branch line cites 319+/474+)
- "Heavy-private-impl extraction must be accompanied by canonical-site CONTRACT GAP enumeration; do NOT default to wf_lineage.py GAPs alone after extraction" candidate per v2 PFR Advisor F3 fix-substantive-leak empirical
- "Header-field-table framings (e.g., Contract 2.0.5 14-field table) must explicitly enumerate any required-adjacent fields outside the table that are validated by consumer helpers; do NOT default to assuming all required fields are in the header table" candidate per sub-plan SEAL-eve Round 1 Codex F1 MEDIUM empirical (T_obs gap traced to Contract 2.0.5 14-field framing inheritance through v1 BLOCKING F1 restructure; 4 PFR rounds + cycle saturation missed; SEAL-eve adversarial caught)
- Codification to METHODOLOGY_NOTES §35 deferred to B-C-extended cycle SEAL boundary per anti-pre-emption discipline; T1.6 sub-plan does NOT pre-codify

---

## §10 Task SEAL chain (post-ratify execution log; populated as cycle progresses)

| Step | Status | Charlie register | Commit | Notes |
|---|---|---|---|---|
| T1.6 cycle ENTRY | AUTHORIZED 2026-05-24 | "Authorize Option 1" 2026-05-24 | — | per handoff pre-authorization |
| Scope-class Path A | REGISTERED 2026-05-24 | "Path A+Mixed" 2026-05-24 | — | all 7 deliverables in scope |
| Drafting modality Mixed | REGISTERED 2026-05-24 | "Path A+Mixed" 2026-05-24 | — | M1 for (a)-(d) + γ Hybrid for (e)-(g); subagent_type = `general-purpose` per DS3 revised lean |
| Sub-plan v1 drafting fire | AUTHORIZED 2026-05-24 | bundled in Option 1 | (v1 superseded by v2 then v3) | M1 orchestrator-direct for sub-plan itself |
| Sub-plan v1 PFR-rule-Y 2-leg dispatch | COMPLETED 2026-05-24 (Advisor #N1) | "Path α" 2026-05-24 | — | CONVERGED BLOCKING-CONCERNS; 16/16 ADOPT; Codex F1-F7 + Advisor F1-F12 |
| Sub-plan v1 PFR adjudication | COMPLETED 2026-05-24 | "Authorize v2 sub-plan drafting fire" 2026-05-24 | — | 16/16 ADOPT; 0 PUSHBACK / 0 DEFER |
| Sub-plan v2 drafting fire | AUTHORIZED 2026-05-24 | "Authorize v2 sub-plan drafting fire" 2026-05-24 | (v2 superseded by v3) | M1 orchestrator-direct for sub-plan itself |
| Sub-plan v2 PFR-rule-Y 2-leg dispatch | COMPLETED 2026-05-24 (Advisor #N2) | "v2 PFR-rule-Y 2-leg dispatch" 2026-05-24 | — | APPROVE-WITH-FINDINGS both legs; 9/9 ADOPT (Codex 2 + Advisor 8 = 10 findings; 9 unique items 1 CONVERGED) |
| Sub-plan v2 PFR adjudication | COMPLETED 2026-05-24 | "Path α''" 2026-05-24 | — | 9/9 ADOPT; 0 PUSHBACK / 0 DEFER |
| Sub-plan v3 drafting fire | COMPLETED 2026-05-24 | bundled in "Path α''" | (v3 superseded by v4) | M1 orchestrator-direct for sub-plan itself |
| Sub-plan v3 PFR-rule-Y 2-leg dispatch (Advisor #N3) | COMPLETED 2026-05-24 | bundled in "Path α''" | — | APPROVE-WITH-FINDINGS both legs; Codex 2 (1 MEDIUM + 1 LOW) + Advisor 7 LOW = 9 findings; Codex F1 MEDIUM load-bearing catch (stale upstream authority paths) |
| Sub-plan v3 PFR adjudication | COMPLETED 2026-05-24 | "Path α'''" 2026-05-24 | — | 8 ADOPT + 1 PUSHBACK (Advisor F7 Mode A verified 5 failure tests correct count) |
| Sub-plan v4 drafting fire | COMPLETED 2026-05-24 | bundled in "Path α'''" | (v4 superseded by v5) | M1 orchestrator-direct via targeted Edits; 8 ADOPT fixes integrated |
| Sub-plan v4 PFR-rule-Y 2-leg dispatch (Advisor #N4) | COMPLETED 2026-05-24 | bundled in "Path α'''" | — | APPROVE-WITH-FINDINGS both legs; Codex 2 (LOW) + Advisor 5 (LOW) + 1 INFO = 7 findings (5 unique + 2 CONVERGED); 0 BLOCKING/HIGH/MEDIUM; cycle saturation empirically REAFFIRMED |
| Sub-plan v4 PFR adjudication | COMPLETED 2026-05-24 | "Path β'''''" 2026-05-24 | — | 5 ADOPT + 1 INFO (cycle saturation observation) |
| Sub-plan v5 inline cleanup | COMPLETED 2026-05-24 | bundled in "Path β'''''" | (v5 superseded by v6) | M1 orchestrator-direct via targeted Edits; 5 ADOPT fixes integrated; skip-v5-PFR-rule-Y per cycle saturation criterion + T1.5 v5→v6 SKIP precedent + R5.2 register #7 + Advisor F6 explicit recommendation |
| Sub-plan SEAL-eve adversarial 2-leg dispatch (Round 1 per §6.2 dual-SEAL-eve discipline; Advisor #N5) | COMPLETED 2026-05-24 | bundled in "Path β'''''" | — | Rule 2 EMPIRICALLY VINDICATED: Codex F1 MEDIUM (T_obs documentation gap that 4 PFR rounds + cycle saturation missed) + Codex F2/F3 LOW + Advisor F1 LOW; 4 findings total; 0 BLOCKING/HIGH |
| Sub-plan SEAL-eve Round 1 adjudication | COMPLETED 2026-05-24 | "Path B'''''" 2026-05-24 | — | 4 ADOPT; 0 PUSHBACK; per per-fix adjudication discipline |
| Sub-plan v6 inline cleanup | COMPLETED 2026-05-24 | bundled in "Path B'''''" | (v6 superseded by v7) | M1 orchestrator-direct via targeted Edits; 4 ADOPT fixes integrated including substantive Codex F1 T_obs §2.1.2 item 5b addition (~25 lines new content) |
| Sub-plan SEAL-eve Round 1b 2-leg dispatch (Advisor #N6) | COMPLETED 2026-05-24 | bundled in "Path B'''''" | — | APPROVE-WITH-FINDINGS both legs; Codex 4 LOW + Advisor 3 LOW + 1 INFO = 7 findings; 0 BLOCKING/HIGH/MEDIUM; cycle saturation EMPIRICALLY REAFFIRMED at LOW-only floor |
| Sub-plan SEAL-eve Round 1b adjudication | COMPLETED 2026-05-24 | "Path A'''''''" 2026-05-24 | — | 4 ADOPT + 1 PUSHBACK (Advisor F3 §9.8 bounded-claim deferred to B-C-extended cycle SEAL §35 codification per Advisor's own framing) |
| Sub-plan v7 inline cleanup | COMPLETED 2026-05-24 (this doc) | bundled in "Path A'''''''" | this doc | M1 orchestrator-direct via targeted Edits; 4 ADOPT mechanical fixes integrated; skip-Round-1c per cycle saturation criterion + Rule 2 satisfied (Round 1+1b both ran) |
| **Sub-plan v_seal ratify (v7 = v_final)** | **COMPLETED 2026-05-24 (this commit)** | **"Authorize sub-plan v_seal ratify" 2026-05-24** | (this commit at SEAL bundle) | sub-plan LOCKED per §8.1 anti-pre-emption; all substantive risks at sub-plan layer closed across 4 PFR + 2 SEAL-eve rounds; ~50 unique ADOPT items integrated; cycle saturation empirically validated at 2-cycle scale; Rule 2 SEAL-eve catch class empirically validated at 2-cycle scale |
| Sub-plan v_final ratify | COMPLETED 2026-05-24 | "Authorize sub-plan v_seal ratify" | `b6da611` | post-SEAL-eve Round 1; pushed to origin/main |
| Implementation work fire | AUTHORIZED + EXECUTED 2026-05-24 to 2026-05-25 | Option A authorization | (worktree; SEAL bundle pending) | sub-plan-ratify gate cleared |
| γ Hybrid §2.5 subagent dispatch (`general-purpose` sonnet) | COMPLETED 2026-05-24 | parallel dispatch authorization | (worktree) | 9 + 14 + T_obs + 3 parquet columns filled; 0 Mode A flags |
| γ Hybrid §2.6 subagent dispatch (`general-purpose` sonnet) | COMPLETED 2026-05-24 | parallel dispatch authorization | (worktree) | 10 placeholders + 6 GAP verbatim extractions; 0 Mode A flags |
| γ Hybrid §2.7 subagent dispatch (`general-purpose` opus per Charlie register) | COMPLETED 2026-05-24 | parallel dispatch authorization | (worktree) | 212 rows classified; HANDLES=181/NO-OP=31/NEEDS-EXTENSION=0; 0 Mode A flags |
| Implementation PFR 2-leg dispatch (Advisor #N7 + Codex #N7) | COMPLETED 2026-05-25 | "N1 fire PFR" 2026-05-25 | — | 12 findings total (Adv 4 + Codex 8); Rule 3 reaffirmed (Codex DIVERGED catches LineageContext field-count + scope gap + #N6 status that Advisor missed) |
| Implementation PFR adjudication + v_impl_polish v1 | COMPLETED 2026-05-25 | "authorized" 2026-05-25 | (worktree) | 6 ADOPT fixes (P1-P6) per per-fix adjudication; 110 pytest passed + 0 regressions |
| **T1.6 SEAL-eve adversarial 2-leg dispatch** (Round 2 per §6.2; Advisor #N8 + Codex #N8) | COMPLETED 2026-05-25 | "#N8 SEAL-eve Round 2 authorized" 2026-05-25 | — | 6 findings total (Adv 3 + Codex 3); Rule 3 reaffirmed (Advisor F1 caught P2 fix doc-vs-code drift via own-finding-anchoring at v_impl_polish empirical) |
| **T1.6 SEAL-eve adjudication + v_impl_polish v2** | COMPLETED 2026-05-25 | "adopt, pfr round 2 after" 2026-05-25 | (worktree) | 5 ADOPT fixes (P7-P11) per per-fix adjudication |
| **PFR Round 2 (post v_impl_polish v2; Advisor #N9 + Codex)** | COMPLETED 2026-05-25 | "adopt, pfr round 2 after" 2026-05-25 | — | Both legs ADDITIONAL-V_IMPL_POLISH-NEEDED; v_impl_polish v3 ADOPTed 5 fixes (Q1-Q5) per per-fix adjudication; recursive own-finding-anchoring pattern empirically demonstrated |
| **PFR Round 3 (post v_impl_polish v3; Advisor #N10 + Codex #N10)** | COMPLETED 2026-05-25 | (parallel dispatch under "adopt, pfr round 2 after" + Path A "strict-iterate per APPROVE rule") | — | DIVERGENT verdicts: Advisor APPROVE-with-LOW (2 LOW polish + 1 INFO; severity damping MEDIUM→MEDIUM→LOW); Codex ADDITIONAL-V_IMPL_POLISH-NEEDED (3 MEDIUM + 1 LOW); per Charlie's "both APPROVE" rule → v_impl_polish v4 ADOPTed 3 MEDIUM fixes (R1+R2+R3); R4 LOW DEFER per Codex's own caveat (D2-b semantic ownership, not direct stamp claim) + test keyword expectation risk |
| **PFR Round 4 (post v_impl_polish v4; Advisor #N11 + Codex #N11)** | COMPLETED 2026-05-25 | (parallel dispatch under "Path A") | — | DIVERGENT: Advisor ADDITIONAL (1 MED R1-drift + 1 LOW + 1 INFO); Codex ADDITIONAL (1 MED R3-ledger-incomplete); recursive own-finding-anchoring pattern empirically robust at 4-iteration scale; v_impl_polish v5 ADOPTed 3 fixes S1+S2+S3 |
| **PFR Round 5 (post v_impl_polish v5; Advisor #N12 + Codex #N12)** | PENDING — Charlie register required | — | — | per Charlie "Path A" strict-iterate; APPROVE both legs required before T1.6 SEAL ratify; v_impl_polish v5 fixes are mechanical (MD-only cite shifts) so per Advisor #N11 analysis "no new line shifts can be introduced" + expected APPROVE convergence |
| T1.6 SEAL ratify | PENDING — Charlie register required | — | — | per §7; post-PFR-Round-5 (or later if recursive ratchet continues) |
| B-C-extended cycle SEAL bundle | PENDING — Charlie register required (post-T1.6 SEAL) | — | — | arc-level closeout per Charlie pre-authorization 2026-05-23 |

**Cross-leg instance tracking per phase entry (8-instance scheme + post-sub-plan-ratify extensions):**
- #N1 — v1 PFR Advisor opus instance (12 findings; COMPLETED)
- #N2 — v2 PFR Advisor opus instance (8 findings; COMPLETED)
- #N3 — v3 PFR Advisor opus instance (7 findings; COMPLETED)
- #N4 — v4 PFR Advisor opus instance (5 findings + 1 INFO; COMPLETED; cycle saturation REAFFIRMED)
- #N5 — Sub-plan SEAL-eve Round 1 Advisor opus instance (1 LOW; COMPLETED; Rule 2 VINDICATED — Codex caught MEDIUM at same round)
- #N6 — Sub-plan SEAL-eve Round 1b Advisor opus instance (3 LOW + 1 INFO; COMPLETED 2026-05-24; cycle saturation reaffirmed at v6→v7)
- #N7 — Implementation PFR Advisor opus instance (4 findings = 1 MEDIUM + 3 LOW; COMPLETED 2026-05-25; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; Codex parallel returned 8 findings; Rule 3 reaffirmed)
- #N8 — T1.6 SEAL-eve Round 2 Advisor opus instance (3 findings = 1 MEDIUM + 2 LOW; COMPLETED 2026-05-25; verdict ADDITIONAL-V_IMPL_POLISH-NEEDED; Codex parallel returned 3 findings; Rule 3 reaffirmed BOTH legs DIVERGED catches)
- #N9 — PFR Round 2 Advisor opus instance (5 findings; ADDITIONAL-V_IMPL_POLISH-NEEDED; COMPLETED 2026-05-25; v_impl_polish v3 ADOPTed 5 fixes Q1-Q5; recursive own-finding-anchoring at v_impl_polish v2 patches empirically demonstrated)
- #N10 — PFR Round 3 Advisor opus instance (2 LOW + 1 INFO; APPROVE-FOR-T1.6-SEAL-RATIFY per severity damping; COMPLETED 2026-05-25; Codex DIVERGENT 3 MEDIUM → v_impl_polish v4 ADOPTed R1+R2+R3 per Charlie "both APPROVE" rule)
- #N11 — PFR Round 4 Advisor opus instance (1 MEDIUM-R1-drift + 1 LOW + 1 INFO; ADDITIONAL; COMPLETED 2026-05-25; Codex DIVERGENT 1 MEDIUM R3-ledger-incomplete; v_impl_polish v5 ADOPTed S1+S2+S3; recursive own-finding-anchoring pattern empirically robust at 4-iteration scale)
- #N12 — PFR Round 5 Advisor opus instance (pending per Charlie register Path A; APPROVE both legs required before T1.6 SEAL ratify)
- All distinct per §6.4 cross-leg discipline (8-instance scheme expanded at v6 + #N9-#N12 added post-v_impl_polish per Charlie Path A strict-iterate register)

---

## §11 Revision log

### v1 (2026-05-24 sub-plan drafting fire + integration; M1 orchestrator-direct)

[**v1 superseded by v2 2026-05-24 per v1 PFR-rule-Y 16/16 ADOPT; v2 superseded by v3 2026-05-24 per v2 PFR-rule-Y 9/9 ADOPT.**]

**v1 PFR-rule-Y 2-leg outcome (2026-05-24):**
- Codex: 7 findings (F1 BLOCKING + F2-F3 HIGH + F4-F7 MEDIUM)
- Advisor opus instance #N1: 12 findings (F1 BLOCKING + F2-F4 HIGH + F5-F9 MEDIUM + F10-F12 LOW); MAX anti-anchoring framing applied
- CONVERGED BLOCKING: F1 (both) — `b_c_extended_v1` tuple/routing model wrong
- Verdict: BLOCKING-CONCERNS both legs
- Mode A Layer-3 verified: 19+ critical claims; 0 hallucinations either leg
- Per-fix adjudication: 16/16 ADOPT
- Charlie register: "Authorize v2 sub-plan drafting fire" 2026-05-24

### v2 (2026-05-24 sub-plan drafting fire; M1 orchestrator-direct; 16/16 ADOPT from v1 PFR)

[**v2 superseded by v3 2026-05-24 per v2 PFR-rule-Y 9/9 ADOPT.**]

**v2 substantive corrections (16 ADOPT from v1 PFR; see prior v2 revision log table preserved at git history for full row-by-row detail):**
- BLOCKING F1 CONVERGED: §2.1/§2.6/§2.7 restructured around 3-tuple + 3-helper + CONTRACT BOUNDARY architecture
- Codex F2-F7 + Advisor F1-F12 (mix of HIGH/MEDIUM/LOW): 15 corrections including upstream propagation chain catch
- Mode A Layer-3 verified all 19 critical claims at v2 drafting

**v2 PFR-rule-Y 2-leg outcome (2026-05-24):**
- Codex: 2 findings (F1 MEDIUM Class A instance-tracking ledger divergence + F2 MEDIUM Class B future-schema migration over-prescription)
- Advisor opus instance #N2: 8 findings (F1-F3 MEDIUM + F4-F8 LOW); MAX + own-finding-anchoring warning brief applied; produced 8 findings NOT APPROVE/LOW-only (MAX discipline empirically validated)
- CONVERGED MEDIUM: Codex F1 + Advisor F2 — §3.1/§6.4/§10 instance numbering ledger inconsistency
- Verdict: APPROVE-WITH-FINDINGS both legs (no BLOCKING/HIGH)
- **Substantive fix-substantive-leak catch:** Advisor F3 (3 omitted CONTRACT GAPs in `artifact_schema.py` at lines 684/692/972 — canonical-site GAPs missed at v1 BLOCKING F1 restructure)
- Mode A Layer-3 verified: function def lines (250, 340, 654) + 3 artifact_schema.py CONTRACT GAPs + canonicalize range 91-193 + instance numbering inconsistency; 0 hallucinations either leg
- Per-fix adjudication: 9/9 unique items ADOPT (10 raw findings; Codex F1 + Advisor F2 CONVERGED counted once); 0 PUSHBACK / 0 DEFER
- Charlie register: "Path α''" 2026-05-24 (= v3 sub-plan drafting fire + v3 PFR-rule-Y 2-leg dispatch bundled)

### v3 (2026-05-24 sub-plan drafting fire; M1 orchestrator-direct; 9/9 ADOPT from v2 PFR)

**Charlie register chain:**
- v2 PFR-rule-Y 2-leg dispatch: AUTHORIZED ("v2 PFR-rule-Y 2-leg dispatch") 2026-05-24
- v2 PFR adjudication outcome: 9/9 ADOPT
- v3 sub-plan drafting fire + v3 PFR-rule-Y 2-leg dispatch: AUTHORIZED ("Path α''") 2026-05-24 (bundled)

**v3 drafting modality:** M1 orchestrator-direct (sub-plan itself; same as v1+v2).

**v3 substantive corrections (9/9 ADOPT from v2 PFR):**

| # | Source | Severity | Substantive change |
|---|---|---|---|
| 1 | Codex F1 + Advisor F2 (CONVERGED) | MEDIUM | §3.1 PASS criterion 7 + §6.4 reconciled to §10's 6-instance scheme: #N1 (v1 PFR) + #N2 (v2 PFR) + #N3 (v3 PFR) + #N4 (sub-plan SEAL-eve) + #N5 (Implementation PFR) + #N6 (T1.6 SEAL-eve); v2 collapsed v1+v2 PFR into single instance class which conflicted with §10's proper separation |
| 2 | Codex F2 | MEDIUM | §2.4.2 item 8 + §2.4.3 PASS criterion 8: reframed future-schema migration note conditionally — update `MIGRATION_COLUMNS` + relevant test mirrors only when registry columns added; do NOT prescribe `_T1X_NEW_COLUMNS` update as default future-schema action |
| 3 | Advisor F1 | MEDIUM | §2.1.2 item 4 + §2.1.3 PASS criterion 7 + §11 [VERIFIED] bullets: corrected function `def` line cites — `check_wf_semantics_or_raise` at `wf_lineage.py:250` (NOT 319+); `check_evaluation_semantics_or_raise` at `wf_lineage.py:340` (NOT 474+); inner schema_version validation branch lines documented separately as `:318-337` and `:474-488` |
| 4 | Advisor F3 (substantive new content) | MEDIUM | §2.6.2 item 3 + §2.6.4 PASS criterion 3 + §9.7: extended from 3 CONTRACT GAPs (wf_lineage.py only) to **6 §2.6 extension-protocol/code-surface CONTRACT GAPs total (per sub-plan SEAL-eve Round 1 Codex F3 scope-qualification; T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21,95,126,179,379,392` are OUT-OF-SCOPE for §2.6 extension protocol per T1.5 SEAL artifact authoritative)** (3 wf_lineage.py + 3 NEW artifact_schema.py at `:684` regime_key + `:692` execution_config_path canonicalization producer-side + `:972` sibling helper canonicalization); fix-substantive-leak from v1 BLOCKING F1 restructure (moved canonical site without enumerating canonical-site GAPs) |
| 5 | Advisor F4 | LOW | §5.2 + §8.2 DS-NEW (NEEDS-EXTENSION): reframed "NEEDS-EXTENSION row count target = 0" → "count locked empirically at §2.7 Phase 1 grep audit; NO a-priori target" per anti-pre-emption discipline (predicting target = 0 creates mild pressure to re-classify ambiguous rows) |
| 6 | Advisor F5 | LOW | §11 v3 revision log row 3 update: Codex F3 + Advisor F10 credited as CONVERGED (parallel to row 1 framing); preserves cross-leg empirical record accuracy for cycle SEAL boundary memory codification |
| 7 | Advisor F6 | LOW | §2.3.2 item 3 + §2.3.3 PASS criterion 5: canonicalize_execution_config_path() range "91-185 approximate" → "91-193 (verified Mode A; closing line `return rel.replace(os.sep, "/")` at :193)" |
| 8 | Advisor F7 | LOW | §2.1.2 item 2: reframed `ACCEPTED_ARTIFACT_SCHEMA_VERSIONS` from "backward-compat union of EVALUATION + WF domains only" → "backward-compat legacy alias holding the pre-domain-split tuple contents (currently identical contents to both per-domain tuples since EVALUATION and WF accept same `phase2c_7_1` + `phase2c_8_1`); see CONTRACT GAP at `wf_lineage.py:114-116` for the migration trigger"; aligns with sealed code framing at lines 112-113 |
| 9 | Advisor F8 | LOW | §9.6 (b) sub-plan drafting class N=3 (v1 + v2 + v3; each distinct M1 episode with PFR-input integration); explicit reasoning for non-collapse per Advisor F8 documented; reassessment at cycle SEAL boundary per Charlie R4' caveat |

**v3 provenance:**
- Drafted by orchestrator (M1) at session 2026-05-24 post-v2 PFR adjudication
- Source materials: parent plan v5 §2.6 + T1.5 sub-plan v_final + T1.5 SEAL bundle `9d9a40d` + v1 sub-plan + v1 PFR outputs + v2 sub-plan + v2 PFR Codex + Advisor outputs + Mode A Layer-3 grep verifications + Charlie register chain 2026-05-23 to 2026-05-24
- Mode A Layer-3 spot-verification at v3 drafting (verified during v2 PFR adjudication):
  - `backtest/wf_lineage.py:250` `def check_wf_semantics_or_raise` [VERIFIED via grep]
  - `backtest/wf_lineage.py:340` `def check_evaluation_semantics_or_raise` [VERIFIED via grep]
  - `backtest/artifact_schema.py:654` `def check_b_c_extended_semantics_or_raise` [VERIFIED via grep]
  - `backtest/artifact_schema.py:684` CONTRACT GAP regime_key (B-C-extended domain) [VERIFIED via grep]
  - `backtest/artifact_schema.py:692` CONTRACT GAP execution_config_path canonicalization producer-side [VERIFIED via grep]
  - `backtest/artifact_schema.py:972` CONTRACT GAP execution_config_path canonicalization sibling helper [VERIFIED via grep]
  - `backtest/artifact_schema.py:91-193` `canonicalize_execution_config_path` function range (closing line `return rel.replace(os.sep, "/")` at :193) [VERIFIED via Read]
- All v1 + v2 [VERIFIED] items preserved (see v1 + v2 revision log entries above)
- Cycle-pattern observation: v3 drafting demonstrated full 3-layer safety architecture in operation across 2 PFR rounds; M1 sub-plan drafting class now at N=3 (v1 + v2 + v3) with each version having PFR-input integration distinct from prior

**v3 status:** SUPERSEDED by v4 2026-05-24 per v3 PFR-rule-Y 8 ADOPT + 1 PUSHBACK.

### v4 (2026-05-24 sub-plan drafting fire; M1 orchestrator-direct; 8 ADOPT + 1 PUSHBACK from v3 PFR)

**Charlie register chain:**
- v3 PFR-rule-Y 2-leg dispatch: AUTHORIZED ("Path α''") 2026-05-24
- v3 PFR adjudication outcome: 8 ADOPT + 1 PUSHBACK
- v4 sub-plan drafting fire + v4 PFR-rule-Y 2-leg dispatch: AUTHORIZED ("Path α'''") 2026-05-24 (bundled)

**v4 drafting modality:** M1 orchestrator-direct (sub-plan itself; same as v1+v2+v3).

**v3 PFR-rule-Y 2-leg outcome (2026-05-24):**
- **Codex:** 2 findings (F1 MEDIUM Class A v2-fix-integration stale upstream authority paths + F2 LOW Class B new-finding column subgroup mislabeling)
- **Advisor opus instance #N3:** 7 findings (F1-F7 all LOW; MAX + 8-instance own-finding-anchoring warning brief applied; cycle saturation observation noted matching T1.5 v5 PFR-rule-Y pattern)
- **Verdict:** APPROVE-WITH-FINDINGS both legs (no BLOCKING/HIGH; 0 CONVERGED this round; trajectory toward LOW-only saturation)
- **Load-bearing catch:** Codex F1 MEDIUM (stale docstring at `wf_lineage.py:373-375` + parent plan v5 §2.0.2 line 76 could re-introduce v1 BLOCKING F1 framing through γ Hybrid Phase 2 subagent reference resolution) — Rule 3 Codex MEDIUM catches at implementation-review iteration class LOAD-BEARING when Advisor returns only LOW
- **Mode A Layer-3 verified:** Codex F1 stale docstring text at `wf_lineage.py:373-375` confirmed; Codex F2 MIGRATION_COLUMNS 3 per-bar + 5 SYS-fix-1 split at `:148-170` confirmed; Advisor F7 5 distinct failure tests at `tests/test_t1_5_registry_integrity.py` confirmed (PUSHBACK on F7)
- **Per-fix adjudication outcome:** 8 ADOPT + 1 PUSHBACK (Advisor F7 — Mode A verified 5 failure tests correct count; no fix required)
- **Charlie register:** "Path α'''" 2026-05-24 (= v4 sub-plan drafting fire + v4 PFR-rule-Y 2-leg dispatch bundled)

**v4 substantive corrections (8 ADOPT from v3 PFR):**

| # | Source | Severity | Substantive change |
|---|---|---|---|
| 1 | Codex F1 | MEDIUM | §2.6.2 item 3 + §2.6.3 Phase 2 brief + §2.6.4 PASS criteria 10+11: stale upstream authority paths flagged + qualified at canonical doc + T1.6 implementation MUST update docstring at `wf_lineage.py:373-375` to per-domain tuple wording + qualify parent plan v5 §2.0.2 line 76 cross-reference as superseded by T1.2 Sub-decision A; §2.6.3 subagent brief includes "STALE AUTHORITY DISCLOSURE" warning preventing γ Hybrid Phase 2 subagent from re-introducing v1 BLOCKING F1 framing |
| 2 | Codex F2 | LOW | §2.4.2 item 3 split 8 non-cost_anchor_id columns into 3 T1.1 FIX-B1 per-bar (`returns_per_bar_path` + `returns_per_bar_sha256` + `T_obs` at `MIGRATION_COLUMNS:155-157`) + 5 SYS-fix-1 LineageContext persistence (`regime_key` + `current_git_sha` + `execution_config_path` + `execution_config_sha256` + `parquet_data_sha256` at `:166-170`) per sealed registry subgroups |
| 3 | Advisor F1 | LOW | §2.4.2 item 3: clarified "7 reference sites across 5 distinct test methods" with exact line numbers (700, 727, 1251, 1457, 1607, 1614, 1648) |
| 4 | Advisor F2 | LOW | §9.7 BOUNDARY enumeration harmonized: 2 cross-domain CONTRACT BOUNDARYs (`:47-51` + `:95-96`) + 1 heavy-private-impl extraction CONTRACT BOUNDARY (`:535-537`) = 3 total, distinguished by category |
| 5 | Advisor F3 | LOW | §2.1.2 item 4 inner-branch line ranges symmetric: both ranges exclude comment headers; WF helper `:322-337` (was `:318-337`; lines 318-321 = comment header) + evaluation helper `:474-488` (unchanged; line 474 starts at executable schema_version block) |
| 6 | Advisor F4 | LOW | §2.6.2 item 3 `artifact_schema.py:692` description clarified direction: "deferred FROM consumer-side helper TO producer-side `engine._write_to_registry()`" (was "producer-side canonicalization deferral" which was ambiguous) |
| 7 | Advisor F5 | LOW | §5.2 + §9.5 + §9.6: γ Hybrid N=4 harmonized with §9.6 sub-plan drafting class N=4 non-collapse reasoning (T1.5 sub-plan + T1.6 (e) + (f) + (g) = 4 distinct observations; each Phase 2 dispatch distinct brief + substrate + skeleton starting point) — substantive cycle-pattern-threshold reasoning resolved at sub-plan, not deferred to memory codification |
| 8 | Advisor F6 | LOW | §11 v4 revision log: updated discipline framing to "MAX anti-anchoring framing + own-finding-anchoring warning at 8+ instance cumulative pattern (escalated framing per cycle saturation per v3 PFR brief)" |

**v3 PFR Advisor F7 PUSHBACK (Mode A verified):**
- F7 claimed §2.3.2 "5 failure cases" might collapse to 4 if bullets 4+5 (mismatched cost_anchor_id + un-mapped path) are same failure class
- Mode A Layer-3 grep on `tests/test_t1_5_registry_integrity.py` revealed 5 distinct failure tests at lines 331/387/449/481/552: `test_failure_1_duplicate_run_id` + `test_failure_2_missing_hypothesis_hash` + `test_failure_3_missing_batch_id` + `test_failure_4_cost_anchor_id_tamper_via_setattr` + `test_failure_5_unmapped_path_via_lineage_context_construction`
- v3 §2.3.2 "5 failure cases" count CORRECT as-is; no fix required
- PUSHBACK per [`feedback_reviewer_suggestion_adjudication.md`](file:///Users/yutianyang/.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_suggestion_adjudication.md): "don't adopt fixes for non-existent defects"

**v4 provenance:**
- Drafted by orchestrator (M1) at session 2026-05-24 post-v3 PFR adjudication via targeted Edit calls (more efficient than full Write at v4 since fewer substantive sections changed than v1→v2 or v2→v3)
- Source materials: v3 sub-plan + v3 PFR Codex + Advisor outputs + Mode A Layer-3 grep verifications + Charlie register chain
- Mode A Layer-3 spot-verification at v3 PFR adjudication (all VERIFIED):
  - `backtest/wf_lineage.py:373-375` stale docstring text [VERIFIED via Read]
  - `backtest/experiment_registry.py:147-170` MIGRATION_COLUMNS 1 + 3 + 5 = 9 T1.x columns [VERIFIED via Read]
  - `tests/test_t1_5_registry_integrity.py:331/387/449/481/552` 5 distinct failure tests [VERIFIED via grep]
- All v1 + v2 + v3 [VERIFIED] items preserved
- Cycle-pattern observation at v3 PFR: cycle saturation evident (BLOCKING gone post-v1; HIGH gone post-v1; MEDIUM 8→3→1; LOW stable). Trajectory matches T1.5 v5 PFR-rule-Y 4 LOW micro-findings + cycle saturation. v4 PFR is being run per Path α''' authorization (full diligence path) — empirical observation: cycle saturation may be reaffirmed at v4 if v4 PFR returns LOW-only or APPROVE.

**v4 status:** SUPERSEDED by v5 2026-05-24 per v4 PFR-rule-Y 5 ADOPT + 1 INFO; skip-v5-PFR-rule-Y per cycle saturation criterion.

### v5 (2026-05-24 sub-plan inline cleanup + skip-v5-PFR-rule-Y + SEAL-eve Round 1 dispatch; M1 orchestrator-direct; 5 ADOPT from v4 PFR)

**Charlie register chain:**
- v4 PFR-rule-Y 2-leg dispatch: AUTHORIZED ("Path α'''") 2026-05-24
- v4 PFR adjudication outcome: 5 ADOPT + 1 INFO (cycle saturation observation)
- v5 inline cleanup + skip-v5-PFR-rule-Y + sub-plan SEAL-eve Round 1 2-leg dispatch: AUTHORIZED ("Path β'''''") 2026-05-24 (bundled per cycle saturation criterion + T1.5 v5→v6 SKIP precedent + R5.2 register #7 + Advisor F6 explicit recommendation)

**v5 drafting modality:** M1 orchestrator-direct via targeted Edit calls (5 ADOPT fixes localized; full-Write modality not needed at this saturation iteration).

**v4 PFR-rule-Y 2-leg outcome (2026-05-24):**
- **Codex:** 2 findings (F1 LOW Class A 2 stale-text sites at line 28 + line 665 + F2 LOW Class B §9.7 BOUNDARY count under-qualified)
- **Advisor opus instance #N4:** 5 LOW + 1 INFO (F1 §7 stale 6-instance + F2 §9.7 BOUNDARY enumeration incomplete vs sealed sources + F3 5→6 methods arithmetic + F4 Step 3b vs Step 4 ambiguity + F5 stale "T1.6 v1+v2 PFR" counter at 3 sites + F6 cycle saturation INFO recommendation skip v5 PFR)
- **Verdict:** APPROVE-WITH-FINDINGS both legs (0 BLOCKING / 0 HIGH / 0 MEDIUM; cycle saturation EMPIRICALLY REAFFIRMED — trajectory v1→v4: BLOCKING 1→0→0→0; HIGH 4+→0→0→0; MEDIUM 8→3→1→0; LOW 6→7→8→7)
- **2 CONVERGED LOW:** (Codex F1 + Advisor F1 — stale 6-instance + N=3 references) + (Codex F2 + Advisor F2 — §9.7 BOUNDARY count incomplete vs sealed sources)
- **3 unique Advisor LOW:** F3 (5→6 methods) + F4 (Step 3b/4 ambiguity) + F5 (stale counter at 3 sites)
- **Mode A Layer-3 verified:** all 6 critical claims (artifact_schema.py 3 additional BOUNDARYs at :25/:241/:624 + test method boundaries at :1582+:1621 [v6 SEAL-eve Round 1b correction: original v4 verification cited :1622 docstring line; corrected to :1621 def line] + stale text sites at v4 line 28+665); 0 hallucinations either leg
- **Per-fix adjudication outcome:** 5 unique ADOPT items (2 CONVERGED + 3 unique) + 1 INFO (cycle saturation observation)
- **Charlie register:** "Path β'''''" 2026-05-24 (= v5 inline cleanup + skip-v5-PFR-rule-Y + sub-plan SEAL-eve Round 1 dispatch bundled)

**v5 substantive corrections (5 ADOPT from v4 PFR):**

| # | Source | Severity | Substantive change |
|---|---|---|---|
| 1 | Codex F1 + Advisor F1 (CONVERGED) | LOW | 2 stale-text sites: v3→v4 summary line ~28 "§9.6 N=3" → "§9.6 N=4" (γ Hybrid harmonized with M1 sub-plan drafting N=4); §7 SEAL gate criterion 2 line ~665 "verifiable 6-instance" → "verifiable 7-instance" (per current Advisor opus instance ledger #N1-#N7) |
| 2 | Codex F2 + Advisor F2 (CONVERGED) | LOW | §9.7 BOUNDARY enumeration scope-qualified: original 3-BOUNDARY count preserved + scope explicitly bound to "§2.6 EXTENSION-PROTOCOL SCOPED" + out-of-scope BOUNDARYs documented for completeness (`artifact_schema.py:25` module-side extraction sibling + `:241` LineageContext co-location + `:624` SSOT discipline; with rationale that these are adjacent architectural concerns, not extension protocol; future cycles may codify in dedicated catalog) |
| 3 | Advisor F3 | LOW | §2.4.2 item 3 arithmetic correction: "5 distinct test methods" → "**6 distinct test methods**" (sites at lines 1607+1614 both within `test_pre_t1_3_db_state_migration_adds_columns_preserves_rows` at line 1582; remaining 5 sites in 5 distinct methods at 700/727/1251/1457/1648 (latter within `test_partial_migration_state_adds_remaining_columns` at line 1621); total 6 methods × 7 sites) |
| 4 | Advisor F4 | LOW | §2.6.2 step-count renumbered for unambiguous structure: Step 3 now contains integral sub-steps 3a (tuple-append) + 3b (helper-add), both mandatory; Step 4 = optional heavy-private-impl extraction; PASS criterion 1 updated to "Step 1 + Step 2 + Step 3 [with integral sub-steps 3a + 3b, both mandatory] + optional Step 4"; removes Step 3b vs Step 4 ambiguity |
| 5 | Advisor F5 | LOW | §6.4 + §9.1 + §9.3 stale "T1.6 v1+v2 PFR" counter updated to "T1.6 v1+v2+v3+v4 PFR" at 3 sites; §9.3 LOAD-BEARING evidence list includes v3 PFR Codex F1 catch + v4 PFR Codex F2+Advisor F2 CONVERGED catch as additional empirical instances |

**v4 PFR Advisor F6 INFO (cycle saturation observation; NOT ADOPT/PUSHBACK item):**
- Advisor #N4 explicit observation: cycle saturation reaffirmed; recommend skip v5 PFR-rule-Y per T1.5 v6 SKIP precedent + R5.2 register #7
- Per Charlie register "Path β'''''" — skip-v5-PFR-rule-Y AUTHORIZED; proceed directly to sub-plan SEAL-eve Round 1
- Codification of cycle saturation pattern empirical: 4 PFR rounds with monotonic severity decay (BLOCKING gone post-v1; HIGH gone post-v1; MEDIUM 8→3→1→0; LOW stable around 6-8); LOW-only floor at v4 with both legs CONVERGED on saturation framing

**v5 provenance:**
- Drafted by orchestrator (M1) at session 2026-05-24 post-v4 PFR adjudication via targeted Edit calls (v4 modality lesson: Edit-call risk = stale text in unchanged sections; v5 Edits applied with explicit verification of cross-section consistency at stale-counter sites)
- Source materials: v4 sub-plan + v4 PFR Codex + Advisor outputs + Mode A Layer-3 grep verifications + Charlie register chain
- Mode A Layer-3 spot-verification at v4 PFR adjudication (all VERIFIED):
  - `backtest/artifact_schema.py:25 + :241 + :624` 3 additional CONTRACT BOUNDARY markers [VERIFIED via grep]
  - `tests/test_t1_4_backward_compat.py:1582 + :1621` test method boundaries [v6 SEAL-eve Round 1b Codex F4 + Advisor F2 CONVERGED LOW correction: original v4 verification cited :1622 docstring line; corrected to :1621 def line]; sites at 1607+1614 within method at :1582; site at 1648 within method at :1621; total 6 distinct methods × 7 sites [VERIFIED via Read + awk]
  - v4 sub-plan line ~28 "§9.6 N=3" + line ~665 "6-instance" stale text [VERIFIED via own-Read of v4]
- All v1 + v2 + v3 + v4 [VERIFIED] items preserved
- Cycle-pattern observation at v4 PFR: cycle saturation evident (4 PFR rounds + LOW-only floor + both legs CONVERGED). Skip-v5-PFR-rule-Y is the cycle-saturation-canonical path per T1.5 v5→v6 SKIP precedent + R5.2 register #7 + Advisor F6 explicit recommendation. SEAL-eve Round 1 adversarial preserves catch capacity per Rule 2 OPERATIONALLY REQUIRED.
- Forward discipline codified at §9.7 v4 PFR cycle-pattern observation: when enumeration fixes happen (GAP, BOUNDARY, or other categorical markers), the fix MUST audit ALL categorically-relevant markers across ALL files in the architectural scope; OR explicitly scope-qualify the enumeration to bound the original claim. v5 §9.7 demonstrates scope-qualification as cycle-saturation-appropriate resolution.

**v5 status:** DRAFTING COMPLETE (5 ADOPT inline cleanup); sub-plan SEAL-eve Round 1 2-leg dispatch firing this turn per "Path β'''''" Charlie register bundled authorization. SEAL-eve briefs include MAX anti-anchoring framing + own-finding-anchoring warning at 10+ instance cumulative pattern + explicit "assume hidden bugs and find them" Rule 2 SEAL-eve discipline. Next gate after SEAL-eve return: per-fix adjudication of SEAL-eve findings (requires fresh Charlie register-event per anti-pre-emption).

**Sub-plan SEAL-eve Round 1 dispatch readiness:** sub-plan v5 ready for 2-leg dispatch (Codex cross-model + Advisor opus instance #N5) per §6.2 Round 1 + Path β''''' authorization. Per cycle saturation context, SEAL-eve catch expectation: if SEAL-eve returns 0 findings or LOW-only, v_seal ratify is well-precedented; if SEAL-eve catches HIGH or BLOCKING (analog of T1.5 SEAL-eve v1+v2 catching HIGH that PFR convergent APPROVE missed), v_impl_polish iterations + possibly extended SEAL-eve required.

**v5 status:** SUPERSEDED by v6 2026-05-24 per sub-plan SEAL-eve Round 1 4 ADOPT (Rule 2 SEAL-eve VINDICATED — Codex F1 MEDIUM T_obs gap caught).

### v6 (2026-05-24 sub-plan inline cleanup + SEAL-eve Round 1b dispatch; M1 orchestrator-direct; 4 ADOPT from sub-plan SEAL-eve Round 1)

**Charlie register chain:**
- Sub-plan SEAL-eve Round 1 2-leg dispatch: AUTHORIZED ("Path β'''''") 2026-05-24
- Sub-plan SEAL-eve Round 1 adjudication outcome: 4 ADOPT (Codex F1 MEDIUM T_obs gap + Codex F2/F3 LOW + Advisor F1 LOW)
- v6 inline cleanup + sub-plan SEAL-eve Round 1b 2-leg dispatch: AUTHORIZED ("Path B'''''") 2026-05-24 (bundled; per Codex F1 substantive new content addition warranting another SEAL-eve adversarial round)

**v6 drafting modality:** M1 orchestrator-direct via targeted Edit calls.

**Sub-plan SEAL-eve Round 1 2-leg outcome (2026-05-24):**
- **Codex:** 3 findings (F1 MEDIUM Class 1 hidden-defect T_obs documentation gap + F2 LOW Class 2 status label inconsistency §3.1/§6.4 vs §10 + F3 LOW Class 1 6 CONTRACT GAPs scope ambiguity)
- **Advisor opus instance #N5:** 1 LOW finding (F1 Class 1 1622→1621 off-by-one citation propagation at 2 sites)
- **Verdict:** APPROVE-WITH-FINDINGS both legs (0 BLOCKING / 0 HIGH; 1 MEDIUM + 3 LOW; 0 CONVERGED)
- **LOAD-BEARING SEAL-eve catch class:** Codex F1 MEDIUM (T_obs gap traced to Contract 2.0.5 14-field framing inheritance through v1 BLOCKING F1 restructure; 4 PFR rounds + cycle saturation missed; SEAL-eve adversarial caught) — **Rule 2 SEAL-eve OPERATIONALLY REQUIRED EMPIRICALLY VINDICATED at T1.6 cycle** (cumulative empirical: T1.5 SEAL-eve v1+v2 caught HIGH at implementation; T1.6 sub-plan SEAL-eve Round 1 caught MEDIUM at sub-plan layer)
- **Cross-model leg LOAD-BEARING reaffirmed**: Codex caught 3 findings (F1 MEDIUM + 2 LOW); Advisor caught 1 LOW; 0 CONVERGED; Codex's structural-coverage-gap probe class caught the T_obs gap that Advisor own-finding-anchoring at SEAL-eve gate would mask
- **Mode A Layer-3 verified all 4 critical claims**: 0 hallucinations either leg at SEAL-eve gate
- **Per-fix adjudication outcome:** 4 ADOPT (0 PUSHBACK / 0 DEFER)
- **Charlie register:** "Path B'''''" 2026-05-24 (= v6 inline cleanup + sub-plan SEAL-eve Round 1b 2-leg dispatch bundled per Codex F1 substantive new content)

**v6 substantive corrections (4 ADOPT from sub-plan SEAL-eve Round 1):**

| # | Source | Severity | Substantive change |
|---|---|---|---|
| 1 | Codex F1 | MEDIUM | §2.1.2 added item 5b: T_obs as required-adjacent 15th field; sealed declaration text at `artifact_schema.py:307-309` verbatim + sealed validation at `:802-807` verbatim + type (int positive, bool explicitly rejected) + semantic (per-bar return count) + why-required-adjacent-vs-in-14-table reasoning (T_obs is per-bar-artifact-content-shape attribute, not header metadata) + registry linkage cross-ref to `MIGRATION_COLUMNS:155-157` + producer/consumer validation contract; §2.1.3 added PASS criterion 12 for T_obs documentation requirement |
| 2 | Codex F2 | LOW | §3.1 PASS criterion 7 + §6.4 instance scheme: #N4 status updated to "COMPLETED" + #N5 status updated to "Sub-plan SEAL-eve Round 1 (1 LOW; COMPLETED)" + #N6 added as "Sub-plan SEAL-eve Round 1b (current dispatch per Path B''''')"; consistent with §10 |
| 3 | Codex F3 | LOW | §2.6.2 + §2.6.4 + §9.7 "6 CONTRACT GAPs total" scope-qualified to "6 §2.6 extension-protocol/code-surface CONTRACT GAPs total"; explicit note that T1.5 test-side CONTRACT GAP markers at `tests/test_t1_5_smoke_end_to_end.py:21/95/126/179/379/392` are OUT-OF-SCOPE for §2.6 extension protocol (T1.5 SEAL artifact authoritative) |
| 4 | Advisor F1 | LOW | §2.4.2 item 3 + §11 v5 row 3: "at line 1622" → "at line 1621" at 2 sites; `def test_partial_migration_state_adds_remaining_columns` at line 1621 (line 1622 = docstring) per Mode A grep |

**v6 provenance:**
- Drafted by orchestrator (M1) at session 2026-05-24 post-sub-plan SEAL-eve Round 1 adjudication via targeted Edit calls
- Source materials: v5 sub-plan + SEAL-eve Round 1 Codex + Advisor #N5 outputs + Mode A Layer-3 grep verifications + Charlie register chain
- Mode A Layer-3 spot-verification at SEAL-eve Round 1 adjudication (all VERIFIED):
  - `backtest/artifact_schema.py:307-309` T_obs declaration "not in 14-field header table but is required" [VERIFIED via Read]
  - `backtest/artifact_schema.py:815-820` T_obs validation enforcement [VERIFIED via Read]
  - `tests/test_t1_4_backward_compat.py:1621` `def test_partial_migration_state_adds_remaining_columns` [VERIFIED via grep]
  - `tests/test_t1_5_smoke_end_to_end.py:21/95/126/179/379/392` 6 CONTRACT GAP markers (test-side; out-of-scope) [VERIFIED via grep]
- All v1-v5 [VERIFIED] items preserved
- Cycle-pattern observation at SEAL-eve Round 1: **Rule 2 SEAL-eve discipline VINDICATED YET AGAIN** — Codex F1 MEDIUM T_obs gap is the substantive structural coverage gap that 4 PFR rounds + cycle saturation framing structurally missed. Pattern empirically robust at 2-cycle scale (T1.5 + T1.6). Cycle-pattern threshold for Rule 2 SEAL-eve OPERATIONALLY REQUIRED at sub-plan + implementation gates is now 2-cycle-substantiated.
- Upstream propagation chain catch (4th cycle empirical instance): T_obs gap inherits from Contract 2.0.5 14-field framing at parent plan v5 §2.0.5 (header table excludes T_obs by design — T_obs is per-bar-artifact-content-shape attribute, not header metadata). v1 BLOCKING F1 restructure inherited the 14-field framing without explicit T_obs-as-required-adjacent acknowledgment. Forward discipline codified at §9.8: "Header-field-table framings must explicitly enumerate required-adjacent fields outside the table; do NOT default to assuming all required fields are in header table."

**v6 status:** DRAFTING COMPLETE (4 ADOPT inline cleanup); sub-plan SEAL-eve Round 1b 2-leg dispatch firing this turn per "Path B'''''" Charlie register bundled authorization. SEAL-eve Round 1b scope: verify Codex F1 T_obs §2.1.2 item 5b substantive new content + 3 mechanical fixes (Codex F2/F3 + Advisor F1) integrated correctly + look for hidden defects at the T_obs section addition.

**Sub-plan SEAL-eve Round 1b dispatch readiness:** sub-plan v6 ready for 2-leg dispatch (Codex cross-model + Advisor opus instance #N6) per Path B''''' authorization. Brief includes MAX anti-anchoring framing + own-finding-anchoring warning at 11+ instance cumulative pattern + explicit "verify T_obs section accuracy + assume hidden bugs" SEAL-eve discipline. If Round 1b returns clean or LOW-only, sub-plan v_seal ratify gate next; if Round 1b catches HIGH/BLOCKING at T_obs section, additional v_impl_polish iteration warranted.

**v6 status:** SUPERSEDED by v7 2026-05-24 per sub-plan SEAL-eve Round 1b 4 ADOPT + 1 PUSHBACK (cycle saturation EMPIRICALLY REAFFIRMED at LOW-only floor; skip-Round-1c authorized per Path A''''''').

### v7 (2026-05-24 sub-plan inline cleanup + skip-Round-1c + sub-plan v_seal ratify pending; M1 orchestrator-direct; 4 ADOPT + 1 PUSHBACK from sub-plan SEAL-eve Round 1b)

**Charlie register chain:**
- Sub-plan SEAL-eve Round 1b 2-leg dispatch: AUTHORIZED ("Path B'''''") 2026-05-24
- Sub-plan SEAL-eve Round 1b adjudication outcome: 4 ADOPT + 1 PUSHBACK (Advisor F3 §9.8 bounded-claim discipline deferred to §35 codification cycle per Advisor's own framing)
- v7 inline cleanup + skip-Round-1c per cycle saturation criterion + Rule 2 satisfied: AUTHORIZED ("Path A'''''''") 2026-05-24
- Sub-plan v_seal ratify (v7 = v_final): PENDING separate Charlie register-event required per anti-pre-emption

**v7 drafting modality:** M1 orchestrator-direct via targeted Edit calls (4 mechanical literal application fixes; no substantive new content).

**Sub-plan SEAL-eve Round 1b 2-leg outcome (2026-05-24):**
- **Codex:** 4 LOW findings (F1 T_obs cite range :802-807 should extend to :802-814; F2 "7-instance" text not updated to "8-instance"; F3 §2.6.2 "SIX CONTRACT GAPs total" heading missed scope-qualification; F4 §11 historical block stale 1622 cite)
- **Advisor opus instance #N6:** 3 LOW + 1 INFO (F1 same as Codex F1 CONVERGED on cite range; F2 same as Codex F4 CONVERGED on stale 1622 + adds line 1000 site; F3 §9.8 bounded-claim discipline PUSHBACK candidate; F4 INFO cycle saturation reaffirmed)
- **2 CONVERGED LOW:** F1 cite range :802-807→:802-814 + §11 stale 1622→1621
- **3 UNIQUE LOW:** Codex F2 (7-instance text); Codex F3 (§2.6.2 heading); Advisor F3 (§9.8 bounded-claim)
- **Verdict:** APPROVE-WITH-FINDINGS both legs (0 BLOCKING / 0 HIGH / 0 MEDIUM)
- **Mode A Layer-3 verified critical claims:** positivity branch at `artifact_schema.py:811-814` confirmed; §11 stale 1622 sites at lines 1000+1024 confirmed via orchestrator own-knowledge of v6 edits; 0 hallucinations either leg
- **Per-fix adjudication outcome:** 4 ADOPT + 1 PUSHBACK (Advisor F3 §9.8 bounded-claim discipline per Advisor's own recommendation deferred to B-C-extended cycle SEAL boundary §35 codification cycle; T1.6 §9.8 is candidate list not binding-rule list; wording refinable at SEAL boundary)
- **Charlie register:** "Path A'''''''" 2026-05-24 (= v7 inline cleanup + skip-Round-1c + sub-plan v_seal ratify gate pending)

**v7 substantive corrections (4 ADOPT from sub-plan SEAL-eve Round 1b):**

| # | Source | Severity | Substantive change |
|---|---|---|---|
| 1 | Codex F1 + Advisor F1 (CONVERGED) | LOW | §2.1.2 item 5b T_obs sealed validation cite extended from `artifact_schema.py:815-820` to `:802-814` (covers full T_obs validation block: presence at :803-804 + type at :806-807 + positivity at :811-814); §2.6.4 PASS criterion 12 same extension |
| 2 | Codex F2 | LOW | §3.1 PASS criterion 7 + §6.4 body text count "7-instance scheme" → "8-instance scheme" (instance scheme expanded at v6 per Round 1b addition; ledger entries #N1-#N8 were correct but count claim text lagged) |
| 3 | Codex F3 | LOW | §2.6.2 heading "SIX CONTRACT GAPs total documented" → "6 §2.6 extension-protocol/code-surface CONTRACT GAPs total documented" + explicit out-of-scope T1.5 test-side markers note (matches §2.6.4 + §9.7 scope-qualification applied at v6 via replace-all that missed §2.6.2 heading's spelled-out "SIX") |
| 4 | Codex F4 + Advisor F2 (CONVERGED) | LOW | §11 historical v4 PFR Mode A Layer-3 verification block at lines 1000 + 1024 stale "1622" → "1621 [v6 SEAL-eve Round 1b correction: original v4 verification cited :1622 docstring line; corrected to :1621 def line]"; inline footnote per Advisor F2 option (b) preserves historical record + corrects factual cite |

**Sub-plan SEAL-eve Round 1b Advisor F3 PUSHBACK (per Advisor's own framing):**
- F3 LOW: §9.8 candidate codification item for T_obs lacks scope-bounding language per CLAUDE.md §8 bounded-claim discipline
- **PUSHBACK rationale (Advisor's own framing):** "the codification is DEFERRED to B-C-extended cycle SEAL boundary per §9.8 explicit 'Codification to METHODOLOGY_NOTES §35 deferred'; T1.6 §9.8 is a candidate list not a binding-rule list; the wording is refinable at the SEAL boundary codification cycle"
- Deferred to B-C-extended cycle SEAL §35 codification per Advisor's own recommendation
- Carry-forward to §11 v7 + v_seal entries for cycle SEAL boundary memory codification

**v7 provenance:**
- Drafted by orchestrator (M1) at session 2026-05-24 post-sub-plan SEAL-eve Round 1b adjudication via targeted Edit calls (all 4 ADOPT fixes mechanical literal application)
- Source materials: v6 sub-plan + SEAL-eve Round 1b Codex + Advisor #N6 outputs + Mode A Layer-3 verifications + Charlie register chain
- Mode A Layer-3 spot-verification at v7 Edits:
  - `backtest/artifact_schema.py:811-814` positivity branch confirmed via Advisor verbatim quote of sealed code + orchestrator independent check (full validation block spans :802-814) [VERIFIED]
  - §11 stale 1622 sites at v6 file lines ~1000 + ~1024 confirmed via orchestrator own-knowledge of v6 Edit history + grep [VERIFIED]
  - §2.6.2 heading "SIX" spelled-out confirmed missed by v6 replace_all "6 CONTRACT GAPs total" [VERIFIED via own-knowledge of v6 Edits]
  - §3.1 + §6.4 body text "7-instance" stale residual confirmed [VERIFIED via own-knowledge]
- All v1-v6 [VERIFIED] items preserved
- Cycle-pattern observation at SEAL-eve Round 1b: cycle saturation EMPIRICALLY REAFFIRMED at LOW-only floor (0 MEDIUM at Round 1b post-Codex F1 T_obs MEDIUM resolution at v6); T_obs section structurally clean per both reviewer legs; Rule 2 SEAL-eve discipline fully satisfied via Round 1 (caught MEDIUM) + Round 1b (confirmed cleanup); skip-Round-1c canonical per cycle saturation + R5.2 register #7 + T1.5 v5→v6 SKIP precedent + own-cycle Path β''''' precedent.
- Edit-call modality empirical update: v6 replace_all on "6 CONTRACT GAPs total" missed the §2.6.2 heading "SIX" spelled-out variant. Forward discipline: when applying replace_all, audit for spelled-out vs digit variants (e.g., "SIX" vs "6" vs "six") to prevent leak.

**v7 status:** DRAFTING COMPLETE (4 ADOPT inline cleanup); sub-plan ready for v_seal ratify (v7 = v_final) pending separate Charlie register-event per anti-pre-emption. After v_seal ratify: cycle progresses to Implementation work fire per §10 task SEAL chain (next register-event = Charlie authorize Implementation work fire + γ Hybrid Phase 2 subagent dispatches for (e)/(f)/(g) deliverables).

**Sub-plan v_seal ratify readiness:** sub-plan v7 ready for v_final ratify. All substantive risks at sub-plan layer closed across:
- 4 PFR rounds (v1→v4; trajectory BLOCKING gone post-v1; HIGH gone post-v1; MEDIUM 8→3→1→0)
- 2 SEAL-eve rounds (Round 1 caught MEDIUM Codex F1 T_obs gap; Round 1b confirmed LOW-only saturation)
- 31 unique substantive ADOPT items integrated across v1-v7 (16 v1 + 9 v2 + 8 v3 + 5 v4 + 4 SEAL-eve Round 1 + 4 SEAL-eve Round 1b; minus 1 PUSHBACK each at v3 Advisor F7 + Round 1b Advisor F3)
- Mode A Layer-3 verifications applied at every PFR + SEAL-eve adjudication boundary (0 hallucinations across 12+ Advisor + 6+ Codex dispatches post-/agents-fix opus regime)
- Cycle saturation pattern empirically validated at 2-cycle scale (T1.5 v5 + T1.6 v4) with Rule 2 SEAL-eve catch class empirically validated at 2-cycle scale (T1.5 v1+v2 caught HIGH at implementation; T1.6 sub-plan SEAL-eve Round 1 caught MEDIUM at sub-plan)

### v_seal (2026-05-24 sub-plan v_final RATIFIED per Charlie "Authorize sub-plan v_seal ratify" register)

**Charlie register:** "Authorize sub-plan v_seal ratify" 2026-05-24 — sub-plan LOCKED as v_final per §8.1 anti-pre-emption discipline.

**v_seal scope:** sub-plan content frozen at v7; no further sub-plan modifications without fresh Charlie register-event per anti-pre-emption. Cycle progresses to Implementation work fire (next register-event) per §10 task SEAL chain.

**Cumulative sub-plan cycle empirical (T1.6 sub-plan ratify; cycle SEAL boundary memory codification carry-forward):**
- 7 sub-plan versions (v1 → v7); cumulative ~1100 lines at v_final
- 4 PFR-rule-Y rounds + 2 SEAL-eve rounds = 6 adversarial rounds
- ~12+ Advisor opus instances (#N1-#N6 completed; #N7+#N8 pending at Implementation PFR + T1.6 SEAL-eve Round 2) + ~6 Codex dispatches = ~18+ reviewer dispatches
- Mode A Layer-3 verifications: ~30+ critical claim verifications across cycle; 0 hallucinations either leg post-/agents-fix opus regime
- 8-instance Advisor opus ledger (#N1-#N8) per §6.4 cross-leg discipline
- 31 unique ADOPT items + 2 PUSHBACK items
- Rule 2 SEAL-eve VINDICATED (SEAL-eve Round 1 caught Codex F1 MEDIUM T_obs gap that 4 PFR + cycle saturation missed)
- Cycle saturation pattern + Rule 2 catch class empirically validated at 2-cycle scale (T1.5 + T1.6 sub-plan)
- Upstream propagation chain pattern empirically validated at 4-instance scale (parent plan v5 §2.0.4 line 91 + handoff prompt + `_T1X_NEW_COLUMNS` as production + inner-branch line cites + T_obs framing inheritance from Contract 2.0.5 14-field table)
- Edit-call modality empirical: replace_all leaks at spelled-out variants ("SIX" vs "6"); replace_all at "at line 1622" hits body sites but not §11 historical block sites; forward discipline: audit spelled-out + historical-block variants at v_n→v_n+1 edits
- Empirical contributions queued for B-C-extended cycle SEAL boundary memory codification per Charlie R4' caveat (7+ items: γ Hybrid + M1 N=4 + Rule 2 vindication + cross-model leg LOAD-BEARING + 3-layer safety architecture Layer 3 + upstream propagation chain pattern + cycle saturation criterion + Edit-call modality leak pattern + §35 codification candidates at §9.8 including T_obs header-table framing + others)

**Next register-event:** Implementation work fire per Charlie register at separate boundary (per anti-pre-emption). After Implementation work + Implementation PFR + T1.6 SEAL-eve Round 2 + T1.6 SEAL ratify → cycle progresses to B-C-extended cycle SEAL bundle (arc-level closeout per Charlie pre-authorization 2026-05-23).

**Addendum (post-sub-plan-ratify Implementation cycle; updated 2026-05-25):** The §11 v_seal entry above records reviewer ledger and cumulative empirical through sub-plan v_final ratify gate (#N1-#N6 completed; #N7-#N8 enumerated as pending). Post-sub-plan-ratify Implementation cycle outcomes (#N7 Implementation PFR + #N8 T1.6 SEAL-eve Round 2 + #N9 PFR Round 2 + #N10 PFR Round 3 + #N11 PFR Round 4 if dispatched) are recorded live at **§10 task SEAL chain** above. §11 is intentionally frozen at sub-plan ratify gate per `feedback_claude_md_freshness.md`-analog discipline; cross-cycle empirical contributions from #N7+#N8+#N9+#N10+#N11 + the recursive own-finding-anchoring at v_impl_polish iterations pattern (now empirically robust at 3-cycle scale v1→v2→v3→v4) will be batch-codified at T1.6 SEAL artifact (separate cycle SEAL commit) + B-C-extended cycle SEAL boundary memory codification per Charlie R4' caveat.

---

**END OF SUB-PLAN v7 v_final RATIFIED 2026-05-24**
