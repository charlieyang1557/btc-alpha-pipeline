# R4_1_PHASE_B_VENUE_COMMITMENT_NOTE.md

**Canonical artifact for Phase B Pre-Sequence Roadmap V3 register-event R4.1 (Template B Bucket-1 investigation note; structural analog to PHASE5_1_COST_MODEL_INVESTIGATION_NOTE, PHASE5_2_VENUE_RECONCILIATION_NOTE, PHASE5_A_CLARIFICATION_NOTE, R1_2_IS_OOS_RANK_CORRELATION_NOTE, R3_1A_VENUE_INFRASTRUCTURE_NOTE).**

**Status:** V3 REVISED-POST-PFR-RULE-Y (current; pre-V_SEAL) for combined V1 reviewer round + PFR rule-Y round adjudicated; awaiting Charlie V_SEAL register. Combined scope per X6' covers R4.1 SEAL artifact + R3.1a §12 Errata + `config/execution.yaml` line 42 patch as a single atomic SEAL bundle.

---

## §0 Cycle Metadata

**Cycle type:** Template B Bucket-1 single-deliverable formal commitment note (cycle shape α). Cadence shape A (1-cadence: formal venue commitment per Charlie register + atomic E.3 errata trigger on R3.1a §7.2 inverted trigger logic; no analytical computation in this cycle).

**Cycle boundary:** R4.1 register-event from Phase B Pre-Sequence Roadmap V3 Tier 4. Cycle entry authorized by Charlie at register 2026-05-18 ("fire, authorized R3.1a E.3 upgrade trigger and Tier 4 R4.1; reviewer dispatch after").

**Trigger:** Phase 5.2 §6.4 named Branch.A/B/C/D venue commitment options without pre-naming the answer ("This note proposes the STRUCTURE for adjudication, not the answer. No directional claim is made about which Path 1/2/3 outcomes are 'likely' within any branch — each sub-decision within each branch is independently Charlie-determined at the venue commitment register-event or downstream register-events"). R3.1a sealed the venue-infrastructure formalization under Branch.A *working assumption* with the surface tension between SPOT data layer and futures-attribution comment preserved per §5.5 disposition rationale, and named R4.1 venue commitment as eligible-not-named for separate Charlie register-event boundary. Charlie's R4.1 fire formally commits Branch.A (SPOT execution), elevating working-assumption status to formal commitment per Phase 5.2 §6.4 corrected semantics, and satisfying R3.1a §7.2 inverted trigger logic for E.3 errata eligibility on `config/execution.yaml` line 42.

**Scope binding (pre-bound under Charlie register chain through "ratify all"; sealed before draft):**

| Sub-decision | Locked answer | Charlie register |
|---|---|---|
| 1 | **Formal Branch.A commitment** = SPOT execution per Phase 5.2 §6.4 (canonical data layer = Binance SPOT 1h OHLCV from Binance Vision + CCXT incremental updates per CLAUDE.md Tech Stack); distinct from prior Branch.A *working assumption* status under R3.1a (per X1' tighten) | "fire, authorized R3.1a E.3 upgrade trigger and Tier 4 R4.1" + "ratify all" 2026-05-18 |
| 2 | Bucket-1 investigation note class (Template B); NO git tag at SEAL per CLAUDE.md Tag policy shorthand + [`docs/discipline/METHODOLOGY_NOTES.md`](../discipline/METHODOLOGY_NOTES.md) §32 sub-§ "Tag policy at SEAL register-event boundaries" (line 7079) standalone codified policy + Phase 5.1 + Phase 5.2 + Phase A + R1.2 + R3.1a precedent | Same register |
| 3 | Branch.A formal commitment only; Path 1 paper trading basis / Path 2 L2 replay marginal value / Stratum A D-I classification under spot-venue framing remain eligible-not-named per anti-pre-emption (per X2' enumeration); R3.1d post-venue cost-grid re-anchor also explicit not resolved by E.3 (per X8) | Same register + PFR ratification |
| 4 | E.3 errata replacement comment wording on `config/execution.yaml` line 42 per §8.2 (proposed: `# Effective SPOT execution cost model (7 bps/side simplification; not venue-accurate; see CLAUDE.md Execution Convention §4)`) | Same register + PFR adjudication (Codex PUSHBACK on "Spot VIP 0 basis" false-attribution risk adopted; Advisor wording withdrawn) |
| 5 | R3.1a §12 Errata appendix per Phase A `9c00f59` post-SEAL append precedent ("appended post-SEAL per sealed-content invariance discipline" framing in §12 header) | Same register + PFR PUSHBACK on Advisor Risk N1 hallucination (Phase A §11 Errata verified post-SEAL commit, NOT part of original SEAL commit) |
| 6 | Single atomic SEAL bundle: R4.1 SEAL artifact + R3.1a §12 Errata + `execution.yaml` line 42 patch + CLAUDE.md Phase Marker advance + `docs/phase_marker_history.md` atomic update (commits may be logically grouped within one push) | Same register |
| 7 | Explicit `config_hash` invalidation acknowledgment in R4.1 §8 + R3.1a §12 Errata per X3'+X4'+X5+X7+X8 | Same register |

**§0 scope-bleed trip-wire status:** clean. Cycle work bounded to formal Branch.A venue commitment + atomic E.3 errata trigger on `config/execution.yaml` line 42. No analytical computation. No engine runs. No API spend. No new data acquired. Path 1 / Path 2 / Stratum A D-I sub-questions remain eligible-not-named per anti-pre-emption invariant.

**Charlie register chain (R4.1 cycle through V_SEAL):**

| # | Decision | Register surface |
|---|----------|------------------|
| 1 | R4.1 cycle entry + R3.1a E.3 upgrade trigger authorize | "fire, authorized R3.1a E.3 upgrade trigger and Tier 4 R4.1; reviewer dispatch after" |
| 2 | Sub-decision option Round 1 reviewer dispatch | "dispatch to reviewer for sub decision point. gather their thoughts and adjudicate and give your lean" |
| 3 | PFR cross-validation round dispatch | "PHR cross validation round dispatch to reviewer" |
| 4 | Sub-decision lock ratification (SD1-SD7 + X1-X8) | "ratify all" |
| 5 | V1 SEAL bundle reviewer dispatch (combined R4.1 + §12 + execution.yaml diff per X6') | "fire the V1 reviewer dispatch (2-leg subagent default: codex:codex-rescue + quant-research-advisor parallel; BL-Y-refined Phase 1 lean hidden in brief; review scope = R4.1 + §12 Errata + execution.yaml diff combined per X6')" |
| 6 | V1 adjudication ratification + V2 patches authorize | "ratify with the following: Ratify adjudication (P1 + P2 PUSHBACK + L1-L7 ADOPT); Proceed with V2 revisions per adjudication (mine to apply); PFR rule-Y dispatch after V2 (per L1 new-content magnitude trigger)" |
| 7 | PFR rule-Y dispatch + V2 → V3 PFR adjudication ratification | "ratify" 2026-05-18 (P3 PUSHBACK on Advisor PFR-F1 METHODOLOGY_NOTES.md hallucination + A1-A5 ADOPT V3 patches; SKIP further PFR per mechanical literal landing) |
| 8 | V_SEAL fire + SEAL bundle commit authorize | PENDING (next Charlie register) |

**Anti-pre-naming binding (preserved throughout):** this note formally commits Branch.A (SPOT execution) per Phase 5.2 §6.4 and triggers R3.1a E.3 errata on `config/execution.yaml` line 42. It does NOT pre-name:
- Path 1 paper trading basis (spot-realistic ~15bps per Phase 5.2 §6.4 line 263 estimate, vs venue-corrected research-time 7bps per CLAUDE.md Execution Convention §4 effective simplification, vs rescoped, vs invalidated) — eligible-not-named for separate Charlie register-event
- Path 2 (full L2 replay infrastructure) firing or deferral under spot-venue framing — eligible-not-named
- Stratum A D-I classification preservation / re-interpretation / invalidation under spot-venue framing — eligible-not-named
- R3.1b/c empirical small-lot venue-conditional cost measurement scope or method (first crossover from research → operational capital under SPOT venue) — eligible-not-named
- R3.1d post-venue cost-grid re-anchor scope or method (whether 7bps simplification persists, upgrades to spot-realistic ~15bps, or moves to volatility-scaled per Phase 5.1 successor paths) — eligible-not-named
- Phase 1-2 cost-model semantics (locked at `effective_7bps_per_side` per CLAUDE.md Execution Convention §4; venue-agnostic by construction; not re-evaluated by R4.1)
- Phase 5.1 cost-model investigation findings (not re-opened)
- Phase 5.2 venue reconciliation findings (R4.1 builds on §6.4 structures without re-evaluating them)
- R3.1a R3.1a outcome (V4 SEAL preserved byte-identical for §§0-11; §12 Errata is post-SEAL append per Phase A precedent)
- R5.1 candidate-subset commitment, R5.2 predeclared selection-inflation handling, R6.1 promotion class — eligible-not-named
- Tier-0 pause / strategic-absorption — eligible-not-named
- Pre-existing noise cleanup (`.DS_Store` + `docs/d7_stage2c/*`; 12+ session carry-forward) — eligible-not-named
- Phase 2.5 bandit-dedup activation (parked) — eligible-not-named
- Memory codification investigation on advisor hallucination rate elevation — eligible-not-named

---

## §1 Scope and Objective

**§1.1 Question this cycle resolves:**

What is Phase B Pre-Sequence Roadmap V3 Tier 4 R4.1 formal venue commitment per Phase 5.2 §6.4 Branch.A/B/C/D structures?

**Answer (locked per Charlie register chain):** **Branch.A formal commitment = SPOT execution** (matching canonical data layer per Phase 5.2 §6.4 corrected semantics). This formalizes the working-assumption status established at R3.1a cycle into a formal commitment per Phase 5.2 §6.4 line 263 definition; under §6.4, Branch.A is the IF-clause "Charlie's venue commitment register-event resolves to spot execution."

**§1.2 What this cycle does NOT resolve:**

Per anti-pre-naming binding (§0):

1. Path 1 paper trading basis under Branch.A — eligible-not-named for separate Charlie register-event (per Phase 5.2 §6.4 sub-question; per X2' explicit enumeration)
2. Path 2 (full L2 replay infrastructure) marginal value under Branch.A — eligible-not-named (per Phase 5.2 §6.4 sub-question; per X2' explicit enumeration)
3. Stratum A D-I classification preservation / re-interpretation / invalidation under spot-venue framing — eligible-not-named (per Phase 5.2 §6.4 sub-question; per X2' explicit enumeration)
4. R3.1b/c empirical small-lot venue-conditional cost measurement — eligible-not-named
5. R3.1d post-venue cost-grid re-anchor — eligible-not-named (per X8 explicit non-resolution by E.3)
6. Phase 1-2 cost-model semantics — locked at `effective_7bps_per_side` per CLAUDE.md Execution Convention §4 (venue-agnostic by construction)
7. R5.1 candidate-subset commitment, R5.2 selection-inflation handling, R6.1 promotion class
8. Tier-0 pause / strategic-absorption
9. R3.1a R3.1a sealed-content invariance for §§0-11 (E.3 errata appends §12 only; §§0-11 remain byte-identical per Phase A `9c00f59` precedent)

---

## §2 Headline Finding — Formal Branch.A Commitment

**§2.1 Commitment statement:**

Per Phase 5.2 §6.4 line 263 (verified): "**Branch.A**: IF Charlie's venue commitment register-event resolves to **spot execution** (matching canonical data layer) → realistic cost basis is **fee-schedule-anchored at 10 bps spot taker per §3.1 plus heuristic slippage component per §3.2 (literature-grounded heuristic, NOT empirically verified)**, total ≈ 15 bps per side at upper end of heuristic range."

Charlie's register fire 2026-05-18 "fire, authorized R3.1a E.3 upgrade trigger and Tier 4 R4.1; reviewer dispatch after" + "ratify all" formally resolves the venue commitment register-event named at Phase 5.2 §6.4 to **Branch.A = spot execution**. Phase B Pre-Sequence Roadmap V3 Tier 4 R4.1 register-event is hereby sealed under this formal commitment.

**§2.2 What "formal commitment" means here (distinct from working-assumption status):**

R3.1a operated under Branch.A *working assumption* — a scope-bounded operational prior within R3.1a's E.2 derivation logic, explicitly NOT a formal R4.1 commitment per R3.1a §0 anti-pre-naming binding ("Whether Branch.A (SPOT execution) will be formally committed at R4.1 (working-assumption status ≠ formal commitment)"). R4.1 SEAL elevates this status to **formal commitment** — the operational venue under which all subsequent Phase B Tier 5-6 work is conducted, all sub-questions Path 1 / Path 2 / Stratum A D-I are evaluated, R3.1b/c empirical cost measurement is anchored, and R3.1d cost-grid re-anchor (if it fires) is framed.

**§2.3 What Branch.A formal commitment DOES propagate:**

- R3.1a §7.2 inverted trigger logic activates: under Branch.A formal commitment, the line-42 comment "Binance perpetual futures, VIP 0" becomes operationally misleading (futures-attribution comment on SPOT-execution operational venue); E.3 errata fires under this register
- Phase B Tier 5 R5.1 candidate-subset commitment will be framed under SPOT venue context
- Phase B Tier 6 R6.1 promotion class will be framed under SPOT venue context
- R3.1b/c empirical cost measurement (if fired) will be SPOT venue-conditional
- R3.1d cost-grid re-anchor (if fired) will re-evaluate the 7bps simplification against spot-realistic ~15bps per Phase 5.2 §6.4 estimate

**§2.4 What Branch.A formal commitment does NOT propagate:**

- No numeric cost-model change in this cycle. `effective_7bps_per_side` remains in force per CLAUDE.md Execution Convention §4. Whether 7bps remains, upgrades to ~15bps, or moves to volatility-scaled is R3.1d's scope, not R4.1's.
- No backtest invalidation. All sealed Phase 1-5 results were computed correctly under the venue-agnostic simplification. Pillar 1 (R3.1a §6.1) is invariant under R4.1 formal commitment.
- No execution-timing change. Signal-at-close, fill-at-next-open semantics preserved per `config/execution.yaml` `execution` block.
- No data layer change. Canonical `data/raw/btcusdt_1h.parquet` remains as-is; sources `binance_vision + ccxt_binance` confirmed in R3.1a §3.3.

---

## §3 Source Verification (Line-Anchored Citations)

**§3.1 Phase 5.2 §6.4 Branch.A definition (verified 2026-05-18):**

[`docs/phase5/PHASE5_2_VENUE_RECONCILIATION_NOTE.md` line 263](../phase5/PHASE5_2_VENUE_RECONCILIATION_NOTE.md#L263) (verified via direct Read):

> **Branch.A**: IF Charlie's venue commitment register-event resolves to **spot execution** (matching canonical data layer) → realistic cost basis is **fee-schedule-anchored at 10 bps spot taker per §3.1 plus heuristic slippage component per §3.2 (literature-grounded heuristic, NOT empirically verified)**, total ≈ 15 bps per side at upper end of heuristic range.

**§3.2 R3.1a §7.2 inverted trigger logic table (verified 2026-05-18):**

[`docs/phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md` §7.2 (line 252-265)](../phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md) (verified via direct Read):

| R4.1 outcome | Comment status | Errata trigger |
|---|---|---|
| Branch.A (SPOT execution) formally committed at R4.1 | Futures-attribution comment becomes operationally misleading for the formally-committed SPOT venue | **E.3 errata becomes eligible** (separate Charlie register to align comment with operational venue) |
| Branch.B (futures execution) formally committed at R4.1 | Comment matches formally-committed venue | No errata needed; E.2 remains terminal |
| Branch.C (both / defer) formally committed at R4.1 | Mixed disposition; per-venue framing | Charlie determines per-Branch.C resolution |
| Branch.D (alternative framing) at R4.1 | Charlie-specified disposition | Per Branch.D resolution |

R4.1 outcome row 1 (Branch.A) is the activating row for this cycle.

**§3.3 Canonical data layer (verified 2026-05-18):**

`data/raw/btcusdt_1h.parquet` — BTC/USDT 1h SPOT. Verified `source` column unique values per R3.1a §3.3: `['binance_vision', 'ccxt_binance']`. All present sources are SPOT venues. Branch.A formal commitment aligns the operational venue (SPOT) with this canonical data layer.

**§3.4 R3.1a R3.1a §0 anti-pre-naming binding (verified 2026-05-18):**

[`docs/phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md` §0 (line 39-47)](../phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md#L39-L47) (verified via direct Read):

> **Anti-pre-naming binding (preserved throughout):** this note resolves only the venue-label discrepancy as administrative documentation per E.2 option choice under Branch.A working assumption. It does NOT pre-name:
> - Eventual R4.1 venue commitment outcome (Branch.A/B/C/D per Phase 5.2 §6.4)
> - Whether Branch.A (SPOT execution) will be formally committed at R4.1 (working-assumption status ≠ formal commitment)

R4.1 SEAL is the deferred register-event that this R3.1a anti-pre-naming binding reserved.

**§3.5 Phase A `9c00f59` post-SEAL errata append precedent (verified 2026-05-18):**

`git log --oneline -- docs/phase5/PHASE5_A_CLARIFICATION_NOTE.md` (default order = newest first):
- `9c00f59` post-SEAL errata adding §11 (21 insertions; "appended post-SEAL per sealed-content invariance discipline" framing in §11 section header) — newer
- `ab62b2e` Phase A SEAL fire (original SEAL commit) — older

This precedent governs SD5 protocol: R3.1a §12 Errata is appended via separate post-SEAL commit; §§0-11 of R3.1a V4 SEAL remain byte-identical.

**§3.6 `compute_config_hash()` technical scope (verified 2026-05-18):**

[`backtest/experiment_registry.py:188`](../../backtest/experiment_registry.py#L188) — `def compute_config_hash() -> str:`
[`backtest/experiment_registry.py:48`](../../backtest/experiment_registry.py#L48) — `CONFIG_FILES` definition listing `execution.yaml`, `environments.yaml`, `schemas.yaml`

Function implementation (lines 188-198, verified via direct Read):
- Hashes `path.read_bytes()` for each file in `CONFIG_FILES` (byte-level hash; comments included)
- Returns `f"sha256:{hasher.hexdigest()[:16]}"` (sha256 prefix + 16 hex chars truncation; 18 chars total)

**Current pre-E.3 `config_hash` (verified via direct Python hashlib reproduction 2026-05-18):**

```
sha256:3850424a0ef2d292
```

(Pre-E.3 parent commit SHA: `46615cf` at time of V1 DRAFT; recompute at SEAL landing in case of intervening commits.)

---

## §4 Historical Context

**§4.1 Phase 5.2 venue-mismatch identification:**

Phase 5.2 §2 (Headline Finding: Venue-Mismatch) identified the discrepancy between `config/execution.yaml` line 42 "Binance perpetual futures, VIP 0" attribution and the canonical SPOT data layer. Phase 5.2 §6.4 enumerated Branch.A/B/C/D venue commitment options for R4.1 without pre-naming the answer.

**§4.2 R3.1a venue-infrastructure formalization under Branch.A working assumption:**

R3.1a SEAL (commit `16099b8` + Phase Marker advance `46615cf`) resolved the parked venue-infrastructure formalization decision as Option E.2 (methodology note only; no `config/execution.yaml` modification) under Branch.A *working assumption*. R3.1a §5.5 surface tension acknowledgment explicitly preserved the residual question of whether Branch.A would be *formally committed* at R4.1, with §7.2 inverted trigger logic naming E.3 errata as eligible under that formal commitment outcome. R4.1 is the deferred register-event that activates §7.2.

**§4.3 R3.1a anti-pre-naming binding (Branch.A working-assumption ≠ formal commitment):**

R3.1a §0 explicitly distinguished working-assumption status from formal commitment, preserving the latter as eligible-not-named for separate Charlie register-event. R4.1 elevates working-assumption to formal commitment per Charlie's 2026-05-18 register fire.

**§4.4 What R4.1 does NOT re-evaluate:**

R4.1 builds on Phase 5.2 §6.4 + R3.1a §7.2 without re-opening either:
- Phase 5.2 §2 venue-mismatch identification — sealed
- Phase 5.2 §6.4 Branch.A/B/C/D structures — sealed (R4.1 selects Branch.A from this menu)
- R3.1a E.2 methodology note (§§1-10) — sealed-content invariance preserved
- R3.1a §7.2 inverted trigger logic — sealed; R4.1 activates row 1 of the table

---

## §5 Methodology Implications

**§5.1 Cost-model semantics under Branch.A formal commitment:**

`effective_7bps_per_side` per CLAUDE.md Execution Convention §4 remains in force. The simplification is venue-agnostic by construction — it does not assert venue accuracy at any single venue. Under Branch.A formal commitment, the simplification is now applied to a formally-committed SPOT operational venue. Whether the simplification is "pessimistic enough" for spot-realistic execution (Phase 5.2 §6.4 estimates ~15bps spot-realistic upper bound) is **R3.1d's scope, not R4.1's**.

**§5.2 What R4.1 does NOT change in cost-model:**

- `default_fee_bps: 4.0` — unchanged (per R3.1a §6.1 Pillar 1: venue-agnostic by construction)
- `slippage_bps: 3.0` — unchanged
- `effective_cost_bps = 7` per side — unchanged
- Round trip = 14bps — unchanged
- `fee_model = "effective_7bps_per_side"` registry field — unchanged per CLAUDE.md Experiment Tracking rule

**§5.3 Backtest validity preservation:**

All sealed Phase 1-5 results were computed correctly under the venue-agnostic `effective_7bps_per_side` simplification. R4.1 formal commitment does not retroactively invalidate any sealed result. Pillar 1 (R3.1a §6.1) remains intact:

> The `effective_7bps_per_side` model is by CLAUDE.md Execution Convention §4 a SIMPLIFICATION abstracted from venue-specific fee structure. This is the load-bearing pillar of grandfathering: the cost computation in Phase 1-5 backtests is a single-parameter simplification (`fee_bps + slippage_bps = effective_cost_bps`), explicitly agnostic to whether the underlying venue would have been perpetual or SPOT at live execution.

**§5.4 Phase B Tier 3-6 sequencing under Branch.A formal commitment:**

- Tier 3 R3.1b/c (empirical small-lot venue-conditional cost) — eligible-not-named; if fired, anchored to SPOT venue per R4.1
- Tier 3 R3.1d (post-venue cost-grid re-anchor) — eligible-not-named; if fired, re-evaluates 7bps simplification vs spot-realistic ~15bps per Phase 5.2 §6.4 estimate
- Tier 5 R5.1 (candidate-subset commitment) — eligible-not-named; framed under SPOT venue context
- Tier 5 R5.2 (predeclared selection-inflation handling) — conditional on R5.1
- Tier 6 R6.1 (promotion class) — eligible-not-named; framed under SPOT venue context

R4.1 does not pre-name sequencing or sub-answers for any of these.

---

## §6 Eligible-Not-Named Sub-Questions Under Branch.A (Per X2' Enumeration)

**§6 scope note:** §6 is the contextual NOT-resolved-by-this-cycle narrative for Phase 5.2 §6.4 Branch.A sub-questions; §10 is the canonical reserved-decisions enumeration for the cycle as a whole (§10 is a superset of §6 plus other eligible-not-named items: Tier 2 R2.1/R2.2/R2.3 conditional prereqs, R3.1b/c empirical cost, R5.1/R5.2/R6.1, Tier-0 pause, etc.). §6 sub-questions Path 1/Path 2/Stratum A D-I are explicitly authorized for enumeration per X2' (PFR ratification — "R4.1 must explicitly enumerate Path 1/Path 2/Stratum A D-I as eligible-not-named (anti-pre-emption preservation)"); per-sub-question content descriptions below are descriptive context for the eligibility surface, NOT pre-naming of outcome.

Per Phase 5.2 §6.4 line 263-266, Branch.A formal commitment includes three independently-Charlie-determined sub-questions, each eligible-not-named for separate Charlie register-event boundary:

**§6.1 Path 1 paper trading basis (eligible-not-named):**

Per Phase 5.2 §6.4 line 264: "Whether Path 1 paper trading proceeds at spot-realistic basis, at the venue-corrected research-time basis, or is rescoped or invalidated"

Options eligible-not-named for separate Charlie register:
- Spot-realistic basis (~15bps per Phase 5.2 §6.4 upper-bound estimate; would invalidate Phase 1-5 conclusions about Phase B candidate viability against the higher cost band)
- Venue-corrected research-time basis (preserve 7bps simplification; treat R4.1 as administrative documentation under Pillar 1 invariance)
- Rescoped (different Phase B framing, e.g., promote only candidates that survive a higher cost band sensitivity test)
- Invalidated (Phase B does not proceed to paper trading under current candidate set)

R4.1 does not pre-name which option fires.

**§6.2 Path 2 (full L2 replay infrastructure) marginal value (eligible-not-named):**

Per Phase 5.2 §6.4 line 265: "Whether Path 2 marginal value warrants L2 replay infrastructure given desk-research bounds in hand"

Options eligible-not-named for separate Charlie register:
- Fire Path 2 (build L2 replay infrastructure; substantial engineering investment)
- Defer Path 2 (current desk-research cost bounds sufficient for Phase B promotion decisions)
- Conditional fire (Path 2 fires only if Phase B candidate set passes higher-cost sensitivity test)

R4.1 does not pre-name which option fires.

**§6.3 Stratum A D-I classification under spot-venue framing (eligible-not-named):**

Per Phase 5.2 §6.4 line 266: "Whether Stratum A D-I classification is preserved, re-interpreted, or invalidated under spot-venue framing"

Stratum A D-I classification (per Phase 5.1 cost-model investigation §3.2) labels the cost-grid confidence levels (D1 highest confidence, etc.) for the `effective_7bps_per_side` cost band. Under Branch.A formal commitment with spot-realistic cost band ~15bps estimated, Stratum A D-I labels may:
- Be preserved (D-I classification independent of venue label; venue-agnostic by Pillar 1)
- Be re-interpreted (D-I labels re-anchored to spot-realistic cost band)
- Be invalidated (D-I framework re-evaluated for spot-venue framing)

R4.1 does not pre-name which interpretation applies.

**§6.4 R3.1d post-venue cost-grid re-anchor (eligible-not-named per X8):**

R3.1d is reserved for whether the current 7bps cost-grid persists under Branch.A formal commitment, upgrades to spot-realistic ~15bps per Phase 5.2 §6.4 estimate, or moves to volatility-scaled per Phase 5.1 successor paths. R4.1 explicitly does NOT resolve R3.1d. The E.3 errata in this cycle is **comment-level documentary alignment only** — it does not change the numeric cost-model semantics. Per X8 acknowledgment: §12 Errata + this §6.4 explicitly state numeric cost-model semantics are unchanged + no R3.1d cost-grid re-anchor is implied by E.3 + no claim of spot fee-schedule accuracy is made by the replacement comment wording.

---

## §7 Forward-Treatment Policy

**§7.1 Under Branch.A formal commitment (R4.1 outcome):**

Branch.A formal commitment is now the operational venue under which all subsequent Phase B Tier 5-6 work is conducted. All eligible-not-named sub-questions (§6.1 / §6.2 / §6.3 / §6.4) are framed under SPOT venue context but remain Charlie-determined at their respective register-event boundaries.

**§7.2 R3.1a E.3 errata fires under this register (per §7.2 inverted trigger logic table row 1):**

R3.1a §7.2 inverted trigger logic activates: Branch.A formal commitment makes the line-42 comment "Binance perpetual futures, VIP 0" operationally misleading. E.3 errata fires per Charlie register and is documented in R3.1a §12 Errata appendix (post-SEAL append per Phase A `9c00f59` precedent). The line-42 comment is updated to a venue-agnostic effective-cost-model framing per SD4 locked wording. See §8 for `config_hash` impact.

**§7.3 What §7.2 activation does NOT change:**

- R3.1a V4 SEAL §§0-11 byte-identical preservation (only §12 appended; sealed-content invariance preserved per Phase A precedent)
- Phase 1-2 cost-model semantics (`effective_7bps_per_side` per CLAUDE.md Execution Convention §4; venue-agnostic)
- Pillar 1 research validity (R3.1a §6.1; venue-invariant by construction)
- Numeric values in `cost_model` block of `execution.yaml` (`default_fee_bps`, `slippage_bps`, derived `effective_cost_bps`)
- Phase 4 forward-window holdout artifacts (use separate `config/execution_phase4_{07,13,15,17}bps.yaml` family per R3.1a §8 OBS 4; NOT affected by E.3)

**§7.4 Sequencing of Phase B successor register-events under Branch.A formal commitment:**

R4.1 does not pre-name sequencing. Each successor cycle is a separate Charlie register-event boundary per anti-pre-emption discipline:
- Tier 2 R2.1 (volume_divergence DSL audit; informed by R1.2 OBS 3; requires pre-commitment of audit criterion before inspection per Codex Round 2 Catch B)
- Tier 2 R2.2 (Monday-pattern mechanism; informed by R1.2 OBS 4)
- Tier 2 R2.3 (OBS 10 theme provenance)
- Tier 3 R3.1b/c (empirical small-lot venue-conditional cost measurement)
- Tier 3 R3.1d (post-venue cost-grid re-anchor)
- Tier 5 R5.1 (candidate-subset commitment under SPOT venue context; R1.2 AMBIGUOUS verdict binds harder; advisor Round 2 post-hoc timing finding requires pre-commitment of filter criterion)
- Tier 5 R5.2 (selection-inflation handling)
- Tier 6 R6.1 (promotion class)
- Tier-0 pause / strategic-absorption per V3 top note

---

## §8 R3.1a E.3 Errata Trigger + `config_hash` Impact

**§8.1 E.3 errata fire context:**

Per R3.1a §7.2 inverted trigger logic table row 1, Branch.A formal commitment at R4.1 makes line-42 "Binance perpetual futures, VIP 0" operationally misleading. E.3 errata fires under Charlie register chain "fire, authorized R3.1a E.3 upgrade trigger" + "ratify all" + this R4.1 SEAL. E.3 patch is documented in:
- R3.1a §12 Errata appendix (post-SEAL append per Phase A `9c00f59` precedent)
- `config/execution.yaml` line 42 patch (atomic with SEAL bundle commit)

**§8.2 Locked replacement wording (per SD4):**

Old (sealed in `config/execution.yaml` line 42 pre-E.3):
```yaml
  # Fee structure (Binance perpetual futures, VIP 0)
```

New (post-E.3):
```yaml
  # Effective SPOT execution cost model (7 bps/side simplification; not venue-accurate; see CLAUDE.md Execution Convention §4)
```

Wording rationale (per Codex Round 1 PUSHBACK + PFR ratification):
- Removes false "perpetual futures" attribution under Branch.A formal commitment
- Avoids false "Spot VIP 0 basis" attribution (Binance Spot VIP 0 taker is ~10bps per Phase 5.2 §3, not 4bps — saying "Spot VIP 0" would substitute one false attribution for another)
- "Effective" + "simplification" + "not venue-accurate" framing makes documentary not operational claim explicit
- "see CLAUDE.md Execution Convention §4" anchors to the authoritative source for the simplification framing

**§8.3 `config_hash` impact (technical scope per X3'):**

`compute_config_hash()` at [`backtest/experiment_registry.py:188`](../../backtest/experiment_registry.py#L188) hashes the byte content of three config files defined at [`backtest/experiment_registry.py:48`](../../backtest/experiment_registry.py#L48) (`CONFIG_FILES`):
- `config/execution.yaml`
- `config/environments.yaml`
- `config/schemas.yaml`

The hash uses `hasher.update(path.read_bytes())` per file and returns `f"sha256:{hasher.hexdigest()[:16]}"` (sha256 prefix + 16 hex chars; 18 chars total). Byte content includes YAML comments. Modifying the line-42 comment changes the `execution.yaml` byte content and therefore changes the returned hash.

**§8.4 Pre-E.3 + Post-E.3 canonical hash state (per X4' + A4 V3 update):**

- HEAD parent commit at V1 DRAFT time: `46615cf` (recompute at SEAL bundle landing in case of intervening commits)
- **Pre-E.3** `config_hash` (computed on current `execution.yaml` with line 42 = "Binance perpetual futures, VIP 0"; independently verified via Python `hashlib.sha256` reproduction of `compute_config_hash()` 2026-05-18): `sha256:3850424a0ef2d292` — Codex Round 1 LBR + PFR Codex both independently corroborated
- **Post-E.3** `config_hash` (computed on proposed post-patch `execution.yaml` with line 42 = "Effective SPOT execution cost model (7 bps/side simplification; not venue-accurate; see CLAUDE.md Execution Convention §4)"; Codex PFR independent computation via byte-level reproduction): `sha256:db2ce75bd41e8513`
- Both hashes use `hasher.update(path.read_bytes())` per file in `CONFIG_FILES` (3-file scope: `execution.yaml` + `environments.yaml` + `schemas.yaml`); both are 16-hex-char truncations of full SHA-256
- Pre-E.3 byte content of `execution.yaml` is recoverable post-E.3 via `git show <pre-E.3-parent-commit-SHA>:config/execution.yaml` for any auditor who needs to verify historical `config_hash` values
- Both hashes subject to recomputation at SEAL bundle landing time in case of intervening commits to `environments.yaml` or `schemas.yaml` between V3 and V_SEAL fire

**§8.5 Pillar 1 preservation (per R3.1a §6.1; per X8):**

Research validity of all sealed Phase 1-5 backtests is **unaffected** by E.3 fire. The `effective_7bps_per_side` cost computation uses structured numeric keys (`default_fee_bps`, `slippage_bps`), not the line-42 comment. Per R3.1a §6.1:

> The cost computation in Phase 1-5 backtests is a single-parameter simplification (`fee_bps + slippage_bps = effective_cost_bps`), explicitly agnostic to whether the underlying venue would have been perpetual or SPOT at live execution. The line-42 comment's perpetual attribution does not propagate into the cost computation because the computation uses the structured numeric keys directly.

E.3 changes only the documentary comment; the numeric keys and computation are byte-identical post-E.3. No backtest result requires retroactive recomputation.

**§8.6 Pillar 2 forensic traceability cost (per X4'):**

E.3 invalidates byte-identity between historical `experiments.db` entries (which logged `config_hash` values computed against the pre-E.3 byte content) and the post-E.3 `execution.yaml` content. A future inspector re-hashing `execution.yaml` after E.3 will get a different hash than the registry entries. This is the Pillar 2 cost being spent at R4.1 SEAL. Per R3.1a §6.3, Pillar 2 is "additional benefit specific to E.2", not load-bearing for research correctness.

Reconstruction path: pre-E.3 byte content of `execution.yaml` is recoverable via `git show <pre-E.3-commit-SHA>:config/execution.yaml` for any auditor who needs to verify historical `config_hash` values.

**§8.7 Historical `experiments.db` entries retain pre-E.3 hash (per X7):**

All Phase 1-5 walk-forward and Phase 2C experiment_registry entries that logged `config_hash` retain their **pre-E.3 hash value** in the SQLite database. These entries are **NOT** retroactively updated to post-E.3 hash values. This is intentional and expected post-E.3. A future reader encountering hash mismatches between registry entries and current `execution.yaml` content should understand this as documented expected state, not a data-integrity failure.

**§8.8 Phase 4 forward-window artifact independence carve-out (per X5):**

Phase 4 forward-window holdout artifacts (sealed at `data/phase2c_evaluation_gate/phase4_forward_2026_*bps_v1/`) use a SEPARATE execution config FAMILY: `config/execution_phase4_{07,13,15,17}bps.yaml` (verified via `ls config/execution_phase4_*bps.yaml` — 4 files for 4 cost bands per Phase 5.1 §3.2 D-classification framework). Phase 4 holdout artifacts integrity-lock against these specific per-band files via `execution_config_path` + `execution_config_sha256` fields, NOT against `config/execution.yaml`. **E.3 does NOT affect Phase 4 forward-window holdout artifact integrity.** Phase 4 `execution_config_sha256` values remain byte-identical to their per-band config files post-E.3.

**§8.9 Numeric semantics + R3.1d non-resolution explicit (per X8):**

E.3 is **comment-level documentary alignment only**. It does NOT:
- Change any numeric value in `cost_model` block of `execution.yaml`
- Imply or resolve R3.1d (post-venue cost-grid re-anchor)
- Claim spot fee-schedule accuracy (the replacement comment explicitly frames the 7bps as "not venue-accurate" + "simplification")
- Re-open Phase 1-2 cost-model semantics (locked at `effective_7bps_per_side` per CLAUDE.md Execution Convention §4)
- Re-open Phase 5.1 cost-model investigation findings
- Pre-name Path 1 paper trading basis or any §6 sub-question

R3.1d remains eligible-not-named for separate Charlie register-event boundary.

**§8.10 Verification method — code path, NOT parsed-YAML form (per Codex V1 LBR):**

Canonical verification of `config_hash` MUST use the [`compute_config_hash()`](../../backtest/experiment_registry.py#L188) code path at [`backtest/experiment_registry.py:188`](../../backtest/experiment_registry.py#L188), which performs `hasher.update(path.read_bytes())` per file in `CONFIG_FILES` (byte-level hash including YAML comments). Verification MUST NOT use a parsed-YAML form (e.g., `yaml.safe_load(path) → json.dumps → sha256`) because parsed-YAML **strips comments** during parsing and would silently report a stable hash even after the line-42 comment is replaced. Any SEAL checklist item or future audit script that uses parsed-YAML form would produce a **false-negative invariance check** post-E.3, masking the expected hash change.

Verified by Codex V1 + PFR round LBRs via independent reproductions. Parsed-YAML form is NON-CANONICAL by definition — different serialization choices (e.g., `yaml.safe_load + json.dumps(sort_keys=True)` vs `yaml.safe_load + json.dumps(sort_keys=False)` vs `yaml.dump` round-trip) produce DIFFERENT non-canonical hash values for the same source file. Empirically Codex PFR computed `aa265c14a4760d80` for one parsed-YAML form; another reproduction yielded a different value. The canonical `compute_config_hash()` byte-level hash is well-defined: pre-E.3 = `sha256:3850424a0ef2d292`; post-E.3 (Codex PFR independent computation via byte-level reproduction of the proposed line-42 patch) = `sha256:db2ce75bd41e8513`. Critical asymmetry: any parsed-YAML form silently produces the SAME hash before and after the comment change (because comments are stripped during parse); the canonical form changes deterministically. SEAL verification correctness requires the code path.

**§8.11 No read-time `config_hash` recomputation guard (per Advisor V1 LBR1; verified):**

`compute_config_hash()` is called at write-time only. Verified via two grep commands:

Narrow grep `grep -n "compute_config_hash" backtest/experiment_registry.py` returns exactly 2 lines:
- Line 188: `def compute_config_hash() -> str:` (function definition)
- Line 265: `run_data.setdefault("config_hash", compute_config_hash())` — **sole call site**, inside the insert/create path of `insert_run()`

Broader grep `grep -n "config_hash" backtest/experiment_registry.py` returns 4 lines (above 2 plus):
- Line 62: `config_hash TEXT,` (schema declaration only)
- Line 243: `Auto-generates run_id (UUID), created_at_utc, git_commit, config_hash, ...` (docstring mention only)

No read-time integrity guard exists in `experiment_registry.py`. Pre-E.3 `config_hash` values in `experiments.db` persist as-is post-E.3; no automated mismatch warning is triggered when historical entries are queried. This is expected per X7 (historical entries retain pre-E.3 hash intentionally).

---

## §9 OBSERVATIONS (Anti-Pre-Naming Forward-Only)

Per anti-pre-emption invariant codified across Phase 5.1, Phase 5.2, Phase A, R1.2, and R3.1a SEAL precedent — observations below are surfaced as forward-only carry-forward signals; they do NOT pre-name any future Charlie register outcome.

**OBSERVATION 1 (Branch.A formal commitment ≠ Path 1/2/Stratum A resolution):** R4.1 formal commitment of Branch.A elevates working-assumption status to operational venue but does NOT pre-name sub-question outcomes within Branch.A. Future Charlie registers on Path 1 / Path 2 / Stratum A D-I are independently scoped per Phase 5.2 §6.4 sub-question structure.

**OBSERVATION 2 (R3.1d cost-grid eligibility):** R4.1 formal commitment makes R3.1d (post-venue cost-grid re-anchor) eligible per spot-venue framing. Whether R3.1d fires, when, and what its scope would be — all eligible-not-named.

**OBSERVATION 3 (Phase 5.1 successor paths under spot-venue framing):** Phase 5.1 §7.1 successor paths (volatility-scaled slippage, regime-aware calibration bands) are framed under Branch.A formal commitment. R4.1 does not pre-name whether or when these paths fire.

**OBSERVATION 4 (R3.1a sealed-content invariance preserved at R4.1 SEAL):** R3.1a V4 SEAL §§0-11 remain byte-identical post-E.3. §12 Errata is appended via separate post-SEAL commit per Phase A `9c00f59` precedent. Sealed-content invariance discipline is preserved.

**OBSERVATION 5 (Pillar 1 invariance + Pillar 2 cost):** Pillar 1 (research validity) is invariant under R4.1 formal commitment. Pillar 2 (forensic traceability via byte-identity `config_hash`) is the cost spent at R4.1 SEAL. The trade-off was explicitly priced at PFR adjudication and ratified by Charlie register "ratify all".

**OBSERVATION 6 (cumulative pilot reviewer reliability — V3 update):** R4.1 cycle full reviewer pipeline data points (sub-decision Round 1 + sub-decision PFR + V1 review + PFR rule-Y on V2): 0/8 Codex stalls this cycle (cumulative pilot ~4%) + **1/8 Codex hallucinations this cycle (FIRST OBSERVED in Reading 3 pilot — V1 reviewer round "section 4 vs §4 mismatch" BLOCKING claim directly contradicted by Codex's own V4 verification step in same response; PFR rule-Y round Codex clean)** + 4/8 verified Advisor hallucinations this cycle (compute_config_hash line-189 sub-decision Round 1 + sealed-content-invariance Risk N1 sub-decision PFR + R4.1 paraphrase / Phase 5.2 §6.4 text fabrication V1 round + **METHODOLOGY_NOTES.md phantom-citation 3-facts fabrication PFR rule-Y round**). Cumulative Reading 3 pilot stats post-PFR-rule-Y: ~4% Codex stalls + **~2% Codex hallucinations (one observation)** + **~17-20% Advisor hallucinations (7 cumulative across ~40+ dispatches; elevated trend from baseline ~22%)**. **Symmetric reliability framing now applies to both legs** — Codex reliability dimension is not only stall mode; Advisor reliability dimension is consistent hallucination across cycles. Cross-model diversity LOAD-BEARING again this PFR round despite Advisor hallucination: Codex caught stale V1 status text BLOCKING + L6 grep command drift + L1 hash reproduction issue + independently computed post-E.3 canonical hash `sha256:db2ce75bd41e8513` that Advisor missed; Advisor caught hash version state ambiguity + V-anchor advance discipline + R3.1a §12 sealed-content-invariance preservation framing that Codex missed; Advisor's PFR-F1 phantom-citation hallucination directly catchable via 2 commands (`wc -l METHODOLOGY_NOTES.md` returns 7122 not 519; `sed -n '7079p'` returns the actual Tag policy section header) — citation verification discipline operationally load-bearing per Option II codification, applied to both legs symmetrically.

**OBSERVATION 7 (Phase B Tier 5-6 framing under SPOT venue):** All subsequent Phase B Tier 5-6 work operates under SPOT venue context per R4.1 formal commitment. R5.1 candidate-subset commitment, R5.2 selection-inflation handling, R6.1 promotion class — each framed under SPOT venue but each eligible-not-named for separate Charlie register-event boundary.

---

## §10 Reserved Decisions (Eligible-Not-Named)

Per anti-pre-emption invariant — no decision pre-named in this cycle. Reserved for separate Charlie register-event boundary:

1. Path 1 paper trading basis under Branch.A — spot-realistic / venue-corrected research-time / rescoped / invalidated (per Phase 5.2 §6.4 line 264; per X2' explicit enumeration)
2. Path 2 (full L2 replay infrastructure) marginal value — fire / defer / conditional (per Phase 5.2 §6.4 line 265; per X2' explicit enumeration)
3. Stratum A D-I classification under spot-venue framing — preserved / re-interpreted / invalidated (per Phase 5.2 §6.4 line 266; per X2' explicit enumeration)
4. R3.1b/c empirical small-lot venue-conditional cost measurement scope + method
5. R3.1d post-venue cost-grid re-anchor scope + method (per X8 explicit non-resolution by E.3)
6. Phase B Tier 2 conditional prereqs (R2.1 / R2.2 / R2.3) — informed by R1.2 SEAL findings; sequencing eligible-not-named
7. R5.1 Phase B candidate-subset commitment under SPOT venue context (R1.2 AMBIGUOUS verdict binds harder; 3-candidate IS-OOS-consistent sub-cohort framing eligible per R1.2 OBS 5; advisor R3.1a Round 2 post-hoc timing finding requires pre-commitment of filter criterion)
8. R5.2 predeclared selection-inflation handling cycle (conditional on R5.1)
9. R6.1 Phase B promotion class (minimum-trade-count gate + per-strategy multiplicity pre-bind; pause branch eligible)
10. Tier-0 pause / strategic-absorption per V3 top note
11. Cost-model semantics upgrade beyond `effective_7bps_per_side` (deferred to Phase 3 per CLAUDE.md Execution Convention §4; Phase 5.1 successor paths apply)
12. Memory codification investigation on advisor hallucination rate elevation (R3.1a forward-only carry-forward OBS 2 + R4.1 forward-only carry-forward OBS 6)
13. Pre-existing noise cleanup register-event (`.DS_Store` + `docs/d7_stage2c/*`; 12+ session carry-forward)
14. Phase 2.5 bandit-dedup activation (parked per `docs/parked/PARKED_BRANCHES.md`)
15. Project pause / strategic-absorption register-event
16. Other Charlie-specified reserved decisions

---

## §11 V# Anchor Chain (R4.1)

| V# | State | Description |
|----|-------|-------------|
| V1 | ARCHIVED | Initial draft pre-V1-reviewer-round; sub-decisions A/B/C + X1-X8 locked per Charlie register chain "fire, authorized R3.1a E.3 upgrade trigger and Tier 4 R4.1" + Round 1 dispatch + PFR ratification + "ratify all". Structure mirrors R3.1a V4 SEAL format with §8 added for `config_hash` impact (vs R3.1a §6 Grandfathering Rationale). 13 main §§ (§0 through §12) + cycle metadata header. |
| V2 | ARCHIVED | Post-V1-reviewer-round revised draft after 2-leg subagent dispatch (codex:codex-rescue + quant-research-advisor parallel; combined R4.1 + R3.1a §12 Errata + execution.yaml diff scope per X6'). Round 1 verdict: Codex BLOCK (1 BLOCKING hallucination + 2 POLISH + 1 substantive LBR); Advisor APPROVE-WITH-FINDINGS (1 SUBSTANTIVE hallucination + 4 POLISH + 2 LBRs). Reviewer reliability this round: 0/2 stalls; **1/2 Codex hallucinations (FIRST verified Codex hallucination in Reading 3 pilot — "section 4 vs §4 mismatch" BLOCKING claim directly contradicted by Codex own V4 verification step)** + 1/2 verified Advisor hallucinations (R4.1 paraphrase "SPOT-execution-only assumed from data source through Phase B" fabrication + Phase 5.2 §6.4 text "SPOT execution assumption retained" fabrication). PUSHBACK applied on both BLOCKING findings per Option II citation verification discipline (each verified via 1 grep command). V1 → V2 applied 7 ADOPT patches (L1-L7): **L1 SUBSTANTIVE/NEW-CONTENT** parsed-YAML hash false-negative invariance check warning; **L2 POLISH** METHODOLOGY_NOTES.md §32 line 7079 standalone Tag policy cross-ref; **L3 POLISH** git log re-order; **L4 POLISH** §6 scope note + X2' authorization cross-ref; **L5 POLISH** §6 vs §10 clarification; **L6 SUBSTANTIVE** read-time hash semantics (verified `compute_config_hash()` write-time only); **L7 OPERATIONAL** Phase Marker pre-commit drafting note. OBS 6 cumulative pilot reliability stats updated: first Codex hallucination observed; symmetric reliability framing now applies to Codex too. |
| **V3** | **REVISED-POST-PFR-RULE-Y (current; pre-V_SEAL)** | Post-PFR-rule-Y re-review revised draft after 2-leg subagent dispatch (codex:codex-rescue + quant-research-advisor parallel) on V2 state per Charlie register "PFR rule-Y dispatch after V2" + adjudication ratification "ratify". PFR Round 1 verdict: Codex APPROVE-WITH-FINDINGS (1 BLOCKING-before-V_SEAL stale-text + 1 SUBSTANTIVE grep-command drift + 1 POLISH parsed-YAML hash + 1 POLISH meta-review grep-trap NOT-ADOPT); Advisor APPROVE-WITH-FINDINGS (**1 SUBSTANTIVE hallucination on 3 fabricated facts** + 2 POLISH). Reviewer reliability this PFR round: 0/2 stalls; 0/2 Codex hallucinations (clean this round); **1/2 Advisor hallucinations (PFR-F1 METHODOLOGY_NOTES.md phantom-citation hallucination — 3 fabricated facts: claimed file is 519 lines (actually 7122), §32 doesn't exist (actually at line 7079), R4.1/R3.1a don't contain METHODOLOGY_NOTES cross-refs (actually at lines 22 + 467); Codex PFR L2 APPROVE independently corroborated; PUSHBACK applied)**. V2 → V3 applied 5 ADOPT patches (A1-A5): **A1 SUBSTANTIVE/BLOCKING-before-V_SEAL** stale V1 status text correction in R4.1 lines 5 + 39 + 489 (V1 DRAFT → V3 REVISED-POST-PFR-RULE-Y status; register chain rows 5-7 LANDED with dispatch text; "Cycle awaits V1 reviewer dispatch" → "Cycle awaits V_SEAL register"); **A2 SUBSTANTIVE** L6 grep command form clarification (narrow `compute_config_hash` grep returns 2 lines 188+265; broader `config_hash` grep returns 4 lines 62+188+243+265); **A3 POLISH** parsed-YAML hash value `96ad75eadb28ee0a` dropped as under-specified (different serializers produce different non-canonical hashes); post-E.3 canonical hash `sha256:db2ce75bd41e8513` (Codex PFR independent computation) added to §8.4 + §12.4 (R4.1 + R3.1a) + §8.10 + §12.12 (R4.1 + R3.1a); **A4 POLISH** hash version state ambiguity resolution in §8.4 + §12.4 (R4.1 + R3.1a) — explicit pre-E.3 / post-E.3 labeling; **A5 DISCIPLINE** V-anchor chain advance (this row). V3 patches all mechanical literal landings of PFR findings → SKIP further PFR per `feedback_reviewer_routing_subagent_default.md` routing routine. OBS 6 cumulative pilot reliability stats updated for PFR round: 7th Advisor hallucination this pilot. |
| V_SEAL | PENDING (awaiting Charlie register) | Canonical sealed artifact at register-event boundary; SEAL bundle commit + push authorized only at explicit Charlie register. |

---

## §12 References

**Sealed project artifacts (read-only at this cycle):**
- [`docs/phase5/PHASE5_2_VENUE_RECONCILIATION_NOTE.md`](../phase5/PHASE5_2_VENUE_RECONCILIATION_NOTE.md) (Phase 5.2 V4 SEAL; §6.4 Branch.A definition load-bearing for §2.1 + §3.1; §3 fee table load-bearing for §8.2 false-attribution avoidance)
- [`docs/phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md`](../phase5/R3_1A_VENUE_INFRASTRUCTURE_NOTE.md) (R3.1a V4 SEAL; §0 anti-pre-naming binding load-bearing for §3.4 + §4.3; §7.2 inverted trigger logic load-bearing for §3.2 + §7.2; §6.1 Pillar 1 load-bearing for §5.3 + §8.5; §6.2 Pillar 2 load-bearing for §8.6; §6.3 Pillar 2 cost classification load-bearing for §8.6; §8 OBS 4 Phase 4 carve-out load-bearing for §8.8)
- [`docs/phase5/PHASE5_A_CLARIFICATION_NOTE.md`](../phase5/PHASE5_A_CLARIFICATION_NOTE.md) (Phase A V4 SEAL + §11 Errata appended at commit `9c00f59`; load-bearing precedent for SD5 + §3.5 post-SEAL errata append protocol)
- [`docs/phase5/R1_2_IS_OOS_RANK_CORRELATION_NOTE.md`](../phase5/R1_2_IS_OOS_RANK_CORRELATION_NOTE.md) (R1.2 V4 SEAL; AMBIGUOUS verdict informs Phase B Tier 5 R5.1 framing per §10 item 7)
- [`docs/phase5/PHASE5_1_COST_MODEL_INVESTIGATION_NOTE.md`](../phase5/PHASE5_1_COST_MODEL_INVESTIGATION_NOTE.md) (Phase 5.1 V4 SEAL; §3.2 Stratum A D-I classification framework referenced in §6.3; §7.1 successor paths referenced in OBS 3)
- [`config/execution.yaml`](../../config/execution.yaml) (pre-E.3 byte content; line 42 patched at SEAL bundle commit per §8.2)
- [`CLAUDE.md`](../../CLAUDE.md) Phase Marker + HARD CONSTRAINTS (`config/execution.yaml` modification approval rule satisfied by Charlie register fire) + Execution Convention §4 (`effective_7bps_per_side` simplification rule) + Tag policy shorthand (Bucket-1 ≠ arc-level closeout per §3.5 + SD2 lock)
- [`docs/discipline/METHODOLOGY_NOTES.md`](../discipline/METHODOLOGY_NOTES.md) §32 sub-§ "Tag policy at SEAL register-event boundaries" at line 7079 (standalone codified tag policy; CLAUDE.md Phase Marker shorthand references this)

**Live code references (verified 2026-05-18):**
- [`backtest/experiment_registry.py:48`](../../backtest/experiment_registry.py#L48) (`CONFIG_FILES` definition; 3-file scope load-bearing for §8.3)
- [`backtest/experiment_registry.py:188`](../../backtest/experiment_registry.py#L188) (`compute_config_hash()` def; load-bearing for §8.3 + §8.6)
- [`backtest/slippage.py:135-136`](../../backtest/slippage.py#L135-L136) (`config["cost_model"]["default_fee_bps"]` + `slippage_bps`; load-bearing for §5.2 Pillar 1 structured-keys-only invariance)
- [`backtest/execution_model.py:137-138`](../../backtest/execution_model.py#L137-L138) (`ConstantSlippage.from_config(config)`; same)

**Verified state at V1 DRAFT time:**
- HEAD parent commit: `46615cf`
- Pre-E.3 `config_hash`: `sha256:3850424a0ef2d292` (Python `hashlib.sha256` reproduction; matches Codex Round 1 + PFR independent computation)
- Both subject to recomputation at SEAL bundle landing time

**Cross-reviewer adjudication corpus (this cycle):**
- R4.1 Round 1 reviewer dispatch: Codex (APPROVE-WITH-FINDINGS; 7 sub-decisions adjudicated + 3 load-bearing risks named) + quant-research-advisor (APPROVE-WITH-FINDINGS; 7 sub-decisions + 4 risks A-D; Risk A line-189 hallucination identified + PUSHBACK applied)
- R4.1 PFR rule-Y cross-validation: Codex (APPROVE-WITH-FINDINGS; 10 mechanical tightenings + pre-E.3 hash independently computed) + quant-research-advisor (APPROVE-WITH-FINDINGS; X7 historical-entry retention + Risk N1 sealed-content-invariance hallucination + PUSHBACK applied per Phase A `9c00f59` precedent verification)
- "ratify all" Charlie register: SD1-SD7 + X1-X8 locked per PFR-updated sub-decision lock proposal

**External literature posture:** None invoked. All factual claims grounded in sealed in-repo artifacts + direct file inspection + Python `hashlib.sha256` reproduction.

---

**End of V3 REVISED-POST-PFR-RULE-Y.** R4.1 Phase B Tier 4 venue commitment cycle drafted with Branch.A formal commitment as headline finding. 7 OBSERVATIONS named anti-pre-naming + 16 reserved decisions eligible-not-named. R3.1a E.3 errata trigger documented in §7.2 + §8 with explicit `config_hash` impact (per X3'+X4'+X5+X7+X8). Companion artifacts at V3: R3.1a §12 Errata appendix (separate post-SEAL append per Phase A `9c00f59` precedent) + `config/execution.yaml` line 42 patch (executed at SEAL bundle commit landing per X6 atomicity). Pre-E.3 canonical `config_hash` (computed on current line 42 = "Binance perpetual futures, VIP 0"): `sha256:3850424a0ef2d292`; post-E.3 canonical (independently computed by Codex PFR via byte-level reproduction): `sha256:db2ce75bd41e8513`. Cycle awaits Charlie V_SEAL register for atomic SEAL bundle commit per SD6.
