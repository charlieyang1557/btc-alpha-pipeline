# Phase 5 Diagnostic Sub-Spec

> **Status:** WORKING DRAFT (session-11; §0–§5 authoring; §6–§8 pending)
> **Cycle:** Phase 5 sub-spec drafting cycle (Path 1 per scoping decision §6 successor cycle eligible-not-named slot)
> **Meta-plan:** [`docs/superpowers/plans/2026-05-09-phase5-diagnostic-subspec.md`](../superpowers/plans/2026-05-09-phase5-diagnostic-subspec.md) (sealed at `0e305bc`)
> **Spec source:** [`PHASE5_SCOPING_DECISION.md`](PHASE5_SCOPING_DECISION.md) (sealed at `697c26b` 2026-05-10T01:29:10Z)

---

## §0 — Scope + structure

This sub-spec operationalizes the Phase 5 diagnostic-attribution procedure pre-registered at [`PHASE5_SCOPING_DECISION.md`](PHASE5_SCOPING_DECISION.md) §2.1–§2.5. Its purpose is to lock — before any diagnostic execution fires — the indicator-extraction procedures, cutoffs, multi-mode coexistence rules, ambiguity-handling rules, successor-cycle-class mapping, framing-question resolution-pressure assessment procedure, terminal-conclusion criteria, and attribution-report structure that Phase 5 will operate under.

Scope is constrained by scoping decision §2.2 operationalization-freeze: thresholds must be frozen before any diagnostic execution can fire. This sub-spec does NOT execute the diagnostic; this sub-spec does NOT resolve framing-question (1)/(2)/(3)/(4); this sub-spec is the procedural lockpoint upstream of the diagnostic fire.

Section-by-section:

- **§1 Locked inputs** — existing sealed artifacts the diagnostic consumes; derived-vs-new-data boundary per scoping §4.1.
- **§2 Per-mode operationalization (§2.a–§2.f)** — indicator extraction + cutoffs per failure mode in scoping §2.1 taxonomy. Load-bearing.
- **§3 Multi-mode rules + diagnostic ambiguity** — §3.1 coexistence (multiple modes positively detected) + §3.2 ambiguity (procedure fails to reliably separate).
- **§4a Successor-cycle-class mapping** — operationalization of scoping §2.3 mapping.
- **§4b Framing-question resolution-pressure assessment procedure** — operationalization of scoping §2.4. Procedure ≠ resolution; resolution happens at Phase 5 SEAL based on diagnostic findings.
- **§5 Terminal-conclusion criteria** — operationalization of scoping §2.5; preserves substantive-availability + operational-availability distinction per scoping §2.5 / Mod-pass-2 F1 disambiguation.
- **§6 Attribution report deliverable structure** — Phase 5 closeout deliverable framework; substantively depends on §5.
- **§7 Verification + reviewer disposition** — V# verification chain to be fired at sub-spec SEAL pre-fire boundary; reviewer pass routing at sub-spec SEAL.
- **§8 Cross-references** — anchored references to sealed corpus.

---

## §1 — Locked inputs

Phase 5 diagnostic procedure consumes only sealed artifacts at Phase 5 cycle entry. Source set is frozen; acquisition of new empirical data is prohibited per scoping §4.1.

**Sealed inputs binding the diagnostic procedure:**

- **Phase 4 null result anchor** — closeout deliverable at [`docs/closeout/PHASE4_RESULTS.md`](../closeout/PHASE4_RESULTS.md) sealed at `e8f62f1`; tag `phase4-forward-test-v1` at seal commit; finding content + per-stratum statistics consumed by §2 / §6 from this sealed source.
- **Candidate-cohort reference** — `data/phase4_scoping/cohort_a_candidate_reference.csv` sealed at `11b39f2`; 39 candidates from PHASE2C_15 `cohort_a` with Phase 4 analysis partition per PHASE4_PLAN §1.3 (calendar_effect n=22, non-calendar n=17).
- **Engine + WF lineage anchor** — engine commit `eb1c87f` (corrected walk-forward implementation); lineage chain anchor at tag `wf-corrected-v1` (commit `3d24fcb`; anchors engine fix `eb1c87f` + lineage guard `5f53ee5`).
- **Phase 4 forward-test artifacts** — `data/phase2c_evaluation_gate/phase4_forward_2026_{07,13,15,17}bps_v1/` cost-sweep + `data/phase2c_evaluation_gate/phase4_smoke_15bps_v0/` smoke; parquet anchor `db4ce1d2a2e5e7b556975837260f7aaa29ee4fd5ddc603690d1bc57912aa7035` invariant across artifacts; forward window `[2026-01-01T00:00:00Z, 2026-04-16T07:00:00Z]`; 2528 forward bars.
- **PHASE2C_15 main-fire artifacts** — sealed per-regime evaluation-gate artifacts at `data/phase2c_evaluation_gate/phase2c_15_main_fire_{bear_2022,audit_2024,eval_2020,eval_2021}_v1[_filtered]/` + walk-forward at `data/phase2c_walkforward/batch_phase2c_15_main_fire_combined_corrected/`.
- **Sealed prior-cycle corpus** — closeout MDs (PHASE2C_10/11/12/13/15) + PHASE2C_14 sub-spec MDs at `docs/phase2c/PHASE2C_14_{PLAN,SCOPING_DECISION}.md` + METHODOLOGY_NOTES.md sealed sections + CLAUDE.md Phase Marker entries through Phase 5 entry SEAL. Treated as read-only within the Phase 5 diagnostic cycle per scoping §4.6.

**Derived-vs-new-data boundary** (per scoping §4.1; §8 will anchor the verbatim source quote):

- **ALLOWED**: derived statistics + diagnostic computations from sealed artifacts (re-aggregations, alternative metric profiles on existing per-candidate trade data, statistical decomposition of existing forward Sharpe distributions, regime-conditioning of existing cohort indicators, etc.).
- **NOT ALLOWED**: additional BTC OHLCV history beyond what is already on disk, additional candidate generation, additional regime data, additional Critic/Proposer API calls, new methodology codification.

**Scope binding:** All downstream sections (§2–§6) shall consume only the inputs declared in this section; classification of "derived" vs "new" per the boundary above is binding throughout.

---

## §2 — Per-mode operationalization

Phase 5 diagnostic procedure comprises six per-mode indicator computations corresponding to the failure-mode taxonomy at scoping §2.1. Each sub-section pre-registers: (i) indicator-extraction procedure, (ii) judgment rule producing the per-mode judgment outcome, (iii) descriptive supplementary statistics for §6 attribution-report nuance, (iv) data source classification per §1 binding, (v) register-class tag.

Judgment rules and any pre-registered thresholds are §2.2-frozen at sub-spec SEAL register-event boundary; subsequent modification requires errata register-event at separate Charlie register-event boundary.

### §2.a Signal decay

Operationalizes scoping §2.1(a): "candidate-level temporal deterioration despite training/test validity." Diagnostic question: did each cohort_a candidate's alpha decay between training/test period and the 2026 forward window, holding cost basis constant at the PHASE2C-research 7bps-per-side effective cost?

**Indicator construction.** For each candidate i ∈ cohort_a (n=39):

- `training_test_sharpe[i]` = arithmetic mean of `holdout_bear_2022_sharpe` and `holdout_validation_2024_sharpe` columns from [`data/phase4_scoping/cohort_a_candidate_reference.csv`](../../data/phase4_scoping/cohort_a_candidate_reference.csv) (sealed at `11b39f2`). These are PHASE2C_15 selection-stage per-regime holdout Sharpes (OOS by regime classification), NOT walk-forward test-period Sharpes (`wf_test_period_sharpe` column; not used as baseline). OOS-only baseline; `eval_2020_v1` and `eval_2021_v1` excluded from the primary baseline to avoid train-overlap inflating the decay reference point (PHASE2C_8.1 train-overlap regime classification per CLAUDE.md; `eval_2020_v1` and `eval_2021_v1` evaluation windows fall entirely within v2 train_windows).
- `forward_sharpe[i]` = `holdout_sharpe` column from [`data/phase2c_evaluation_gate/phase4_forward_2026_07bps_v1/holdout_results.csv`](../../data/phase2c_evaluation_gate/phase4_forward_2026_07bps_v1/holdout_results.csv). Phase 4 07bps descriptive-supplement cost-run was authored at PHASE2C_15-comparability cost basis per Phase 4 closeout §3 — same 7bps-per-side effective cost as PHASE2C_15 training/test, enabling clean temporal-isolation per D1 lock.
- `delta[i] = forward_sharpe[i] − training_test_sharpe[i]`. Non-finite / NaN / missing values in either leg drop that candidate pair from the §2.a Wilcoxon input; any such drop must be enumerated in §6 with hypothesis_hash, regime, and original raw value.

**Judgment rule.** Wilcoxon signed-rank one-sided test on the paired deltas at α = 0.05; H_0: median(delta) = 0, H_1: median(delta) < 0. Zero deltas use SciPy `wilcoxon(..., zero_method="wilcox", alternative="less")`; zero-delta pairs are excluded from the signed-rank statistic. If all deltas are zero after zero-method exclusion (degenerate edge), §2.a outcome is `not detected` and §6 must enumerate the zero-delta condition. Canonical §2.a outcome is binary: **signal decay detected** if p ≤ 0.05; **not detected** otherwise. Computed at cohort-aggregate level (effective n ≤ 39 after non-finite-pair exclusions); per-stratum tests on Stratum A (calendar_effect, n ≤ 22) and Stratum B (non-calendar, n ≤ 17) at α = 0.05 each are supplementary indicators and do not modify the cohort-aggregate canonical outcome.

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs): `mean(delta)`, `median(delta)`, `prop(delta < 0)` over valid paired deltas after non-finite-pair exclusions, paired Cohen's d = mean(delta) / sample standard deviation(delta), per-stratum decomposition; and a **mean-of-4 supplementary baseline**: `training_test_sharpe[i]` = mean of all 4 PHASE2C_15 regime holdout Sharpes (bear_2022 + validation_2024 + eval_2020_v1 + eval_2021_v1), including the two train-overlap regimes excluded from the primary baseline; surfaces the train-overlap-inflation differential vs OOS-only primary baseline.

**Data source per §1:** on-disk (both legs read directly from sealed CSV artifacts); paired-delta + Wilcoxon + supplements are §1-ALLOWED derived statistics on existing per-candidate Sharpe data.

**Register-class:** binding | quantitative (α = 0.05) | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance.** §2.a output operates at 7bps temporal-isolation register; it does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion). §6 attribution-report wording is forward-bound to make this invariance explicit (forward-binding obligation tracked at sub-spec drafting cycle register).

### §2.b Cost drag

Operationalizes scoping §2.1(b): "realistic-cost-model differential. Alpha exists at gross level (low/zero bps); killed by realistic 15bps round-trip costs." Diagnostic question: does the cost-induced deterioration mechanism operate at per-candidate level across the Phase 4 cost-sweep range?

**Indicator construction.** For each candidate i ∈ cohort_a (n=39):

- Read `holdout_sharpe` per cost level k ∈ {7, 13, 15, 17} bps from `data/phase2c_evaluation_gate/phase4_forward_2026_{07,13,15,17}bps_v1/holdout_results.csv` (Phase 4 forward-test cost-sweep; all 4 cost-runs evaluated on the identical 2026 forward-test candidate set and time window per Phase 4 closeout §4 anchor invariance).
- `ρ_i = scipy.stats.spearmanr([7, 13, 15, 17], [Sharpe_i[7bps], Sharpe_i[13bps], Sharpe_i[15bps], Sharpe_i[17bps]]).correlation`. Cost-vector convention: ascending [7, 13, 15, 17] (locked). SciPy `spearmanr` average-rank tie handling is accepted implicitly via library-default semantics; no custom tie correction is applied. Negative ρ indicates monotonic Sharpe deterioration as transaction cost increases.
- Constant per-candidate Sharpe across all 4 cost levels produces undefined Spearman correlation (ρ = nan) due to zero variance in the Sharpe vector: drop that candidate from the §2.b Wilcoxon input; enumerate in §6 with hypothesis_hash and per-cost Sharpe values (parallel to §2.a NaN protocol).

**Judgment rule.** Wilcoxon signed-rank one-sided test on the per-candidate ρ values at α = 0.05; H_0: median(ρ) = 0, H_1: median(ρ) < 0. SciPy semantics: `wilcoxon(..., zero_method="wilcox", alternative="less")`; zero-ρ candidates excluded from the signed-rank statistic. If all ρ values are zero after zero-method exclusion (degenerate edge), §2.b outcome is `not detected` and §6 must enumerate the zero-ρ condition. Canonical §2.b outcome is binary: **cost drag detected** if p ≤ 0.05; **not detected** otherwise. Computed at cohort-aggregate level (effective n ≤ 39 after nan/zero-variance exclusions); per-stratum tests on Stratum A (calendar_effect, n ≤ 22) and Stratum B (non-calendar, n ≤ 17) at α = 0.05 each are supplementary indicators (inherits §2.a stratification pattern) and do not modify the cohort-aggregate canonical outcome.

**Orthogonal-axis decomposition with §2.a.** §2.a measures the temporal-isolation axis at fixed 7bps cost basis; §2.b measures the cost-isolation axis across the 7-vs-17 bps range at fixed forward 2026 window. The algebraic decomposition identity is reserved for §6 attribution-synthesis register.

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs): `mean(ρ)`, `median(ρ)`, `prop(ρ < 0)` over valid candidates after nan/zero-variance exclusions, paired Cohen's d on the ρ distribution = mean(ρ) / sample standard deviation(ρ), per-stratum decomposition; multi-pair magnitude supplements: paired delta `forward_sharpe[i, a bps] − forward_sharpe[i, b bps]` for `(a, b) ∈ {(7, 13), (7, 15), (7, 17), (13, 15)}` (cohort-level mean and Wilcoxon per pair) at descriptive register only — feeds §6 magnitude narrative, not §2.b judgment.

**Data source per §1:** on-disk (4 cost-sweep CSVs read directly from sealed Phase 4 cost-sweep artifacts); per-candidate Spearman + cohort Wilcoxon + supplements are §1-ALLOWED derived statistics on existing per-candidate Sharpe data.

**Register-class:** binding | quantitative (α = 0.05) | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (inherited from §2.a discipline).** §2.b output operates at the cost-axis-decomposition register; combined with §2.a output, it does NOT modify the Phase 4 closeout verdict at 15bps. §6 attribution-report wording is forward-bound to preserve this invariance.

### §2.c Cohort weakness

Operationalizes scoping §2.1(c): "population-level economic weakness even when statistical gate passes. Statistical AND-gate criterion is satisfied but the underlying candidate population is economically weak in aggregate." Diagnostic question: did the PHASE2C_15 AND-gate produce materially distinct candidate selection from the broader generation pool, or is cohort_a a thin-margin selection from an economically shallow universe? §2.c evaluates selection-time cohort separation only; it does not evaluate subsequent forward persistence, temporal decay, or transaction-cost robustness. Per D3'' lock: §2.c assumes AND-gate evaluation criterion is reliable and tests cohort substrate strength under that assumption; §2.d (future authoring) tests AND-gate's own validity.

**Indicator construction.**

- **Target cohort:** 39 cohort_a candidates per [`data/phase4_scoping/cohort_a_candidate_reference.csv`](../../data/phase4_scoping/cohort_a_candidate_reference.csv) (sealed at `11b39f2`).
- **Broader-universe baseline:** 993 PHASE2C_15 main fire candidates (the broader generated/evaluated universe referenced by CLAUDE.md PHASE2C_15 SEAL); per-candidate per-regime artifacts at `data/phase2c_evaluation_gate/phase2c_15_main_fire_{bear_2022,audit_2024,eval_2020,eval_2021}_v1/<hash>/holdout_summary.json`. cohort_a ⊂ broader-universe (cohort_a is the AND-gate-passing subset); cohort_a candidates remain included in `broader_universe_score` — the comparison anchor is the full generated/evaluated universe (993), not the cohort_a-excluded subset (954).
- **Selection-time score (primary, OOS-only):** `score_i = arithmetic mean of (sharpe_bear_2022_i, sharpe_validation_2024_i)` per candidate, where each regime Sharpe is the `holdout_metrics.sharpe_ratio` field in the candidate's per-regime `holdout_summary.json`. OOS-only baseline (sub-spec internally consistent with §2.a primary baseline); `eval_2020_v1` and `eval_2021_v1` train-overlap regimes excluded from primary score to avoid train-overlap-differential-inflation contamination of percentile-rank test (selection pressure on train-overlap regimes preferentially picks high-inflation candidates → asymmetric inflation between cohort_a and broader-universe → under-detection bias). Computed for all 993 broader-universe candidates AND the 39 cohort_a candidates on the same OOS gate-evidence surface.
- **Selection-time score (supplementary, mean-of-4):** `score_4regime_i = arithmetic mean of all 4 PHASE2C_15 regime holdout Sharpes (bear_2022 + validation_2024 + eval_2020_v1 + eval_2021_v1)`. Gate-metric-faithful aggregation including train-overlap regimes; recomputed identical percentile-rank structure for §2.c supplementary descriptive register; surfaces train-overlap-differential-inflation magnitude vs OOS-only primary.
- **Non-finite handling:** any candidate with non-finite / missing `sharpe_ratio` in any required regime (2 regimes for primary; 4 regimes for supplementary) drops that candidate from the respective §2.c computation; enumerate in §6 (parallel to §2.a / §2.b NaN protocols).

**Selection-bias caveat (binding interpretation constraint).** cohort_a passed the PHASE2C_15 AND-gate; broader-universe candidates outside cohort_a (954 candidates) did not. The AND-gate criterion is a conjunction of per-regime sub-criteria (per regime: min_sharpe = −0.5, max_drawdown = 0.25, min_total_return = −0.15, min_total_trades = 5 per CLAUDE.md PHASE2C v2 split definition; per-candidate per-regime pass/fail recorded via cohort_a CSV `holdout_*_passed` columns). cohort_a's score distribution is structurally conditioned by gate passage; broader-universe distribution is unrestricted. **The §2.c percentile-rank test discriminates whether cohort_a achieves material separation from the broader-universe distribution (top-quartile-strict median + top-decile-strict upper-quartile), not whether cohort_a is statistically distinct from the broader-universe, which is not the diagnostic target under this mode.** §6 attribution-report wording is forward-bound to preserve this interpretive distinction explicitly.

**Judgment rule.** Two-condition AND on cohort_a's OOS-only score distribution against the broader-universe OOS-only score distribution. **Percentile-rank convention:** `percentile_rank(x, broader_universe_score) = share of broader-universe candidates with score ≤ x`; computed via `scipy.stats.percentileofscore(broader_universe_score, x, kind='weak')`. Under `kind='weak'`, candidates exactly equal to the comparison score are counted in the percentile rank numerator. **Quantile convention:** `p75(cohort_a_score)` computed via `numpy.quantile(cohort_a_score, 0.75, method='linear')`.

- Condition (1): `percentile_rank(median(cohort_a_score), broader_universe_score) < 0.75` — cohort_a median fails to exceed the broader-universe third quartile.
- Condition (2): `percentile_rank(p75(cohort_a_score), broader_universe_score) < 0.90` — cohort_a's upper quartile fails to exceed the broader-universe top decile.
- Detection: **cohort weakness detected** iff both Condition (1) AND Condition (2) hold; **not detected** otherwise.

**Substantive prior justification for 75th / 90th cutoffs (pre-registered per §2.2 freeze).** Under meaningful gate-pass selection from a healthy candidate pool, cohort_a median should exceed the broader-universe third quartile — selection should produce strict upper-half dominance against the broader-universe. Under meaningful elite selection at cohort's upper tail, cohort_a's own third quartile (p75) should exceed the broader-universe top decile — cohort's upper tier should reach the broader-universe top tier. Cutoffs are IQR-derived economic-separation priors at "material gate-pass selection" register; not standard statistical priors (e.g., α = 0.05); locked at this §2.c register-event boundary per §2.2 freeze; alternative cutoff values would shift §2.c sensitivity calibration.

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs): `mean(cohort_a_score)`, `median(cohort_a_score)`, percentile-rank values at additional positions (cohort p25, cohort p90); mean-of-4 supplementary score distribution + percentile ranks (train-overlap-inclusive view; surfaces inflation differential vs OOS-only primary); cohort_a (39) vs broader-universe (993) `wf_test_period_sharpe` distribution comparison via `walk_forward_results.csv` at `data/phase2c_walkforward/batch_phase2c_15_main_fire_combined_corrected/` (§1 sealed walk-forward output; cohort_a subset filtered by hypothesis_hash list from cohort_a reference CSV) — upstream cross-check not directly conditioned on AND-gate passage, while still upstream-correlated with gate passage; per-regime cohort_a vs broader-universe holdout Sharpe distributions (4 regimes × cohort/broader split).

**Data source per §1:** on-disk (993-candidate broader-universe per-regime artifacts + cohort_a CSV + walk_forward_results.csv); selection-time score + percentile-rank + supplements are §1-ALLOWED derived statistics on existing per-candidate Sharpe data.

**Register-class:** binding | quantitative (OOS-only primary score; 75th + 90th percentile-rank cutoffs; `kind='weak'` percentile convention; linear quantile interpolation) | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (inherited from §2.a discipline).** §2.c output operates at the selection-time cohort-substrate register; combined with §2.a/§2.b outputs, it does NOT modify the Phase 4 closeout verdict at 15bps. §6 attribution-report wording is forward-bound to preserve this invariance.

### §2.d Overfit AND-gate

Operationalizes scoping §2.1(d): "gate-construction flaw causing unstable selection logic. The *gate criterion itself* overfits regime-pair statistical noise; the selection mechanism is the subject of the flaw." Diagnostic question: do broader-universe per-regime AND-gate sub-pass patterns exhibit structure consistent with per-regime independence (each regime's sub-gate firing on regime-specific statistical noise; AND-combination producing observed 39-candidate cohort as a near-product-of-independent-filters outcome) versus strong positive correlation (sub-gates identifying shared underlying signal that AND-combination amplifies)? Per D3'' lock at §2.c: §2.d tests AND-gate's own validity (gate may be flawed → cohort_a is illusory winners); §2.c assumed gate-validity. §2.d operates at gate-construction internal register; does NOT consume Phase 4 forward-test data.

**Indicator construction (primary — Approach C).**

- **Per-regime pass-rate** for r ∈ {bear_2022, audit_2024, eval_2020, eval_2021}: `pᵣ = count_pass_r / count_finite_r` where `count_pass_r` is the number of broader-universe candidates passing the AND-gate sub-criterion at regime r and `count_finite_r` is the number with all 4 AND-gate input fields finite/non-missing in regime r.
- **AND-gate sub-criterion verbatim per CLAUDE.md PHASE2C v2 split definition:** `sharpe_ratio ≥ −0.5 ∧ max_drawdown ≤ 0.25 ∧ total_return ≥ −0.15 ∧ total_trades ≥ 5`, applied independently per regime to fields under `holdout_metrics`.
- **Canonical source:** computed fresh from [`data/phase2c_evaluation_gate/phase2c_15_main_fire_<regime>_v1/<hash>/holdout_summary.json`](../../data/phase2c_evaluation_gate/) `holdout_metrics` fields + the AND-gate criterion as cited above. **Audit consistency field:** pre-computed top-level `holdout_passed` per candidate's `holdout_summary.json`. **Pre-execution verification gate:** cross-validate canonical-fresh ≡ pre-computed-audit across 993 × 4 = 3972 pass-pattern bits; mismatch at any bit → §2.d execution suspends; §6 enumerates discrepancy; Charlie register re-adjudicates. §7 V# anchor binds this gate (FB #16). **Following successful verification, all §2.d primary-statistic computations use the canonical-fresh recomputed pass indicators rather than the stored audit field.**
- **Expected count under independence null:** `exp = Π_{r=1}^4 pᵣ × N` where `N = intersection_count = candidates finite in all 4 regimes` (empirically N = 993 per Path A probe; full broader-universe in current data state).
- **Observed count:** `obs = 39` (= cohort_a cardinality per [`data/phase4_scoping/cohort_a_candidate_reference.csv`](../../data/phase4_scoping/cohort_a_candidate_reference.csv) sealed at `11b39f2`; equivalently the count of broader-universe candidates passing the AND-gate in ALL 4 regimes).

**Indicator construction (second-angle supplementary — Approach A).**

- **Perturbation:** simultaneously perturb all 4 AND-gate sub-criteria thresholds by ±1σ where σ is the per-sub-criterion × per-regime standard deviation of the broader-universe distribution of that input metric (16 σ values: 4 sub-criteria × 4 regimes; computed at execution time after D3''' lock).
- **Stricter perturbation** (raise floor / lower ceiling per sub-criterion): `min_sharpe + σ_sharpe_r`, `max_drawdown − σ_dd_r`, `min_total_return + σ_return_r`, `min_total_trades + σ_trades_r`; produces a more-restrictive AND-gate.
- **Looser perturbation** (opposite direction per sub-criterion): produces a less-restrictive AND-gate.
- **Domain-validity clipping:** perturbed thresholds are clipped to domain-valid ranges where applicable (e.g., non-negative trade counts; drawdown bounds within [0, 1]). Prevents pathological perturbations from silently invalidating Approach A second-angle reporting.
- **Perturbed cohorts:** `stricter_cohort` and `looser_cohort` = candidates passing the perturbed AND-gate in all 4 regimes.
- **Stability metric:** `Jaccard(cohort_a, stricter_cohort)` and `Jaccard(cohort_a, looser_cohort)`, both bounded [0, 1]. **Substantive interpretation:** Jaccard near 1.0 = AND-gate calibration robust (perturbation produces near-identical cohort); Jaccard substantially below 1.0 = calibration brittle (small threshold movement substantially changes cohort membership). §6 attribution narrative scope handles concrete value-to-attribution mapping. Both reported as §6 attribution-narrative input only; do NOT fire binary judgment (no dual-gate composition with Approach C).

**Judgment rule (Approach C TOST equivalence test on `|obs − exp|`).** Two One-Sided Tests (TOST) equivalence-test framework at α = 0.05 with Binomial-exact noise scale.

- **Equivalence margin:** `ε = 2 · √(N · p · (1 − p))` where `N = intersection_count` and `p = Π_{r=1}^4 pᵣ`. ε is the standard deviation of the AND-gate pass count under `Binomial(N, p)` distribution scaled by k = 2 (i.e., ±2σ Binomial-exact noise scale under independence null).
- **TOST framework:** H₀: `|obs − exp| ≥ ε` (non-equivalent / non-independent); H₁: `|obs − exp| < ε` (equivalent / independent). Detection event = reject H₀ at α = 0.05 = confirm observed AND-gate pass count statistically consistent with the independence-model expectation derived from the per-regime pass-rates within the Binomial-exact equivalence margin, indicating AND-gate fires on regime-specific statistical noise rather than requiring strong shared underlying signal to explain the observed cohort size.
- **Canonical §2.d outcome is binary: overfit AND-gate detected** if TOST rejects H₀ at α = 0.05; **not detected** otherwise.

*(NB on TOST polarity: detection-of-overfit corresponds to the* equivalence *outcome — observed consistent with the independence-model expectation — not the* non-equivalence *outcome; this inverts standard 'reject H₀ = positive finding' intuition. §6 narrative writers preserve this inversion explicitly.)*

Approach A reports `Jaccard(cohort_a, stricter_cohort)` and `Jaccard(cohort_a, looser_cohort)` scalars to §6 attribution narrative; does NOT fire binary judgment. The §2.d binary outcome derives from Approach C TOST result alone.

**Substantive prior justification for ε k = 2 (pre-registered per §2.2 freeze).** Under independence null with N = 993 and `Π pᵣ ≈ 0.04` (illustrative; actual `Π pᵣ` computed at execution), the AND-gate pass count follows `Binomial(N, p)` with standard deviation `√(N·p·(1−p))`. The k = 2 multiplier corresponds to ±2σ equivalence margin, which aligns with α = 0.05 normal-approximation TOST convention. Tighter (k = 1) would impose ≈ ±1σ ≈ 68% noise tolerance, making detection bar substantively strict; looser (k = 3) would impose ≈ ±3σ ≈ 99.7% noise tolerance, making detection bar substantively permissive. k = 2 is the standard statistical convention at the regime-class register; cutoffs are σ-family statistical priors at "Binomial-exact independence noise tolerance" register, locked at this §2.d register-event boundary per §2.2 freeze. **Note:** Approach A independently uses k = 1 for σ-of-distribution perturbation at a register-class-distinct purpose (calibration noise scale rather than detection bar); the k = 1 / k = 2 / k = 3 discussion in this paragraph applies to Approach C TOST ε only.

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs): 4 per-regime pass-rates `pᵣ` reported; pairwise Cramér's V 4×4 symmetric matrix (6 unique pairwise associations + 4 diagonal sanity = 1.0; regime-pair correlation structure); 2⁴ pass-pattern distribution OPTIONAL §6 descriptive material (16-cell counts; **NOT primary statistic input — D2''' Point 2 lock — AND NOT eligible for post-hoc reinterpretation into the canonical §2.d binary outcome**); Approach A `Jaccard(cohort_a, stricter_cohort)` and `Jaccard(cohort_a, looser_cohort)` scalars.

**NaN-handling rule (defensive pre-registration; empirical no-fire per Path A probe).** Any broader-universe candidate with non-finite/missing value in any AND-gate input field in any regime drops from that regime's pass-rate computation `pᵣ`; per-regime denominator becomes `count_finite_r`. N for exp computation per above: intersection_count = candidates finite in all 4 regimes. §6 enumerates any drops with hypothesis_hash + regime + missing field name (§6 FB #18 forward-binding). Empirically per Path A probe: ZERO drops; N = 993 = full broader-universe = intersection_count.

**Data source per §1:** on-disk (993-candidate broader-universe per-regime artifacts + AND-gate sub-criterion + sealed cohort_a CSV); per-regime pass-rate computation + Binomial-exact expected count + TOST inference + Approach A perturbation + Jaccard stability + Cramér's V matrix are §1-ALLOWED derived statistics on existing per-candidate AND-gate input metrics.

**Register-class:** binding | quantitative (TOST equivalence-test framework; Binomial-exact noise scale `ε = 2·√(N·p·(1−p))`; α = 0.05; Approach A reports stability scalars, no binary judgment) | irreversible-interpretation-constraint per §2.2.

**§2.c/§2.d framing-chain (upstream interpretive gate structure).** §2.d acts as upstream interpretive gate for §2.c output:

- **§2.d not fires** (gate-validity assumption survives): §2.c output read at face value.
- **§2.d fires** (gate-overfit detected): §2.c output reading suspended; §6 attribution narrative must surface multi-interpretation ambiguity. Specific 2×2 attribution combinations (§2.d × §2.c outcomes) enumerated at §6 attribution narrative scope (FB #17).

**Anti-circularity caveat (brief; full narrative §6 forward-bound — FB #19).** Approach C tests broader-universe per-regime pass-patterns against the independence null. Strong positive correlation (large `|obs − exp|` with `obs > exp` exceeding the TOST equivalence margin) would suggest real shared signal underlying the AND-gate. However, correlation may alternatively reflect shared *unobserved* market-factor selection bias rather than real alpha. This disambiguation is §6 attribution narrative scope and does not affect §2.d primary judgment binary outcome.

**Phase 4 verdict invariance (inherited from §2.a/b/c precedent).** §2.d output operates at the gate-construction internal register; combined with §2.a/§2.b/§2.c outputs, it does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion). §6 attribution-report wording is forward-bound to preserve this invariance.

### §2.e Bad success metric

Operationalizes scoping §2.1(e): "success-criterion misalignment. Positive forward Sharpe fraction (PLAN §1.5) is too coarse / wrong metric for 'deployable alpha.'" Diagnostic question: would substituting PLAN §1.5's binary-positive-fraction test framework with a continuous-Sharpe test framework, while holding cost basis, strata partition, and α-regime constant, have changed Phase 4's null verdict? §2.e tests success-criterion machinery itself at the framework-substitution register, counterfactually substituting one variable (test statistic) and holding all other PLAN §1.5 specification constant. Per the scoping language quoted above, "too coarse" and "wrong metric" are alternative natural-language characterizations of the same single misalignment between PLAN §1.5 framework and deployable-alpha concept; not register-class-distinct sub-modes.

**Indicator construction.**

- **Target cohort and stratification:** 39 cohort_a candidates per [`data/phase4_scoping/cohort_a_candidate_reference.csv`](../../data/phase4_scoping/cohort_a_candidate_reference.csv) (sealed at `11b39f2`), partitioned by Phase 4 PLAN §1.3 stratification: Stratum A calendar_effect n_A=22, Stratum B non-calendar n_B=17 (identical to PLAN §1.5 partition).
- **Primary input (15bps, PLAN-§1.5-alignment register):** `forward_sharpe_15bps[i]` = `holdout_sharpe` column from [`data/phase2c_evaluation_gate/phase4_forward_2026_15bps_v1/holdout_results.csv`](../../data/phase2c_evaluation_gate/phase4_forward_2026_15bps_v1/holdout_results.csv) per candidate. 15bps cost basis matches PLAN §1.5 success-criterion basis exactly per Phase 4 closeout §1.5.
- **Supplementary input (7bps, descriptive register):** `forward_sharpe_7bps[i]` = `holdout_sharpe` column from [`data/phase2c_evaluation_gate/phase4_forward_2026_07bps_v1/holdout_results.csv`](../../data/phase2c_evaluation_gate/phase4_forward_2026_07bps_v1/holdout_results.csv) per candidate. 7bps cost basis at descriptive register only; feeds §6 cost-basis cross-check narrative.
- **Non-finite handling:** any candidate with non-finite/missing `holdout_sharpe` at the relevant cost basis drops from that cost-basis Wilcoxon input; enumerate in §6 with hypothesis_hash + stratum + cost basis + missing field name (parallel §2.a/b/c/d NaN protocols).

**Detection logic (4-cell interpretive scaffolding).** §2.e detection conditions on PLAN §1.5 outcome × §2.e Wilcoxon outcome 4-cell table:

| Cell | PLAN §1.5 | Wilcoxon | §2.e outcome |
|---|---|---|---|
| (1) | fail to reject | fail to reject | not detected (both frameworks agree on null) |
| (2) | fail to reject | reject | **detected** (continuous framework caught what binary missed) |
| (3) | reject | fail to reject | foreclosed |
| (4) | reject | reject | foreclosed |

Per §1 sealed inputs: [`PHASE4_RESULTS.md`](../closeout/PHASE4_RESULTS.md) (sealed at `e8f62f1`) reports PLAN §1.5 success criterion `fail to reject` at both strata (Stratum A: 11/22 positive Sharpe at 15bps, binomial p=0.5841; Stratum B: 7/17 positive Sharpe at 15bps, binomial p=0.8338). This sealed outcome forecloses Cells (3) and (4); §2.e operational detection reduces to Cell (2): Wilcoxon rejects H_0 in either stratum under Bonferroni-corrected α=0.025.

**Judgment rule.** One-sided Wilcoxon signed-rank test per stratum on `forward_sharpe_15bps` vs 0 at Bonferroni-corrected α=0.025 per stratum (matching PLAN §1.5 Bonferroni discipline per Phase 4 closeout §1.5). SciPy invocation: `scipy.stats.wilcoxon(forward_sharpe_15bps_stratum, alternative="greater", zero_method="wilcox")`. H_0: median(forward_sharpe_15bps_stratum) = 0; H_1: median(forward_sharpe_15bps_stratum) > 0. Zero-Sharpe candidates excluded from signed-rank statistic per `zero_method="wilcox"`; if all candidates within a stratum are zero after exclusion (degenerate edge), that stratum's outcome is `not detected` and §6 must enumerate the zero-Sharpe condition. **Canonical §2.e outcome is binary: framework-substitution detected** if Wilcoxon rejects H_0 in either stratum under Bonferroni-corrected α=0.025; **not detected** otherwise. Effective n per stratum after non-finite/zero exclusions: n_A_eff ≤ 22, n_B_eff ≤ 17.

**Substantive prior justification for Bonferroni α=0.025/stratum (pre-registered per §2.2 freeze).** §2.e is a counterfactual comparison against PLAN §1.5, not a discovery screen. The counterfactual diagnostic isolates one variable (test statistic) by holding all other PLAN §1.5 specification constant (cost basis, strata partition, correction procedure). PLAN §1.5 used Bonferroni at α=0.025 per stratum (per Phase 4 closeout §1.5). Matching Bonferroni at α=0.025 per stratum preserves counterfactual cleanliness: if §2.e Wilcoxon rejects, the only changed variable from PLAN §1.5 is the test statistic — clean attribution of detection to framework substitution. Alternative correction procedures (**Benjamini-Hochberg step-up at FDR q=0.05** — substitutes Bonferroni with FDR-controlled step-up; **uncorrected α=0.05 per stratum** — abandons multiple-testing correction entirely) are reported as descriptive supplements but are not eligible for post-hoc promotion into the canonical §2.e binary outcome. Either substitution beyond test statistic alone would confound attribution between framework choice and correction choice (BH at correction-procedure layer; uncorrected at correction-discipline-presence layer).

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs):

- Per-stratum `mean(forward_sharpe_15bps)`, `median(forward_sharpe_15bps)`, `prop(forward_sharpe_15bps > 0)` (mirroring PLAN §1.5 binarization for reader cross-reference); per-stratum Wilcoxon p-values reported at the 15bps PLAN-§1.5-alignment primary judgment register.
- **Descriptive supplement A (BH step-up reading):** per-stratum p-values evaluated under Benjamini-Hochberg step-up at FDR q=0.05; K=2 step-up detection rule: reject both strata if max(p) ≤ 0.05, else reject smallest-p stratum only if min(p) ≤ 0.025, else reject neither. §6 cost-basis cross-check narrative may surface BH-vs-Bonferroni detection-cell asymmetry at descriptive register.
- **Descriptive supplement B (uncorrected α=0.05 reading):** per-stratum p-values evaluated at uncorrected α=0.05 per stratum (family-wise α inflated; informational disclosure of power-bar shift between Bonferroni-corrected primary and uncorrected baseline).
- **7bps Wilcoxon at descriptive register:** same SciPy invocation + zero-handling on `forward_sharpe_7bps` per stratum; reports the Bonferroni-threshold reading + BH step-up supplement A + uncorrected supplement B at 7bps; cost-basis cross-check at 7bps PHASE2C-research register-class-distinct from 15bps PLAN-§1.5-alignment primary register.

**General discipline lock:** BH step-up and uncorrected α=0.05 readings (at both 15bps primary cost basis and 7bps supplementary cost basis) may be reported descriptively but are not eligible for post-hoc promotion into the canonical §2.e binary outcome. This lock parallels §2.d 16-cell distribution hardening and applies as general principle at §2.e descriptive supplements register.

**§2.a / §2.e non-conflation note (binding interpretation constraint).** §2.e operates at PLAN-§1.5-alignment register at 15bps cost basis; register-class-distinct from §2.a temporal-isolation register at 7bps. Both sections consume forward Sharpe per-candidate from sealed Phase 4 cost-sweep artifacts; cost basis is the register-class-distinguishing axis. §2.e detection does NOT replicate or invalidate §2.a's temporal-isolation finding; the two sections operate at register-class-distinct diagnostic modes per scoping §2.1(a) vs §2.1(e). §6 attribution-report wording is forward-bound to preserve this register-class distinction explicitly.

**Anti-circularity caveat (brief; full narrative §6 forward-bound).** §2.e Wilcoxon detection in either stratum is interpretable as "continuous-framework signal recovery where binary-framework failed at identical statistical discipline" only under joint disambiguation against (A) **continuous-framework noise sensitivity** (Wilcoxon may detect distributional shifts that reflect residual cohort noise rather than deployable alpha; signal-vs-noise disambiguation at continuous register parallel to but register-class-distinct from PLAN §1.5's binary register) and (B) **selection-bias inheritance** (cohort_a was AND-gate-selected at PHASE2C_15 selection register; §2.d tests AND-gate validity; §2.c tests cohort weakness against broader universe; §2.e fires on cohort_a's conditionally-biased forward Sharpe distribution, and the detection event may reflect selection-bias artifact rather than substantive framework-substitution effect; §6 attribution narrative must reconcile §2.e detection against §2.c and §2.d primary outcomes to disambiguate selection-bias-artifact vs framework-substitution-substantive). Full disambiguation including (C) §2.a temporal-isolation interaction is §6 attribution-narrative scope and does not affect §2.e primary judgment binary outcome.

**Data source per §1:** on-disk (Phase 4 15bps + 7bps `holdout_results.csv` + cohort_a CSV for stratification); per-stratum Wilcoxon + multi-cutoff descriptive readings + 7bps supplementary Wilcoxon are §1-ALLOWED derived statistics on existing per-candidate forward Sharpe data.

**Register-class:** binding | quantitative (one-sided Wilcoxon signed-rank framework-substitution on per-candidate 15bps forward Sharpe vs 0; Bonferroni-corrected α=0.025 per stratum at PLAN-§1.5-alignment register; BH step-up at FDR q=0.05 and uncorrected α=0.05 readings as descriptive supplements not eligible for post-hoc promotion into canonical outcome; 7bps Wilcoxon at descriptive register inheriting Bonferroni-threshold reading + descriptive supplements at 3 cutoffs) | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (sealed-invariance + interpretation-color framing).** §2.e output operates at the success-criterion-machinery-internal register; it does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. §2.e detection (if fired) informs §6 attribution-report interpretation by surfacing that alternative success-criterion framework at identical statistical discipline would have produced a different outcome; §6 narrative may color Phase 4 verdict interpretation with §2.e finding without overriding the sealed verdict. **§2.e/§5 framing-chain (forward-binding to §5 authoring).** §2.e detection outcome is a candidate input to §5 authoring at session-N, not §5 resolution. §5 authoring at its register adjudicates how the §2.e finding (detected / not detected) integrates with §5 substantive scope. Specific §5 response logic is not pre-locked at §2.e register per anti-pre-naming discipline.

### §2.f Regime change

Operationalizes scoping §2.1(f): "external market-structure shift invalidating historical transferability. 2026 BTC market structure fundamentally differs from 2020-2025 training universe; the *market context* is the subject of the change." Diagnostic question: did the 2026 forward-test window's BTC 1h realized volatility distribution substantively shift relative to the PHASE2C v2 reference universe (2020-2024), in ways that would condition forward-applicability of cohort_a candidate strategies from 2020-2024-trained/selected substrate to 2026 forward window? §2.f operates at the market-context / universe register, register-class-distinct from §2.a/b/c/d/e candidate-level / gate-internal registers.

**Indicator construction (primary — reference-universe rolling-window empirical null, Option E).**

- **Reference universe:** contiguous 2020-01-01T00:00:00Z to 2024-12-31T23:00:00Z, covering PHASE2C v2 train + holdout + validation periods per CLAUDE.md PHASE2C v2 split. PHASE2C v2 `test_2025` EXCLUDED per touch-once spirit anchor (CLAUDE.md Date Split Rules: "Test data is touched ONCE for final evaluation"; test_2025 has zero touches at this register; Phase 5 anti-pre-naming preserves option for successor-cycle test use). Forward window 2026 EXCLUDED per §2.f mode definition.
- **Primary T:** `T(block) = std(log-returns within block)` where log-returns are 1h consecutive close-to-close differences `log(close_{t+1}) − log(close_t)` from canonical BTC OHLCV at [`data/raw/btcusdt_1h.parquet`](../../data/raw/btcusdt_1h.parquet). Equivalent statistic forms (a) std / (b) realized variance / (c) annualized realized vol produce identical quantile-detection outcomes under monotonic-rank invariance; form (d) GARCH-implied vol does NOT preserve rank-equivalence under per-block model-fitting variability and is excluded.
- **Block construction:** non-overlapping 2528-bar blocks anchored at 2020-01-01T00:00:00Z; expected 17 full forward-window-sized blocks under current sealed parquet anchor; leftover ~830 hours dropped (parallel non-overlapping discipline). §7 V# anchor verifies exact block count at pre-execution gate. Note: primary blocks anchored at calendar 2020-01-01 cycle through Q1/Q2/Q3/Q4 calendar phases as time progresses; forward window is single Q1 calendar phase, motivating calendar-aligned Q1 supplement below.
- **Forward window:** `[2026-01-01T00:00:00Z, 2026-04-16T07:00:00Z]` = 2528 bars = 1 block (matches Phase 4 forward window per §1 sealed input lineage per parquet anchor `db4ce1d2a2e5e7b556975837260f7aaa29ee4fd5ddc603690d1bc57912aa7035`).

**Indicator construction (calendar-aligned Q1 supplement; descriptive register only).** To disambiguate seasonality-artifact from regime-change at §6 narrative reconciliation: 5 calendar-aligned Q1 blocks anchored at Jan 1 through the same 2528-bar horizon as the forward window for each year 2020/2021/2022/2023/2024; same `T(block)` statistic; n=5 at calendar-aligned register; non-overlapping by calendar-year separation. **Reporting form:** forward Q1 T empirical percentile rank in historical Q1 distribution, computed as share of historical Q1 blocks with T ≤ forward_Q1_T; not binary detection; preserves general discipline lock. §6 narrative scope interprets the scalar at attribution-reconciliation register (specific interpretation thresholds not pre-locked at §2.f register per anti-pre-naming discipline).

**Judgment rule.** §2.f detected iff `forward_T < numpy.quantile(training_T, 0.025, method="linear")` OR `forward_T > numpy.quantile(training_T, 0.975, method="linear")`; equality to either boundary is NOT detected. Quantile convention `numpy.quantile(..., method="linear")` consistent with §2.c quantile convention. Canonical §2.f outcome is binary: **market-structure shift detected** if quantile-band condition triggers; **not detected** otherwise.

**Substantive prior justification for quantile [2.5%, 97.5%] cutoff (pre-registered per §2.2 freeze).** Symmetric non-parametric quantile band at 95% empirical coverage corresponds to standard α = 0.05 two-sided detection convention. Non-parametric empirical-quantile detection at expected n=17 primary blocks yields effective extreme-value detection — forward T must exceed max or fall below min of 17 historical observations to fire detection. At typical normal-approximation empirical null, this requires approximately ±2σ effect size to detect with ~65% power (n=17 quantile-band detection; correction of earlier n=14 inheritance per verification-discipline forward-only log instance #3). Tighter band ([5%, 95%]) would be less stringent / higher sensitivity / higher false-positive risk; looser band ([1%, 99%]) is under-resolved at n=17 (boundaries outside observed empirical range). Cutoffs are empirical-quantile statistical priors at "extreme-value regime-change detection" register; locked at this §2.f register-event boundary per §2.2 freeze.

**Descriptive supplements** (for §6 attribution-report nuance; not judgment inputs; not eligible for post-hoc promotion into canonical §2.f outcome per general discipline lock — parallel §2.d 16-cell hardening + §2.e descriptive supplements A/B lock):

- **Supplement A:** lag-1 autocorrelation of 1h log-returns at forward-window-sized blocks; reported as scalar per block at descriptive register.
- **Supplement B:** trend strength via linear regression slope `slope(log(close) ~ normalized_time_index)` per block at forward-window-sized blocks, where `normalized_time_index ∈ [0, 1]` within each block (log-close formulation chosen for scale-invariance across BTC price regimes; time-index normalization ensures cross-block comparability for quantile-rank reporting; raw close slope and unnormalized time-index slope are scale-dependent and excluded).
- **Supplement C:** downside tail ratio `count(returns < −σ_block) / count(returns > +σ_block)` per block at forward-window-sized blocks; if denominator = 0, ratio reported as nan and §6 enumerates affected block(s) (no imputation; parallel §2.a/b/c/d/e NaN-handling discipline).
- **Supplement D:** HODL Sharpe at forward-window-sized blocks (economic-context only; explicitly flagged as strategy-performance-projection at descriptive register; §6 narrative trading-relevance input).
- **Calendar-aligned Q1 supplement** (per indicator construction above; n=5; scalar empirical percentile rank reporting at descriptive register): forward Q1 T empirical percentile rank in historical Q1 distribution informs §6 calendar-phase-vs-regime-change disambiguation narrative; primary §2.f outcome is canonical regardless of Q1 supplement value per general discipline lock.

**NaN-handling rule (defensive pre-registration; quantitative materiality threshold).** Realized vol computed directly from consecutive parquet rows; no smoothing or special-case logic. Zero-volume bars (3 known per CLAUDE.md "Known Data Characteristics") contribute zero log-return at position; minor downward bias to that block's vol. Missing-hour gaps (31 known per CLAUDE.md, all in 2020-2023 within this reference universe) cause adjacent-bar log-returns to span multi-hour intervals; minor upward bias to that block's vol. §6 enumerates per-block gap count + per-block T value with and without gap exclusion at sensitivity threshold ≥ 5% T value change (materiality threshold for explicit reporting). (§6 FB forward-binding)

**Anti-circularity caveat (4-axis bilateral + 2-axis upstream-conditioning brief; full narrative §6 forward-bound).** §2.f detection has substantive cross-section interactions with other §2.x outputs; full reconciliation narrative is §6 attribution-report scope. Briefly:

- **(A) §2.f vs §2.a (signal decay):** bilateral interpretation linkage. If §2.a fires temporal decay, the substantive cause may include universe shift (i.e., §2.f-class phenomenon expressed at candidate level via decay) or candidate-intrinsic forgetting (orthogonal to §2.f). If §2.f fires regime change, §2.a "decay" reading may be partially or fully attributable to universe shift rather than candidate-intrinsic forgetting.
- **(B) §2.f vs §2.b (cost drag):** bilateral conditioning. If §2.f detects regime change, the cost basis for trading in 2026 may differ from training-period assumption (spread / liquidity regime sub-axis); §2.b cost-drag reading may be regime-conditional.
- **(C) §2.f vs §2.e (bad success metric):** bilateral conditioning. Regime change is a candidate upstream cause for BOTH PLAN §1.5 binary AND alternative Wilcoxon continuous framework failures; if §2.f fires alongside §2.e, regime change is a plausible common cause shifting attribution.
- **(D) Attribution-non-uniqueness caveat:** even if §2.f fires regime change, that does NOT prove cohort_a candidates would have succeeded under no-regime-change counterfactual; multiple attributions remain admissible at §6 narrative. (§6 FB forward-binding)

**Brief upstream-conditioning (univocal §2.f → §2.c / §2.d).** §2.c (cohort weakness) and §2.d (AND-gate overfit) primary judgments operate against 2020-2024 reference universe per their §2.c/§2.d operational scope. §2.f detection conditions forward-applicability of these primary outputs (cohort weakness verdict at 2020-2024 substrate doesn't automatically transfer to 2026 forward window; gate validity at 2020-2024 universe doesn't automatically generalize). **§2.f does NOT invalidate §2.c or §2.d within their 2020-2024 reference register; it conditions whether those findings transfer forward to the 2026 market context.** (§6 FB forward-binding)

**Data source per §1:** on-disk (canonical BTC 1h OHLCV parquet at [`data/raw/btcusdt_1h.parquet`](../../data/raw/btcusdt_1h.parquet) per §1 sealed inputs envelope). **Pre-execution verification gate:** parquet sha256 anchor verify against `db4ce1d2a2e5e7b556975837260f7aaa29ee4fd5ddc603690d1bc57912aa7035` (Phase 4 cross-artifact-invariant anchor per §1 lineage) + row count + date range coverage + schema validation; mismatch at any check suspends §2.f execution; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' precedent. Reference universe extraction + block aggregation + realized vol computation + quantile detection + supplements are §1-ALLOWED derived statistics on existing per-bar OHLCV data.

**Register-class:** binding | quantitative (non-parametric quantile detection at α = 0.05 symmetric empirical 95% band; `numpy.quantile(..., method="linear")`; strict-inequality boundary convention; descriptive supplements A/B/C/D + calendar-aligned Q1 supplement at register-class-distinct from primary judgment) | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e precedent).** §2.f output operates at the market-context / universe register; combined with §2.a/§2.b/§2.c/§2.d/§2.e outputs, it does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. §6 attribution-report wording is forward-bound to preserve this invariance. (§6 FB forward-binding) **§2.f / §5 framing-chain (forward-binding to §5 authoring).** §2.f detection outcome is a candidate input to §5 authoring at session-N, not §5 resolution. Per §2.f register-class identity at market-context / universe scope, §2.f detection has substantively distinct upstream-conditioning implication for §5: if §2.f fires regime change detected, §5 authoring may face register-class question on whether successor-cycle research is justified under stable-regime substrate assumption or requires regime-aware framework redesign. This question is §5 authoring scope per anti-pre-naming discipline.

## §3 — Multi-mode rules + diagnostic ambiguity handling

Phase 5 diagnostic procedure §2 produces six per-mode binary detection outcomes; §3 operationalizes the interpretation framework consuming those outcomes when 2+ modes simultaneously detect (§3.1 multi-mode coexistence rules) or when the procedure fails to reliably separate modes (§3.2 diagnostic ambiguity handling). §3 register-class operates downstream of §2 measurement; §3 introduces no new measurement, no new cutoffs, and no new evidence — it produces categorical interpretation labels at §3's own register-class.

§3 sub-sections honor scoping §2.2 anti-circularity guard (ChatGPT P2 inheritance) and §2 → §5 sequencing lock (ChatGPT P3): §3 may consume §2 outputs but may not retroactively redefine §2 thresholds or alter §2 operationalization once frozen. §3.1 sealed at session-7 SEAL register-event boundary; §3.2 deferred per sub-spec drafting cycle Option A pacing decision (separate session-N entry per anti-momentum-binding strict reading).

### §3.1 Multi-mode coexistence rules

Operationalizes scoping §2.2 (multi-mode coexistence rules) and meta-plan §3.1: how the diagnostic procedure handles 2+ §2.x modes simultaneously firing at registered detection thresholds. §3.1 produces categorical interpretation labels via rule-tree primary + supplementary partition mapping (δ hybrid framing locked at D1''''''^a + per-cell composition rules locked at D2''''''^a + anti-circularity / framing-chain / wording precision locked at D3''''''^a). §3.1 consumes §2.x sealed outputs as binary inputs and produces categorical interpretation labels at §3.1's own register-class; it introduces no new measurement, no new cutoffs, and no new evidence.

**Indicator construction — NOT APPLICABLE** (no measurement layer at §3.1 register; §3.1 consumes §2.x sealed outputs as inputs not raw data; per §-symmetric template adherence D3''''''^a Sub-decision 2 explicit applicability declaration).

**Judgment rule (primary — δ hybrid: rule-tree primary + supplementary partition mapping).** Primary rule-tree γ-sub-component traverses §2.x mode outputs along nested upstream lines per D1''''''^a γ-sub-component locked structure with Line 1 wording re-lock (intra-session-7 re-adjudication per P5 substantive escalation; ChatGPT lock specification + parallel-reasoning extension):

- **Line 1 (universe upstream gate):** §2.f? → if §2.f fires, distinguish §2.f's two register-class-distinct relationships with downstream §2.x modes per sealed §2.f content:
  - **Content-attribution bilateral** (sealed §2.f anti-circularity (A)/(B)/(C) scope): §2.a / §2.b / §2.e readings receive `regime-conditional` prefix labeling at content-attribution scope. Substantive semantics: decay reading may be partially or fully attributable to universe shift (per (A)); cost-drag reading regime-conditional (per (B)); success-metric reading shares regime-change as common cause (per (C)).
  - **Forward-applicability univocal upstream-conditioning** (sealed §2.f line 259 scope): §2.c / §2.d readings receive `forward-applicability-conditioned` prefix labeling at forward-applicability scope. Substantive semantics: §2.c and §2.d findings remain valid within their 2020-2024 reference register; §2.f only conditions whether those findings transfer forward to the 2026 market context. Internal validity at 2020-2024 reference register UNCHANGED; only forward-applicability scope conditioned.
- **Line 2 (gate-validity sub-gate; §2.c only):** §2.d? → if §2.d fires AND-gate-overfit, §2.c reading receives `gate-validity-conditioned` modifier at compositional label register; interpretation/prose layer carries stronger semantics — the cohort-substrate inference is partially or fully invalidated by detected AND-gate overfit per sealed §2.d D3''' framing-chain lock. §2.d does NOT condition §2.a/§2.b/§2.e (register-class-distinct from cohort substrate test).
- **Tier within candidate-level:** §2.a / §2.b / §2.e bilateral readings per their sealed §2.x anti-circularity caveat specifications (§2.e D3'''' (A)(B)(C) per session-5 lock; §2.a/§2.b session-3 D3 locks); §2.f bilateral overlay with §2.a/§2.b/§2.e per sealed §2.f anti-circularity 4-axis bilateral (A)/(B)/(C) (Line 1 universe-upstream prefix already applied at outer scope per locked composition order); when a §2.f-bilateral-applicable mode (§2.a / §2.b / §2.e) fires alongside §2.f, suffix `(with bilateral attribution caveat)` is added to that mode's base label. Tier composition is strictly intra-class bilateral evaluation per locked composition order; mixed-submodifier coexistence (e.g., §2.f + §2.a + §2.d firing) is co-occurrence of independently composed labels per Line 1 modifier 2-way differentiation, NOT a higher-order Tier ontology. Tier logic remains exactly what D2''''''^a Sub-decision 4 (2') locked.
- **Residual:** cells uncovered by Line 1 / Line 2 / Tier default to single-mode interpretation per §2.x sealed register.

Compositional label assembly per D2''''''^a Sub-decision 1 (b) compositional: 6 base labels (one per §2.x mode) + composition rules per Line, with Line 1 modifier rules differentiating by downstream mode register-class per D1''''''^a Line 1 re-lock (`regime-conditional` for §2.a/§2.b/§2.e; `forward-applicability-conditioned` for §2.c/§2.d). Composition order locked at D2''''''^a Sub-decision 4 (2'): Line 1 modifier first (with Line 1 sub-modifier class determined by downstream mode register-class) → Line 2 modifier (for §2.c only) → Tier bilateral overlay → Residual single-mode default. Order is semantic algebra per sealed §2.f anti-circularity caveat (A) wording — upstream conditioners modify meaning of downstream readings; not cosmetic prefix.

Supplementary partition mapping (D2''''''^a Sub-decision 1 (A) derivation): 5-column structured table enumerates rule-tree branch termini for §4a / §6 downstream consumer reference. Cross-section context multi-consumer column scope per D2''''''^a Sub-decision 2 βL-2 lock: generic cross-section reconciliation context; does not pre-name §4a successor-cycle-class mapping per anti-circularity (iv).

**Substantive prior justification (rule-tree structure + nested upstream lines + composition order + Line 1 2-way distinction).** Rule-tree primary structure honors register-class hierarchy explicitly established at sealed §2.x content: §2.f universe-upstream conditions all candidate-level readings (per §2.f Phase 4 invariance + cross-section framing-chain); §2.d gates §2.c interpretation per sealed §2.d D3''' framing-chain lock §2.d-specific gate scope. §2.f's two register-class-distinct downstream relationships per sealed §2.f content are preserved at compositional level: content-attribution bilateral with §2.a/§2.b/§2.e (sealed §2.f anti-circularity (A)/(B)/(C)) at content-modification scope; forward-applicability univocal upstream-conditioning of §2.c/§2.d (sealed §2.f line 259) at forward-applicability scope. Pure exhaustive enumeration would flatten this hierarchy at 2⁶ = 64 cells (many substantively equivalent under upstream-conditioning collapses); pure partition-based grouping would lose register-class-distinct hierarchy at substantively-load-bearing precision; universal Line 1 modifier without 2-way distinction would under-specify §2.f's two register-class-distinct downstream relationships at the compositional label register; the prose layer would carry the distinction informally without compositional precision enforcement at label format register. δ hybrid resolves: rule-tree primary respects sealed register-class structure; supplementary partition derivation serves §4a / §6 downstream consumers without independent grouping logic; Line 1 2-way modifier preserves §2.f's two distinct downstream relationships. Composition order locked at D2 substantive register because upstream conditioners modify downstream reading meaning per sealed §2.f / §2.d wording — not D3 wording precision register.

**Descriptive supplements — NOT APPLICABLE** (no measurement framework at §3.1 register; per §-symmetric template adherence D3''''''^a Sub-decision 2).

**NaN-handling rule (D2''''''^a Sub-decision 3 (ε') hybrid + (ε'-narrow) input class scope + propagation rule per nested upstream lines).** §2.x mode outputs at execution time possible non-canonical states are register-class-distinguished:

- **Execution invalidity (infrastructure failure):** if any upstream §2.x is self-suspended (parquet sha256 mismatch / cross-validation gate failure / data-source corruption per sealed §2.x precedent), §3.1 execution suspends per (ε') execution-invalidity branch; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent. (§6 FB forward-binding)
- **Epistemic indeterminacy ((ε'-narrow) input class scope):** only (a) NaN-from-real-data degenerate cases (e.g., §2.b ρ undefined on zero-variance) reach §3.1 as genuine indeterminate; (b) boundary equality / (c) §2.x NaN-handling firing / (d) descriptive supplements all resolved by §2.x sealed prose before §3.1 sees. Indeterminate cases receive labeled propagation per **locally-scoped propagation default + absorbing-propagation only when indeterminate mode is itself upstream-conditioning** (propagation rule scope follows D1''''''^a γ-sub-component nested upstream lines exactly):
  - §2.f indeterminate → absorbs all candidate-level readings (§2.a/§2.b/§2.c/§2.d/§2.e) per Line 1 universe-upstream scope; downstream labels per D2''''''^a Sub-decision 3 (b-symm-improved) 2 register-class indeterminate labels: `indeterminate-regime-conditional <base label>` for §2.a/§2.b/§2.e (e.g., `indeterminate-regime-conditional signal-decay reading`); `indeterminate-forward-applicability-conditioned <base label>` for §2.c/§2.d (e.g., `indeterminate-forward-applicability-conditioned cohort-substrate-weakness reading`).
  - §2.d indeterminate → absorbs §2.c reading only per sealed §2.d D3''' §2.d-specific gate scope; downstream label `indeterminate-gate-validity-conditioned cohort-substrate-weakness reading` (absorbing-propagation per upstream-conditioner indeterminacy at gate-validity scope; (b-symm-improved) symmetry extension); §2.a/§2.b/§2.e unaffected.
  - §2.a/§2.b/§2.c/§2.e indeterminate → locally scoped to that mode's base label only; downstream conditioning unaffected.

Substantive principle: indeterminacy propagates only along the semantic dependency path it conditions; it does not globally poison unrelated branches. Indeterminate label format mirrors normal label format compositionally per §-symmetric internal consistency.

Indeterminate label words: `indeterminate <base label>` prefix on §2.X base label for (a)-class real-data NaN at non-upstream-conditioning mode (e.g., `indeterminate cost-drag reading` if §2.b indeterminate) / `indeterminate-regime-conditional <base label>` for §2.a/§2.b/§2.e under §2.f indeterminate (absorbing-propagation per upstream-conditioner indeterminacy at content-attribution scope) / `indeterminate-forward-applicability-conditioned <base label>` for §2.c/§2.d under §2.f indeterminate (absorbing-propagation per upstream-conditioner indeterminacy at forward-applicability scope) / `indeterminate-gate-validity-conditioned <base label>` for §2.c under §2.d indeterminate (absorbing-propagation per §2.d-specific gate scope; per sealed §2.d D3''' framing-chain lock) / `§3.1 execution suspends; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent` (infrastructure-layer execution invalidity per (ε') execution-invalidity branch).

**Anti-circularity caveat (4-axis per D3''''''^a Sub-decision 1; axis (ii) strengthened).**

- **(i) §3.1 → §2 threshold guard (ChatGPT P2 inheritance):** §3.1 may consume §2.x outputs but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds (α=0.05 per-mode, etc.) remain authoritative.
- **(ii) §3.1 → §2 register-class identity guard:** §3.1 does not retroactively re-frame §2.x register-class identities. §2.f remains universe-upstream; §2.d remains gate-validity-for-§2.c-only; §2.a/§2.b/§2.c/§2.e remain at their sealed candidate-level / selection-criterion / cohort-substrate / success-criterion register-classes per sealed identity. **Specifically: §3.1 preserves §2.f's two register-class-distinct downstream relationships per sealed §2.f content — content-attribution bilateral with §2.a/§2.b/§2.e (sealed §2.f anti-circularity (A)/(B)/(C)) at content-modification scope; forward-applicability univocal upstream-conditioning of §2.c/§2.d (sealed §2.f line 259) at forward-applicability scope. §3.1 internal labeling reflects this 2-way distinction at all register-classes (compositional labels per Categorical label set Line 1 modifier 2-way split; propagation labels per (ε') hybrid + 2 register-class indeterminate labels).**
- **(iii) §3.1 → §6 narrative non-pre-emption with explicit context-vs-content distinction:** §3.1 partition table provides cross-section context to §6 narrative author per D2''''''^a Sub-decision 2 column scope (generic context per cross-section reconciliation); §3.1 does NOT pre-name §6 attribution narrative specific content (i.e., what each cell ultimately means in §6 narrative interpretation); substantive narrative determination retained at §6 author register.
- **(iv) §3.1 → §4a non-pre-emption:** §3.1 partition table cross-section context column does not pre-name §4a successor-cycle-class mapping; §4a consumes §3.1 partition table as input but does its own grouping work per §4a register.

**Anti-pre-naming.** §3.1 does NOT pre-name §4a successor-cycle-class assignments. §3.1 does NOT pre-name §5 terminal-conclusion criteria. §3.1 does NOT pre-name session-N-specific operational details. §3.1 register-class binds to current sealed §2.a/b/c/d/e/f scope; composition logic outside this scope is outside §3.1's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration D3''''''^a Sub-decision 2): §2.x sealed outputs at canonical binary detection results per each §2.x sealed prose ({detected / not-detected / (ε'-narrow)-class-real-data-NaN / §2.x-self-suspended}). No raw data consumption. §3.1 V# anchor pre-execution check (below) verifies input space canonical-ness before §3.1 execution.

**Register-class:** binding | rule-based primary + categorical interpretation outputs (no new quantitative thresholds; consumes §2.x quantitative outputs as binary inputs) | downstream-consumer of §2.x sealed outputs | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f precedent).** §3.1 output operates at the interpretation framework register, integrating multi-mode §2.x outputs into categorical labels; the integration does not modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. Multi-mode composition at §3.1 register does not retroactively rewrite Phase 4 verdict; only feeds §5 terminal-conclusion criteria at §5 author register per §3.1 → §5 framing-chain. §6 attribution-report wording is forward-bound to preserve this invariance. (§6 FB forward-binding)

**Framing-chain (4-direction per D3''''''^a Sub-decision 3).**

- **§3.1 → §4a:** Partition table consumed by §4a successor-cycle-class mapping work; cross-section context column scope per D2 lock; anti-pre-naming preserved per anti-circularity (iv). (§6 FB forward-binding)
- **§3.1 → §5:** Categorical labels feed §5 terminal-conclusion criteria; ChatGPT P2 anti-circularity guard binding (§5 may consume §3.1 outputs but may not retroactively redefine §3.1 rule-tree); ChatGPT P3 sequencing lock binding (§5 downstream consumer; cannot alter §3.1 once frozen). §3.1 outputs are §5 inputs not §5 resolution.
- **§3.1 → §6:** Categorical labels + partition table feed §6 attribution narrative; FB items added to forward-binding tracker per session-6 §2.f precedent. (§6 FB forward-binding)
- **§3.1 → §7:** V# anchor pre-execution check on §2.x output completeness (see V# anchor below). (§7 FB forward-binding)

**V# anchor pre-execution check (per D3''''''^a Sub-decision 7).** §3.1 pre-execution verifies all 6 §2.x mode outputs are in canonical input space {detected / not-detected / (ε'-narrow)-class-real-data-NaN / §2.x-self-suspended}; mismatch (e.g., §2.x output schema violation, missing mode output, unexpected output class) suspends §3.1 execution per (ε') execution-invalidity branch + §6 enumerate + Charlie register re-adjudicate.

**Categorical label set (D3''''''^a Sub-decision 6 wording cascade).** Compositional label assembly per Line/Tier/Residual structure with locked composition order:

- **Base labels (6, one per §2.x mode):** `signal-decay reading` (§2.a) / `cost-drag reading` (§2.b) / `cohort-substrate-weakness reading` (§2.c) / `AND-gate-overfit reading` (§2.d) / `success-metric-mismatch reading` (§2.e) / `regime-change-detected reading` (§2.f).
- **Line 1 (§2.f) modifier — 2 register-class sub-modifier classes per D1''''''^a Line 1 re-lock + D2''''''^a Sub-decision 1 (b) update:**
  - `regime-conditional` prefix on §2.a/§2.b/§2.e base labels when §2.f fires (content-attribution bilateral per sealed §2.f anti-circularity (A)/(B)/(C); sealed §2.f line 255 inheritance for term).
  - `forward-applicability-conditioned` prefix on §2.c/§2.d base labels when §2.f fires (forward-applicability univocal upstream-conditioning per sealed §2.f line 259; §2.c/§2.d 2020-2024 internal validity preserved per interpretation layer).
- **Line 2 (§2.d) modifier on §2.c only:** `gate-validity-conditioned` prefix on §2.c base label when §2.d fires at compositional layer; interpretation/prose layer carries stronger semantics — cohort-substrate inference partially or fully invalidated by detected AND-gate overfit (split wording model preserves both label compactness and epistemic strength per sealed §2.d D3''' framing-chain lock).
- **Tier bilateral overlay:** `(with bilateral attribution caveat)` parenthetical suffix on §2.a / §2.b / §2.e base labels when a §2.f-bilateral-applicable mode (§2.a / §2.b / §2.e) fires alongside §2.f (Line 1 universe-upstream prefix already applied at outer scope per locked composition order).
- **Indeterminate labels (per (ε') hybrid + propagation rule + (b-symm-improved) synthesis):**
  - `indeterminate <base label>` prefix on §2.X base label for (a)-class real-data NaN at non-upstream-conditioning mode X (e.g., `indeterminate cost-drag reading`).
  - `indeterminate-regime-conditional <base label>` for §2.a/§2.b/§2.e under §2.f indeterminate (absorbing-propagation per upstream-conditioner indeterminacy at content-attribution scope).
  - `indeterminate-forward-applicability-conditioned <base label>` for §2.c/§2.d under §2.f indeterminate (absorbing-propagation per upstream-conditioner indeterminacy at forward-applicability scope).
  - `indeterminate-gate-validity-conditioned <base label>` for §2.c under §2.d indeterminate (absorbing-propagation per §2.d-specific gate scope; per sealed §2.d D3''' framing-chain lock).
  - `§3.1 execution suspends; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent` (infrastructure-layer execution invalidity per (ε') execution-invalidity branch).

**Partition table (D2''''''^a Sub-decision 2 (ii); 5-column × rule-tree branch termini rows; 6 illustrative schema rows demonstrating composition pattern).** Structured table per D2''''''^a Sub-decision 2 (ii) lock:

| rule-tree branch | active modes | categorical label | interpretation | downstream-consumer note |
|---|---|---|---|---|
| Line 1=N, Line 2=N, Tier=single §2.a fires | {§2.a} | `signal-decay reading` | (resolved at §6 narrative) | (downstream sections may use this branch as context; specific successor-class mapping is §4a scope) |
| Line 1=Y (§2.f fires), Line 2=N, Tier=single §2.a fires | {§2.f, §2.a} | `regime-conditional signal-decay reading (with bilateral attribution caveat)` | (resolved at §6 narrative per §2.f anti-circularity (A) bilateral) | (downstream sections may use this branch as context; specific successor-class mapping is §4a scope) |
| Line 1=Y (§2.f fires), Line 2=N, Tier=single §2.c fires | {§2.f, §2.c} | `forward-applicability-conditioned cohort-substrate-weakness reading` | (interpretation layer notes §2.c 2020-2024 internal validity preserved; §2.f only conditions transfer to 2026; resolved at §6 narrative) | (downstream sections may use this branch as context per §2.f → §2.c univocal upstream-conditioning) |
| Line 1=N, Line 2=Y (§2.d fires), Tier=§2.c fires | {§2.d, §2.c} | `gate-validity-conditioned cohort-substrate-weakness reading` | (interpretation layer notes cohort-substrate inference partially/fully invalidated by detected AND-gate overfit; resolved at §6 narrative) | (downstream sections may use this branch as context per §2.d D3''' upstream-gate framing-chain) |
| Line 1=Y (§2.f fires), Line 2=Y (§2.d fires), Tier=§2.c fires | {§2.f, §2.d, §2.c} | `forward-applicability-conditioned gate-validity-conditioned cohort-substrate-weakness reading` | (interpretation layer: cohort-substrate inference partially/fully invalidated by detected AND-gate overfit AND forward-applicability conditioned by detected regime change; both layers stacked per locked composition order; resolved at §6 narrative) | (downstream sections may use this branch as context per §2.f universe-upstream + §2.d D3''' stacking) |
| Line 1=Y (§2.f fires), Line 2=N, Tier=§2.a + §2.d fires | {§2.f, §2.a, §2.d} | `regime-conditional signal-decay reading (with bilateral attribution caveat); forward-applicability-conditioned AND-gate-overfit reading` | (Line 1 sub-modifier differentiation per D1 re-lock: §2.a content-attribution bilateral per §2.f anti-circularity (A); §2.d forward-applicability univocal per §2.f line 259; both labels stacked per single partition row at locked composition order; resolved at §6 narrative) | (downstream sections may use this branch as context per §2.f → §2.x mixed register-class downstream relations) |

Rows are schema rows demonstrating composition pattern; full enumeration at execution time mechanical from locked rules per partition derivation (A) D2''''''^a Sub-decision 1 lock. Partition table is static at §3.1 register (schema rows above); execution enumerates partition rows based on actual §2.x output combination at §3.1 execution time (typical execution: 1-10 rows depending on number of firing modes; 0-mode case: no partition table row; all-mode case: upper bound on row count per rule-tree branch termini count). **No new interpretation logic introduced at execution time** — only mechanical traversal of locked rule-tree. Partition table introduces no independent grouping ontology; substantively no new design lever beyond rule-tree.

### §3.2 Diagnostic ambiguity handling

Operationalizes scoping §2.2 (diagnostic ambiguity handling per ChatGPT P1 register-class-distinction; Charlie-confirmed at session-1) and meta-plan §3.2: how the diagnostic procedure handles the case where reliable separation between modes fails. §3.2 produces categorical label + terminal-trigger flag via Case A single-case scope (δ hybrid framing analog at §3.2 narrowed register per D1''''''^b lock; per-element §-symmetric template applicability per D3''''''^b Sub-decision 2 + βU-1 table; (b'+) framing-chain synthesis + XOR cross-section complementarity codification at substantive prior register per D3''''''^b Sub-decision 3). §3.2 consumes §2.x sealed outputs as binary inputs and produces categorical label + terminal-trigger at §3.2's own procedural-state register-class; it introduces no new measurement, no new cutoffs, and no new evidence.

§3.2 register-class is downstream-consumer of §2.x measurement at procedural-state register; register-class-distinct from §3.1 multi-mode coexistence per ChatGPT P1 anchor: §3.1 covers cases where 1+ modes are detected (single-mode + multi-mode composition); §3.2 covers the case where the procedure produces 0-mode output (all §2.x not-detected). §3.1 and §3.2 fire complementary at execution time per (C3) parallel sequencing + XOR firing pattern (§3.1 emits on ≥1 §2.x detected; §3.2 emits on 0 §2.x detected).

**Indicator construction — NOT APPLICABLE** (no measurement layer at §3.2 register; §3.2 consumes §2.x sealed outputs as inputs not raw data; per §-symmetric template applicability declaration D3''''''^b Sub-decision 2 + βU-1 element).

**Judgment rule (Case A condition evaluation + emission per D2''''''^b Sub-decision 5).** Case A condition: all 6 §2.x mode outputs are valid canonical binary outputs and resolve to `not-detected`; any §2.x-self-suspended or (ε'-narrow)-class-real-data-NaN state routes to the §3.2 edge-handling branch (see NaN-handling rule section below), not Case A. §3.2 always evaluates Case A eligibility on every execution; emits substantive output only when Case A condition (all §2.x not-detected at canonical binary register) satisfied. Evaluation-vs-emission distinction preserved at register-precision per §7 verification chain semantics.

When Case A condition satisfied, §3.2 emits separate-field output:
- Categorical label: `no-mode-detected reading` (procedural-state register; per D2''''''^b Sub-decision 1)
- Terminal-trigger: `{triggered: True, basis: {canonical_0_mode: True, suspended_modes: [], nan_modes: []}}` (basis structured object capturing §2.x output state composition that triggered Case A; basis does NOT pre-name §5_routing per anti-pre-naming preservation; per D2''''''^b Sub-decision 2 + βT-2)
- Output structure: `{label, terminal_trigger}` separate fields (per D2''''''^b Sub-decision 3)

When Case A condition not satisfied (1+ §2.x detected at canonical binary register), §3.2 emits no substantive output (silent register per XOR complementarity with §3.1 emission; §3.1 fires labels per its register-class). For non-canonical §2.x states (suspended or NaN), the §3.2 edge-handling branch (NaN-handling rule section below) determines outcome.

**Substantive prior justification (single-case scope rationale + §3.1 ↔ §3.2 XOR cross-section complementarity codification per D3''''''^b Sub-decision 3 (b'+)).**

§3.2 narrowed single-case scope rationale: meta-plan §3.2 enumerated 4 cases (insufficient-evidence + conflicting-indicators + weak-signal + cannot-distinguish), but on substantive review against sealed §2.x + scoping §2.2: Case 2 (conflicting indicators) tensions with scoping §2.2 explicit anchor "Modes are NOT fully mutually exclusive" — co-firing is §3.1 Tier composition, not §3.2 conflict; Case 3 (weak-signal) subsumes to §2.x frozen thresholds at register per §2.2 operationalization-freeze lock; Case 4 (cannot-distinguish among co-firers) tensions with §3.1 anti-circularity (iii) + partition table `(resolved at §6 narrative)` — attribution-dominance retained at §6 author register, not §3.2 register-class. §3.2 narrowed to single-case Case A per D1''''''^b (β-narrow-1) lock; Cases 2/3/4 retained as explicit no-action locks routing to §3.1 / §2.x / §6-narrative-authority respectively (Case 4 specifically: **not a §3.2 ambiguity class**).

§3.1 ↔ §3.2 cross-section complementarity (XOR firing pattern) per (b'+) register-class precision: §3.1 sealed prose specifies partition table as empty at 0-mode case (per session-7 βR-3 reader-anchor: "0-mode case: no partition table row") + §3.1 Judgment rule rule-tree mechanically produces no labels at 0-mode (no Line 1 / Line 2 / Tier / Residual firing path; mechanically derivable from sealed rule structure). §3.2 architectural cross-section coverage logic at substantive prior register: §3.1 emits substantive outputs (labels + partition table rows) conditional on ≥1 §2.x detected; §3.2 emits substantive outputs (Case A label + terminal-trigger) conditional on all §2.x not-detected. For valid canonical binary §2.x inputs, either §3.1 OR §3.2 emits substantive output, never both, never neither; if upstream §2.x inputs are suspended or indeterminate, the edge-handling branch applies (see NaN-handling rule section below). §3.2 picks up substantive coverage where §3.1 produces no substantive output at 0-mode case; XOR complementarity is architectural cross-section coverage logic codified at §3.2 substantive prior register (cross-referenced from Framing-chain section below per register-class precision per advisor βU-2 catch), not framing-chain directional dependency.

**Descriptive supplements — NOT APPLICABLE** (no measurement framework at §3.2 register; per §-symmetric template applicability declaration D3''''''^b Sub-decision 2 + βU-1 element).

**NaN-handling rule (D2''''''^b Sub-decision 4 (a) inherit §3.1 (ε') hybrid pattern + §3.2-specific adaptation per βT-3).** §2.x mode outputs at execution time possible non-canonical states are register-class-distinguished:

- **Execution invalidity (infrastructure failure):** if any §2.x is self-suspended (parquet sha256 mismatch / cross-validation gate failure / data-source corruption per sealed §2.x precedent), §3.2 execution suspends per (ε') execution-invalidity branch per D2''''''^b Sub-decision 4 (a) §3.1 (ε') hybrid inheritance; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.
- **Epistemic indeterminacy ((ε'-narrow) input class scope):** (a) NaN-from-real-data degenerate cases at §2.x output reach §3.2 as genuine indeterminate. Indeterminate label format: `indeterminate-Case-A reading` at procedural-state register, parallel structure to §3.1 `indeterminate-X reading` per-mode register per §-symmetric base label inheritance with §3.2-specific adaptation ("Case A" identifier preserves §3.2 procedural-state register-class-distinct from §3.1 per-mode register-class per βT-3 catch). Terminal-trigger basis field reflects NaN state: `{canonical_0_mode: False, suspended_modes: [], nan_modes: [<§2.x identifier list>]}`.

Substantive principle: §3.2 inherits §3.1 (ε') hybrid execution-invalidity + (ε'-narrow) indeterminacy pattern at procedural-state register; indeterminate label format is partial-inheritance per §-symmetric base label register-class distinction (§3.1 per-mode vs §3.2 procedural-state).

**Anti-circularity caveat (5-axis per D3''''''^b Sub-decision 1).**

- **(i) §3.2 → §2 threshold guard:** §3.2 may consume §2.x outputs but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds remain authoritative.
- **(ii) §3.2 → §2 register-class identity guard:** §3.2 does not retroactively re-frame §2.x register-class identities. §2.f remains universe-upstream; §2.d remains gate-validity-for-§2.c-only; §2.a/§2.b/§2.c/§2.e remain at sealed candidate-level / selection-criterion / cohort-substrate / success-criterion register-classes per sealed identity.
- **(iii) §3.2 → §6 narrative non-pre-emption with explicit context-vs-content distinction:** §3.2 trigger basis field provides cross-section context (canonical 0-mode state composition) to §6 narrative author; §3.2 does NOT pre-name §6 attribution narrative specific content (substantive narrative determination of what Case A occurrence means in §6 narrative interpretation); substantive narrative determination retained at §6 author register.
- **(iv) §3.2 → §4a non-pre-emption:** §3.2 Case A label consumed by §4a successor-cycle-class mapping; §3.2 does NOT pre-name §4a successor-cycle-class assignment for 0-mode case. §4a register retains successor-class adjudication authority.
- **(v) §3.2 → §5 non-pre-emption (NEW per §3.2-specific direct §5 coupling):** §3.2 terminal-trigger flag flags terminal-eligibility (Case A satisfied → §5 terminal-conclusion assessment is explicitly engaged) but does NOT pre-name §5 terminal-conclusion outcome between operational availability (no-successor-cycle-currently-justified per scoping §2.5) vs substantive availability (paradigm exhaustion per scoping §2.5). §5 author register retains terminal-conclusion authority per Mod-pass-2 F1 paragraph-disambiguation lock — substantive degree-of-freedom preservation at §5.

**Anti-pre-naming (parallel §3.1 structure + §3.2-specific terminal-trigger reinforcement per D3''''''^b Sub-decision 4 hybrid).** §3.2 does NOT pre-name §4a successor-cycle-class assignments. §3.2 does NOT pre-name §5 terminal-conclusion criteria (anti-circularity (v) primary register for terminal-trigger non-pre-emption substantive content; this section reinforces anti-pre-naming framing-class declaration per parallel §3.1 anti-pre-naming structure; basis field anti-§5_routing-pre-naming per D2''''''^b Sub-decision 2). §3.2 does NOT pre-name session-N-specific operational details. §3.2 register-class binds to current sealed §2.a/b/c/d/e/f scope; procedural-state register outside this scope is outside §3.2's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration D3''''''^b Sub-decision 2): §2.x sealed outputs at canonical binary detection results per each §2.x sealed prose ({detected / not-detected / (ε'-narrow)-class-real-data-NaN / §2.x-self-suspended}). No raw data consumption. §3.2 V# anchor pre-execution check (below) verifies input space canonical-ness before §3.2 execution.

**Register-class:** binding | rule-based + categorical interpretation outputs (no new quantitative thresholds; consumes §2.x quantitative outputs as binary inputs) | downstream-consumer of §2.x sealed outputs at procedural-state register | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f + §3.1 precedent + D1''''''^b Case A wording reinforcement).** §3.2 output (Case A firing when condition met) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. Case A is the **procedure-level analogue of Phase 4's null forward-persistence result, but remains a Phase 5 diagnostic output, not a restatement of Phase 4** (per D1''''''^b lock). §2.x diagnostic modes may still fire (1+ §2.x detected) even though Phase 4 produced null at PLAN §1.5 success criterion (forward persistence detection register); §2.x diagnostic mode detection is register-class-distinct from PLAN §1.5 success criterion register per scoping §2.1 mode definitions; §3.2 Case A only fires when all §2.x diagnostic modes not-detected. Phase 4 register-class is distinct from §3.2 Case A register-class at: procedure-level analogue at substantive prior register vs Phase 4 verdict at PLAN §1.5 framework register. (§6 FB forward-binding)

**Framing-chain (4-direction per D3''''''^b Sub-decision 3 (b'+) synthesis).**

- **§3.2 → §4a:** Case A label `no-mode-detected reading` consumed by §4a successor-cycle-class mapping; anti-pre-naming preserved per anti-circularity (iv). (§6 FB forward-binding)
- **§3.2 → §5:** Terminal-trigger flag feeds §5 terminal-conclusion criteria; Case A satisfied → §5 terminal-conclusion assessment is explicitly engaged at §5 author register (per anti-circularity (v) non-pre-emption); ChatGPT P2 anti-circularity guard binding (§5 may consume §3.2 outputs but may not retroactively redefine §3.2 Case A condition or basis field semantics); ChatGPT P3 sequencing lock binding (§5 downstream consumer; cannot alter §3.2 once frozen). §3.2 trigger is §5 input not §5 resolution per anti-circularity (v).
- **§3.2 → §6:** Case A label + basis field cross-section context feed §6 attribution narrative; basis field structured object provides canonical 0-mode state composition; substantive narrative determination retained at §6 author register per anti-circularity (iii). (§6 FB forward-binding)
- **§3.2 → §7:** V# anchor pre-execution check on §2.x output completeness (see V# anchor below). (§7 FB forward-binding)

**Cross-reference**: §3.1 ↔ §3.2 cross-section complementarity (XOR firing pattern) codified at Substantive prior justification section above per D3''''''^b Sub-decision 3 (b'+) register-class precision — architectural cross-section coverage logic at substantive prior register, not framing-chain direction-cohort.

**V# anchor pre-execution check (per D3''''''^b Sub-decision 7 + βU-3 wording inheritance).** §3.2 pre-execution verifies all 6 §2.x mode outputs are in canonical input space {detected / not-detected / (ε'-narrow)-class-real-data-NaN / §2.x-self-suspended}; mismatch (e.g., §2.x output schema violation, missing mode output, unexpected output class) suspends §3.2 execution per (ε') execution-invalidity branch per D2''''''^b Sub-decision 4 (a) §3.1 (ε') hybrid inheritance; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

**Categorical label set (D3''''''^b Sub-decision 6 wording inventory + βU-1 applicability declaration).**

- **Base label (single):** `no-mode-detected reading` (Case A condition satisfied at canonical binary register).
- **Indeterminate label:** `indeterminate-Case-A reading` (for (a)-class real-data NaN at one or more §2.x outputs; per §3.2-specific adaptation).
- **Terminal-trigger structure:** `{triggered: Bool, basis: {canonical_0_mode: Bool, suspended_modes: [§2.x_id], nan_modes: [§2.x_id]}}` (basis does NOT pre-name §5_routing per anti-pre-naming preservation).
- **Output structure:** separate fields `{label, terminal_trigger}`.
- **Firing condition:** "§3.2 always evaluates Case A eligibility on every execution; emits substantive output only when Case A condition (all §2.x not-detected at canonical binary register) satisfied" (XOR complementarity with §3.1 silence-on-0-mode pattern; suspended/NaN states route to NaN-handling rule edge-handling branch).
- **Suspension language:** §3.2 execution suspends per (ε') execution-invalidity branch per D2''''''^b Sub-decision 4 (a) §3.1 (ε') hybrid inheritance; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.
- **Composition operators — NOT APPLICABLE** (single-case scope per D1''''''^b (β-narrow-1); no compositional label assembly required; no Line / Tier / Residual / modifier-class structure).

**Partition table — DOES NOT APPLY** (per βU-1 element; single-case scope per D1''''''^b (β-narrow-1); no multi-row enumeration required; §3.2 produces categorical label + terminal-trigger directly without intermediate partition representation).

## §4 — Successor-cycle interpretation + framing-question resolution-pressure assessment procedure

Phase 5 diagnostic procedure §3 produces categorical interpretation labels (§3.1 partition table rows + §3.2 Case A label + terminal-trigger) consumed by §4's two register-class-distinct interpretation operationalizations: §4a successor-cycle-class mapping (this register; consumes §3.x outputs + emits successor-cycle-class assignments per scoping §2.3) + §4b framing-question (1)/(2)/(3)/(4) resolution-pressure assessment procedure (separate register; deferred to fresh session-N register-event boundary per anti-momentum-binding strict reading). §4 register-class operates downstream of §2 measurement + §3 interpretation labels; §4 introduces no new measurement, no new cutoffs, and no new evidence beyond §2/§3 outputs — it produces structured interpretation outputs at §4's own register-class.

§4 sub-sections honor scoping §2.2 anti-circularity guard inheritance + §2 → §5 sequencing lock (ChatGPT P2 + P3): §4 may consume §2/§3 outputs but may not retroactively redefine §2 thresholds, §3 rule-tree, or §3 partition table semantics. §4a sealed at session-9 SEAL register-event boundary; §4b deferred per sub-spec drafting cycle pacing decision per fresh session-N entry boundary. **§4a successor-cycle-class mapping is sealed independently of any later §4b framing-question interpretation cycle** — §4 umbrella existence does NOT pre-authorize §4b authoring per anti-momentum-binding strict reading; §4b entry requires fresh Charlie register authorization at separate register-event boundary per `feedback_authorization_routing.md`.

### §4a Successor-cycle-class mapping operationalization

Operationalizes scoping §2.3 (diagnostic findings → successor-cycle class mapping; ChatGPT lock) and meta-plan §4a: how Phase 5 diagnostic procedure maps §3.x interpretation outputs to pre-registered successor-cycle classes per scoping §2.3 + §6 7-register-class-eligible enumeration. §4a produces structured-object successor-cycle-class assignments via δ'' hybrid-contextual union mapping (D2''''''^c locked structure preserving §3.1 compositional structure + §2.d → §2.c precedence + §2.d indeterminate sub-case explicit routing per cross-reviewer DIVERGENCE synthesis at register-precision; γ-revised selective carry of upstream-conditioning structural prefixes per D2.5 locked at §3.1 anti-circularity (iii) preservation; 7-axis anti-circularity caveat including new (vii) anti-dominance axis per D3.1 locked at scoping §2.3 source-language register-precision discipline). §4a consumes §3.1 partition table rows + §3.2 `{label, terminal_trigger}` as inputs and produces structured-object successor-cycle-class assignments at §4a's own register-class; it introduces no new measurement, no new cutoffs, and no new evidence.

§4a register-class is downstream-consumer of §3.x measurement + interpretation labels at successor-cycle-class mapping register; register-class-distinct from §3.1 (multi-mode interpretation labels) + §3.2 (procedural-state category + terminal-trigger). §4a does NOT decide attribution dominance (per (vii) anti-dominance axis); §4a does NOT pre-name §4b framing-question assessment, §5 terminal-conclusion outcome, or §6 attribution narrative content; §4a is mechanical mapping per pre-registered lookup table + composite mapping rules + edge-handling rules.

**Indicator construction — NOT APPLICABLE** (no measurement layer at §4a register; §4a consumes §3.x sealed outputs as inputs not raw data; per §-symmetric template applicability declaration analogous to §3.x D3 Sub-decision 2 element-by-element applicability).

**Judgment rule (δ'' hybrid-contextual union + §2.d → §2.c precedence + §2.d indeterminate sub-case explicit routing + γ-revised selective carry-through + γ structured terminal object per D2''''''^c lock-set).**

Output structure (per D1.2 + D1.3 + D2.3 lock): hybrid object format `{class, disposition: eligible|required, qualifier (optional structural prefix), verbatim_label (scoping §2.3 source-language text)}` for single-class assignments; list of hybrid objects for multi-class assignments; structured terminal object with options set for Case A (see Case A mapping below).

Input space (per D1.1 lock): (i) §3.1 partition table rows at canonical binary §2.x states (single-mode or multi-mode compositions per §3.1 rule-tree); (ii) §3.2 Case A output `{label, terminal_trigger}` (0-mode case); (iii) §3.x edge-handling outputs (indeterminate variants per §3.x (ε'-narrow) + execution-suspended state per §3.x (ε') execution-invalidity branch).

Single-mode mapping (per D2.1 lock; direct lookup from scoping §2.3 verbatim source language):

| §3.x input | Successor-cycle-class | Disposition | Verbatim scoping §2.3 source language |
|---|---|---|---|
| `signal-decay reading` (§2.a only) | empirical-continuation | eligible | "empirical-continuation eligible (extended forward window, additional regime testing)" |
| `cost-drag reading` (§2.b only) | cost-model investigation | eligible | "cost-model investigation eligible (gross-vs-realistic decomposition)" |
| `cohort-substrate-weakness reading` (§2.c only) | cohort-paradigm pivot | required | "cohort-paradigm pivot required (alternative cohort sourcing)" |
| `AND-gate-overfit reading` (§2.d only) | gate-criterion pivot | required | "gate-criterion pivot required (alternative gate design)" |
| `success-metric-mismatch reading` (§2.e only) | metric-redefinition cycle | eligible | "metric-redefinition cycle eligible (alternative success criteria)" |
| `regime-change-detected reading` (§2.f only) | meta-paradigm pivot | eligible | "meta-paradigm pivot eligible (different research framing or universe)" |

Multi-mode composite mapping (per D2.2 (δ'') hybrid-contextual union; load-bearing per cross-reviewer DIVERGENCE synthesis):

- Emit **union** of single-mode successor classes per scoping §2.3 — NO collapse to single class (anti-dominance per (vii); §4a does not decide attribution dominance).
- Upstream-conditioning structural prefixes carry through per D2.5 (γ-revised) selective carry rule (see "§2.f / §2.d upstream-conditioning carry-through" subsection below).
- **§2.d detected + §2.c detected** (precedence rule per cross-reviewer convergence at sealed-prose-grounded merit): §2.c successor-class assignment is `gate-criterion pivot required` (replacing the §2.c-only `cohort-paradigm pivot required` per single-mode mapping) — because §2.d invalidates §2.c interpretation per sealed §2.d D3''' framing-chain ("interpretation/prose layer carries stronger semantics — the cohort-substrate inference is partially or fully invalidated by detected AND-gate overfit"). §2.d still emits its own `gate-criterion pivot required` (de-duplicated naturally at union register; single entry at output).
- **§2.d indeterminate + §2.c detected** (absorbing-propagation per §3.1 (b-symm-improved) inheritance): §2.c successor-class assignment is `indeterminate-cycle-class` per D2.4 — §4a defers §2.c successor-class until §2.d gate-validity resolves; §2.a/§2.b/§2.e/§2.f successor-classes unaffected per locally-scoped propagation.
- Non-§2.d-conditioned multi-mode cases (including §2.f-conditioned non-§2.d cases): straight union of single-mode successor classes per scoping §2.3. In §2.f-conditioned composites, the `regime-conditional` qualifier **attaches only to downstream-recipient successor-class outputs derived from §2.a/§2.b/§2.e** (empirical-continuation / cost-model-investigation / metric-redefinition cycle classes), **not to the §2.f-derived meta-paradigm-pivot eligible output itself**. Similarly: `forward-applicability-conditioned` qualifier attaches only to downstream-recipient successor-class outputs derived from §2.c/§2.d (cohort-paradigm-pivot / gate-criterion-pivot classes when §2.f fires), not to the §2.f-derived class output itself. `gate-validity-conditioned` semantic from §2.d Line 2 conditioning of §2.c is absorbed into §2.c-elevation per D2.2 precedence rule (see "Elevation ↔ qualifier interaction" subsection below). **No qualifier attaches to the upstream/source-mode class output itself** (§2.f is source of Line 1, not recipient; §2.d is source of Line 2, not recipient).

Case A (§3.2 0-mode) terminal mapping (per D2.3 (γ) structured terminal object; anti-pre-naming preservation per §3.2 anti-circularity (v) + §4a anti-circularity (iv)):

§3.2 Case A label `no-mode-detected reading` + terminal-trigger `{triggered: True, basis: ...}` consumed by §4a produces structured terminal object: `{class: terminal-conclusion-routing, options: [{class: meta-paradigm-pivot, disposition: eligible, verbatim_label: "meta-paradigm pivot eligible"}, {class: project-pause-declared-research-line-exhausted-close, disposition: eligible, verbatim_label: "project pause / declared-research-line-exhausted close"}], basis: <§3.2 trigger basis field>, anti_pre_naming: True}`. §4a does NOT pre-name §5 terminal-conclusion outcome between operational availability (no-successor-cycle-currently-justified per scoping §2.5) vs substantive availability (paradigm exhaustion per scoping §2.5). §5 author register retains terminal-conclusion authority per Mod-pass-2 F1 paragraph-disambiguation lock.

Indeterminate state mapping (per D2.4 (α) indeterminate-cycle-class; locally-scoped propagation per §3.x pattern; absorbing-propagation per upstream-conditioner indeterminacy at relevant scope):

- §3.1 `indeterminate-X reading` (per-mode (ε'-narrow) NaN at non-upstream-conditioning mode X) → §4a output `{class: indeterminate-cycle-class, source: §2.X}` for that mode; other modes' successor-classes unaffected.
- §3.1 `indeterminate-regime-conditional X reading` / `indeterminate-forward-applicability-conditioned X reading` (absorbing-propagation per §3.1 (b-symm-improved) when §2.f indeterminate) → §4a emits `indeterminate-cycle-class` for all affected downstream modes per §2.f universe-upstream scope.
- §3.1 `indeterminate-gate-validity-conditioned cohort-substrate-weakness reading` (absorbing-propagation when §2.d indeterminate) → §4a emits `indeterminate-cycle-class` for §2.c successor-class per §2.d-specific gate scope.
- §3.2 `indeterminate-Case-A reading` (per §3.2 (ε'-narrow) NaN at one or more §2.x outputs) → §4a emits `{class: indeterminate-terminal-routing, source: <list of NaN §2.x>}` deferring terminal-conclusion routing.

§2.f / §2.d upstream-conditioning carry-through (per D2.5 (γ-revised) selective carry; SEAL-blocker patch per cross-reviewer βW-1 ADOPTED at register-precision):

Structural composition prefixes from §3.1 rule-tree output (`regime-conditional` / `forward-applicability-conditioned` / `gate-validity-conditioned`) CARRY THROUGH to §4a successor-class output as qualifier field at register-precision per §3.1 compositional semantic — but **attach only to the conditioned-mode (downstream/recipient) class outputs, NOT to the conditioning-mode (upstream/source) class outputs**. Specifically: `regime-conditional` (from §2.f Line 1 content-attribution bilateral) attaches to §2.a/§2.b/§2.e-derived successor-classes (empirical-continuation / cost-model-investigation / metric-redefinition cycle); `forward-applicability-conditioned` (from §2.f Line 1 forward-applicability univocal) attaches to §2.c/§2.d-derived successor-classes (cohort-paradigm-pivot / gate-criterion-pivot); `gate-validity-conditioned` (from §2.d Line 2) semantic is absorbed into §2.c-elevation at §4a per D2.2 precedence rule. §2.f's own successor-class output `meta-paradigm-pivot eligible` carries no Line 1 qualifier (§2.f is upstream source of Line 1, not recipient). §2.d's own successor-class output `gate-criterion-pivot required` carries no Line 2 qualifier (§2.d is upstream source of Line 2, not recipient).

**Elevation ↔ qualifier interaction (per Advisor βX-1 explicit specification ADOPTED at register-precision):** When §2.c successor-class is elevated from cohort-paradigm-pivot-required to gate-criterion-pivot-required per D2.2 §2.d → §2.c precedence rule, **§2.c-specific qualifiers from §3.1 conditioning of §2.c are DROPPED at the elevation** (semantic transfer to §2.d's source register; §2.d is source of Line 2, hence the elevated class output behaves at §2.d-source-register identity, not §2.c-recipient-register). After de-duplication with §2.d-direct's own gate-criterion-pivot-required contribution, the surviving qualifiers come from §2.d-direct's own conditioning (e.g., `forward-applicability-conditioned` from §2.f Line 1 conditioning §2.d). Net effect: §2.d detected + §2.c detected without §2.f → no qualifier on de-duplicated entry; §2.f detected + §2.d detected + §2.c detected → `forward-applicability-conditioned` qualifier survives via §2.d-direct contribution after de-dup.

Bilateral attribution caveat parenthetical (`(with bilateral attribution caveat)` per §3.1 Tier bilateral overlay) DROPS at §4a register per §3.1 anti-circularity (iii) §6 narrative authority preservation — bilateral attribution caveat is narrative scope deferred to §6, NOT structural composition. Example: §3.1 output `regime-conditional signal-decay reading (with bilateral attribution caveat)` → §4a output `{class: empirical-continuation, disposition: eligible, qualifier: regime-conditional, verbatim_label: "..."}` (qualifier on §2.a-derived class; §2.f-derived class output `{class: meta-paradigm pivot, disposition: eligible}` separately, no qualifier). The qualifier field preserves structural composition at conditioned-mode register; bilateral attribution caveat semantic retained at §6 narrative author register, not at §4a successor-class register.

**Substantive prior justification (δ'' hybrid-contextual union over priority rule + γ-revised selective carry over carry-all + anti-pre-naming for terminal cases).**

δ'' hybrid-contextual union over priority rule rationale: §3.1's compositional rule-tree PRESERVES multi-mode labels via modifier prefixes; it does NOT collapse to single dominant class. §4a inherits this compositional preservation discipline by emitting union of all implicated successor classes rather than collapsing to single class. Attribution dominance (whether §2.f vs §2.a is primary cause) is §4b / §5 / §6 author register scope per (vii) anti-dominance axis — §4a does not invent dominance beyond what scoping §2.3 source language or §3.1 sealed prose explicitly specifies. §2.d → §2.c precedence is the only pre-specified dominance per §3.1 sealed prose ("cohort-substrate inference partially or fully invalidated by detected AND-gate overfit") and is operationalized as precedence rule at successor-class register.

γ-revised selective carry over carry-all rationale: structural composition prefixes encode §3.1 rule-tree composition semantic (compositional information); bilateral attribution caveat parenthetical encodes §6 narrative scope semantic (narrative information). Carry-all would propagate bilateral attribution caveat to §4a register-precision in violation of §3.1 anti-circularity (iii) §6 narrative authority. Selective carry preserves structural composition (load-bearing for §4b / §5 / §6 downstream consumers) without violating §6 narrative authority.

Anti-pre-naming for terminal cases rationale: Case A → terminal-conclusion-routing emits options set rather than collapsing to single outcome because §5 terminal-conclusion outcome between operational vs substantive availability is §5 author register scope per scoping §2.5 + Mod-pass-2 F1. §4a pre-naming would violate anti-circularity (iv) §4a → §5 non-pre-emption.

**Descriptive supplements — NOT APPLICABLE** (no measurement framework at §4a register; per §-symmetric template applicability declaration analogous to §3.x).

**NaN-handling rule (inherits §3.1 / §3.2 (ε') hybrid pattern at §4a register).**

- **Execution invalidity (infrastructure failure):** if §3.1 or §3.2 execution suspended per (ε') execution-invalidity branch, §4a execution suspends per (ε') hybrid pattern inheritance; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.
- **Epistemic indeterminacy ((ε'-narrow) input class scope):** §3.x indeterminate label outputs propagate to §4a `indeterminate-cycle-class` / `indeterminate-terminal-routing` assignments per Indeterminate state mapping rule above.

Substantive principle: §4a inherits §3.1 (ε') hybrid + §3.2 (ε') hybrid execution-invalidity + (ε'-narrow) indeterminacy pattern at successor-cycle-class mapping register; indeterminate-cycle-class label format is partial-inheritance per §-symmetric base label register-class distinction (§3.1 per-mode / §3.2 procedural-state / §4a successor-cycle-class).

**Anti-circularity caveat (7-axis per D3''''''^c Sub-decision 1).**

- **(i) §4a → §2 threshold guard (ChatGPT P2 inheritance):** §4a may consume §3.x outputs (which themselves consume §2.x outputs) but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds remain authoritative.
- **(ii) §4a → §2 register-class identity guard:** §4a does not retroactively re-frame §2.x register-class identities (§2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-classes per sealed identity).
- **(iii) §4a → §6 narrative non-pre-emption with explicit context-vs-content distinction:** §4a successor-cycle-class assignments provide cross-section context to §6 narrative author (mapping table cell traversal context); §4a does NOT pre-name §6 attribution narrative specific content (substantive narrative determination of what successor-cycle-class assignment means in §6 narrative interpretation); substantive narrative determination retained at §6 author register. Bilateral attribution caveat (per §3.1 anti-circularity (iii)) preserved at §6 narrative scope, NOT at §4a successor-class register per D2.5 (γ-revised) selective carry rule.
- **(iv) §4a → §5 non-pre-emption (terminal-conclusion routing):** §4a Case A → terminal-conclusion-routing emits options set (meta-paradigm-pivot eligible + project-pause eligible) but does NOT pre-name §5 outcome between operational availability vs substantive availability per scoping §2.5 + Mod-pass-2 F1. §5 author register retains terminal-conclusion authority.
- **(v) §4a → §4b non-pre-emption (NEW per §4a's direct §4b coupling):** §4a successor-cycle-class assignments consumed by §4b framing-question resolution-pressure assessment procedure per advisor Finding 3 §4a → §4b sequential sub-section boundary; §4a does NOT pre-name §4b assessment outcome for any framing-question (1)/(2)/(3)/(4) resolution-pressure.
- **(vi) §4a → §3.x non-modification (ChatGPT P2/P3 parallel inheritance precedent):** §4a consumes §3.1 partition table + §3.2 outputs as inputs without retroactive modification of §3.x rule-tree, partition table, Case A condition, or terminal-trigger basis semantics per ChatGPT P2 + P3 anti-circularity guards at §3.x register; §4a is downstream consumer not upstream modifier.
- **(vii) Anti-dominance axis (NEW per cross-reviewer convergence at D2.2 (δ'') synthesis):** §4a does NOT rank or prioritize successor classes among multi-mode composite outputs. **Single explicit exception, exhaustively enumerated**: §2.d detected + §2.c detected → §2.c successor-class assignment elevated from `cohort-paradigm-pivot required` to `gate-criterion-pivot required` (§2.d invalidates §2.c interpretation per sealed §2.d D3''' framing-chain). **The §2.d → §2.c precedence rule is a gate-validity inheritance from sealed §2.d framing-chain semantics, not a substantive successor-class ranking rule** (per cross-reviewer ChatGPT-2 + Advisor-2 SYNTHESIS): the elevation encodes validity-gating (§2.d invalidates §2.c's cohort-substrate interpretation, requiring gate-criterion-pivot to resolve before cohort-paradigm-pivot becomes meaningful), NOT a priority ranking among otherwise-valid successor classes. No other sealed precedence rules; exception scope is single-case exhaustive — any future precedence rule additions require fresh sealed scoping authority (not §4a register). No invention of dominance at §4a register (e.g., NO §2.f-dominates-§2.a collapse); attribution dominance retained at §4b / §5 / §6 register-scopes per the structural distinction between successor-class mapping (§4a scope) and attribution narrative (§6 scope).

**Anti-pre-naming.** §4a does NOT pre-name §4b framing-question assessment outcomes. §4a does NOT pre-name §5 terminal-conclusion criteria or outcome. §4a does NOT pre-name §6 attribution narrative specific content. §4a does NOT pre-name session-N-specific operational details or successor cycle activation timing. §4a register-class binds to current sealed §2.a/b/c/d/e/f + §3.1 + §3.2 scope; successor-cycle-class mapping outside this scope is outside §4a's register-class.

**Input source per §1**: §3.1 partition table rows at sealed-output canonical register + §3.2 `{label, terminal_trigger}` at sealed-output canonical register + §3.x edge-handling outputs (indeterminate variants + execution-suspended state). No raw data consumption at §4a register. §4a V# anchor pre-execution check (below) verifies input space canonical-ness before §4a execution.

**Register-class:** binding | rule-based mapping logic (no new quantitative thresholds; consumes §3.x sealed outputs as structured inputs) | downstream-consumer of §3.x interpretation labels at successor-cycle-class mapping register | irreversible-interpretation-constraint per §2.2.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f + §3.1 + §3.2 precedent).** §4a output (successor-cycle-class assignments at mapping register) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. Successor-cycle-class mapping integrates §3.x outputs into structured-object successor-cycle assignments at §4a register; the integration does not retroactively rewrite Phase 4 verdict; only feeds §4b assessment procedure + §5 terminal-conclusion criteria + §6 attribution narrative at downstream register-scopes per framing-chain below. (§6 FB forward-binding)

**Framing-chain (4-direction per D3''''''^c Sub-decision 3).**

- **§4a → §4b:** Successor-cycle-class assignments consumed by §4b framing-question resolution-pressure assessment procedure per advisor Finding 3 sub-section sequential boundary; anti-pre-naming preserved per anti-circularity (v). (§6 FB forward-binding)
- **§4a → §5:** Successor-cycle-class assignments feed §5 terminal-conclusion criteria at §5 author register (Case A → terminal-conclusion-routing options set engages §5 assessment per scoping §2.5); ChatGPT P2 anti-circularity guard binding (§5 may consume §4a outputs but may not retroactively redefine §4a mapping table); ChatGPT P3 sequencing lock binding (§5 downstream consumer; cannot alter §4a once frozen). §4a outputs are §5 inputs not §5 resolution per anti-circularity (iv).
- **§4a → §6:** Successor-cycle-class assignments + mapping table cell traversal feed §6 attribution narrative; FB items added to forward-binding tracker per session-3-8 §6 binding accumulation precedent. (§6 FB forward-binding)
- **§4a → §7:** V# anchor pre-execution check on §3.x output completeness (see V# anchor below). (§7 FB forward-binding)

**V# anchor pre-execution check (per D3''''''^c Sub-decision 7; §3.x-parallel structure with full inheritance wording).** §4a pre-execution verifies §3.1 partition table + §3.2 `{label, terminal_trigger}` are at sealed-output canonical register (well-formed rows per §3.1 rule-tree schema + valid Case A object per §3.2 schema) AND §3.x edge-handling outputs are at sealed-pattern canonical states (indeterminate variants per §3.x (ε'-narrow) / execution-suspended state per §3.x (ε') execution-invalidity branch); mismatch (e.g., §3.x output schema violation, missing field, unexpected output class) suspends §4a execution per (ε') execution-invalidity branch inherited per §3.x; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

**Successor-cycle-class label set (per D3''''''^c Sub-decision 6 wording inventory; verbatim scoping §2.3 source language per D1.3 (iii) hybrid object lock).**

- **Base classes (7 per scoping §2.3 + §6 enumeration):** `empirical-continuation` (eligible) / `cost-model investigation` (eligible) / `cohort-paradigm pivot` (required) / `gate-criterion pivot` (required) / `metric-redefinition cycle` (eligible) / `meta-paradigm pivot` (eligible) / `project pause / declared-research-line-exhausted close` (eligible per scoping §2.5 terminal-conclusion).
- **Hybrid object format**: `{class, disposition, qualifier (optional), verbatim_label}` per D1.2 + D1.3 lock.
- **Terminal Case A object**: `{class: terminal-conclusion-routing, options: [...], basis: <§3.2 trigger basis>, anti_pre_naming: True}` per D2.3 lock.
- **Indeterminate label**: `{class: indeterminate-cycle-class, source: <§2.X identifier>, qualifier: <X-mode-specific>}` per D2.4 lock; `{class: indeterminate-terminal-routing, source: <list of NaN §2.x>}` per §3.2 indeterminate-Case-A propagation.
- **Suspension label**: §4a execution suspends per (ε') execution-invalidity branch inherited per §3.x; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

**Mapping table (per D3''''''^c Sub-decision 4 (α) enumerate by input pattern; 8 schema rows demonstrating composition pattern).**

| §3.x input pattern | §4a successor-cycle-class output | Notes |
|---|---|---|
| §3.1 single-mode §2.a | `{class: empirical-continuation, disposition: eligible, verbatim_label: scoping §2.3}` | (resolved at §6 narrative) |
| §3.1 single-mode §2.c | `{class: cohort-paradigm pivot, disposition: required, verbatim_label: scoping §2.3}` | (resolved at §6 narrative) |
| §3.1 multi-mode {§2.f, §2.a} (`regime-conditional signal-decay reading`) | Union: `[{class: meta-paradigm pivot, disposition: eligible}, {class: empirical-continuation, disposition: eligible, qualifier: regime-conditional}]` | NO collapse (anti-dominance (vii)); `regime-conditional` qualifier attaches to §2.a-derived class only (conditioned mode); §2.f-derived class output carries no Line 1 qualifier (upstream source); bilateral attribution caveat dropped per §3.1 anti-circularity (iii) |
| §3.1 multi-mode {§2.d, §2.c} (`gate-validity-conditioned cohort-substrate-weakness reading`) | Single entry: `{class: gate-criterion pivot, disposition: required, sources: [§2.d-direct, §2.c-elevated-per-§2.d-precedence]}` (§2.c successor-class elevated from cohort-paradigm-pivot to gate-criterion-pivot per D2.2 precedence rule; de-duplicates with §2.d's own gate-criterion-pivot-required entry at successor-class register) | §2.d → §2.c precedence rule per §3.1 sealed prose §2.d D3''' framing-chain; `gate-validity-conditioned` semantic absorbed into §2.c-elevation per D2.2 (NOT carried as separate qualifier at §4a register; elevation IS the gate-validity-conditioning at successor-class register) |
| §3.1 multi-mode {§2.f, §2.d, §2.c} (`forward-applicability-conditioned gate-validity-conditioned cohort-substrate-weakness reading`) | Union: `[{class: meta-paradigm pivot, disposition: eligible}, {class: gate-criterion pivot, disposition: required, qualifier: forward-applicability-conditioned, sources: [§2.d-direct, §2.c-elevated-per-§2.d-precedence]}]` | §2.f's own class output: no Line 1 qualifier (upstream source); §2.d/§2.c-elevated class outputs receive `forward-applicability-conditioned` per §2.f Line 1 (downstream recipients); `gate-validity-conditioned` semantic absorbed into §2.c-elevation per D2.2 (stacking at §3.1 register; elevation at §4a register) |
| §3.1 indeterminate (§2.d indeterminate + §2.c detected; `indeterminate-gate-validity-conditioned cohort-substrate-weakness reading`) | `{class: indeterminate-cycle-class, source: §2.c, qualifier: gate-validity-conditioned-indeterminate}` for §2.c; §2.a/§2.b/§2.e/§2.f successor-classes per their respective single-mode mapping if firing | §2.c successor-class defers until §2.d gate-validity resolves per §3.1 (b-symm-improved) absorbing-propagation inheritance |
| §3.2 Case A (`no-mode-detected reading + {triggered: True, basis}`) | `{class: terminal-conclusion-routing, options: [{meta-paradigm-pivot eligible}, {project-pause-declared-research-line-exhausted-close eligible}], basis: <§3.2 trigger basis>, anti_pre_naming: True}` | §5 author register determines operational vs substantive availability outcome per anti-circularity (iv) |
| §3.x execution-suspended state | §4a execution suspends per (ε') execution-invalidity branch inherited; §6 enumerates; Charlie register re-adjudicates | Per (ε') execution-invalidity branch inherited per §3.x |

Rows are schema rows demonstrating composition pattern; full enumeration at execution time mechanical from locked mapping rules per §3.x partition table + §3.2 Case A + §3.x edge-handling output set. Mapping table is static at §4a register (schema rows above); execution enumerates mapping rows based on actual §3.x output at §4a execution time. **No new interpretation logic introduced at execution time** — only mechanical traversal of locked mapping rules.

### §4b Framing-question resolution-pressure assessment procedure operationalization

Operationalizes scoping §2.4 (diagnostic findings → framing-question (1)/(2)/(3)/(4) resolution-pressure mapping; advisor Refinement 1; load-bearing) and meta-plan §4b: how Phase 5 SEAL conducts framing-question resolution-pressure **assessment procedure** for the framing-question-resolution-decision at SEAL boundary. §4b produces structured-object pressure-label-set output via β union-of-labels mapping (D2''''''^d locked structure preserving §3.x compositional preservation discipline at §4b register; no §4b-internal pressure precedence per D2.2 β lock; 9-axis anti-circularity caveat including new (viii) intra-§4b anti-dominance inheritance from §4a (vii) + new (ix) cross-register-dominance-prohibition per D3.1 locked at scoping §2.4 source-language verbatim-strict discipline). §4b consumes (i) §3.x finding labels at sealed-output canonical register + (ii) §4a successor-cycle-class assignments at sealed-output canonical register + (iii) §3.2 Case A `{label, terminal_trigger}` at sealed-output canonical register as inputs and produces structured-object pressure-label-set output at §4b's own register-class; it introduces no new measurement, no new cutoffs, and no new evidence.

§4b register-class is downstream-consumer of §3.x interpretation labels + §4a successor-class assignments at framing-question-resolution-pressure assessment register; register-class-distinct from §3.1 (multi-mode interpretation labels) + §3.2 (procedural-state category + terminal-trigger) + §4a (successor-cycle-class mapping). §4b does NOT decide inter-finding pressure dominance (per (viii) anti-dominance inheritance); §4b does NOT pre-name §5 terminal-conclusion outcome, framing-question (1)/(2)/(3)/(4) resolution, or §6 attribution narrative content; §4b is procedure (assessment-procedure pre-registration), not resolution (framing-question-resolution happens at Phase 5 SEAL author register based on diagnostic findings).

**Critical distinction (per meta-plan §4b binding + advisor Refinement 1 load-bearing):** procedure ≠ resolution. Sub-spec pre-registers the assessment procedure (how Phase 5 SEAL evaluates pressure on the framing question); framing-question resolution itself happens (or doesn't) at Phase 5 SEAL based on diagnostic findings. §4b does NOT resolve framing question.

**Indicator construction — NOT APPLICABLE** (no measurement layer at §4b register; §4b consumes §3.x + §4a sealed outputs as inputs not raw data; per §-symmetric template applicability declaration analogous to §3.x / §4a D3 Sub-decision 2 element-by-element applicability).

**Judgment rule (β union-of-labels + γ tripartite input space + α scalar terminal-conclusion mapping + γ upstream-indeterminacy propagation + β qualifier carry-through-as-context per D''''''^d lock-set).**

Output structure (per D1.2 β''-refined + D1.3 5-label + propagation_state-separate-field lock + finding_source provenance field per β-1 schema extension): per active §3.x finding contribution, structured object `{pressure_label, pivot_direction_dependency, basis, propagation_state, context_qualifiers (optional), finding_source}`. The `basis` field carries state-conditional content: when `propagation_state = active`, `basis` content = §2.4 verbatim source language for the §3.x finding; when `propagation_state = upstream-indeterminacy-propagated`, `basis` content = upstream §3.x or §4a indeterminate label; when `propagation_state = execution-suspended-from-upstream`, `basis` content = upstream §3.x or §4a suspension cause. The `finding_source` field carries §3.x finding-type provenance (e.g., `§2.a` / `§2.b` / `§2.c` / `§2.d` / `§2.e` / `§2.f` / `terminal-conclusion`) — load-bearing for §6 narrative author register disambiguation when multiple findings emit identical `pressure_label` (e.g., §2.d + §2.c co-fire). Multi-finding composite output is a `list` of such objects (one per active finding contribution); β union-of-labels emits all active findings' pressure objects, no §4b-internal precedence among them.

Input space (per D1.1 γ tripartite lock): (i) §3.1 partition table rows + §3.2 finding labels at sealed-output canonical register (per-finding-type access to §3.x labels for direct scoping §2.4 case routing); (ii) §4a successor-cycle-class assignments at sealed-output canonical register (consumes §4a output for downstream-consumer cross-section); (iii) §3.2 Case A output `{label, terminal_trigger}` at sealed-output canonical register (explicit Case A pathway separation per §4a precedent tripartite-input shape).

Single-finding mapping (per D2.1 lock; direct lookup from scoping §2.4 verbatim source language):

**Qualifier-stripping rule (per D2.5 β lock + β-4 explicit articulation):** §4b mapping operates on the bare §2.x reading-class component of the §3.1 reading label; qualifier prefixes (e.g., `regime-conditional` / `gate-validity-conditioned` / `forward-applicability-conditioned`) are stripped for §2.4 case routing purposes and carried as `context_qualifiers` field per D2.5 β lock. The §2.4 (a)/(b)/(c)/(d)/(e)/(f) case routing is keyed on raw §2.x finding type only; qualifier compositional context preserved separately for §6 narrative author register without affecting pressure-label assignment.

| §3.x finding input | Pressure label | Pivot direction dependency | Verbatim scoping §2.4 source language |
|---|---|---|---|
| §3.1 `signal-decay reading` (§2.a single-mode) | `deferred` | none ("defensible under any framing") | "framing question can stay deferred under (4) indeterminate; empirically-conditioned continuation defensible under any framing" |
| §3.1 `cost-drag reading` (§2.b single-mode) | `deferred` | none ("defensible under any framing") | "framing question can stay deferred under (4) indeterminate; empirically-conditioned continuation defensible under any framing" |
| §3.1 `cohort-substrate-weakness reading` (§2.c single-mode) | `load-bearing-at-Phase-5-SEAL` | depends-on-framing ("pivot direction depends on framing") | "framing question becomes load-bearing at Phase 5 SEAL; pivot direction depends on framing; Charlie register engagement required at successor cycle entry" |
| §3.1 `AND-gate-overfit reading` (§2.d single-mode) | `load-bearing-at-Phase-5-SEAL` | depends-on-framing ("pivot direction depends on framing") | "framing question becomes load-bearing at Phase 5 SEAL; pivot direction depends on framing; Charlie register engagement required at successor cycle entry" |
| §3.1 `success-metric-mismatch reading` (§2.e single-mode) | `ambiguous` | implicit-via-Phase-5-SEAL-surfacing ("ambiguous") | "ambiguous; Phase 5 SEAL surfaces framing engagement explicitly" |
| §3.1 `regime-change-detected reading` (§2.f single-mode) | `load-bearing` | depends-on-framing-substantively ("pivot direction substantively determined by framing") | "framing question becomes load-bearing; pivot direction substantively determined by framing" (NB: temporal qualifier underspecified in §2.4 verbatim; preserved as-is per D''''''^d (f) reconciliation lock A verbatim-strict; downstream §5/§6 consumer registers decode temporal binding from basis field) |
| §3.2 Case A `no-mode-detected reading + {triggered: True, basis: ...}` (terminal-conclusion finding) | `load-bearing-immediately` | project-identity-engagement-implicit ("engages directly with project-identity question") | "framing question becomes load-bearing immediately; engages directly with project-identity question" |

Multi-finding composite mapping (per D2.2 β union-of-labels; load-bearing per cross-reviewer convergence + advisor PUSHBACK adjudication at §4a (vii) inheritance register-precision):

**Example field-display convention (per β-2 standardization):** Examples below show a subset of the 6-field schema for illustration, omitting fields that do not vary across the example or are inferable from context; ellipsis `, ...` marks omitted fields where ambiguity could arise. Full 6-field schema (D1.2 β''-refined: `pressure_label, pivot_direction_dependency, basis, propagation_state, context_qualifiers (optional), finding_source`) applies at every pressure object at execution time. Indeterminate-state and execution-suspended examples below focus on `propagation_state` + `basis` fields specifically because `pressure_label = null` is invariant under those states; the active-state and composite examples may show fewer fields with `, ...` markers.

- Emit **union** of per-finding pressure-label-objects — NO §4b-internal precedence among them (no dominance rule invented at §4b register; no temporal qualifier ladder collapsed to ranking; no §2.5-grounded synthesis exception for terminal-conclusion). Multi-finding composite output is a list of per-finding pressure objects; Phase 5 SEAL author register adjudicates inter-finding pressure dominance at SEAL time based on full diagnostic context.
- Example: §3.1 multi-mode `regime-conditional signal-decay reading` (§2.f + §2.a co-detected) → §4b output: `[{pressure_label: load-bearing, finding_source: §2.f, basis: "(f)...", ...}, {pressure_label: deferred, finding_source: §2.a, basis: "(a) or (b)...", context_qualifiers: [regime-conditional], ...}]`. NO collapse to single pressure label; §2.f-derived and §2.a-derived pressure assessments emit independently; Phase 5 SEAL author register reads both and adjudicates which (or both) is load-bearing at framing-question-resolution scope.
- Example: §3.2 Case A AND §3.1 §2.a co-fire (terminal-conclusion finding AND signal-decay reading both register at same SEAL) → §4b output: `[{pressure_label: load-bearing-immediately, finding_source: terminal-conclusion, basis: terminal, ...}, {pressure_label: deferred, finding_source: §2.a, basis: (a)/(b), ...}]`. NO §4b silent collapse of "incoherent" co-occurrence to single label; "incoherence" of co-fire is a real substantive diagnostic signal (terminal-conclusion firing alongside signal-decay finding is itself surface-able at §6 narrative scope) and is preserved by union emission; SEAL author register adjudicates dominance at full-context register, not §4b at procedure register.
- §2.d-detected + §2.c-detected composite (where §4a applies §2.d → §2.c precedence rule at successor-class register per sealed §2.d D3''' framing-chain): §4a successor-class register collapses §2.c successor-class to gate-criterion-pivot per elevation; this elevation is §4a-register-specific and does NOT extend to §4b finding_source provenance per (viii) anti-dominance inheritance. §4b consumes §3.1 finding labels directly: §2.d-direct as `AND-gate-overfit reading` AND §2.c-with-gate-validity-conditioned as `gate-validity-conditioned cohort-substrate-weakness reading`. §4b output: `[{pressure_label: load-bearing-at-Phase-5-SEAL, finding_source: §2.d, context_qualifiers: [], basis: "(c) or (d) → ..."}, {pressure_label: load-bearing-at-Phase-5-SEAL, finding_source: §2.c, context_qualifiers: [gate-validity-conditioned], basis: "(c) or (d) → ..."}]`. Both findings emit same pressure label (load-bearing-at-Phase-5-SEAL per §2.4 (c)/(d) verbatim); pressure-label de-duplication at output read register is downstream consumer (§5/§6 author register) responsibility — §4b does not pre-collapse identical pressure labels into single entry because finding_source provenance (with context_qualifiers) is load-bearing for §6 narrative author register (§6 narrative may distinguish §2.d-direct and §2.c-with-gate-validity-conditioned contributions even when pressure label coincides).

Case A (terminal-conclusion finding) mapping (per D2.3 α scalar; anti-pre-naming preservation per §4a anti-circularity (iv) + §4b anti-circularity (vii)):

§3.2 Case A `no-mode-detected reading + {triggered: True, basis: ...}` consumed by §4b produces scalar pressure-object: `{pressure_label: load-bearing-immediately, pivot_direction_dependency: project-identity-engagement-implicit, basis: "Terminal-conclusion finding (per §2.5) → framing question becomes load-bearing immediately; engages directly with project-identity question", propagation_state: active, finding_source: terminal-conclusion, ...}`. §4b does NOT pre-name which framing (1)/(2)/(3)/(4) resolves under terminal-conclusion engagement; §4b only assigns the pressure label and pivot dependency. Framing-question resolution (which of (1)/(2)/(3)/(4) becomes load-bearing) is §5 author register + Phase 5 SEAL author register scope per scoping §2.4 advisor Refinement 1 critical distinction (procedure ≠ resolution).

Indeterminate state mapping (per D2.4 γ upstream-indeterminacy preservation under §3.x → §4b mapping; absorbing-propagation per §3.1 (b-symm-improved) + §3.2 (ε'-narrow) inheritance):

- §3.1 `indeterminate-X reading` (per-mode (ε'-narrow) NaN at non-upstream-conditioning mode X) → §4b output `{pressure_label: null, propagation_state: upstream-indeterminacy-propagated, finding_source: §2.X, basis: §3.x indeterminate label}` for that finding; other findings' pressure objects unaffected.
- §3.1 `indeterminate-regime-conditional X reading` / `indeterminate-forward-applicability-conditioned X reading` (absorbing-propagation per §3.1 (b-symm-improved) when §2.f indeterminate) → §4b emits `{pressure_label: null, propagation_state: upstream-indeterminacy-propagated}` for all affected downstream findings per §2.f universe-upstream scope inheritance.
- §3.1 `indeterminate-gate-validity-conditioned cohort-substrate-weakness reading` (absorbing-propagation when §2.d indeterminate per §3.1 inheritance and §4a D2.4 mapping) → §4b emits `{pressure_label: null, propagation_state: upstream-indeterminacy-propagated, finding_source: §2.c, basis: §2.d-gate-validity-indeterminate}` per §2.d-specific gate scope.
- §3.2 `indeterminate-Case-A reading` (per §3.2 (ε'-narrow) NaN at one or more §2.x outputs) → §4b emits `{pressure_label: null, propagation_state: upstream-indeterminacy-propagated, basis: §3.2 indeterminate-Case-A}`; terminal-conclusion pressure assessment deferred until §3.x resolves.
- §3.x or §4a execution-suspended → §4b emits `{pressure_label: null, propagation_state: execution-suspended-from-upstream, basis: <upstream suspension basis>}`; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

§4a upstream-conditioning qualifier carry-through (per D2.5 β qualifier-as-context; carry as `context_qualifiers` field per D1.2 β''-refined output object structure):

§4a structural composition qualifiers (`regime-conditional` / `forward-applicability-conditioned` / `gate-validity-conditioned` per §4a sealed prose) carry through to §4b output as `context_qualifiers` field at the respective per-finding pressure object — but do NOT alter the `pressure_label` field. The §2.4 (a)/(b)/(c)/(d)/(e)/(f) case routing is on raw §3.x finding type per scoping §2.4 verbatim; qualifiers encode §3.x compositional context (load-bearing for §6 narrative author register) but do not shift the §2.4 verbatim case mapping. Example: §3.1 `regime-conditional signal-decay reading` → §4b output for the §2.a-derived pressure object: `{pressure_label: deferred, ..., context_qualifiers: [regime-conditional], finding_source: §2.a}`. Qualifier preserved for §6 narrative scope; pressure label unchanged from single-mode §2.a mapping.

**Substantive prior justification (β union-of-labels over γ max-pressure over δ synthesis with §2.5-anchored single exception).**

β union-of-labels over γ max-pressure-by-temporal-ladder rationale: γ leans on temporal qualifier ladder in scoping §2.4 verbatim (terminal "immediately" > (c)/(d) "at Phase 5 SEAL" > (a)/(b) "deferred") as pre-specified dominance authority. Re-audit at register-precision shows §2.4 verbatim per-finding-type description does NOT contain a composite-handling clause; the temporal qualifier ladder is descriptive of single-finding cases, not prescriptive for composites. Inferring dominance ladder from temporal qualifier ladder = inference at §4b register, invoking §4a (vii) inheritance prohibition on §4-register-internal precedence rules without fresh sealed scoping authority. β preserves §4a (vii) anti-dominance discipline at §4b register; γ violates it via composite-precedence introduction. δ (β core + single §2.5-grounded exception for terminal-conclusion dominance) was proposed as synthesis but disqualified at register-precision: δ extends §2.5 "do not require ambiguity resolution in favor of continuation" (which is about cycle-outcome disposition register, not inter-finding-pressure dominance register) to inter-finding framing-pressure dominance — the extension itself is inference, structurally same-type failure as γ's temporal ladder inference. β passes register-precision audit; γ and δ fail at §4a (vii) inheritance + verbatim-vs-inference discipline.

β cost (less-directive procedure; Phase 5 SEAL author register carries composite dominance burden): treated as feature not bug. (1) Procedure ≠ resolution discipline (advisor Refinement 1 critical distinction) requires §4b NOT resolve composite dominance — punt-to-SEAL is the discipline's operational expression. (2) SEAL author register has full sealed §3.x + §4a + §4b context at SEAL time; better positioned to adjudicate inter-finding dominance than §4b at procedure-drafting register. (3) "Incoherent" co-occurrences (e.g., terminal-conclusion AND signal-decay co-fire) are real substantive diagnostic signals — preserving co-occurrence at §4b register surfaces the substantive anomaly at §6 narrative scope rather than silently collapsing it via §4b-invented precedence.

**Descriptive supplements — NOT APPLICABLE** (no measurement framework at §4b register; per §-symmetric template applicability declaration analogous to §3.x / §4a).

**NaN-handling rule (inherits §3.1 / §3.2 / §4a (ε') hybrid pattern at §4b register).**

- **Execution invalidity (infrastructure failure):** if §3.1 or §3.2 or §4a execution suspended per (ε') execution-invalidity branch, §4b execution suspends per (ε') hybrid pattern inheritance; §4b emits `propagation_state: execution-suspended-from-upstream`; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.
- **Epistemic indeterminacy ((ε'-narrow) input class scope):** §3.x or §4a indeterminate label outputs propagate to §4b `propagation_state: upstream-indeterminacy-propagated` with `pressure_label: null` per Indeterminate state mapping rule above.

Substantive principle: §4b inherits §3.1 / §3.2 / §4a (ε') hybrid execution-invalidity + (ε'-narrow) indeterminacy pattern at framing-resolution-pressure assessment register; propagation_state field at §4b is the register-specific encoding of upstream indeterminacy per D''''''^d (D1.2 + D1.3) lock — register-specific naming preserves cross-§ propagation semantics without conflating with active pressure label inventory.

**Anti-circularity caveat (9-axis per D3''''''^d Sub-decision 1).**

- **(i) §4b → §2 threshold guard (ChatGPT P2 inheritance):** §4b may consume §3.x + §4a outputs (which themselves consume §2.x outputs) but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds remain authoritative.
- **(ii) §4b → §2 register-class identity guard:** §4b does not retroactively re-frame §2.x register-class identities (§2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-classes per sealed identity).
- **(iii) §4b → §6 narrative non-pre-emption with explicit context-vs-content distinction:** §4b pressure-label-set assignments provide cross-section context to §6 narrative author (per-finding pressure objects + basis verbatim + context qualifiers as narrative anchor); §4b does NOT pre-name §6 attribution narrative specific content (substantive narrative determination of what pressure-label-set means in §6 narrative interpretation of framing-question engagement); substantive narrative determination retained at §6 author register.
- **(iv) §4b → §5 non-pre-emption (terminal-conclusion outcome):** §4b emits pressure label + propagation state; does NOT pre-name §5 terminal-conclusion outcome between operational availability vs substantive availability per scoping §2.5 + Mod-pass-2 F1. §5 author register retains terminal-conclusion authority.
- **(v) §4b → §4a non-modification:** §4b consumes §4a successor-cycle-class assignments as input without retroactive modification of §4a mapping table, composite rule, or qualifier carry-through semantics per §4a sealed-content invariance. §4b is downstream consumer not upstream modifier.
- **(vi) §4b → §3.x non-modification (ChatGPT P2/P3 parallel inheritance precedent):** §4b consumes §3.1 partition table + §3.2 outputs as inputs without retroactive modification of §3.x rule-tree, partition table, Case A condition, or terminal-trigger basis semantics per ChatGPT P2 + P3 anti-circularity guards at §3.x register.
- **(vii) Anti-pre-emption of framing-question resolution (NEW per §4b-specific concern; advisor Refinement 1 critical distinction):** §4b emits pressure label only; framing-question RESOLUTION (which of (1)/(2)/(3)/(4) resolves) is §5 author register + Phase 5 SEAL author register scope per scoping §2.4 procedure-≠-resolution discipline. §4b does NOT pre-name which framing wins under any pressure label assignment.
- **(viii) Intra-§4b anti-dominance inheritance from §4a (vii) (NEW per cross-reviewer convergence at D2.2 β synthesis):** §4b does NOT rank pressure labels among multi-finding composite outputs. **No precedence rule invented at §4b register**; β union-of-labels emission preserves all per-finding pressure objects without §4b-internal collapse. Any future §4b-internal precedence rule requires fresh sealed scoping authority (NOT §4b register); current D''''''^d lock-set has no §4b-internal precedence — composite dominance burden carried by Phase 5 SEAL author register per (vii) procedure-≠-resolution discipline. **§2.d → §2.c precedence at §4a register is gate-validity inheritance per §4a (vii) single-case exhaustive exception per sealed §2.d D3''' framing-chain — does NOT extend to §4b register as pressure-dominance rule** (§4a elevation operates at successor-class register; §4b pressure assessment register-class-distinct).
- **(ix) Cross-register-dominance-prohibition (NEW per cross-reviewer convergence + ChatGPT explicit recommendation):** §4b's label-inventory naming (preserving §2.4 verbatim temporal/urgency distinctions: `load-bearing-immediately`, `load-bearing-at-Phase-5-SEAL`, `load-bearing`, `ambiguous`, `deferred`) does NOT authorize successor-class priority ranking at §4a register, §6 attribution narrative dominance ranking, or downstream successor routing dominance. Pressure register and successor-class register are register-class-distinct; temporal and urgency distinctions preserved verbatim from §2.4 source language are informational, not authority-granting at cross-register scope. §4a anti-dominance (vii) and §4b anti-dominance (viii) operate independently at their respective registers; neither register's discipline propagates as cross-register dominance authority.

**Anti-pre-naming.** §4b does NOT pre-name which framing (1)/(2)/(3)/(4) resolves under any pressure label assignment per (vii). §4b does NOT pre-name §5 terminal-conclusion criteria or outcome per (iv). §4b does NOT pre-name §6 attribution narrative specific content per (iii). §4b does NOT pre-name session-N-specific operational details or Phase 5 SEAL-time interpretive choices. §4b register-class binds to current sealed §3.x + §4a scope; framing-resolution-pressure assessment outside this scope is outside §4b's register-class.

**Input source per §1**: §3.1 partition table rows + §3.2 `{label, terminal_trigger}` + §4a successor-cycle-class assignments at sealed-output canonical register + §3.x edge-handling outputs (indeterminate variants + execution-suspended state). No raw data consumption at §4b register. §4b V# anchor pre-execution check (below) verifies input space canonical-ness before §4b execution.

**Register-class:** binding | rule-based mapping logic (no new quantitative thresholds; consumes §3.x + §4a sealed outputs as structured inputs) | downstream-consumer of §3.x interpretation labels + §4a successor-cycle-class assignments at framing-question-resolution-pressure assessment register | irreversible-interpretation-constraint per §2.2 operationalization-freeze lock.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f + §3.1 + §3.2 + §4a precedent).** §4b output (pressure-label-set assignments at framing-resolution-pressure register) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. Framing-pressure assessment integrates §3.x + §4a outputs into structured-object pressure-label-set assignments at §4b register; the integration does not retroactively rewrite Phase 4 verdict; only feeds §5 terminal-conclusion criteria + §6 attribution narrative + §7 V# anchor verification at downstream register-scopes per framing-chain below. (§6 FB forward-binding)

**Framing-chain (3-direction per D3''''''^d Sub-decision 3).**

- **§4b → §5:** Pressure-label-set assignments feed §5 terminal-conclusion criteria at §5 author register (terminal-conclusion finding's pressure-label `load-bearing-immediately` + project-identity-engagement-implicit dependency engages §5 assessment per scoping §2.5); ChatGPT P2 anti-circularity guard binding (§5 may consume §4b outputs but may not retroactively redefine §4b pressure label inventory, composite handling rule, or propagation state semantics); ChatGPT P3 sequencing lock binding (§5 downstream consumer; cannot alter §4b once frozen). §4b outputs are §5 inputs not §5 resolution per anti-circularity (iv); §5 author register adjudicates inter-finding pressure dominance per (vii) procedure-≠-resolution discipline. (§6 FB forward-binding)
- **§4b → §6:** Pressure-label-set assignments + basis + context_qualifiers + propagation_state feed §6 attribution narrative framing-question-resolution-pressure section per scoping §3 deliverable structure. §6 narrative author register adjudicates substantive narrative interpretation of pressure-label-set; §4b cross-section context provided per (iii) anti-circularity. (§6 FB forward-binding; new items added to forward-binding tracker per session-3-9 §6 binding accumulation precedent)
- **§4b → §7:** V# anchor pre-execution check on §3.x + §4a output completeness (see V# anchor below). (§7 FB forward-binding)

**V# anchor pre-execution check (per D3''''''^d Sub-decision 7; §3.x / §4a-parallel structure with full inheritance wording).** §4b pre-execution verifies §3.1 partition table + §3.2 `{label, terminal_trigger}` + §4a successor-cycle-class assignments at sealed-output canonical register (well-formed rows per §3.1 rule-tree schema + valid Case A object per §3.2 schema + valid §4a structured object per §4a output structure) AND §3.x + §4a edge-handling outputs at sealed-pattern canonical states (indeterminate variants per §3.x / §4a (ε'-narrow) / execution-suspended state per §3.x / §4a (ε') execution-invalidity branch); mismatch (e.g., §3.x or §4a output schema violation, missing field, unexpected output class) suspends §4b execution per (ε') execution-invalidity branch inherited per §3.x / §4a; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

**Pressure-label inventory (per D3''''''^d Sub-decision 6 wording inventory; verbatim scoping §2.4 source language per D1.3 5-label lock; propagation_state separate field per D1.3 lock).**

- **Active pressure labels (5 per scoping §2.4):** `deferred` (for (a) or (b)) / `load-bearing-at-Phase-5-SEAL` (for (c) or (d)) / `ambiguous` (for (e)) / `load-bearing` (for (f); bare; preserves §2.4 verbatim temporal underspecification per D''''''^d (f) reconciliation lock A verbatim-strict) / `load-bearing-immediately` (for terminal-conclusion finding).
- **Propagation_state separate field (3 states):** `active` (default; pressure_label populated) / `upstream-indeterminacy-propagated` (pressure_label = null; basis preserved from upstream indeterminate label) / `execution-suspended-from-upstream` (pressure_label = null; basis preserved from upstream suspension cause).
- **Per-finding pressure object format (D1.2 β''-refined; 6-field schema):** `{pressure_label, pivot_direction_dependency, basis, propagation_state, context_qualifiers (optional), finding_source}`. The `basis` field is state-conditional: §2.4 verbatim under `active`; upstream label/cause under `upstream-indeterminacy-propagated` / `execution-suspended-from-upstream`. The `finding_source` field carries §3.x finding-type provenance for §6 narrative author register disambiguation. Multi-finding composite output is a `list` of such objects per active §3.x finding contribution (β union-of-labels).

**Mapping table (per D3''''''^d Sub-decision 4 α enumerate by §3.x input pattern; 8 schema rows demonstrating composition pattern).**

| §3.x input pattern | §4a output context | §4b pressure-label-set output | Notes |
|---|---|---|---|
| §3.1 single-mode §2.a | `{empirical-continuation, eligible}` | `[{pressure_label: deferred, pivot_direction_dependency: none, basis: "(a) or (b) → ...", propagation_state: active, finding_source: §2.a}]` | (resolved at §6 narrative) |
| §3.1 single-mode §2.c | `{cohort-paradigm pivot, required}` | `[{pressure_label: load-bearing-at-Phase-5-SEAL, pivot_direction_dependency: depends-on-framing, basis: "(c) or (d) → ...", propagation_state: active, finding_source: §2.c}]` | (resolved at §6 narrative) |
| §3.1 single-mode §2.e | `{metric-redefinition cycle, eligible}` | `[{pressure_label: ambiguous, pivot_direction_dependency: implicit-via-Phase-5-SEAL-surfacing, basis: "(e) → ...", propagation_state: active, finding_source: §2.e}]` | Phase 5 SEAL author register surfaces framing engagement explicitly per §2.4 verbatim |
| §3.1 single-mode §2.f | `{meta-paradigm pivot, eligible}` | `[{pressure_label: load-bearing, pivot_direction_dependency: depends-on-framing-substantively, basis: "(f) → ...", propagation_state: active, finding_source: §2.f}]` | Temporal qualifier underspecified per §2.4 verbatim; preserved as-is per (f) reconciliation lock A |
| §3.1 multi-mode {§2.f, §2.a} (`regime-conditional signal-decay reading`) | Union `[{meta-paradigm pivot, eligible}, {empirical-continuation, eligible, qualifier: regime-conditional}]` | Union: `[{pressure_label: load-bearing, finding_source: §2.f, ...}, {pressure_label: deferred, finding_source: §2.a, context_qualifiers: [regime-conditional]}]` | NO §4b-internal precedence per (viii); β union; SEAL author register adjudicates inter-finding dominance |
| §3.1 multi-mode {§2.d, §2.c} (`gate-validity-conditioned cohort-substrate-weakness reading` + `AND-gate-overfit reading`) | Single entry `{gate-criterion pivot, required, sources: [§2.d-direct, §2.c-elevated]}` per §4a precedence (elevation absorbs gate-validity-conditioned semantic at §4a register) | `[{pressure_label: load-bearing-at-Phase-5-SEAL, finding_source: §2.d, context_qualifiers: [], ...}, {pressure_label: load-bearing-at-Phase-5-SEAL, finding_source: §2.c, context_qualifiers: [gate-validity-conditioned], ...}]` | Both findings emit same pressure label per (c)/(d) verbatim; finding_source provenance + context_qualifiers preserved at §4b register; §4a elevation at successor-class register does NOT extend to §4b finding_source per (viii) anti-dominance inheritance |
| §3.2 Case A (`no-mode-detected + {triggered: True, basis}`) | `{terminal-conclusion-routing, options, basis, anti_pre_naming: True}` | `[{pressure_label: load-bearing-immediately, pivot_direction_dependency: project-identity-engagement-implicit, basis: "Terminal-conclusion finding ...", propagation_state: active, finding_source: terminal-conclusion}]` | §5 author register adjudicates terminal-conclusion outcome per §4b anti-circularity (iv); §4b does NOT pre-name framing (1)/(2)/(3)/(4) resolution per (vii) |
| §3.x execution-suspended state | §4a execution suspended | `[{pressure_label: null, propagation_state: execution-suspended-from-upstream, basis: <upstream suspension basis>}]` | §6 enumerates; Charlie register re-adjudicates per (ε') hybrid pattern inheritance |

Rows are schema rows demonstrating composition pattern; full enumeration at execution time mechanical from locked mapping rules per §3.x partition table + §3.2 Case A + §4a output structure + §3.x edge-handling output set. Mapping table is static at §4b register (schema rows above); execution enumerates mapping rows based on actual §3.x + §4a output at §4b execution time. **No new interpretation logic introduced at execution time** — only mechanical traversal of locked mapping rules + β union-of-labels emission + §2.4 verbatim case routing.

## §5 — Terminal-conclusion criteria

Operationalizes scoping §2.5 (terminal-conclusion availability per substantive vs operational availability F1 paragraph-disambiguation lock per Mod-pass-2 F1) and meta-plan §5: how Phase 5 SEAL evaluates terminal-conclusion admissibility under sealed §3.x + §4a + §4b outputs. §5 emits structured-object admissibility flags per outcome (substantive + operational) without invoking terminal-conclusion selection — admissibility ≠ selection per anti-circularity (x); Phase 5 SEAL author register retains terminal-conclusion invocation authority.

§5 register-class is downstream-consumer of §3.1 partition table rows + §3.2 Case A `{label, terminal_trigger}` + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects at sealed-output canonical register; register-class-distinct from §4b (framing-question-resolution-pressure assessment register). §5 consumes sealed upstream outputs as structured inputs and produces fixed-shape admissibility outputs per outcome; it introduces no new measurement, no new cutoffs, and no new evidence.

**Critical disclaimer block (two-pillar at §5 register-class declaration; parallel §4b "procedure ≠ resolution" prose-top discipline per advisor refinement at session-11 D2.5 + D3.1 (x) elevation).**

**Pillar 1 (D2.5 α HARD LOCK).** §5 introduces no new measurement authority, cumulative thresholds, or evidence-weighting logic beyond sealed upstream operationalizations. §5 "quantitative" semantic = precise mechanical mapping rule over sealed §3.x / §4a / §4b inputs at sealed-pattern canonical register; §5 does NOT introduce new numeric cutoffs, new partition criteria, new compatibility taxonomy, new precedence rules, or new aggregation logic at §5 register. All thresholds inherited from §2.x sealed outputs via §3.x / §4a / §4b composition.

**Pillar 2 (anti-circularity (x) axis at §5 register-class declaration).** §5 determines admissibility of terminal-conclusion outcomes, not mandatory selection among admissible outcomes. **Admissibility ≠ selection / invocation.** Even substantive admit does not compel Phase 5 SEAL author invocation; SEAL author register may decline invocation, invoke any admissible terminal outcome, defer terminal-conclusion to later cycle, route toward additional clarification cycles, or preserve future both-outcome authority if later sealed scoping authorizes a co-fire pathway. **SEAL author register retains full authority over terminal-conclusion invocation at full-context register** per scoping §4.6 sealed-content invariance + §4.2 anti-pre-naming.

These two pillars bind throughout §5 substantive content below; they are register-class declarations, not procedural rules with case-specific application.

**Indicator construction — NOT APPLICABLE** (no measurement layer at §5 register; §5 consumes §3.x / §4a / §4b sealed outputs as structured inputs not raw data; per §-symmetric template applicability declaration analogous to §3.x / §4a / §4b D3 Sub-decision element-by-element applicability).

**Judgment rule (β'-fixed-shape output structure + γ quadripartite input space + α strict substantive admissibility + 1-CASE α operational admissibility + β possibility-non-exclusivity + §-symmetric NaN-propagation per D''''''^e lock-set).**

Output structure (per D1.1 β'-fixed-shape + D1.2 4-field-per-slot + D1.3 split-availability-type-and-admissible lock; cardinality-bounded register-class-distinct adaptation from §4b list-of-objects per §5 max-2-outcome cardinality bound):

```
{
  substantive: {
    availability_type: "substantive",
    admissible: <True | False | null>,
    basis: <state-conditional content per propagation_state>,
    propagation_state: <active | upstream-indeterminacy-propagated | execution-suspended-from-upstream>
  } | null,
  operational: {
    availability_type: "operational",
    admissible: <True | False | null>,
    basis: <state-conditional content per propagation_state>,
    propagation_state: <active | upstream-indeterminacy-propagated | execution-suspended-from-upstream>
  } | null
}
```

The `basis` field carries state-conditional content: when `propagation_state = active AND admissible = True`, `basis` content = scoping §2.5 verbatim source language for the admitted availability outcome; when `propagation_state = active AND admissible = False`, `basis` content = "criteria not satisfied at canonical execution"; when `propagation_state = upstream-indeterminacy-propagated`, `basis` content = upstream §3.x / §4a / §4b indeterminate label/source; when `propagation_state = execution-suspended-from-upstream`, `basis` content = upstream suspension cause. Output cardinality is fixed at 2 slots (substantive + operational) per output instance; per-slot null only at initialization (not at canonical execution).

Input space (per D1.4 γ quadripartite lock): (i) §3.1 partition table rows + (ii) §3.2 Case A `{label, terminal_trigger}` at sealed-output canonical register + (iii) §4a successor-cycle-class assignments at sealed-output canonical register + (iv) §4b pressure-label-set per-finding objects at sealed-output canonical register.

**Substantive availability admission criterion (per D2.1 α HARD LOCK; canonical sealed pathway via §3.2 only):**

Substantive availability admissible ⟺ §3.2 sealed schema canonical Case A state at execution time:
- §3.2 Case A label = `no-mode-detected reading`
- §3.2 `terminal_trigger.triggered = True`
- §3.2 `terminal_trigger.basis.canonical_0_mode = True`
- §3.2 `terminal_trigger.basis.suspended_modes = []`
- §3.2 `terminal_trigger.basis.nan_modes = []`

When canonical Case A state satisfied, substantive admits with: `admissible: True`, `basis`: scoping §2.5 verbatim "the AND-gate paradigm is likely exhausted for deployable BTC alpha under realistic costs", `propagation_state: active`.

Substantive availability is single-canonical-pathway only — §3.2 canonical Case A is the sole sealed pathway producing substantive admissibility at §5 register.

**β extended REJECTED at register-precision (composite paradigm-exhaustion patterns from §3.1 multi-mode):** §3.1 multi-mode reading composites (e.g., (f) regime-change + (c) cohort-substrate-weakness + (b) cost-drag all-dominant) do NOT produce substantive admissibility at §5 register. Per scoping §2.5 + §3.2 sealed prose, §3.2 canonical Case A is the sole sealed pre-specified substantive-availability pathway. **Future §5 substantive admissibility from §3.1 multi-mode composites or other non-Case-A pathways requires fresh sealed scoping authority at §2.5 / §3.2 register; §5 self-extension prohibited per anti-circularity inheritance.**

**Operational availability admission criterion (per D2.2 1-CASE α LOCK; CASE 1 only per Path α via (x)-axis offloading; CASE 2 + CASE 3 REJECTED).**

Operational availability admissible ⟺ CASE 1 (§4a residual ambiguity):
- §3.2 Case A.`terminal_trigger = False` (interpreted to include §3.2 silent state per §3.1↔§3.2 XOR complementarity sealed property — silent state semantically equivalent to non-triggered Case A at §5 consumption register; i.e., §3.1 fires on ≥1 §2.x detected at canonical binary register and §3.2 emits no substantive output)
- AND §4a output has ≥2 distinct successor-cycle-class assignments at sealed-output canonical register (evaluated at `propagation_state = active` only per NaN-propagation precedence — see Indeterminate state mapping section below)
- AND those ≥2 assignments are NOT resolved by §4a-internal precedence rules (per sealed §4a D2.2 δ'' hybrid-contextual union structure; specifically, §4a-sealed precedence rule §2.d → §2.c elevation does NOT collapse all assignments into single successor class)

This is an admissibility trigger only; it does not assert that every unresolved successor plurality lacks a coherent continuation trajectory. (Patch closes ambiguity-inflation concern at register-precision without introducing compatibility taxonomy; SEAL author register (x)-axis adjudicates final coherence-of-continuation determination per Path α offloading discipline below.)

When CASE 1 satisfied, operational admits with: `admissible: True`, `basis`: scoping §2.5 verbatim "Phase 5 is permitted to conclude that no successor empirical cycle is currently justified" + §4a unresolved successor-cycle-class set reference, `propagation_state: active`.

CASE 1 admits operational ELIGIBILITY at §5 register; SEAL author register adjudicates final coherence-of-continuation determination per (x) axis. §5 procedure does NOT pre-compute compatibility-of-continuation; the "coherent continuation trajectory" determination is a SEAL author register judgment under full sealed §3.x + §4a + §4b context. SEAL author MAY decline operational invocation at benign successor plurality cases (e.g., {empirical-continuation eligible, cost-model-investigation eligible} from §2.a + §2.b co-detected) per (x) axis. **Future §5-register categorization of successor classes (e.g., continuation-cluster vs pivot-cluster) requires fresh sealed scoping authority at §2.3 register; §5 self-extension prohibited per anti-circularity inheritance.**

**CASE 2 REJECTED at sealed-grounding precondition** (verified at session-11 sealed §3.2 schema check): Future §5 operational admissibility from §3.2 Case A substantive interpretation ambiguity (i.e., hypothetical `terminal_trigger=True AND basis indicating substantive ambiguity`) requires fresh sealed scoping authority at §3.2 register. §3.2 sealed schema currently does NOT have a state where Case A is triggered AND basis indicates substantive ambiguity — at canonical Case A, basis is structurally fixed to `{canonical_0_mode: True, suspended_modes: [], nan_modes: []}`; indeterminate-Case-A is a sealed (ε'-narrow) state with `terminal_trigger.triggered = False` and basis nan_modes populated, handled via §-symmetric NaN-propagation (see Indeterminate state mapping below), register-class-distinct from substantive ambiguity. §5 self-extension prohibited per anti-circularity inheritance.

**CASE 3 REJECTED at procedural-entailment register-precision:** Future §5 operational admissibility from "substantive trigger implies operational admit" entailment requires fresh sealed scoping authority. Scoping §2.5 verbatim "either or both can be Phase 5's output" is at SEAL-output register (possibility semantic), NOT §5-procedure register (entailment semantic). §5 procedure layer encodes 1-CASE mutual exclusivity per current sealed §2.5 / §3.2 / §4a / §4b scope (D2.1 α substantive requires `§3.2.terminal_trigger=True`; D2.2 CASE 1 operational requires `§3.2.terminal_trigger=False`; conditions operationally mutually exclusive on §3.2 sealed schema). Co-fire is not pre-encoded at procedure layer. Scoping §2.5 "either or both" independence preserved at SEAL author register selection authority per (x) axis — SEAL author may invoke neither, either. **Any future co-fire pathway requires fresh sealed scoping authority (NOT §5 register self-extension).**

**Both-fire admissibility at procedure layer (per D2.3 β possibility-not-entailment + procedural mutual-exclusivity observation).**

Substantive and operational admissibility are register-distinct outcomes per scoping §2.5 + Mod-pass-2 F1 paragraph-disambiguation lock — substantive about paradigm-state register, operational about Phase 5 SEAL output authorizing-or-not-authorizing register. Per D2.1 α HARD LOCK substantive requires §3.2 Case A.`terminal_trigger=True` at canonical state; per D2.2 1-CASE LOCK operational requires §3.2 Case A.`terminal_trigger=False`. These conditions are operationally mutually exclusive at §5 procedure layer (sealed §3.2 schema cannot have `terminal_trigger=True` AND `terminal_trigger=False` simultaneously). Procedural mutual-exclusivity under current sealed criteria does not collapse F1 distinctness because the two outcomes remain separately typed, separately evaluated, and non-entailing; SEAL output register independence per scoping §2.5 "either or both" preserved at SEAL author register selection authority per (x) axis.

**Indeterminate state mapping (per D3.3 §-symmetric NaN-propagation inheritance from §3.x / §4a / §4b (ε') hybrid pattern; D2.4 absorbed into NaN-handling rule per session-11 §3.2 sealed schema verification).**

**NaN-propagation precedence:** NaN-propagation evaluation precedes canonical D2.1 / D2.2 evaluation. When any upstream input (§3.1 / §3.2 / §4a / §4b) is at indeterminate or execution-suspended state, §5 emits `admissible: null` per `propagation_state` field for both slots; canonical D2.1 / D2.2 criteria not evaluated. NaN-propagation is short-circuit at §5 register, not concurrent.

- §3.2 `indeterminate-Case-A reading` (per §3.2 (ε'-narrow) NaN at one or more §2.x outputs; `terminal_trigger.triggered = False`, basis `nan_modes` populated) → §5 emits for both slots: `{availability_type: <substantive | operational>, admissible: null, basis: §3.2 indeterminate-Case-A reading, propagation_state: upstream-indeterminacy-propagated}`. Neither substantive nor operational admits at upstream indeterminate; SEAL author register may invoke §3.2 re-execution per Charlie register re-adjudicate path inherited from §-symmetric (ε') hybrid pattern.
- §3.1 indeterminate-X reading variants / §4a indeterminate output / §4b `propagation_state = upstream-indeterminacy-propagated` → §5 emits for both slots: `{admissible: null, propagation_state: upstream-indeterminacy-propagated, basis: <upstream indeterminate label/source>}`. Same propagation discipline.
- §3.x / §4a / §4b execution-suspended state → §5 execution suspends per (ε') execution-invalidity branch inherited; §5 emits for both slots: `{admissible: null, propagation_state: execution-suspended-from-upstream, basis: <upstream suspension basis>}`; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

Substantive principle: §5 inherits §3.1 / §3.2 / §4a / §4b (ε') hybrid execution-invalidity + (ε'-narrow) indeterminacy pattern at terminal-conclusion-availability register; per-slot `admissible: null` + `propagation_state` field is the §5-register-specific encoding of upstream indeterminacy per D''''''^e lock-set. Register-specific encoding preserves cross-§ propagation semantics without conflating with active admissibility True/False inventory.

**Substantive prior justification (α strict + 1-CASE α + α-no-new-thresholds + Path α (x)-axis offloading + five-fold rejection register-precision discipline consolidates §5 architectural integrity as anti-circularity exemplar).**

Five rejected expansion patterns share common architectural self-restraint discipline at §5 register:

1. **D2.1 β extended REJECT** (round-1 reviewer convergent rejection; Claude Code self-PUSHBACK at register-precision): §3.1 multi-mode composite paradigm-exhaustion → substantive admission. §2.5 sealed prose pre-specifies §3.2 Case A only as substantive pathway; §3.1 multi-mode composite is §5-register synthesis of new substantive-admissibility criteria without sealed scoping authority. Inference-not-verbatim discipline failure (same-type as §4b session-10 D2.2 γ + δ rejection).
2. **CASE 2 REJECT (verification-grounded at session-11 §3.2 sealed schema check):** §3.2 Case A substantive interpretation ambiguity → operational admission. Sealed §3.2 schema has NO state where `terminal_trigger=True AND basis indicates substantive ambiguity`; "ambiguous-Case-A" is a §5-register conflation of sealed §3.2 indeterminate-Case-A (epistemic indeterminacy from NaN inputs, register-class NaN-propagated) with hypothetical substantive ambiguity (not a sealed state). §5 cannot admit operational on a non-sealed §3.2 state without fresh sealed scoping authority.
3. **CASE 3 REJECT (procedural-entailment register-precision; cross-reviewer DIVERGENCE adjudicated at sealed-prose-grounded merit, session-11):** Substantive triggers → operational also admits via F1 entailment. Scoping §2.5 "either or both" is possibility semantic at SEAL-output register, not entailment semantic at §5-procedure register. §5 procedure layer encoding automatic substantive → operational entailment is §5-register inference over §2.5 verbatim without sealed authorization.
4. **D2.5 α HARD LOCK over β cumulative-thresholds:** §5 cumulative thresholds / weighted evidence scoring / aggregation logic would re-determine §2 sealed thresholds at §5 register, violating ChatGPT P2 anti-circularity guard at §5 → §2 register-boundary. §5 must consume sealed §2.x indicators via §3.x / §4a / §4b composition only; no new measurement at §5 (Pillar 1 disclaimer block).
5. **Path α (x)-axis offloading over Path β §5-register compatibility taxonomy:** §5 introducing continuation-cluster vs pivot-cluster categorization of sealed §2.3 successor classes would be §5-register synthesis of new structure not pre-specified at sealed scoping; same-type expansion as D2.1 β extended. Coherent-continuation determination retained at SEAL author register per (x) axis under full-context register.

Common self-restraint discipline: **§5 self-extension WITHOUT fresh sealed scoping authority prohibited.** Future introduction of any of the five rejected expansion patterns requires fresh sealed scoping authority at the appropriate sealed § register (§2.5 / §3.2 / §2.3 / §2.x sealed thresholds), NOT §5 register self-creation. This five-fold rejection register-precision discipline consolidates §5 architectural integrity as one of the strongest anti-circularity exemplars in the Phase 5 sub-spec.

Procedure ≠ resolution discipline (advisor Refinement 1 critical distinction at §4b register, inherited at §5 register): §5 emits admissibility flags; terminal-conclusion invocation and SEAL author outcome adjudication happen at Phase 5 SEAL author register per anti-circularity (x).

**Descriptive supplements — NOT APPLICABLE** (no measurement framework at §5 register; per §-symmetric template applicability declaration analogous to §3.x / §4a / §4b).

**NaN-handling rule (inherits §3.1 / §3.2 / §4a / §4b (ε') hybrid pattern at §5 register; see Indeterminate state mapping above for §5-register-specific encoding).**

- **Execution invalidity (infrastructure failure):** if §3.1 or §3.2 or §4a or §4b execution suspended per (ε') execution-invalidity branch, §5 execution suspends per (ε') hybrid pattern inheritance; §5 emits `propagation_state: execution-suspended-from-upstream` for both slots; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.
- **Epistemic indeterminacy ((ε'-narrow) input class scope):** §3.x / §4a / §4b indeterminate outputs propagate to §5 `propagation_state: upstream-indeterminacy-propagated` with `admissible: null` for both slots per Indeterminate state mapping rule above.

**Anti-circularity caveat (10-axis per D3''''''^e Sub-decision 1).**

**Inheritance pattern at §5 register:** (i)-(iii) + (vi) are 1:1 inheritances from §4b register-shape (§5 → §2 threshold guard / §5 → §2 register-class / §5 → §6 narrative non-pre-emption / §5 → §3.x non-modification); (iv) is the §5-register parallel-instantiation of §4b's (v) at §4a consumption boundary (§5 → §4a non-modification parallels §4b → §4a non-modification with subject shifted from §4b to §5 per §5's downstream position); (v) is a NEW §5-specific axis (§5 → §4b non-modification; no §4b counterpart since §4b cannot self-reference at its own register); (vii) inherited from §4b (vii) framing-resolution non-pre-emption at §5 register; (viii) + (ix) + (x) are §5-specific axes (NEW per §5 register-class concerns). [Errata at session-11 errata register-event: corrected from prior "role-inverted from §4b (iv)" framing per advisor post-SEAL Anchor 4 register-precision audit — §4b (iv) was §4b → §5 non-pre-emption with §5 as object; §5 has no role-inversion of that axis; §5 (iv) is parallel of §4b (v) at §4a consumption boundary.]

- **(i) §5 → §2 threshold guard (ChatGPT P2 inheritance):** §5 may consume §3.x / §4a / §4b outputs (which themselves consume §2.x outputs) but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds remain authoritative.
- **(ii) §5 → §2 register-class identity guard:** §5 does not retroactively re-frame §2.x register-class identities (§2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-classes per sealed identity).
- **(iii) §5 → §6 narrative non-pre-emption with explicit context-vs-content distinction:** §5 admissibility flags + basis + propagation_state provide cross-section context to §6 narrative author (terminal-conclusion fire-or-not section structure anchor); §5 does NOT pre-name §6 attribution narrative specific content (substantive narrative determination of what admissibility means in §6 narrative interpretation of terminal-conclusion engagement); substantive narrative determination retained at §6 author register.
- **(iv) §5 → §4a non-modification:** §5 consumes §4a successor-cycle-class assignments as input without retroactive modification of §4a mapping table, composite rule, qualifier carry-through semantics, or precedence rule per §4a sealed-content invariance. §5 is downstream consumer not upstream modifier.
- **(v) §5 → §4b non-modification:** §5 consumes §4b pressure-label-set per-finding objects as input without retroactive modification of §4b pressure label inventory, composite handling rule (β union-of-labels), propagation state semantics, or schema fields per §4b sealed-content invariance. §5 is downstream consumer not upstream modifier per ChatGPT P3 sequencing lock binding.
- **(vi) §5 → §3.x non-modification (ChatGPT P2/P3 parallel inheritance precedent):** §5 consumes §3.1 partition table + §3.2 outputs as inputs without retroactive modification of §3.x rule-tree, partition table, Case A condition, terminal-trigger basis semantics, or indeterminate-Case-A NaN-propagation semantics per ChatGPT P2 + P3 anti-circularity guards at §3.x register.
- **(vii) Anti-pre-emption of framing-question resolution (inherited from §4b (vii) at §5 register):** §5 emits admissibility flags only; framing-question RESOLUTION (which of (1)/(2)/(3)/(4) resolves) is Phase 5 SEAL author register scope per scoping §2.4 + §4b (vii) procedure-≠-resolution discipline. §5 does NOT pre-name which framing wins under any admissibility outcome.
- **(viii) Substantive vs operational availability F1 distinctness preservation (NEW per §5-specific concern; Mod-pass-2 F1 paragraph-disambiguation lock):** §5 does NOT collapse substantive and operational availability into single outcome at §5 procedure layer or at output schema. Substantive availability is paradigm-state register (admissible ⟺ §3.2 canonical Case A); operational availability is Phase 5 SEAL output authorizing register (admissible ⟺ CASE 1 §4a residual ambiguity). These are register-class-distinct outcomes preserved at fixed-shape `{substantive: ..., operational: ...}` slot independence per D1.1 β'-fixed-shape lock.
- **(ix) §5 → successor-cycle-entry non-pre-emption (NEW per §5-specific concern; per scoping §4.2 anti-pre-naming inheritance):** §5 admits terminal-conclusion outcome eligibility; §5 does NOT pre-name Phase 5.1+ successor cycle scope, successor-class invocation, or specific successor pathway under any admissibility outcome. Successor cycle entry register-event boundary requires explicit Charlie register authorization per `feedback_authorization_routing.md` hard rule.
- **(x) §5 → Phase 5 SEAL outcome non-pre-emption (NEW per cross-reviewer convergent concern at session-11; advisor 10-axis explicit + ChatGPT terminal non-selection legitimacy synthesis):** §5 emits admissibility flags per outcome; Phase 5 SEAL author register adjudicates whether to actually invoke terminal-conclusion outcome based on full SEAL context. **Admissibility ≠ selection / invocation** (advisor framing). **Even substantive admit does not compel invocation; SEAL author may decline invocation, invoke any admissible terminal outcome, defer terminal-conclusion to later cycle, continue exploratory investigation, route toward additional clarification cycles, or preserve future both-outcome authority if later sealed scoping authorizes a co-fire pathway** (ChatGPT framing). **SEAL author register retains full authority over terminal-conclusion invocation at full-context register; §5 admissibility ≠ epistemic-compulsion-to-invoke** (combined). (vii) framing-resolution non-pre-emption and (x) SEAL-outcome non-pre-emption are register-class-distinct: (vii) blocks framing-question pre-naming under any admissibility outcome; (x) blocks "criteria admit ⟹ Phase 5 must invoke" misread.

**Anti-pre-naming.** §5 does NOT pre-name which framing (1)/(2)/(3)/(4) resolves under any admissibility outcome per (vii). §5 does NOT pre-name §6 attribution narrative specific content per (iii). §5 does NOT pre-name Phase 5.1+ successor cycle scope per (ix). §5 does NOT pre-name Phase 5 SEAL author terminal-conclusion invocation decision per (x). §5 does NOT pre-name session-N-specific operational details or Phase 5 SEAL-time interpretive choices. §5 register-class binds to current sealed §2.5 + §3.x + §4a + §4b scope; terminal-conclusion criteria authority outside this scope is outside §5's register-class.

**Input source per §1**: §3.1 partition table rows + §3.2 Case A `{label, terminal_trigger}` + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects at sealed-output canonical register + §3.x / §4a / §4b edge-handling outputs (indeterminate variants + execution-suspended state). No raw data consumption at §5 register. §5 V# anchor pre-execution check (below) verifies input space canonical-ness before §5 execution.

**Register-class:** binding | rule-based admissibility logic at procedure layer (no new quantitative thresholds per D2.5 α HARD LOCK; consumes §3.x / §4a / §4b sealed outputs as structured inputs) | downstream-consumer of §3.x interpretation labels + §4a successor-cycle-class assignments + §4b pressure-label-set at terminal-conclusion-availability register | irreversible-interpretation-constraint per §2.2 operationalization-freeze lock.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f + §3.1 + §3.2 + §4a + §4b precedent).** §5 output (substantive + operational admissibility flags at terminal-conclusion-availability register) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. §5 admissibility integration of §3.x + §4a + §4b outputs into terminal-conclusion admissibility flags does not retroactively rewrite Phase 4 verdict; only feeds §6 attribution narrative + §7 V# anchor verification at downstream register-scopes per framing-chain below. (§6 FB forward-binding)

**Framing-chain (2-direction per D3''''''^e Sub-decision 2).**

- **§5 → §6:** Admissibility flags + basis + propagation_state feed §6 attribution narrative "Terminal-conclusion fire-or-not section" per scoping §3 deliverable structure. §6 narrative author register adjudicates substantive narrative interpretation of admissibility outcomes; §5 cross-section context provided per (iii) anti-circularity. §6's "Terminal-conclusion fire-or-not section" structure substantively depends on §5's substantive vs operational availability F1 distinction per meta-plan §6 advisor Finding 4 dependency. (§6 FB forward-binding; new items added to forward-binding tracker per session-3-10 §6 binding accumulation precedent)
- **§5 → §7:** V# anchor pre-execution check on §3.x + §4a + §4b output completeness (see V# anchor below). (§7 FB forward-binding)

§5 ↔ §4 inverse direction NOT applicable per ChatGPT P3 sequencing lock binding (§5 downstream consumer; cannot alter §4 register once frozen); §5 ↔ §3 inverse direction NOT applicable per ChatGPT P2 + P3 parallel inheritance.

**V# anchor pre-execution check (per D3''''''^e Sub-decision 3; §3.x / §4a / §4b-parallel structure with full inheritance wording).** §5 pre-execution verifies §3.1 partition table + §3.2 `{label, terminal_trigger}` + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects at sealed-output canonical register (well-formed rows per §3.1 rule-tree schema + valid Case A object per §3.2 schema with `label ∈ {no-mode-detected reading, indeterminate-Case-A reading}` + `terminal_trigger.triggered ∈ {True, False}` + `terminal_trigger.basis` well-formed, OR §3.2 silent state (no output emitted) when §3.1 fires per sealed §3.1↔§3.2 XOR complementarity property — silent state satisfies V# anchor at §3.2 register; + valid §4a structured object per §4a output structure + valid §4b structured object per §4b output structure) AND §3.x / §4a / §4b edge-handling outputs at sealed-pattern canonical states (indeterminate variants per §3.x / §4a / §4b (ε'-narrow) / execution-suspended state per §3.x / §4a / §4b (ε') execution-invalidity branch); mismatch (e.g., §3.x / §4a / §4b output schema violation, missing field, unexpected output class) suspends §5 execution per (ε') execution-invalidity branch inherited per §3.x / §4a / §4b; §6 enumerates the discrepancy; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent.

**Admissibility outcome inventory (per D3''''''^e Sub-decision 4 wording inventory; verbatim scoping §2.5 source language anchored at basis field; per D1.3 split-availability-type-and-admissible lock).**

- **Substantive availability (paradigm-state register):** admissible ⟺ §3.2 Case A canonical state per D2.1 α HARD LOCK. Basis verbatim: "the AND-gate paradigm is likely exhausted for deployable BTC alpha under realistic costs" (scoping §2.5 source language).
- **Operational availability (Phase 5 SEAL output authorizing register):** admissible ⟺ CASE 1 §4a residual ambiguity per D2.2 1-CASE LOCK. Basis verbatim: "Phase 5 is permitted to conclude that no successor empirical cycle is currently justified" (scoping §2.5 source language) + §4a unresolved successor-cycle-class set reference.
- **Per-slot admissible value inventory (3 values per slot):** `True` (criteria satisfied) / `False` (criteria not satisfied at canonical execution; default for determinate single-mode findings or §4a-resolved multi-mode findings) / `null` (propagation_state ≠ active; inherited from upstream indeterminate or execution-suspended state per NaN-handling rule).
- **Propagation_state inventory (3 states per slot inherited from §4b precedent):** `active` (default; admissible populated True or False) / `upstream-indeterminacy-propagated` (admissible = null; basis preserved from upstream indeterminate label) / `execution-suspended-from-upstream` (admissible = null; basis preserved from upstream suspension cause).
- **Fixed-shape output cardinality:** exactly 2 slots (substantive + operational) per output instance; slot-level null only at initialization (not at canonical execution); register-class-distinct adaptation from §4b list-of-objects per §5 max-2-outcome cardinality bound per D1.1 β'-fixed-shape lock.

**Criteria table (per D3''''''^e Sub-decision 4 α schema-rows + cardinality cap; parallel §4b 8-row precedent; schema rows demonstrating composition pattern, NOT exhaustive enumeration).**

| §3.x / §4a / §4b input pattern | §5 substantive admissible | §5 operational admissible | Notes |
|---|---|---|---|
| §3.2 canonical Case A (`no-mode-detected reading + {triggered: True, basis: {canonical_0_mode: True, ...}}`) | True | False (per CASE 1 requires Case A.`terminal_trigger=False`) | substantive single-canonical-pathway; operational NOT admit per procedural mutual-exclusivity; SEAL author register (x)-axis adjudicates invocation |
| §3.1 single-mode §2.a (`signal-decay reading`); §4a `{empirical-continuation, eligible}` (single class) | False | False (per CASE 1 requires ≥2 distinct) | clean continuation; neither admits |
| §3.1 single-mode §2.c (`cohort-substrate-weakness reading`); §4a `{cohort-paradigm pivot, required}` (single class) | False | False | clean cohort-pivot path; neither admits |
| §3.1 multi-mode {§2.d, §2.c} (`AND-gate-overfit reading` + `gate-validity-conditioned cohort-substrate-weakness reading`); §4a `{gate-criterion pivot, required, sources: [§2.d-direct, §2.c-elevated]}` (§4a precedence rule resolves to single class) | False | False (per CASE 1 requires NOT §4a-resolved) | §4a-resolved composite; neither admits |
| §3.1 multi-mode {§2.f, §2.a} (`regime-conditional signal-decay reading`); §4a union {`meta-paradigm pivot, eligible`, `empirical-continuation, eligible, qualifier: regime-conditional`} (2 distinct, §4a not-resolved) | False | True (per CASE 1) | operational admits at §5 register; SEAL author register (x)-axis adjudicates coherence-of-continuation under full context |
| §3.1 multi-mode {§2.f, §2.c} (regime + cohort weakness composite; §2.c branch carries `regime-conditional` qualifier per §4a D2.5 γ-revised); §4a union {`meta-paradigm pivot, eligible`, `cohort-paradigm pivot, required, qualifier: regime-conditional`} (2 distinct, §4a not-resolved) | False | True (per CASE 1) | operational admits; SEAL author may invoke or decline per (x) |
| §3.2 `indeterminate-Case-A reading` + `{triggered: False, basis: {canonical_0_mode: False, nan_modes: [...]}}` | null (`propagation_state: upstream-indeterminacy-propagated`) | null (same) | §-symmetric NaN-propagation inherited from (ε'-narrow) hybrid pattern; SEAL author may invoke §3.2 re-execution per Charlie register re-adjudicate path |
| §3.x / §4a / §4b execution-suspended | null (`propagation_state: execution-suspended-from-upstream`) | null (same) | §6 enumerates; Charlie register re-adjudicates per (ε') hybrid pattern inheritance |

Rows are schema rows demonstrating composition pattern; full enumeration at execution time mechanical from locked admissibility criteria per §3.2 sealed schema + §4a sealed output structure + §3.1 partition table + §3.x / §4a / §4b edge-handling output set. Criteria table is static at §5 register (schema rows above); execution enumerates admissibility rows based on actual §3.x / §4a / §4b output at §5 execution time. **No new admissibility logic introduced at execution time** — only mechanical evaluation of locked D2.1 α + D2.2 CASE 1 + D3.3 NaN-handling rules + §-symmetric (ε') hybrid pattern.

## §6 — Attribution report deliverable structure

Operationalizes scoping §3 deliverable and meta-plan §6: pre-registers structural framework for Phase 5 closeout deliverable at `docs/closeout/PHASE5_RESULTS.md`. §6 register-class is downstream-consumer of §2.x measurement outputs + §3.1 multi-mode coexistence partition table + §3.2 Case A `{label, terminal_trigger}` + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects + §5 substantive-vs-operational admissibility flags at sealed-output canonical register; §6 emits structural-template requirements per closeout deliverable section (not admissibility logic, not categorical labels, not measurement) consumed by Phase 5 SEAL author register at closeout authoring time.

§6 register-class is descriptive (structural-template form, parallel scoping §3 narrative-requirement form), register-class-distinct from §2/§3/§4/§5 procedure register-class. §-symmetric template partial-break at §6 register: Indicator construction + Admissibility outcome inventory + Categorical label set + Partition table NOT APPLICABLE (§6 has no measurement, no admissibility, no labels, no enumeration table at §6 register); Judgment rule + Criteria table SUBSTANTIVELY-ADAPTED as structural-template requirement statements; NaN-handling INHERITS upstream §3.x / §4 / §5 propagation flowing through §6 narrative; V# anchor INHERITS §7 verification chain; remaining elements (anti-circularity caveat / anti-pre-naming / input source / register-class / Phase 4 verdict invariance / framing-chain / substantive prior justification / descriptive supplements) APPLICABLE per §-symmetric inheritance.

**3-group split rationale (per Charlie register Option C-3-group lock at session-12 entry).** §6 pre-registers 6 closeout structural elements per scoping §3 deliverable + meta-plan §6 (per-mode finding section / multi-mode disposition section / diagnostic ambiguity disposition section / successor-class mapping disposition / framing-resolution-pressure assessment disposition / terminal-conclusion fire-or-not section). The 6 elements partition into 3 register-class-affinity clusters per upstream § consumption:

- **§6.a (§3.x-class consumer cluster):** per-mode finding + multi-mode disposition + ambiguity disposition — consume §2.x outputs at §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` at sealed-output canonical register.
- **§6.b (§4-class consumer cluster):** successor-class mapping disposition + framing-resolution-pressure disposition — consume §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects at sealed-output canonical register.
- **§6.c (§5-class consumer):** terminal-conclusion fire-or-not section — consumes §5 substantive-vs-operational admissibility flags at sealed-output canonical register; F1 distinct isolation per advisor Finding 4 substantive sequencing dependency + session-11 D3.1 (viii) F1 distinctness lock + session-11 D3.1 (x) admissibility ≠ selection lock.

3-group structure mirrors sealed §3 / §4 / §5 upstream register-boundary at §6 consumer register; inheritance discipline (non-§6-invented grouping; sealed structure reflected at §6 register). Cluster boundaries are substantively register-class-distinct (procedure-register-class upstream §3.x at §6.a / §4 procedure-register-class upstream at §6.b / §5 admissibility-register-class upstream at §6.c register-class-distinct from procedure registers).

**Anti-circularity caveat (8-axis per D3''''''^f Sub-decision 1; right-sized for descriptive register-class with consumer register-class affinity grouping at SEAL author register).**

- **(i) §6 → §2 threshold guard (ChatGPT P2 inheritance):** §6 may consume §2.x outputs (via §3.x / §4 / §5 composition) but does not retroactively redefine §2.x detection thresholds. §2.x sealed thresholds remain authoritative.
- **(ii) §6 → §2 register-class identity guard:** §6 does not retroactively re-frame §2.x register-class identities (§2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-classes per sealed identity).
- **(iii) §6 → §3.x non-modification (binds at §6.a):** §6 consumes §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` outputs as inputs without retroactive modification of §3.x rule-tree, partition table, Case A condition, terminal-trigger basis semantics, or indeterminate-Case-A NaN-propagation semantics per ChatGPT P2 + P3 anti-circularity guards at §3.x register.
- **(iv) §6 → §4 non-modification (binds at §6.b; §4a + §4b consolidated at sealed §4 umbrella):** §6 consumes §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects as inputs without retroactive modification of §4a mapping table / composite rule / qualifier carry-through semantics / precedence rule or §4b pressure label inventory / composite handling rule / propagation state semantics / schema fields per §4a + §4b sealed-content invariance.
- **(v) §6 → §5 non-modification (binds at §6.c):** §6 consumes §5 substantive-vs-operational admissibility flags as inputs without retroactive modification of §5 admissibility criteria, criteria-source / availability-type inventory, NaN-propagation semantics, fixed-shape output structure, or D2.5 α HARD LOCK no-new-thresholds discipline per §5 sealed-content invariance.
- **(vi) §6 → §7 verification non-pre-emption:** §6 structural-template requirements feed §7 V# anchor verification chain at sub-spec SEAL pre-fire; §6 does NOT pre-name §7 V# anchor specifics (V# anchor authority preserved at §7 register).
- **(vii) §6 → Phase 5 SEAL author register non-pre-emption (consolidates SEAL author register downstream concerns):** §6 emits structural-template requirements for closeout deliverable sections; §6 does NOT pre-name Phase 5 SEAL author register adjudications at closeout authoring time — specifically (a) terminal-conclusion invocation decision per §5 (x) admissibility ≠ selection inheritance, (b) framing-question (1)/(2)/(3)/(4) resolution decision per scoping §2.4 + §4b (vii) procedure ≠ resolution inheritance, (c) admissibility-flag interpretive narration at substantive narrative determination register. **SEAL author register authority preserved across both registers at SEAL author register (NOT §6 register) — (vii)(a)+(b) downstream decision authority + (vii)(c) downstream interpretive authority — without §6-imposed pre-emption at either authority register. §6 structural-template requirements are pre-registered structural anchors, not prescriptive narrative content.**
- **(viii) §6 → successor cycle entry non-pre-emption (inherited from §5 (ix) at §6 register):** §6 structural-template requirements do NOT pre-name Phase 5.1+ successor cycle scope, successor-class invocation, or specific successor pathway per scoping §4.2 anti-pre-naming inheritance. Successor cycle entry register-event boundary requires explicit Charlie register authorization per `feedback_authorization_routing.md` hard rule.

**Anti-pre-naming.** §6 does NOT pre-name §7 V# anchor specifics per (vi). §6 does NOT pre-name Phase 5 SEAL author terminal-conclusion invocation decision per (vii)(a). §6 does NOT pre-name framing-question (1)/(2)/(3)/(4) resolution per (vii)(b). §6 does NOT pre-name §5 admissibility-flag interpretive narration per (vii)(c). §6 does NOT pre-name Phase 5.1+ successor cycle scope per (viii). §6 does NOT pre-name session-N-specific operational details or closeout authoring-time interpretive choices. §6 register-class binds to current sealed scoping §3 + meta-plan §6 + sub-spec sealed §2/§3/§4/§5 scope; closeout deliverable structural requirements outside this scope are outside §6's register-class.

**Substantive prior justification (descriptive register-class scope + 3-group affinity rationale + SEAL author register authority preservation).** §6 register-class is descriptive (structural-template framework) not procedure (rules / criteria / labels). Closeout deliverable structure is narrative-requirement form per scoping §3 ("Report contents: per-mode indicator results, multi-mode interpretation, ..."); §6 implementation preserves narrative-requirement form via prose structural-template statements at §6.a / §6.b / §6.c sub-anchors. β schema-object encoding REJECTED at register-precision per D1.2 sealed-grounding precondition (scoping §3 deliverable contents are narrative-requirement form, not schema-object form; §6 schema authority over closeout structure = §6 self-creation of structural authority not pre-specified at scoping §3; same-pattern as session-11 D2.1 β extended REJECT + Path β compatibility taxonomy REJECT at §5 register).

3-group affinity grouping (§6.a §3.x-consumer / §6.b §4-consumer / §6.c §5-consumer) mirrors sealed §3 / §4 / §5 register-class boundary at §6 consumer register; cluster boundaries respect upstream substantive register-class distinction (procedure-register-class vs admissibility-register-class). §6.c terminal isolation grounded in session-11 D3.1 (viii) F1 distinctness lock + (x) admissibility ≠ selection lock; F1 distinct register-class preservation at §6 consumer register propagates upstream register-class precision.

SEAL author register authority preservation (8-axis (vii) primary register; cross-section context vs substantive narrative determination distinction): §6 pre-registers structural anchors for closeout sections; Phase 5 SEAL author register adjudicates substantive narrative interpretation of §3.x / §4 / §5 outputs at closeout authoring time. §6 → SEAL bundle interface is prose narrative-requirement form (per D3.3 α prose lock) — SEAL author consumes narrative requirements and authors closeout prose at full-context register without §6-imposed schema rigidity.

**Descriptive supplements (structural-template content; per D2.2 + D2.3 + D3.3).** §6.a / §6.b / §6.c sub-anchors articulate per-cluster structural templates as prose narrative-requirement statements (e.g., "per-mode finding section consists of one narrative block per §2.x mode, each block containing: indicator value + binary detection status + cross-§ context anchor + framing-question context anchor"). Phase 5 SEAL author register implements structural templates at closeout authoring time per locked structural-template form.

**NaN-handling rule (INHERITS upstream §3.x / §4 / §5 (ε') hybrid + (ε'-narrow) propagation pattern at §6 narrative register).** §6 does NOT introduce new NaN-handling semantics. Upstream indeterminate variants (§3.1 indeterminate-X / §3.2 indeterminate-Case-A / §4a indeterminate / §4b indeterminate / §5 propagation_state = upstream-indeterminacy-propagated) flow through §6 narrative as upstream indeterminate-anchor references at relevant §6.a / §6.b / §6.c structural-template sub-block. Upstream execution-suspended state (§3.x / §4 / §5 (ε') execution-invalidity branch) routes to §6 narrative authoring-suspended at Phase 5 SEAL author register per upstream propagation chain; §6 narrative does NOT re-articulate or extend propagation semantics; specific sealed-prose grounding cite at §6.a D2.3 ambiguity sub-block (sealed §5 D3.3 + sealed §3.2 (ε'-narrow) semantics).

**Input source per §1.** §3.1 partition table rows + §3.2 Case A `{label, terminal_trigger}` + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects + §5 substantive-vs-operational admissibility flags at sealed-output canonical register + §3.x / §4 / §5 edge-handling outputs (indeterminate variants + execution-suspended state).

No raw data consumption at §6 register. V# anchor pre-execution check at §7 register (per (vi) inheritance) verifies upstream output completeness before §6 narrative authoring at Phase 5 SEAL author register.

**Register-class:** binding | structural-template requirements (no new quantitative thresholds, no admissibility logic, no measurement; consumes §3.x / §4 / §5 sealed outputs as upstream narrative inputs) | descriptive register-class-distinct from §2/§3/§4/§5 procedure register-class | downstream-consumer of §3.x / §4 / §5 sealed outputs at deliverable-structure register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §6.a / §6.b / §6.c SEAL register-event boundaries.

**Phase 4 verdict invariance (sealed-invariance per §2.a/b/c/d/e/f + §3.1 + §3.2 + §4a + §4b + §5 precedent).** §6 output (structural-template requirements for Phase 5 closeout deliverable sections) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. §6 structural-template form for closeout sections does not retroactively rewrite Phase 4 verdict; only pre-registers narrative structural anchors at deliverable-structure register-scope per framing-chain below.

**Framing-chain (3-direction per D3''''''^f Sub-decision 2).**

- **§6 → §7:** §6 structural-template requirements feed §7 V# anchor verification chain at sub-spec SEAL pre-fire; §7 V# anchor consumes §6 structural-template form to verify closeout deliverable structure coherence at sub-spec SEAL pre-fire register. Anti-pre-emption preserved per anti-circularity (vi).
- **§6.a → §6.b → §6.c (cross-§6 sub-anchor handoff):** §6.a sealed at session-12 SEAL register-event boundary; §6.b consumes §6.a sealed structural-template form at session-13 SEAL register (or beyond) without modification; §6.c consumes §6.a + §6.b sealed structural-template forms at session-14 SEAL register (or beyond) without modification per sealed §6.a / §6.b invariance at downstream session register per anti-momentum-binding strict reading. Inverse directions blocked: §6.b/§6.c ← §6.a per sealed §6.a invariance at downstream session register.
- **§6 → Phase 5 SEAL bundle:** §6 structural-template form (prose narrative-requirement statements per D3.3 α prose lock) flows into Phase 5 SEAL bundle at closeout authoring register; Phase 5 SEAL author register consumes §6 narrative requirements + sealed §2.x / §3.x / §4 / §5 outputs and authors Phase 5 closeout deliverable at full-context register per SEAL author register authority per (vii) inheritance.

Inverse directions blocked: §6 ← §5/§4a/§4b/§3.x per ChatGPT P3 sequencing lock binding (§6 downstream consumer; cannot alter §5/§4/§3 register once frozen); §6 ← Phase 5 SEAL author register per closeout authoring-time downstream register-event boundary discipline.

**V# anchor (INHERITS §7).** §6 structural-template form is upstream verification dependency at §7 register; §6 does NOT articulate V# anchor specifics at §6 register per anti-circularity (vi). V# anchor pre-execution check at §7 register verifies §6 structural-template completeness before Phase 5 closeout authoring at Phase 5 SEAL author register.

### §6.a — §3.x-class consumer cluster structural templates (per-mode finding + multi-mode disposition + diagnostic ambiguity disposition)

Operationalizes meta-plan §6 first 3 pre-registered closeout elements consuming §3.x outputs: per-mode finding section structure (consuming §2.x individual indicator outputs at §3.1 partition table base-label canonical register) + multi-mode disposition section structure (consuming §3.1 multi-mode composite outputs at sealed-output canonical register) + diagnostic ambiguity disposition section structure (consuming §3.2 Case A `{label, terminal_trigger}` + indeterminate-Case-A NaN-propagation outputs at sealed-output canonical register). §6.a register-class is descriptive structural-template sub-cluster of §6 deliverable structure register; §6.a is downstream-consumer of §3.1 + §3.2 sealed outputs at sub-cluster register-class.

**Indicator construction — NOT APPLICABLE** (no measurement at §6.a register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as structural-template requirement statements per α prose encoding (D1.2 lock; β schema REJECTED at sealed-grounding precondition).** §6.a articulates 3 sub-block structural templates as prose narrative-requirement statements at Phase 5 SEAL author register consumption interface:

**Per-mode finding section structural template (D2.1 α single section + 6 mode-iter lock).** Per-mode finding section is a single closeout section consisting of one narrative block per §2.x mode (§2.a signal-decay / §2.b cost-drag / §2.c cohort-substrate-weakness / §2.d AND-gate-overfit / §2.e success-metric-mismatch / §2.f regime-change-detected), each block containing the following narrative requirements:

- Indicator value at sealed §2.x output canonical register (numeric value or qualitative state per §2.x sealed prose).
- Binary detection status (detected / not-detected / (ε'-narrow)-class-real-data-NaN / §2.x-self-suspended) at sealed §2.x output canonical register.
- Cross-§3.1 context anchor: §3.1 partition table base label for the mode at sealed-output canonical register (e.g., `signal-decay reading` for §2.a) + Line 1 / Line 2 / Tier modifier inheritance per §3.1 sealed compositional label assembly when relevant upstream-conditioning §2.x modes fire.
- Cross-§3.x sealed prose anchor reference: §2.x sealed prose register-class identity (§2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level §2.a/§2.b/§2.e/§2.c register-classes) per §3.1 anti-circularity (ii) inheritance.
- Framing-question context anchor: which scoping §2.4 framing-question(s) (1)/(2)/(3)/(4) the §2.x mode bears on per §4b pressure-label-set per-finding object at sealed-output canonical register (limited §4b consumption boundary at §6.a register for framing-question context anchor reference only; full §4b consumption at §6.b register; cross-section context anchor, not framing-resolution determination at §6.a register). Framing-question context anchors are contextual-reference requirements only and do not authorize §6.a to rank, weight, reinterpret, or reconcile upstream outputs at §6.a register; cross-section context vs substantive narrative determination distinction per anti-circularity (vii)(c).

Per-mode narrative narrates §2.x mode detection state + categorical label narration at §3.1 partition register + cross-section context anchors; Phase 5 SEAL author register adjudicates substantive narrative interpretation of what each per-mode detection state means at closeout deliverable register per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction.

**Multi-mode disposition section structural template (D2.2 concur lock; consume §3.1 multi-mode composite outputs).** Multi-mode disposition section narrates §3.1 multi-mode composite categorical labels at sealed-output canonical register when 2+ §2.x modes fire simultaneously, containing the following narrative requirements:

- §3.1 multi-mode composite categorical label at sealed-output canonical register (e.g., `forward-applicability-conditioned cohort-substrate-weakness reading` per §3.1 Line 1 + Tier compositional assembly when §2.f + §2.c fire simultaneously).
- §3.1 partition table row reference at sealed-output canonical register (active modes set + partition row identifier + downstream-consumer note column).
- §3.1 compositional label assembly disclosure: Line 1 modifier + Line 2 modifier + Tier bilateral overlay + Residual single-mode default — composition order + all modifier values + bilateral attribution caveat locked at sealed §3.1 register per session-7 SEAL; §6 narrative anchors composition order via sealed-§3.1 cross-reference without re-articulating composition logic or reproducing modifier values per anti-circularity (iii).
- Cross-§3.1 anti-circularity (ii) preservation reference: §3.1 internal labeling reflects §2.f 2-way register-class-distinct downstream relationships (content-attribution bilateral with §2.a/§2.b/§2.e at content-modification scope vs forward-applicability univocal upstream-conditioning of §2.c/§2.d at forward-applicability scope) at all register-classes per §3.1 sealed prose; §6 narrative inherits 2-way distinction without re-articulating.

Multi-mode narrative narrates §3.1 multi-mode composite categorical label + compositional disclosure + cross-§3.1 register-class anchors; Phase 5 SEAL author register adjudicates substantive narrative interpretation of multi-mode composite at closeout deliverable register per anti-circularity (vii)(c).

**Diagnostic ambiguity disposition section structural template (D2.3 refined with sealed-prose grounding cite).** Diagnostic ambiguity disposition section narrates §3.2 Case A canonical state + indeterminate-Case-A NaN-propagation + cross-§ propagation outputs at sealed-output canonical register, containing the following narrative requirements:

- §3.2 Case A canonical state narrative: `no-mode-detected reading` label at procedural-state register when Case A condition satisfied (all 6 §2.x mode outputs canonical binary `not-detected`), with terminal-trigger basis structured object reference (`{triggered: True, basis: {canonical_0_mode: True, suspended_modes: [], nan_modes: []}}`) at sealed §3.2 output canonical register. Cross-§5 substantive admissibility precondition anchor reference (§5 D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state; cross-reference to §6.c consumption boundary).
- Indeterminate-Case-A NaN-propagation narrative: `indeterminate-Case-A reading` label at procedural-state register when one or more §2.x outputs are (a)-class real-data NaN at sealed §3.2 (ε'-narrow) input class scope; terminal-trigger basis structured object (`{triggered: False, basis: {canonical_0_mode: False, suspended_modes: [], nan_modes: [<§2.x identifier list>]}}`) at sealed §3.2 output canonical register; cross-§ NaN-propagation discipline narrative referencing **sealed §5 D3.3 NaN-handling rule inheritance + sealed §3.2 (ε'-narrow) semantics; §6 narrative does NOT re-articulate or extend propagation semantics** per anti-circularity (iii) §6 → §3.x non-modification + sealed §5 NaN-handling rule inheritance.
- Execution-suspended state propagation narrative: upstream execution-suspended state (§2.x self-suspended per parquet sha256 mismatch / cross-validation gate failure / data-source corruption per sealed §2.x precedent) routes to §3.2 execution-suspended state per (ε') execution-invalidity branch inheritance; §6 narrative narrates suspension state propagation without re-articulating suspension semantics at §6 register. §6 enumerates the upstream discrepancy at narrative register; Charlie register re-adjudicates per §2.d D2''' / §2.f parquet-mismatch suspension precedent at sealed §2.x precedent register.

Ambiguity narrative narrates §3.2 Case A / indeterminate-Case-A / suspension states + sealed §5 NaN-handling rule inheritance + sealed §3.2 (ε'-narrow) semantics anchors without re-articulating semantics at §6 register; Phase 5 SEAL author register adjudicates substantive narrative interpretation of ambiguity disposition at closeout deliverable register per anti-circularity (vii)(c).

**Substantive prior justification (per D2.4 implicit cross-block inheritance + §3.x-class consumer affinity grouping rationale).** §6.a 3 sub-block structural templates inherit cross-block narrative coherence from sealed §3.1 + §3.2 sealed prose at upstream register; cross-block consistency at §6.a register is implicit inheritance per D2.4 lock — §6.a does NOT introduce explicit cross-block consistency narrative discipline (§6.a self-creating coherence authority REJECTED at sealed-grounding precondition; same-pattern as session-11 D2.4 absorbed-into-NaN-handling-rule discipline). Cross-block narrative coherence at closeout authoring time is Phase 5 SEAL author register responsibility per (vii)(c) cross-section context vs substantive narrative determination distinction — parallel to §5 (x) admissibility ≠ selection discipline (procedure pre-registers structure; SEAL author exercises coherence judgment at full-context register).

§3.x-class consumer affinity grouping rationale at §6.a register: per-mode finding + multi-mode disposition + ambiguity disposition all consume §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` outputs at sealed-output canonical register; substantive register-class affinity at §3.x upstream consumer register; cluster boundary at §6.a register reflects sealed §3 umbrella + §3.1 + §3.2 register-class boundary at consumer register-class.

**Descriptive supplements — APPLICABLE (structural-template content articulated above in 3 sub-block structural templates).**

**NaN-handling rule (INHERITS sealed §3.1 + §3.2 (ε') hybrid + (ε'-narrow) propagation pattern at §6.a narrative register per §6 umbrella NaN-handling rule inheritance).** §6.a does NOT introduce new NaN-handling semantics at §6.a register; upstream indeterminate variants flow through §6.a narrative as upstream indeterminate-anchor references at relevant sub-block (per-mode finding section: §2.x mode-level indeterminate at (a)-class real-data NaN; multi-mode disposition section: §3.1 indeterminate-X compositional propagation per sealed §3.1 (ε') hybrid; ambiguity disposition section: §3.2 indeterminate-Case-A NaN-propagation per sealed §3.2 (ε'-narrow) semantics). Upstream execution-suspended state routes to §6.a narrative authoring-suspended at Phase 5 SEAL author register per upstream propagation chain inheritance.

**Anti-circularity caveat (§6.a-specific axis binding; primary binding at (iii) §6 → §3.x non-modification; other axes inherited from §6 umbrella 8-axis structure).**

- **(iii) §6.a → §3.x non-modification primary binding:** §6.a consumes §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` outputs as inputs without retroactive modification of §3.1 rule-tree / Line 1 / Line 2 / Tier compositional label assembly / partition table / 2-way register-class-distinct downstream relationships at §2.f universe-upstream scope OR §3.2 Case A condition / terminal-trigger basis semantics / indeterminate-Case-A NaN-propagation semantics per sealed §3.1 D3''''''^a + sealed §3.2 D3''''''^b lock structure. §6.a structural-template form provides cross-section context anchors to §6 → SEAL author register; §6.a does NOT pre-name §3.1 / §3.2 sealed substantive content at narrative register.
- **(i)(ii)(iv)(v)(vi)(vii)(viii) §6 umbrella 8-axis structure inherited at §6.a register without re-articulation;** §6.a binds (iii) primary at sealed §3.x consumer cluster register; other axes bind across §6 deliverable structure register at §6 umbrella articulation level.

**Anti-pre-naming (parallel §6 umbrella structure at §6.a register).** §6.a does NOT pre-name §3.1 / §3.2 sealed substantive content per (iii). §6.a does NOT pre-name §4a successor-cycle-class assignment narrative content (§6.b register-class scope). §6.a does NOT pre-name §5 admissibility-flag narrative content (§6.c register-class scope). §6.a does NOT pre-name Phase 5 SEAL author register substantive narrative determination at closeout authoring time per (vii)(c). §6.a does NOT pre-name session-N-specific operational details or closeout authoring-time interpretive choices. §6.a register-class binds to sealed §3.1 + §3.2 + §2.x consumer scope; closeout structural-template requirements outside this scope are outside §6.a's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §6 umbrella + §3.x precedent): §3.1 partition table rows + §3.2 Case A `{label, terminal_trigger}` at sealed-output canonical register + §3.1 / §3.2 edge-handling outputs (indeterminate variants per §3.1 / §3.2 (ε'-narrow) + execution-suspended state per §3.1 / §3.2 (ε') execution-invalidity branch) + §2.x sealed individual indicator outputs at canonical register (consumed at per-mode finding section sub-block) + §4b pressure-label-set per-finding objects at sealed-output canonical register (consumed at per-mode finding section sub-block framing-question context anchor reference only; full §4b consumption at §6.b register).

No raw data consumption at §6.a register. V# anchor pre-execution check at §7 register inheritance verifies §3.1 / §3.2 / §2.x output completeness before §6.a structural-template form is consumed by Phase 5 SEAL author register.

**Register-class:** binding | structural-template requirements (no new quantitative thresholds, no admissibility logic, no measurement at §6.a register; consumes §3.1 + §3.2 + §2.x sealed outputs as upstream narrative inputs) | descriptive sub-cluster register-class within §6 deliverable structure register | downstream-consumer of §3.1 + §3.2 + §2.x sealed outputs at §3.x-class consumer cluster register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §6.a SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §6 umbrella + §3.1 + §3.2 + §4a + §4b + §5 precedent).** §6.a output (3 sub-block structural-template requirements at sealed §3.x consumer cluster register) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers narrative structural anchors at §6.a deliverable-structure sub-cluster register-scope per §6 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^f Sub-decision 2 at §6.a register; inherited from §6 umbrella framing-chain with §6.a-specific binding articulation).**

- **§6.a → §6.b:** §6.a 3 sub-block structural-template form sealed at session-12 SEAL register-event boundary; §6.b consumes §6.a sealed structural-template form at session-13 SEAL register (or beyond) without modification per sealed §6.a invariance at downstream session register per anti-momentum-binding strict reading. §6.b cross-references §6.a per-mode finding section framing-question context anchor at §4b consumption boundary (full §4b pressure-label-set consumption at §6.b register-class).
- **§6.a → §7:** §6.a structural-template form feeds §7 V# anchor verification chain inheritance at sub-spec SEAL pre-fire register per §6 umbrella → §7 framing-chain.
- **§6.a → Phase 5 SEAL bundle:** §6.a 3 sub-block structural-template form (prose narrative-requirement statements per D1.2 α prose lock) flows into Phase 5 SEAL bundle at closeout authoring register; Phase 5 SEAL author register consumes §6.a narrative requirements + sealed §3.1 + §3.2 + §2.x outputs and authors per-mode finding + multi-mode disposition + diagnostic ambiguity disposition sections at full-context register per (vii) SEAL author register authority preservation.

Inverse directions blocked: §6.a ← §3.1/§3.2/§2.x per ChatGPT P3 sequencing lock binding (§6.a downstream consumer; cannot alter §3.x register once frozen); §6.b/§6.c ← §6.a per sealed §6.a invariance at downstream session register; §6.a ← Phase 5 SEAL author register per closeout authoring-time downstream register-event boundary discipline.

**V# anchor (INHERITS §7 per §6 umbrella V# anchor inheritance).** §6.a structural-template form is upstream verification dependency at §7 register; §6.a does NOT articulate V# anchor specifics at §6.a register per anti-circularity (vi) inheritance.

### §6.b — §4-class consumer cluster structural templates (successor-class mapping disposition + framing-resolution-pressure disposition)

Operationalizes meta-plan §6 successor-class mapping disposition + framing-resolution-pressure assessment disposition closeout elements consuming §4a + §4b outputs: successor-class mapping disposition section structure (consuming §4a successor-cycle-class assignments at sealed-output canonical register) + framing-resolution-pressure disposition section structure (consuming §4b pressure-label-set per-finding objects at sealed-output canonical register). §6.b register-class is descriptive structural-template sub-cluster of §6 deliverable structure register; §6.b is downstream-consumer of §4a + §4b sealed outputs at §4-class consumer cluster register.

§-symmetric template applicability declaration at §6.b register inherits from §6 umbrella sealed declaration + §6.a precedent per D1.3 lock: Indicator construction + Admissibility outcome inventory + Categorical label set + Partition table NOT APPLICABLE at §6.b register; Judgment rule + Criteria table SUBSTANTIVELY-ADAPTED as structural-template requirement statements; NaN-handling + V# anchor INHERITS; remaining elements (anti-circularity caveat / anti-pre-naming / input source / register-class / Phase 4 verdict invariance / framing-chain / substantive prior justification / descriptive supplements) APPLICABLE per §-symmetric inheritance.

**Indicator construction — NOT APPLICABLE** (no measurement at §6.b register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as structural-template requirement statements per α prose encoding (D1.2 lock inherited from §6 umbrella; β schema REJECTED at sealed-grounding precondition).** §6.b articulates 2 sub-block structural templates as prose narrative-requirement statements at Phase 5 SEAL author register consumption interface:

**Successor-class mapping disposition section structural template (D2.1 α single section + 6 output-type-iter lock).** Successor-class mapping disposition section is a single closeout section consisting of one narrative block per §4a output type at sealed-output canonical register (single-class assignment / multi-class assignment via β union / terminal Case A routing / indeterminate-cycle-class / indeterminate-terminal-routing / execution-suspended state), each block containing the following narrative requirements:

- **Single-class assignment block** (single-mode §3.1 input → single §4a hybrid object): disclose §4a hybrid object content `{class, disposition, qualifier (optional), verbatim_label}` field values at sealed §4a output canonical register; verbatim scoping §2.3 source language for the assigned successor-cycle-class at canonical register (e.g., "empirical-continuation eligible (extended forward window, additional regime testing)" for §2.a-derived class); cross-§4a sealed prose anchor reference to §4a single-mode mapping subsection at sealed register-class identity; cross-§3.1 sealed context anchor identifying which §2.x mode triggered the single-class assignment per sealed §3.1 partition table row.

- **Multi-class assignment via β union block** (multi-mode §3.1 input → list of §4a hybrid objects OR elevation-collapsed single hybrid object):

  *Basic multi-class structure*: disclose §4a output list contents (per-entry `{class, disposition, qualifier, verbatim_label}` field values) at sealed §4a output canonical register; β union semantic narrative per sealed §4a D2.2 lock (no §4a-internal precedence among multi-class entries per §4a anti-circularity (vii); SEAL author register adjudicates attribution dominance at full-context register if substantive); qualifier carry-through narrative per sealed §4a D2.5 (`regime-conditional` from §2.f Line 1 content-attribution bilateral on §2.a/§2.b/§2.e-derived classes; `forward-applicability-conditioned` from §2.f Line 1 forward-applicability univocal on §2.c/§2.d-derived classes; `gate-validity-conditioned` semantic absorbed into §2.c-elevation per sealed §4a D2.2 precedence rule; bilateral attribution caveat dropped at §4a register per sealed §3.1 anti-circularity (iii)).

  *§2.d → §2.c elevation sub-case narrative requirement*: when §2.d + §2.c co-fire (Row 4 of sealed §4a mapping table single hybrid object output OR Row 5 multi-mode {§2.f, §2.d, §2.c} list output with embedded elevation-collapsed single entry), §6.b narrative discloses elevation operation as **gate-validity inheritance from sealed §2.d D3''' framing-chain** (verbatim sealed §2.d D3''' phrasing: "interpretation/prose layer carries stronger semantics — the cohort-substrate inference is partially or fully invalidated by detected AND-gate overfit") **per sealed §4a anti-circularity (vii) single-explicit-exception exhaustive-enumeration scope; elevation IS NOT §4a-internal ranking authority** (sealed §4a (vii): "the elevation encodes validity-gating, NOT a priority ranking among otherwise-valid successor classes"); elevation ↔ qualifier interaction per sealed §4a (§2.c-specific qualifiers DROPPED at elevation; surviving qualifiers come from §2.d-direct's own conditioning).

  *Cross-references*: cross-§4a sealed prose anchor reference to §4a multi-mode composite mapping + Elevation ↔ qualifier interaction subsections at sealed register-class identity; cross-§3.1 sealed context anchor identifying §3.1 multi-mode partition row + compositional label assembly per sealed §6.a multi-mode disposition section structural template inheritance.

- **Terminal Case A routing block** (§3.2 Case A → §4a structured terminal object): disclose §4a terminal object content `{class: terminal-conclusion-routing, options: [{meta-paradigm-pivot eligible}, {project-pause-declared-research-line-exhausted-close eligible}], basis: <§3.2 trigger basis>, anti_pre_naming: True}` field values at sealed §4a output canonical register; options-set narrative per sealed §4a D2.3 γ structured terminal object lock (§4a does NOT pre-name §5 terminal-conclusion outcome between operational availability vs substantive availability per sealed §4a anti-circularity (iv)); cross-§6.c reference: §6.b register provides terminal-routing options-set cross-section context anchor only at §6.b register; full §5 admissibility-flag consumption at §6.c register-class scope per Option C-3-group lock + sealed §6 umbrella (v) §6 → §5 non-modification binding at §6.c register-class affinity; cross-§4a sealed prose anchor reference to §4a Case A mapping subsection at sealed register-class identity; cross-§3.2 sealed context anchor identifying §3.2 Case A canonical state; cross-§5 substantive admissibility precondition reference (sealed §5 D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state) per family-pattern with sealed §6.a ambiguity disposition section structural template; SEAL author register authority preservation per sealed §6 umbrella anti-circularity (vii)(a) terminal-conclusion invocation decision authority preservation.

- **Indeterminate-cycle-class block** (§3.1 indeterminate → §4a `{class: indeterminate-cycle-class, source: §2.X, qualifier: <X-mode-specific>}` object): disclose §4a indeterminate-cycle-class object content field values at sealed §4a output canonical register per sealed §4a D2.4 (α) lock; indeterminate-cycle-class semantic narrative (§4a defers successor-class assignment for the affected mode until upstream §3.x resolves per sealed §3.1 (b-symm-improved) absorbing-propagation inheritance + sealed §4a NaN-handling rule); cross-§4a sealed prose anchor reference to §4a indeterminate state mapping subsection at sealed register-class identity; cross-§3.1 sealed context anchor identifying which §3.1 indeterminate label triggered the indeterminate-cycle-class assignment per sealed §3.1 (ε'-narrow) input class scope.

- **Indeterminate-terminal-routing block** (§3.2 indeterminate-Case-A → §4a `{class: indeterminate-terminal-routing, source: <list of NaN §2.x>}` object): disclose §4a indeterminate-terminal-routing object content field values at sealed §4a output canonical register per sealed §4a D2.4 indeterminate-Case-A propagation rule; indeterminate-terminal-routing semantic narrative (§4a defers terminal-conclusion routing until §3.x resolves at NaN §2.x modes per sealed §3.2 (ε'-narrow) semantics inheritance); cross-§4a sealed prose anchor reference to §4a indeterminate state mapping subsection at sealed register-class identity; cross-§3.2 sealed context anchor identifying §3.2 indeterminate-Case-A label at sealed-output canonical register.

- **Execution-suspended state block** (upstream §3.x suspension → §4a execution suspends): disclose §4a output suspension per sealed §4a (ε') execution-invalidity branch inheritance (§4a execution suspends; §6 enumerates the discrepancy; Charlie register re-adjudicates per sealed §2.d D2''' / §2.f parquet-mismatch suspension precedent); suspension semantic narrative (upstream §3.x execution-invalidity propagates through §4a register at narrative authoring suspension boundary; §6.b narrative routes to Phase 5 SEAL author register authoring-suspended state per upstream propagation chain inheritance); cross-§4a sealed prose anchor reference to §4a execution-suspended state at sealed-pattern canonical register; cross-§3.x sealed context anchor identifying upstream §3.x execution-suspended state cause.

Successor-class mapping disposition narrative narrates §4a output-type-specific content disclosure + cross-section context anchors per output type; Phase 5 SEAL author register adjudicates substantive narrative interpretation of what each §4a output type means at closeout deliverable register per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction.

**Framing-resolution-pressure disposition section structural template (D2.2 α single section + 5 output-type-iter lock).** Framing-resolution-pressure disposition section is a single closeout section consisting of one narrative block per §4b output type at sealed-output canonical register (single-finding pressure object / multi-finding composite via β union / terminal-conclusion finding `load-bearing-immediately` / upstream-indeterminacy-propagated / execution-suspended-from-upstream), each block containing the following narrative requirements:

- **Single-finding pressure object block** (§3.x single-finding input → single §4b pressure object): disclose §4b 6-field schema `{pressure_label, pivot_direction_dependency, basis, propagation_state: active, context_qualifiers (optional), finding_source}` field values at sealed §4b output canonical register per sealed §4b D1.2 β''-refined lock; per-finding pressure narrative identifying which active pressure label (`deferred` / `load-bearing-at-Phase-5-SEAL` / `ambiguous` / `load-bearing` / `load-bearing-immediately`) for the §3.x finding per sealed §4b single-finding mapping table; pivot direction dependency narrative per sealed §4b verbatim scoping §2.4 source language; cross-§4b sealed prose anchor reference to §4b single-finding mapping subsection at sealed register-class identity; cross-§3.x sealed context anchor identifying which §3.x finding triggered the pressure object per sealed §3.1 partition table row OR §3.2 finding label.

- **Multi-finding composite via β union block** (§3.x multi-finding input → list of §4b pressure objects): disclose §4b output list contents (per-finding pressure object 6-field schema at sealed-output canonical register); β union semantic narrative per sealed §4b D2.2 lock (no §4b-internal precedence among multi-finding pressure objects per sealed §4b anti-circularity (viii); SEAL author register adjudicates inter-finding pressure dominance at full-context register; "incoherent" co-occurrences preserved at union register for §6 narrative scope surfacing per sealed §4b anti-circularity (viii) anti-precedence-rule inheritance + sealed §4b β union semantic at sealed-prose register); finding_source provenance narrative disclosing per-pressure-object finding_source field for §6 narrative author register disambiguation when multiple findings emit identical pressure_label (per sealed §4b D1.2 β''-refined lock); context_qualifiers narrative per sealed §4b D2.5 (qualifier-as-context: `regime-conditional` / `forward-applicability-conditioned` / `gate-validity-conditioned` preserved as `context_qualifiers` field at per-pressure-object scope without altering pressure_label per sealed §4b qualifier-stripping rule); cross-§4b sealed prose anchor reference to §4b multi-finding composite mapping subsection at sealed register-class identity; cross-§3.1 sealed context anchor identifying §3.1 multi-mode partition row + compositional label assembly per sealed §6.a multi-mode disposition section structural template inheritance.

- **Terminal-conclusion finding block** (§3.2 Case A → §4b scalar pressure object `load-bearing-immediately`): disclose §4b scalar pressure object `{pressure_label: load-bearing-immediately, pivot_direction_dependency: project-identity-engagement-implicit, basis: "Terminal-conclusion finding (per §2.5) → framing question becomes load-bearing immediately; engages directly with project-identity question", propagation_state: active, finding_source: terminal-conclusion}` per sealed §4b D2.3 α scalar lock; terminal-conclusion pressure narrative (§4b does NOT pre-name which framing-question (1)/(2)/(3)/(4) resolves under terminal-conclusion engagement per sealed §4b anti-circularity (vii) procedure ≠ resolution discipline); cross-§6.c reference: §6.b register provides terminal-conclusion pressure cross-section context anchor at §6.b register; full §5 admissibility-flag consumption at §6.c register-class scope per Option C-3-group lock; cross-§4b sealed prose anchor reference to §4b Case A mapping subsection at sealed register-class identity; cross-§3.2 sealed context anchor identifying §3.2 Case A canonical state; cross-§5 substantive admissibility precondition reference (sealed §5 D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state).

- **Upstream-indeterminacy-propagated block** (§3.x or §4a indeterminate → §4b `propagation_state: upstream-indeterminacy-propagated`): disclose §4b output content `{pressure_label: null, propagation_state: upstream-indeterminacy-propagated, basis: <upstream indeterminate label>, finding_source: <§2.X or null>}` per sealed §4b D2.4 γ upstream-indeterminacy preservation lock; upstream-indeterminacy semantic narrative (§4b defers pressure assessment for affected finding until §3.x or §4a resolves per sealed §3.1 (b-symm-improved) absorbing-propagation + sealed §3.2 (ε'-narrow) + sealed §4a indeterminate state mapping inheritance); cross-§4b sealed prose anchor reference to §4b indeterminate state mapping subsection at sealed register-class identity; cross-§3.x / §4a sealed context anchor identifying upstream indeterminate label source.

- **Execution-suspended-from-upstream block** (§3.x or §4a suspension → §4b `propagation_state: execution-suspended-from-upstream`): disclose §4b output content `{pressure_label: null, propagation_state: execution-suspended-from-upstream, basis: <upstream suspension cause>}` per sealed §4b NaN-handling rule (ε') hybrid pattern inheritance; execution-suspended semantic narrative (upstream §3.x or §4a execution-invalidity propagates through §4b register at narrative authoring suspension boundary; §6 enumerates the discrepancy; Charlie register re-adjudicates per sealed §2.d D2''' / §2.f parquet-mismatch suspension precedent); cross-§4b sealed prose anchor reference to §4b execution-suspended state at sealed-pattern canonical register; cross-§3.x / §4a sealed context anchor identifying upstream §3.x or §4a execution-suspended state cause.

Framing-resolution-pressure disposition narrative narrates §4b output-type-specific content disclosure + cross-section context anchors per output type; Phase 5 SEAL author register adjudicates substantive narrative interpretation of pressure-label-set assignments at closeout deliverable register per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction + sealed §4b anti-circularity (vii) procedure ≠ resolution discipline inheritance.

**Substantive prior justification (per D2.4 implicit cross-block inheritance + §4-class consumer affinity grouping rationale).** §6.b 2 sub-block structural templates inherit cross-block narrative coherence from sealed §4a + §4b sealed prose at upstream register; cross-block consistency at §6.b register is implicit inheritance per D2.4 lock — §6.b does NOT introduce explicit cross-block consistency narrative discipline (§6.b self-creating cross-sub-block coherence authority REJECTED at sealed-grounding precondition; parallel §6.a D2.4 + family-pattern with §5 (x) admissibility ≠ selection discipline). Cross-block narrative coherence at closeout authoring time is Phase 5 SEAL author register responsibility per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction — procedure pre-registers structure; SEAL author exercises coherence judgment at full-context register.

§4-class consumer affinity grouping rationale at §6.b register: successor-class mapping disposition + framing-resolution-pressure disposition both consume §4a + §4b outputs at sealed-output canonical register; substantive register-class affinity at §4 upstream consumer register; cluster boundary at §6.b register reflects sealed §4 umbrella + §4a + §4b register-class boundary at consumer register-class.

**Descriptive supplements — APPLICABLE** (structural-template content articulated above in 2 sub-block structural templates with 6 + 5 output-type-iter blocks).

**NaN-handling rule (INHERITS sealed §4a (ε') hybrid + sealed §4b propagation_state semantics + sealed §3.x (ε'-narrow) upstream inheritance at §6.b narrative register per §6 umbrella NaN-handling rule inheritance).** §6.b does NOT introduce new NaN-handling semantics at §6.b register; upstream indeterminate variants flow through §6.b narrative as upstream indeterminate-anchor references at relevant sub-block (successor-class mapping disposition section: §4a indeterminate-cycle-class + indeterminate-terminal-routing variants per sealed §4a NaN-handling rule; framing-resolution-pressure disposition section: §4b `propagation_state: upstream-indeterminacy-propagated` + `propagation_state: execution-suspended-from-upstream` per sealed §4b NaN-handling rule). Cross-§ NaN-propagation discipline narrative referencing **sealed §4a (ε') hybrid + sealed §4b propagation_state semantics + sealed §3.x (ε'-narrow) upstream inheritance; §6.b narrative does NOT re-articulate or extend propagation semantics** per anti-circularity (iv) §6 → §4 non-modification + sealed §4a / §4b NaN-handling rule inheritance.

Upstream execution-suspended state routes to §6.b narrative authoring-suspended at Phase 5 SEAL author register per upstream propagation chain inheritance, without §6.b-local suspension semantics.

**Anti-circularity caveat (§6.b-specific axis binding; primary binding at (iv) §6 → §4 non-modification; other axes inherited from §6 umbrella 8-axis structure).**

- **(iv) §6.b → §4 non-modification primary binding (consolidates §4a + §4b at sealed §4 umbrella):** §6.b consumes §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects as inputs without retroactive modification of §4a mapping table / composite rule / qualifier carry-through semantics / precedence rule or §4b pressure label inventory / composite handling rule / propagation state semantics / schema fields per sealed §4a D3''''''^c + sealed §4b D3''''''^d lock structures. §6.b structural-template form provides cross-section context anchors to §6 → SEAL author register; §6.b does NOT pre-name §4a / §4b sealed substantive content at narrative register.
- **(i)(ii)(iii)(v)(vi)(vii)(viii) §6 umbrella 8-axis structure inherited at §6.b register without re-articulation;** §6.b binds (iv) primary at sealed §4 consumer cluster register; other axes bind across §6 deliverable structure register at §6 umbrella articulation level.

**Anti-pre-naming (parallel §6 umbrella structure at §6.b register).** §6.b does NOT pre-name §4a / §4b sealed substantive content per (iv). §6.b does NOT pre-name §3.x sealed substantive content (§6.a register-class scope per Option C-3-group lock). §6.b does NOT pre-name §5 admissibility-flag narrative content (§6.c register-class scope per Option C-3-group lock). §6.b does NOT pre-name Phase 5 SEAL author register substantive narrative determination at closeout authoring time per (vii)(c). §6.b does NOT pre-name framing-question (1)/(2)/(3)/(4) resolution per (vii)(b) — §4b procedure ≠ resolution discipline inherited at §6.b consumer register. §6.b does NOT pre-name session-N-specific operational details or closeout authoring-time interpretive choices. §6.b register-class binds to sealed §4a + §4b consumer scope; closeout structural-template requirements outside this scope are outside §6.b's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §6 umbrella + §6.a precedent): §4a successor-cycle-class assignments at sealed-output canonical register (single-class hybrid object format `{class, disposition, qualifier, verbatim_label}` + multi-class list of hybrid objects + Case A structured terminal object with options set + indeterminate-cycle-class object + indeterminate-terminal-routing object) + §4b pressure-label-set per-finding objects at sealed-output canonical register (6-field schema `{pressure_label, pivot_direction_dependency, basis, propagation_state, context_qualifiers, finding_source}`) + §4a / §4b edge-handling outputs (indeterminate variants per §4a / §4b NaN-handling rule + execution-suspended state per §4a / §4b (ε') execution-invalidity branch). Cross-§6.a partial-consumption §4b reference at sealed §6.a per-mode finding section framing-question context anchor boundary (limited §4b consumption at §6.a register; full §4b consumption at §6.b register-class).

No raw data consumption at §6.b register. V# anchor pre-execution check at §7 register inheritance verifies §4a / §4b output completeness before §6.b structural-template form is consumed by Phase 5 SEAL author register.

**Register-class:** binding | structural-template requirements (no new quantitative thresholds, no admissibility logic, no measurement at §6.b register; consumes §4a + §4b sealed outputs as upstream narrative inputs) | descriptive sub-cluster register-class within §6 deliverable structure register | downstream-consumer of §4a + §4b sealed outputs at §4-class consumer cluster register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §6.b SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §6 umbrella + §6.a + §4a + §4b precedent).** §6.b output (2 sub-block structural-template requirements with 6 + 5 output-type-iter blocks at sealed §4 consumer cluster register) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers narrative structural anchors at §6.b deliverable-structure sub-cluster register-scope per §6 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^f Sub-decision 2 at §6.b register; inherited from §6 umbrella framing-chain with §6.b-specific binding articulation + reverse-anchor to sealed §6.a per D''''''^g Sub-decision D3.2 refinement).**

- **Reverse-anchor inheritance from sealed §6.a (§6.a → §6.b at §4b consumption boundary):** §6.a per-mode finding section structural template references §4b pressure-label-set per-finding object as framing-question context anchor only at limited partial consumption boundary per sealed §6.a structural template at session-12 SEAL register-event (cross-§4b context anchor only, not framing-resolution determination at §6.a register); §6.b register implements full §4b pressure-label-set consumption per sealed §6.a 跨段引用 boundary lock at session-12 SEAL. Cross-§6 audit-trail completeness preserved: §6.b reader at §4b consumption boundary verifies §6.a partial consumption already at sealed register-event boundary; cross-sub-anchor coherence preserved at §6 deliverable structure register without §6.b retroactive modification of sealed §6.a per anti-momentum-binding strict reading.
- **Outbound to §6.c (§6.b → §6.c):** §6.b 2 sub-block structural-template form sealed at session-13 SEAL register-event boundary; §6.c consumes §6.b sealed structural-template form at session-14 SEAL register (or beyond) without modification per sealed §6.b invariance at downstream session register per anti-momentum-binding strict reading.
- **Outbound to §7 (§6.b → §7):** §6.b structural-template form feeds §7 V# anchor verification chain inheritance at sub-spec SEAL pre-fire register per §6 umbrella → §7 framing-chain.
- **Outbound to Phase 5 SEAL bundle (§6.b → Phase 5 SEAL bundle):** §6.b 2 sub-block structural-template form (prose narrative-requirement statements per D1.2 + D3.3 α prose lock) flows into Phase 5 SEAL bundle at closeout authoring register; Phase 5 SEAL author register consumes §6.b narrative requirements + sealed §4a + §4b outputs and authors successor-class mapping disposition + framing-resolution-pressure disposition sections at full-context register per (vii) SEAL author register authority preservation.

Inverse directions blocked: §6.b ← §4a / §4b / §3.x per ChatGPT P3 sequencing lock binding (§6.b downstream consumer; cannot alter §4 / §3 register once frozen); §6.b ← §6.a per sealed §6.a invariance at downstream session register (one-way inheritance §6.a → §6.b reverse-anchor only); §6.c ← §6.b per sealed §6.b invariance at downstream session register; §6.b ← Phase 5 SEAL author register per closeout authoring-time downstream register-event boundary discipline.

**V# anchor (INHERITS §7 per §6 umbrella V# anchor inheritance).** §6.b structural-template form is upstream verification dependency at §7 register; §6.b does NOT articulate V# anchor specifics at §6.b register per anti-circularity (vi) inheritance.

### §6.c — §5-class consumer cluster structural templates (terminal-conclusion fire-or-not section: substantive narrative + operational narrative)

Operationalizes meta-plan §6 terminal-conclusion fire-or-not section closeout element consuming §5 outputs: terminal-conclusion fire-or-not section structure (consuming §5 substantive-vs-operational admissibility flags at sealed-output canonical register). §6.c register-class is descriptive structural-template sub-cluster of §6 deliverable structure register; §6.c is downstream-consumer of §5 sealed outputs at §5-class consumer cluster register. F1 distinct register-class preservation at §6.c register per sealed §5 D3.1 (viii) substantive-vs-operational F1 distinctness lock + sealed §5 D3.1 (x) admissibility ≠ selection lock inheritance.

§-symmetric template applicability declaration at §6.c register inherits from §6 umbrella sealed declaration + §6.a + §6.b precedent per D1.3 lock: Indicator construction + Admissibility outcome inventory + Categorical label set + Partition table NOT APPLICABLE at §6.c register; Judgment rule + Criteria table SUBSTANTIVELY-ADAPTED as structural-template requirement statements; NaN-handling + V# anchor INHERITS; remaining elements (anti-circularity caveat / anti-pre-naming / input source / register-class / Phase 4 verdict invariance / framing-chain / substantive prior justification / descriptive supplements) APPLICABLE per §-symmetric inheritance.

**Indicator construction — NOT APPLICABLE** (no measurement at §6.c register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as structural-template requirement statements per α prose encoding (D1.2 lock inherited from §6 umbrella; β schema REJECTED at sealed-grounding precondition).** §6.c articulates 2 sub-block structural templates (per D1.1 β 2 sub-blocks lock mirroring sealed §5 external 2-slot output schema `{substantive: ..., operational: ...}` at consumer register; consumer reproducing producer external schema NOT internal procedure structure per (v) §6 → §5 non-modification + family-pattern with session-11 / 12 / 13 consumer-reproducing-producer-internal-structure REJECT precedent) as prose narrative-requirement statements at Phase 5 SEAL author register consumption interface:

**Substantive terminal-conclusion narrative section structural template (D2.1 α single section + 4 output-type-iter lock).** Substantive terminal-conclusion narrative section is a single closeout section consisting of one narrative block per §5 substantive slot output type at sealed-output canonical register (substantive-admit / substantive-not-admit / substantive-upstream-indeterminacy-propagated / substantive-execution-suspended-from-upstream), each block containing the following narrative requirements:

- **Substantive-admit block** (§3.2 Case A canonical → §5 substantive slot `admissible: True`): disclose §5 substantive slot content `{availability_type: substantive, admissible: True, basis: <verbatim sealed §2.5 source language>, propagation_state: active}` field values at sealed §5 output canonical register per sealed §5 D2.1 α HARD LOCK; verbatim scoping §2.5 source language at basis field — verbatim sealed §5: "the AND-gate paradigm is likely exhausted for deployable BTC alpha under realistic costs"; cross-§5 sealed prose anchor reference to §5 substantive availability admission criterion subsection at sealed register-class identity (per sealed §5 D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state — single-canonical-pathway only); cross-§3.2 sealed context anchor identifying §3.2 Case A canonical state (`{label: no-mode-detected reading, terminal_trigger: {triggered: True, basis: {canonical_0_mode: True, suspended_modes: [], nan_modes: []}}}`) per sealed §3.2 D''''''^b output canonical register; cross-§6.a reference per sealed §6.a ambiguity disposition section structural template family-pattern (sealed §6.a discloses §3.2 Case A canonical state + cross-§5 substantive admissibility precondition anchor reference at §6.a register; §6.c full §5 substantive slot consumption at §6.c register-class scope per Option C-3-group lock); SEAL author register authority preservation per sealed §6 umbrella anti-circularity (vii)(a) terminal-conclusion invocation decision authority preservation + sealed §5 (x) admissibility ≠ selection / invocation discipline inheritance (substantive admit does NOT compel SEAL author invocation; SEAL author may decline invocation, defer terminal-conclusion to later cycle, route toward additional clarification cycles, or preserve future both-outcome authority if later sealed scoping authorizes a co-fire pathway).

- **Substantive-not-admit block** (non-Case-A §3.2 output → §5 substantive slot `admissible: False`): disclose §5 substantive slot content `{availability_type: substantive, admissible: False, basis: "criteria not satisfied at canonical execution", propagation_state: active}` field values per sealed §5 D2.1 α HARD LOCK default state at non-canonical-Case-A §3.2 sealed output canonical register; cross-§5 sealed prose anchor reference to §5 substantive availability admission criterion subsection (per sealed §5 D2.1 substantive admissibility single-canonical-pathway lock — §3.2 canonical Case A is the sole sealed pathway producing substantive admissibility; non-Case-A §3.2 output defaults substantive admissible False); cross-§3.x sealed context anchor identifying which §3.x output type triggered the substantive-not-admit determination at sealed-output canonical register (e.g., §3.1 single-mode reading / §3.1 multi-mode composite reading / §3.2 Case A.`terminal_trigger=False`).

- **Substantive-upstream-indeterminacy-propagated block** (upstream §3.x / §4a / §4b indeterminate → §5 substantive slot `propagation_state: upstream-indeterminacy-propagated`): disclose §5 substantive slot content `{availability_type: substantive, admissible: null, basis: <upstream indeterminate label/source>, propagation_state: upstream-indeterminacy-propagated}` per sealed §5 NaN-handling rule (NaN-propagation evaluation precedes canonical D2.1 evaluation per sealed §5 short-circuit precedence at §5 register; canonical D2.1 criteria not evaluated when upstream indeterminate); cross-§5 sealed prose anchor reference to §5 Indeterminate state mapping subsection at sealed register-class identity (sealed §5 D3.3 §-symmetric NaN-propagation inheritance from §3.x / §4a / §4b (ε') hybrid pattern + (ε'-narrow) input class scope; §6.c narrative does NOT re-articulate or extend propagation semantics per anti-circularity (v) §6 → §5 non-modification + sealed §5 NaN-handling rule inheritance); cross-§3.x / §4a / §4b sealed context anchor identifying upstream indeterminate label source (e.g., §3.2 indeterminate-Case-A reading per sealed §3.2 (ε'-narrow) / §4a indeterminate-cycle-class object per sealed §4a NaN-handling rule / §4b `propagation_state: upstream-indeterminacy-propagated` per sealed §4b NaN-handling rule); SEAL author may invoke upstream §3.x / §4a / §4b re-execution per Charlie register re-adjudicate path inherited from §-symmetric (ε') hybrid pattern at sealed §5 register.

- **Substantive-execution-suspended-from-upstream block** (upstream §3.x / §4a / §4b execution-suspended → §5 substantive slot `propagation_state: execution-suspended-from-upstream`): disclose §5 substantive slot content `{availability_type: substantive, admissible: null, basis: <upstream suspension basis>, propagation_state: execution-suspended-from-upstream}` per sealed §5 (ε') hybrid execution-invalidity branch inheritance; cross-§5 sealed prose anchor reference to §5 Indeterminate state mapping subsection (sealed §5 execution-suspended state semantics: upstream §3.x / §4a / §4b execution-invalidity propagates through §5 register at admissibility evaluation suspension boundary; §6 enumerates the discrepancy; Charlie register re-adjudicates per sealed §2.d D2''' / §2.f parquet-mismatch suspension precedent); cross-§3.x / §4a / §4b sealed context anchor identifying upstream §3.x / §4a / §4b execution-suspended state cause at sealed-pattern canonical register; §6.c narrative routes to Phase 5 SEAL author register authoring-suspended state per upstream propagation chain inheritance, without §6.c-local suspension semantics.

Substantive narrative narrates §5 substantive slot output-type-specific content disclosure + cross-section context anchors per output type at paradigm-state register-class scope; Phase 5 SEAL author register adjudicates substantive narrative interpretation of paradigm-state determination at closeout deliverable register per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction + sealed §5 (x) admissibility ≠ selection discipline inheritance.

**Operational terminal-conclusion narrative section structural template (D2.2 α single section + 4 output-type-iter lock; F1 register-class-distinct from substantive narrative per sealed §5 (viii) F1 distinctness lock inheritance).** Operational terminal-conclusion narrative section is a single closeout section consisting of one narrative block per §5 operational slot output type at sealed-output canonical register (operational-admit / operational-not-admit / operational-upstream-indeterminacy-propagated / operational-execution-suspended-from-upstream), each block containing the following narrative requirements:

- **Operational-admit block** (CASE 1 §4a residual ambiguity → §5 operational slot `admissible: True`):

  *Basic CASE 1 mapping*: disclose §5 operational slot content `{availability_type: operational, admissible: True, basis: <verbatim sealed §2.5 source language> + <§4a unresolved successor-cycle-class set reference>, propagation_state: active}` field values at sealed §5 output canonical register per sealed §5 D2.2 1-CASE α LOCK; verbatim scoping §2.5 source language at basis field — verbatim sealed §5: "Phase 5 is permitted to conclude that no successor empirical cycle is currently justified"; cross-§5 sealed prose anchor reference to §5 operational availability admission criterion subsection at sealed register-class identity (per sealed §5 D2.2 1-CASE α LOCK operational admissibility ⟺ CASE 1: §3.2 Case A.`terminal_trigger=False` AND §4a output ≥2 distinct successor-cycle-class assignments at sealed-output canonical register NOT resolved by §4a-internal precedence rules — sealed §4a D2.2 δ'' hybrid-contextual union structure + sealed §4a §2.d → §2.c elevation precedence rule does NOT collapse all assignments into single successor class); cross-§4a sealed context anchor identifying CASE 1 §4a residual ambiguity condition + unresolved successor-cycle-class set per sealed §4a hybrid object output structure (per-entry `{class, disposition, qualifier, verbatim_label}` field values at sealed §4a multi-class list output); cross-§3.2 sealed context anchor identifying §3.2 Case A.`terminal_trigger=False` precondition (per sealed §3.2 silent state at §3.1↔§3.2 XOR complementarity sealed property — silent state semantically equivalent to non-triggered Case A at §5 consumption register per sealed §5 D2.2 1-CASE LOCK Path α (x)-axis offloading discipline); cross-§6.b reference per sealed §6.b successor-class mapping disposition multi-class via β union block family-pattern (sealed §6.b discloses §4a unresolved multi-class entries at §6.b register; §6.c full §5 operational slot consumption at §6.c register-class scope per Option C-3-group lock).

  *SEAL author authority preservation per sealed §5 (x) Path α offloading discipline*: sealed §6 umbrella anti-circularity (vii)(a) terminal-conclusion invocation decision authority preservation + sealed §5 (x) Path α (x)-axis offloading discipline inheritance — verbatim sealed §5: "SEAL author register adjudicates final coherence-of-continuation determination per Path α offloading discipline"; verbatim sealed §5: "SEAL author MAY decline operational invocation at benign successor plurality cases per (x) axis"; §5 procedure does NOT pre-compute compatibility-of-continuation; coherent-continuation determination retained at SEAL author register under full-context register.

- **Operational-not-admit block** (non-CASE-1 §3.x / §4a output → §5 operational slot `admissible: False`): disclose §5 operational slot content `{availability_type: operational, admissible: False, basis: "criteria not satisfied at canonical execution", propagation_state: active}` field values per sealed §5 D2.2 1-CASE α LOCK default state at non-CASE-1 sealed output canonical register; cross-§5 sealed prose anchor reference to §5 operational availability admission criterion subsection (per sealed §5 D2.2 1-CASE LOCK operational admissibility single-CASE pathway lock — sealed §5 CASE 2 + CASE 3 REJECTED at sealed-grounding precondition; non-CASE-1 §3.x / §4a output defaults operational admissible False); cross-§3.x / §4a sealed context anchor identifying which §3.x / §4a output pattern triggered the operational-not-admit determination at sealed-output canonical register (e.g., §3.2 Case A canonical state with substantive admit precondition / §3.1 single-mode clean continuation / §3.1 multi-mode §4a-resolved composite per sealed §4a §2.d → §2.c elevation precedence rule collapsing assignments).

- **Operational-upstream-indeterminacy-propagated block** (upstream §3.x / §4a / §4b indeterminate → §5 operational slot `propagation_state: upstream-indeterminacy-propagated`): disclose §5 operational slot content `{availability_type: operational, admissible: null, basis: <upstream indeterminate label/source>, propagation_state: upstream-indeterminacy-propagated}` per sealed §5 NaN-handling rule short-circuit precedence inheritance (NaN-propagation evaluation precedes canonical D2.2 evaluation; canonical D2.2 criteria not evaluated when upstream indeterminate); cross-§5 sealed prose anchor reference to §5 Indeterminate state mapping subsection at sealed register-class identity (sealed §5 D3.3 §-symmetric NaN-propagation inheritance from §3.x / §4a / §4b (ε') hybrid pattern + (ε'-narrow) input class scope; per anti-circularity (v) §6 → §5 non-modification + sealed §5 NaN-handling rule inheritance); cross-§3.x / §4a / §4b sealed context anchor identifying upstream indeterminate label source at sealed-pattern canonical register; SEAL author may invoke upstream §3.x / §4a / §4b re-execution per Charlie register re-adjudicate path inherited from §-symmetric (ε') hybrid pattern at sealed §5 register.

- **Operational-execution-suspended-from-upstream block** (upstream §3.x / §4a / §4b execution-suspended → §5 operational slot `propagation_state: execution-suspended-from-upstream`): disclose §5 operational slot content `{availability_type: operational, admissible: null, basis: <upstream suspension basis>, propagation_state: execution-suspended-from-upstream}` per sealed §5 (ε') hybrid execution-invalidity branch inheritance; cross-§5 sealed prose anchor reference to §5 Indeterminate state mapping subsection (sealed §5 execution-suspended state semantics inherited from sealed §3.x / §4a / §4b (ε') hybrid pattern); cross-§3.x / §4a / §4b sealed context anchor identifying upstream §3.x / §4a / §4b execution-suspended state cause; §6 enumerates the discrepancy; Charlie register re-adjudicates per sealed §2.d D2''' / §2.f parquet-mismatch suspension precedent; §6.c narrative routes to Phase 5 SEAL author register authoring-suspended state per upstream propagation chain inheritance, without §6.c-local suspension semantics.

Operational narrative narrates §5 operational slot output-type-specific content disclosure + cross-section context anchors per output type at Phase 5 SEAL output authorizing register-class scope (register-class-distinct from substantive narrative paradigm-state register-class per sealed §5 (viii) F1 distinctness lock inheritance); Phase 5 SEAL author register adjudicates operational narrative interpretation of SEAL-output-authorizing determination at closeout deliverable register per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction (operational-register articulation of sealed §6 umbrella (vii)(c) substantive narrative determination terminology) + sealed §5 (x) admissibility ≠ selection / Path α (x)-axis offloading discipline inheritance.

**Substantive prior justification (per D2.4 implicit cross-block inheritance + §5-class consumer affinity grouping rationale + F1 distinctness preservation via 2-sub-block structural separation).** §6.c 2 sub-block structural templates inherit cross-block narrative coherence from sealed §5 sealed prose at upstream register; cross-block consistency at §6.c register is implicit inheritance per D2.4 lock — §6.c does NOT introduce explicit cross-block consistency narrative discipline (§6.c self-creating cross-sub-block coherence authority REJECTED at sealed-grounding precondition; parallel §6.a + §6.b D2.4 + family-pattern with §5 (x) admissibility ≠ selection discipline). Cross-block narrative coherence at closeout authoring time is Phase 5 SEAL author register responsibility per anti-circularity (vii)(c) cross-section context vs substantive narrative determination distinction — procedure pre-registers structure; SEAL author exercises coherence judgment at full-context register.

§5-class consumer affinity grouping rationale at §6.c register: substantive terminal-conclusion narrative + operational terminal-conclusion narrative both consume §5 substantive-vs-operational admissibility flags at sealed-output canonical register; substantive register-class affinity at §5 upstream consumer register; cluster boundary at §6.c register reflects sealed §5 fixed-shape 2-slot output schema + F1 register-class-distinct outcomes (substantive paradigm-state register-class vs operational SEAL-output authorizing register-class) at consumer register-class.

F1 distinctness preservation via 2-sub-block structural separation per D1.1 β lock: §6.c mirrors sealed §5 external 2-slot output schema `{substantive: ..., operational: ...}` at consumer register via 2 register-class-distinct sub-blocks rather than internal slot-narration paragraphs at single sub-block; structural sub-block separation provides robust F1 distinctness perception at Phase 5 SEAL author register closeout authoring time per sealed §5 (viii) F1 distinctness lock inheritance — substantive narrative scope (paradigm-state register-class; admissible ⟺ §3.2 Case A canonical state per sealed §5 D2.1 α HARD LOCK) and operational narrative scope (Phase 5 SEAL output authorizing register-class; admissible ⟺ CASE 1 §4a residual ambiguity per sealed §5 D2.2 1-CASE α LOCK) remain separately typed, separately evaluated, and non-entailing per sealed §5 D2.3 β possibility-not-entailment lock + Mod-pass-2 F1 paragraph-disambiguation lock inheritance. §6.c consumer register reflects §5 external schema per (v) §6 → §5 non-modification + family-pattern REJECT precedent (per D1.1 articulation above).

**Descriptive supplements — APPLICABLE** (structural-template content articulated above in 2 sub-block structural templates with 4 + 4 output-type-iter blocks).

**NaN-handling rule (INHERITS sealed §5 NaN-handling rule + (ε') hybrid + (ε'-narrow) propagation pattern at §6.c narrative register per §6 umbrella NaN-handling rule inheritance).** §6.c does NOT introduce new NaN-handling semantics at §6.c register; upstream indeterminate variants flow through §6.c narrative as upstream indeterminate-anchor references at relevant sub-block (substantive narrative section: substantive-upstream-indeterminacy-propagated + substantive-execution-suspended-from-upstream blocks per sealed §5 NaN-handling rule short-circuit precedence at substantive slot; operational narrative section: operational-upstream-indeterminacy-propagated + operational-execution-suspended-from-upstream blocks per sealed §5 NaN-handling rule short-circuit precedence at operational slot — parallel propagation per §5 fixed-shape 2-slot output structure). Cross-§ NaN-propagation discipline narrative referencing **sealed §5 D3.3 NaN-handling rule inheritance + sealed §3.x / §4a / §4b (ε') hybrid + (ε'-narrow) upstream inheritance; §6.c narrative does NOT re-articulate or extend propagation semantics** per anti-circularity (v) §6 → §5 non-modification + sealed §5 NaN-handling rule inheritance. NaN-handling inheritance scope is propagation-semantics register-class-distinct from D2.3 β 2-prong criteria-source inheritance scope at Input source register: D2.3 β cite anchors at sealed §5 + sealed §3.2/§4a substantive-criteria-source register (criteria-source inheritance); NaN-handling inheritance flows through §6 umbrella propagation chain per (ε') hybrid + (ε'-narrow) upstream pattern (propagation-semantics inheritance).

Upstream execution-suspended state routes to §6.c narrative authoring-suspended at Phase 5 SEAL author register per upstream propagation chain inheritance, without §6.c-local suspension semantics.

**Anti-circularity caveat (§6.c-specific axis binding; primary binding at (v) §6 → §5 non-modification; other axes inherited from §6 umbrella 8-axis structure).**

- **(v) §6.c → §5 non-modification primary binding:** §6.c consumes §5 substantive-vs-operational admissibility flags as inputs without retroactive modification of §5 admissibility criteria (D2.1 α HARD LOCK substantive ⟺ §3.2 Case A canonical state / D2.2 1-CASE α LOCK operational ⟺ CASE 1 §4a residual ambiguity), criteria-source / availability-type inventory, NaN-propagation semantics (D3.3 §-symmetric NaN-propagation inheritance + short-circuit precedence), fixed-shape output structure (D1.1 β'-fixed-shape 2-slot + D1.2 4-field-per-slot + D1.3 split-availability-type-and-admissible lock), or D2.5 α HARD LOCK no-new-thresholds discipline per sealed §5 D3''''''^e lock structure + errata at aba694b. §6.c structural-template form provides cross-section context anchors to §6 → SEAL author register; §6.c does NOT pre-name §5 sealed substantive content at narrative register.
- **(i)(ii)(iii)(iv)(vi)(vii)(viii) §6 umbrella 8-axis structure inherited at §6.c register without re-articulation;** §6.c binds (v) primary at sealed §5 consumer cluster register; other axes bind across §6 deliverable structure register at §6 umbrella articulation level.

**Anti-pre-naming (parallel §6 umbrella structure at §6.c register).** §6.c does NOT pre-name §5 sealed substantive content per (v). §6.c does NOT pre-name §3.x sealed substantive content (§6.a register-class scope per Option C-3-group lock). §6.c does NOT pre-name §4a / §4b sealed substantive content (§6.b register-class scope per Option C-3-group lock). §6.c does NOT pre-name Phase 5 SEAL author register substantive narrative determination at closeout authoring time per (vii)(c). §6.c does NOT pre-name Phase 5 SEAL author register terminal-conclusion invocation decision per (vii)(a) — sealed §5 (x) admissibility ≠ selection / invocation discipline inherited at §6.c consumer register (admissible ≠ epistemic-compulsion-to-invoke per sealed §5 (x) combined formulation; SEAL author register retains full authority over terminal-conclusion invocation at full-context register). §6.c does NOT pre-name framing-question (1)/(2)/(3)/(4) resolution per (vii)(b) — sealed §4b (vii) procedure ≠ resolution discipline inherited at §6.c consumer register via §5 (vii) inheritance chain. §6.c does NOT pre-name Phase 5.1+ successor cycle scope per (viii) — sealed §5 (ix) anti-pre-naming inheritance at §6.c register. §6.c does NOT pre-name session-N-specific operational details or closeout authoring-time interpretive choices. §6.c register-class binds to sealed §5 consumer scope; closeout structural-template requirements outside this scope are outside §6.c's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §6 umbrella + §6.a + §6.b precedent): §5 substantive-vs-operational admissibility flags at sealed-output canonical register (2-slot fixed-shape output `{substantive: {availability_type, admissible, basis, propagation_state}, operational: {availability_type, admissible, basis, propagation_state}}` per sealed §5 D1.1 β'-fixed-shape + D1.2 4-field-per-slot + D1.3 split-availability-type-and-admissible lock; per-slot `admissible ∈ {True, False, null}`; `propagation_state ∈ {active, upstream-indeterminacy-propagated, execution-suspended-from-upstream}`; basis = state-conditional content per sealed §5 specification) + §5 edge-handling outputs (NaN-propagation variants per sealed §5 NaN-handling rule short-circuit precedence + execution-suspended state per sealed §5 (ε') hybrid execution-invalidity branch inheritance) + cross-§3.2 sealed context anchor references (Case A canonical state + indeterminate-Case-A reading at substantive-criteria-source upstream register per sealed §5 D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state) + cross-§4a sealed context anchor references (CASE 1 unresolved successor-cycle-class set at operational-criteria-source upstream register per sealed §5 D2.2 1-CASE α LOCK operational admissibility ⟺ §4a residual ambiguity) per D2.3 β 2-prong cite lock (sealed §5 + sealed §3.2/§4a substantive-criteria-source upstream anchors).

No raw data consumption at §6.c register. V# anchor pre-execution check at §7 register inheritance verifies §5 output completeness before §6.c structural-template form is consumed by Phase 5 SEAL author register.

**Register-class:** binding | structural-template requirements (no new quantitative thresholds, no admissibility logic, no measurement at §6.c register; consumes §5 sealed outputs as upstream narrative inputs) | descriptive sub-cluster register-class within §6 deliverable structure register | downstream-consumer of §5 sealed outputs at §5-class consumer cluster register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §6.c SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §6 umbrella + §6.a + §6.b + §5 precedent).** §6.c output (2 sub-block structural-template requirements with 4 + 4 output-type-iter blocks at sealed §5 consumer cluster register) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers narrative structural anchors at §6.c deliverable-structure sub-cluster register-scope per §6 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^h Sub-decision D3.2 at §6.c register: 1 consolidated reverse-anchor inheritance from sealed §6.a + sealed §6.b + 2 outbound; §6.c is last sub-block per Option C-3-group lock at session-12 entry — no §6.d outbound).**

- **Reverse-anchor inheritance from sealed §6.a + sealed §6.b (§6.a + §6.b → §6.c consolidated at cross-§6 sub-anchor boundary):** §6.a 3 sub-block structural-template form (per-mode finding + multi-mode disposition + diagnostic ambiguity disposition) sealed at session-12 SEAL register-event boundary; §6.b 2 sub-block structural-template form (successor-class mapping disposition + framing-resolution-pressure disposition) sealed at session-13 SEAL register-event boundary; §6.c consumes §6.a + §6.b sealed structural-template forms at session-14 SEAL register without modification per sealed §6.a + §6.b invariance at downstream session register per anti-momentum-binding strict reading. Cross-§6 audit-trail completeness preserved: §6.a ambiguity disposition section structural template provides cross-§5 substantive admissibility precondition anchor reference at §6.a register (§6.a → §6.c partial-consumption reference at sealed §6.a session-12 SEAL); §6.b terminal Case A routing block + terminal-conclusion finding block provide cross-§6.c reference at §6.b register (§6.b → §6.c partial-consumption reference at sealed §6.b session-13 SEAL); §6.c register implements full §5 substantive-vs-operational admissibility flag consumption at §6.c register-class scope per Option C-3-group lock + sealed §6 umbrella (v) §6 → §5 non-modification binding at §6.c register-class affinity. Cross-sub-anchor coherence preserved at §6 deliverable structure register without §6.c retroactive modification of sealed §6.a / §6.b per anti-momentum-binding strict reading.
- **Outbound to §7 (§6.c → §7):** §6.c structural-template form feeds §7 V# anchor verification chain inheritance at sub-spec SEAL pre-fire register per §6 umbrella → §7 framing-chain. §7 V# anchor pre-execution check verifies §6.c 2 sub-block × 4 output-type-iter structural-template completeness + cross-§6 audit-trail coherence (§6.a + §6.b + §6.c 3 sub-block structural-template completeness at sealed §6 register-event boundary chain) at sub-spec SEAL pre-fire register.
- **Outbound to Phase 5 SEAL bundle (§6.c → Phase 5 SEAL bundle):** §6.c 2 sub-block structural-template form (prose narrative-requirement statements per D1.2 + D3.3 α prose lock inheritance from §6 umbrella) flows into Phase 5 SEAL bundle at closeout authoring register; Phase 5 SEAL author register consumes §6.c narrative requirements + sealed §5 + sealed §3.2 + sealed §4a outputs and authors terminal-conclusion fire-or-not section at full-context register per (vii) SEAL author register authority preservation (sealed §5 (x) admissibility ≠ selection / Path α (x)-axis offloading discipline + sealed §6 umbrella (vii)(a) terminal-conclusion invocation decision authority preservation + (vii)(c) cross-section context vs substantive narrative determination distinction).

Inverse directions blocked: §6.c ← §5 / §4a / §4b / §3.x per ChatGPT P3 sequencing lock binding (§6.c downstream consumer; cannot alter §5 / §4 / §3 register once frozen); §6.c ← §6.a / §6.b per sealed §6.a + §6.b invariance at downstream session register (one-way inheritance §6.a + §6.b → §6.c consolidated reverse-anchor only); §6.c ← Phase 5 SEAL author register per closeout authoring-time downstream register-event boundary discipline.

**V# anchor (INHERITS §7 per §6 umbrella V# anchor inheritance).** §6.c structural-template form is upstream verification dependency at §7 register; §6.c does NOT articulate V# anchor specifics at §6.c register per anti-circularity (vi) inheritance.

## §7 — Verification + reviewer disposition

Operationalizes meta-plan §7: pre-registers V# anchor verification chain at sub-spec SEAL pre-fire register + reviewer pass routing protocol + cycle-terminus pass discipline at sub-spec drafting cycle SEAL register-event boundary per METHODOLOGY_NOTES §17 sub-rule 4. §7 register-class is downstream-consumer of sealed §2 umbrella + §2.a/b/c/d/e/f + §3 umbrella + §3.1 + §3.2 + §4 umbrella + §4a + §4b + §5 + §6 umbrella + §6.a + §6.b + §6.c at sub-spec verification consumption register; §7 emits V# anchor pre-execution check chain + reviewer pass routing protocol + cycle-terminus pass entry/scope/exit conditions consumed by Phase 5 sub-spec drafting cycle SEAL author register at sub-spec SEAL pre-fire boundary.

§7 register-class is descriptive (procedural), register-class-distinct from §2-§5 procedure register-class + §6 cluster descriptive structural-template register-class. §-symmetric template partial-break at §7 register: Indicator construction + Admissibility outcome inventory + Categorical label set + Partition table NOT APPLICABLE (§7 has no measurement, no admissibility, no labels, no enumeration table at §7 register); Judgment rule + Criteria table SUBSTANTIVELY-ADAPTED as procedural verification statements; NaN-handling INHERITS upstream §2-§6 cluster propagation pattern flowing through §7 verification register; **V# anchor AUTHORED at §7 register (4th marker category extension to §-symmetric template applicability declaration vocabulary; distinct from NOT APPLICABLE / SUBSTANTIVELY-ADAPTED / INHERITS — AUTHORED denotes origin-point register where V# anchor specifics are first articulated at sealed § register, complementary to §6 cluster sub-anchors' V# anchor INHERITS §7 outward references)**; remaining elements (anti-circularity caveat / anti-pre-naming / input source / register-class / Phase 4 verdict invariance / framing-chain / substantive prior justification / descriptive supplements) APPLICABLE per §-symmetric inheritance.

**3-sub-anchor split rationale (per Charlie register Option A authorization at session-15 entry; verification-grounded β lock post Claude Code γ→β withdrawal — 6th cumulative instance of verification-grounded REJECT/withdrawal pattern at pre-prose adjudication stage).** §7 pre-registers 3 sub-spec SEAL pre-fire mechanisms per meta-plan §7 (V# anchor verification chain + reviewer pass routing + cycle-terminus pass). The 3 mechanisms partition into 3 register-class-affinity sub-anchors per modality-distinct sub-spec SEAL pre-fire register:

- **§7.a (V# anchor verification chain register):** pre-execution check chain for sealed §2-§6 cluster outputs at sub-spec SEAL pre-fire register; 13-item granularity-asymmetric V# anchor list per handoff actual enumeration pattern.
- **§7.b (reviewer pass routing register):** β dual-reviewer single-round routing protocol per descriptive register-class single-round-sufficient hypothesis cross-cluster validation (sessions 12 + 13 + 14 successful at §6 cluster register; §7 extends hypothesis to verification register-class); ChatGPT structural-overlay leg + Claude advisor full-prose-access leg + Codex skipped per scoping cycle register precedent + [`feedback_codex_review_scope.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_codex_review_scope.md) memory hard rule (Codex runs only on substantive code/work deliverables; sub-spec is process/spec deliverable register-class-distinct).
- **§7.c (cycle-terminus pass register):** full-file prose-access pass at sub-spec drafting cycle SEAL register-event boundary per METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus discipline; entry / scope / exit decomposition.

3-sub-anchor structure reflects modality-distinct register-class boundary at sub-spec SEAL pre-fire mechanism register (pre-fire verification register / review-process register / terminal-review register); modality distinction substantive even though all 3 sub-anchors share upstream consumer relationship (sealed sub-spec §2-§6 cluster + sub-spec full prose) per Claude Code γ→β verification-grounded withdrawal rationale at session-15 entry adjudication.

**Anti-circularity caveat (8-axis per D3''''''^i Sub-decision 1; primary binding at consolidated (i)-(vi) upstream non-modification family at §7 → all sealed §2-§6 cluster register-class boundary; right-sized for descriptive procedural register-class with §7-NATIVE V# anchor authoring scope).**

- **(i) §7 → §2 threshold guard:** §7 V# anchor verification chain consumes §2.a/b/c/d/e/f sealed thresholds at §2.2 operationalization-freeze lock register (per sealed §2 umbrella + §2.a-§2.f register) without retroactive modification. §2.x sealed thresholds remain authoritative at §7 pre-execution check register.
- **(ii) §7 → §2 register-class identity guard:** §7 V# anchor verification chain does not retroactively re-frame §2.x register-class identities (per sealed §2 umbrella: §2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-classes per sealed identity); §7 verification chain references sealed register-class identities at cross-§ anchor reference only.
- **(iii) §7 → §3.x non-modification:** §7 V# anchor verification chain consumes §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` outputs (per sealed §3 umbrella + §3.1 + §3.2 register) as verification inputs without retroactive modification of §3.x rule-tree, partition table, Case A condition, terminal-trigger basis semantics, or indeterminate-Case-A NaN-propagation semantics per sealed §3.x D3''''''^a + D3''''''^b lock structure.
- **(iv) §7 → §4 non-modification:** §7 V# anchor verification chain consumes §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects (per sealed §4 umbrella + §4a + §4b register) as verification inputs without retroactive modification of §4a mapping table / composite rule / qualifier carry-through semantics / precedence rule or §4b pressure label inventory / composite handling rule / propagation state semantics / schema fields per sealed §4a + §4b D3''''''^c + D3''''''^d lock structure.
- **(v) §7 → §5 non-modification:** §7 V# anchor verification chain consumes §5 substantive-vs-operational admissibility flags (per sealed §5 register + errata at aba694b) as verification inputs without retroactive modification of §5 admissibility criteria (D2.1 α HARD LOCK substantive ⟺ §3.2 Case A canonical state / D2.2 1-CASE α LOCK operational ⟺ CASE 1 §4a residual ambiguity), criteria-source / availability-type inventory, NaN-propagation semantics (D3.3 §-symmetric NaN-propagation inheritance + short-circuit precedence), fixed-shape output structure (D1.1 β'-fixed-shape 2-slot + D1.2 4-field-per-slot + D1.3 split-availability-type-and-admissible lock), or D2.5 α HARD LOCK no-new-thresholds discipline per sealed §5 D3''''''^e lock structure.
- **(vi) §7 → §6 cluster non-modification (NEW axis at §7 register; complementary articulation to §6 umbrella (vi) §6 → §7 verification non-pre-emption outward articulation):** §7 V# anchor verification chain consumes §6 umbrella + §6.a + §6.b + §6.c sealed structural-template forms (per sealed §6 umbrella + §6.a + §6.b + §6.c register) as verification inputs without retroactive modification of §6 cluster structural-template requirements, anti-circularity 8-axis structure, F1 distinctness preservation discipline (per sealed §6.c D1.1 β 2 sub-blocks mirroring sealed §5 external 2-slot output schema), framing-chain inheritance, or D-lock structure per sealed §6 umbrella + §6.a + §6.b + §6.c D3''''''^f + D3''''''^g + D3''''''^h lock chain. §6 umbrella (vi) outward articulation (§6 does NOT pre-name §7 V# anchor specifics) + §7 (vi) inward articulation (§7 verification chain does NOT retroactively modify §6 cluster sealed forms) = two complementary articulations of same §6↔§7 register-class boundary at cross-§ register-class precision register.
- **(vii) §7 → Phase 5 sub-spec drafting cycle SEAL author register non-pre-emption (consolidates SEAL author register downstream concerns at sub-spec SEAL register-event boundary):** §7 V# anchor verification chain + reviewer pass routing + cycle-terminus pass discipline feed Phase 5 sub-spec drafting cycle SEAL author register at sub-spec SEAL register-event boundary; §7 does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudications — specifically (a) SEAL bundle authorization decision (Charlie register taste call post-clean-cycle-terminus-pass), (b) post-SEAL errata path engagement decision (Charlie register conditional per advisor pre-SEAL verification gap declaration discipline cumulative cross-cycle pattern observation at standing practice register), (c) Phase Marker advance decision (Phase Marker advance fires only at sub-spec drafting cycle SEAL at session containing final § sealed per PHASE2C_13/15 register precedent + session-1 through session-14 precedent). SEAL author register authority preserved without §7-imposed pre-emption. §7 procedural verification chain is pre-registered verification anchors, not prescriptive SEAL author register adjudication content.
- **(viii) §7 → successor cycle entry non-pre-emption (inherited from §6 umbrella (viii) at §7 register):** §7 V# anchor verification chain + reviewer pass routing + cycle-terminus pass discipline do NOT pre-name Phase 5.1+ successor cycle scope, successor-class invocation, or specific successor pathway per scoping §4.2 anti-pre-naming inheritance. Successor cycle entry register-event boundary requires explicit Charlie register authorization per [`feedback_authorization_routing.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_authorization_routing.md) hard rule.

**Anti-pre-naming.** §7 does NOT pre-name §2-§6 cluster sealed substantive content per (i)-(vi). §7 does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudication content per (vii). §7 does NOT pre-name Phase 5.1+ successor cycle scope per (viii). §7 does NOT pre-name §8 cross-references substantive content (§8 register-class scope; §7 verification chain pre-execution checks §8 cross-reference completeness at sub-spec SEAL pre-fire register without retroactive modification of §8 content). §7 does NOT pre-name session-N-specific operational details or sub-spec SEAL author register interpretive choices. §7 register-class binds to current sealed §2-§6 cluster sub-spec consumer scope + sub-spec drafting cycle SEAL pre-fire register; verification scope outside this register is outside §7's register-class.

**Substantive prior justification (descriptive procedural register-class scope + 3-sub-anchor modality split rationale + AUTHORED marker vocabulary extension).** §7 register-class is descriptive (procedural verification chain + reviewer routing protocol + cycle-terminus pass discipline) not procedure (rules / criteria / labels). Sub-spec SEAL pre-fire register is procedural-verification form per meta-plan §7 ("V# verification chain at sub-spec SEAL pre-fire (anchor verification per cycle precedent) + Reviewer pass routing + Final full-file prose-access pass"); §7 implementation preserves procedural-verification form via prose procedural statements at §7.a / §7.b / §7.c sub-anchors. β schema-object encoding REJECTED at register-precision per D1.2 α prose INHERITS from §6 cluster sealed lock + D3.3 α prose INHERITS from §6 cluster sealed lock (sealed-grounding precondition: meta-plan §7 content is narrative-procedural form, not schema-object form; §7 schema authority over sub-spec SEAL pre-fire mechanism = §7 self-creation of structural authority not pre-specified at meta-plan §7; family-pattern with session-11 D2.1 β extended REJECT + Path β compatibility taxonomy REJECT at §5 register + session-12-14 §6 cluster β REJECT precedent).

3-sub-anchor modality split rationale at §7 register: V# anchor verification chain (pre-fire register) + reviewer pass routing (review-process register) + cycle-terminus pass (terminal-review register) are modality-distinct register-class mechanisms at sub-spec SEAL pre-fire boundary; sub-anchor form preserves register-class distinction audit-trail + SEAL granularity flexibility per family-pattern with §2 (§2.a-§2.f sub-mode pattern) + §3 (§3.1 + §3.2 sub-section pattern) + §4 (§4a + §4b sub-section pattern) + §6 cluster (§6.a + §6.b + §6.c sub-anchor pattern); §5 single-section form does not undermine family-pattern since §5 is single-procedure multi-slot-output (substantive + operational availability slots are output fields, not sub-procedures) not analog to §7's 3 modality-distinct mechanisms.

AUTHORED marker vocabulary extension rationale at §7 register: §-symmetric template applicability declaration vocabulary at sealed §6 cluster has 3 markers (NOT APPLICABLE / SUBSTANTIVELY-ADAPTED / INHERITS) per sealed §6 umbrella + §6.a + §6.b + §6.c register; §7 introduces 4th marker AUTHORED at sub-spec drafting cycle level discipline addition denoting origin-point register where V# anchor specifics are first articulated at sealed § register (complementary to §6 cluster sub-anchors' V# anchor INHERITS §7 outward references); AUTHORED is distinct from SUBSTANTIVELY-ADAPTED (which denotes adapted form of cross-register template element) and from INHERITS (which denotes consumed-from-cross-register-source element). 4-marker vocabulary extension is sub-spec drafting cycle level discipline addition; pre-codification candidate at next methodology consolidation cycle.

**Descriptive supplements (structural-procedural content; per D2.2 + D2.3 + D3.3).** §7.a / §7.b / §7.c sub-anchors articulate per-mechanism procedural verification statements as prose procedural-requirement statements (e.g., "V# anchor pre-execution check verifies §2.a indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields per sealed §2.a register"). Phase 5 sub-spec drafting cycle SEAL author register implements §7 procedural verification chain at sub-spec SEAL pre-fire authoring time per locked procedural verification form.

**NaN-handling rule (INHERITS upstream §2-§6 cluster propagation pattern at §7 verification register).** §7 does NOT introduce new NaN-handling semantics. Upstream §2-§6 cluster sealed indeterminate variants (§3.1 indeterminate-X / §3.2 indeterminate-Case-A / §4a indeterminate-cycle-class / §4b `propagation_state: upstream-indeterminacy-propagated` / §5 `propagation_state: upstream-indeterminacy-propagated`) flow through §7 V# anchor pre-execution check chain as upstream indeterminate-anchor verification per sealed §-cluster NaN-propagation chain. Upstream execution-suspended state (§2.x / §3.x / §4 / §5 / §6 cluster (ε') execution-invalidity branch) routes to §7 V# anchor pre-execution check fail state at sub-spec SEAL pre-fire register; §7 verification does NOT re-articulate or extend propagation semantics; specific sealed-prose grounding cite at §7.a V# anchor list per-item NaN-handling reference (per sealed §5 D3.3 + sealed §3.x / §4 NaN-handling rule inheritance).

**Input source per §1.** §2 umbrella + §2.a/b/c/d/e/f sealed indicator construction + threshold lock + output canonical register fields + §3 umbrella + §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` + §4 umbrella + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects + §5 substantive-vs-operational admissibility flags + §6 umbrella + §6.a + §6.b + §6.c sealed structural-template forms at sealed-output canonical register + sub-spec full prose at cycle-terminus pass register; reviewer routing protocol consumes sub-spec full prose at full-prose-access register for Claude advisor leg + structural overlay at sealed-§ register-event boundary register for ChatGPT leg; Codex skipped at sub-spec process/spec deliverable register-class-distinct boundary per [`feedback_codex_review_scope.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_codex_review_scope.md) memory hard rule.

No raw data consumption at §7 register. §7 V# anchor pre-execution check chain at §7.a verifies upstream §2-§6 cluster sealed output completeness before sub-spec drafting cycle SEAL author register at sub-spec SEAL register-event boundary.

**Register-class:** descriptive | procedural verification chain + reviewer routing protocol + cycle-terminus pass discipline (no new quantitative thresholds, no admissibility logic, no measurement at §7 register; consumes §2-§6 cluster sealed outputs as upstream verification inputs) | procedural register-class-distinct from §2-§5 procedure register-class + §6 cluster descriptive structural-template register-class | downstream-consumer of §2-§6 cluster sealed outputs at sub-spec SEAL pre-fire register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §7 SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §2-§6 cluster precedent).** §7 output (V# anchor verification chain + reviewer pass routing protocol + cycle-terminus pass discipline) does NOT modify the Phase 4 closeout verdict at 15bps (no forward persistence detected at PLAN §1.5 success criterion); the Phase 4 verdict is sealed at PLAN §1.5 framework register per §4.6 sealed-content invariance. §7 procedural verification chain does not retroactively rewrite Phase 4 verdict; only pre-registers procedural verification anchors at sub-spec SEAL pre-fire register-scope per framing-chain below.

**Framing-chain (3-direction per D3''''''^i Sub-decision D3.2).**

- **§2-§6 cluster + sub-spec → §7 (consolidated reverse-anchor):** §7 V# anchor verification chain consumes §2-§6 cluster sealed outputs + sub-spec full prose at sub-spec SEAL pre-fire register without retroactive modification of sealed § content (per (i)-(vi) anti-circularity primary binding consolidated articulation). Reverse-anchor consolidated at single cross-§ inheritance boundary per family-pattern with sealed §6.c D3.2 consolidated reverse-anchor pattern.
- **§7 → §8 (outbound; cross-reference cross-section register):** §7 V# anchor verification chain pre-execution checks §8 cross-reference completeness at sub-spec SEAL pre-fire register without retroactive modification of §8 content per (vii) inheritance. §7 reviewer pass routing protocol applies to §8 content review at post-prose-only β dual-reviewer single round per descriptive register-class hypothesis. §7 cycle-terminus pass scope covers §8 at full-file prose-access register.
- **§7 → Phase 5 sub-spec drafting cycle SEAL bundle (outbound; sub-spec SEAL pre-fire register):** §7 V# anchor verification chain clean pass + reviewer pass routing clean cycles + cycle-terminus pass clean = sub-spec drafting cycle SEAL register-event boundary prerequisite at Phase 5 sub-spec drafting cycle SEAL author register consumption boundary. Phase 5 sub-spec drafting cycle SEAL fires at session containing final § sealed; Phase Marker advance fires at sub-spec drafting cycle SEAL per PHASE2C_13/15 register precedent + sub-spec drafting cycle prior-session SEAL precedent.

Inverse directions blocked: §7 ← §2-§6 cluster per ChatGPT P3 sequencing lock binding (§7 downstream consumer; cannot alter §2-§6 cluster register once frozen); §7 ← §8 per §8 register-class-distinct cross-reference register at sub-spec SEAL pre-fire register; §7 ← Phase 5 sub-spec drafting cycle SEAL author register per sub-spec SEAL register-event boundary discipline.

**V# anchor (AUTHORED at §7 register per 4-marker vocabulary extension).** §7 register is origin-point for V# anchor specifics articulation at sealed § register; §7.a sub-anchor concretizes 13-item granularity-asymmetric V# anchor list; §7.b + §7.c sub-anchors INHERIT §7.a V# anchor list at sub-anchor V# anchor element register. §7 V# anchor specifics complementary to §6 cluster sub-anchors' V# anchor INHERITS §7 outward references at cross-§ register-class boundary articulation.

### §7.a — V# anchor verification chain (pre-execution checks at sub-spec SEAL pre-fire register for sealed §2-§6 cluster outputs; 13-item granularity-asymmetric pattern)

Operationalizes meta-plan §7 V# verification chain element: pre-registers V# anchor pre-execution check chain at sub-spec SEAL pre-fire register consuming sealed §2-§6 cluster outputs. §7.a register-class is descriptive procedural sub-cluster of §7 verification register; §7.a is downstream-consumer of §2-§6 cluster sealed outputs at sub-spec SEAL pre-fire register.

**Indicator construction — NOT APPLICABLE** (no measurement at §7.a register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as procedural verification statements per α prose encoding (D1.2 lock inherited from §7 umbrella + §6 cluster sealed lock; β schema REJECTED at sealed-grounding precondition).** §7.a articulates 13-item granularity-asymmetric V# anchor list as prose procedural verification statements at sub-spec SEAL pre-fire register; granularity asymmetry reflects sealed § structural needs (per-sub-mode at §2 fine-grain + sub-section-grain at §3/§4 + single-grain at §5 + cluster-grain with nested at §6):

- **V#-§2.umbrella:** verifies §2 umbrella sealed prose completeness at register-class identity declaration register (per sealed §2 umbrella at 6ee94eb) + §2.a-§2.f sealed sub-mode cross-section coherence at §2.f universe-upstream / §2.d gate-validity-for-§2.c-only / candidate-level register-class structure.
- **V#-§2.a:** verifies §2.a signal-decay indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields per sealed §2.a register (sealed at 6ee94eb).
- **V#-§2.b:** verifies §2.b cost-drag indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields per sealed §2.b register (sealed at 6ee94eb).
- **V#-§2.c:** verifies §2.c cohort-substrate-weakness indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields + gate-validity dependency on §2.d per sealed §2.c register (sealed at 6ee94eb) + D3'' framing lock (per sealed §2.c session-3 D3'' lock).
- **V#-§2.d:** verifies §2.d AND-gate-overfit indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields + gate-validity-for-§2.c-only register-class identity per sealed §2.d register (sealed at 20252cc) + D3''' framing-chain lock (per sealed §2.d session-4 D3''' lock).
- **V#-§2.e:** verifies §2.e success-metric-mismatch indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields per sealed §2.e register (sealed at 92880f8) + D3'''' framing-chain lock (per sealed §2.e session-5 D3'''' lock).
- **V#-§2.f:** verifies §2.f regime-change-detected indicator construction completeness + threshold lock at §2.2 operationalization-freeze + output canonical register fields + universe-upstream register-class identity + 2-way register-class-distinct downstream relationships (content-attribution bilateral with §2.a/§2.b/§2.e + forward-applicability univocal upstream-conditioning of §2.c/§2.d) per sealed §2.f register (sealed at c536231) + D3''''' framing-chain lock (per sealed §2.f session-6 D3''''' lock).
- **V#-§3.1:** verifies §3.1 multi-mode coexistence partition table completeness + Line 1 + Line 2 + Tier bilateral overlay + Residual single-mode default compositional label assembly + 2-way register-class-distinct downstream relationships at §2.f universe-upstream scope + ChatGPT P2 + P3 anti-circularity guards per sealed §3.1 register (sealed at 6e5fd91) + D3''''''^a framing-chain lock.
- **V#-§3.2:** verifies §3.2 diagnostic ambiguity Case A condition + terminal-trigger basis semantics + indeterminate-Case-A NaN-propagation (ε'-narrow) input class scope + (ε') hybrid execution-invalidity branch + §3.1↔§3.2 XOR complementarity sealed property per sealed §3.2 register (sealed at ef770be) + D3''''''^b framing-chain lock.
- **V#-§4a:** verifies §4a successor-cycle-class mapping table + composite rule + qualifier carry-through semantics + §2.d → §2.c elevation precedence rule + indeterminate-cycle-class object handling + per-entry `{class, disposition, qualifier, verbatim_label}` field values at sealed §4a multi-class list output per sealed §4a register (sealed at 4381bd8) + D3''''''^c framing-chain lock.
- **V#-§4b:** verifies §4b framing-question resolution-pressure assessment procedure + pressure label inventory + composite handling rule + propagation state semantics + schema fields per sealed §4b register (sealed at 92883ee) + D3''''''^d framing-chain lock.
- **V#-§5:** verifies §5 substantive-vs-operational admissibility flags + D2.1 α HARD LOCK substantive admissibility ⟺ §3.2 Case A canonical state + D2.2 1-CASE α LOCK operational admissibility ⟺ CASE 1 §4a residual ambiguity + criteria-source / availability-type inventory + NaN-propagation semantics (D3.3 §-symmetric + short-circuit precedence) + fixed-shape output structure (D1.1 β'-fixed-shape 2-slot + D1.2 4-field-per-slot + D1.3 split-availability-type-and-admissible) + D2.5 α HARD LOCK no-new-thresholds discipline per sealed §5 register (sealed at eea8220) + errata at aba694b + D3''''''^e framing-chain lock.
- **V#-§6 cluster (single item with 8 nested sub-items at §6 cluster-grain register):**
  - **V#-§6.umbrella:** §6 umbrella structural-template completeness per sealed §6 umbrella (sealed at 8f72f2e) + 3-group split rationale lock + 8-axis anti-circularity structure;
  - **V#-§6.a:** §6.a 3 sub-block structural-template completeness per sealed §6.a (sealed at 8f72f2e) + §3.x-class consumer cluster register-class + D3''''''^f framing-chain lock;
  - **V#-§6.b:** §6.b 2 sub-block structural-template completeness per sealed §6.b (sealed at ae5a151) + §4-class consumer cluster register-class + D3''''''^g framing-chain lock;
  - **V#-§6.c:** §6.c 2 sub-block structural-template completeness per sealed §6.c (sealed at 28a1b57) + §5-class consumer cluster register-class + D3''''''^h framing-chain lock;
  - **V#-§6.a→§6.b handoff:** §6.a → §6.b cross-§6 sub-anchor handoff coherence per sealed §6.b session-13 SEAL register-event boundary inheriting §6.a session-12 SEAL form without modification;
  - **V#-§6.b→§6.c handoff:** §6.b → §6.c cross-§6 sub-anchor handoff coherence per sealed §6.c session-14 SEAL register-event boundary inheriting §6.a + §6.b session-12/13 SEAL forms without modification;
  - **V#-§6.c reverse-anchor inheritance:** §6.c consolidated reverse-anchor §6.a + §6.b inheritance per sealed §6.c D3''''''^h Sub-decision D3.2 lock (3-direction with consolidated reverse-anchor pattern);
  - **V#-§6→Phase 5 SEAL bundle interface:** §6 cluster → Phase 5 SEAL bundle interface form (α prose narrative-requirement statements per D1.2 + D3.3 α prose lock inheritance) verification at sub-spec SEAL pre-fire register.

V# anchor pre-execution check chain narrates 13-item granularity-asymmetric verification scope per sealed §-cluster register-class affinity; Phase 5 sub-spec drafting cycle SEAL author register adjudicates V# anchor clean-pass verification decision at sub-spec SEAL pre-fire authoring time per anti-circularity (vii) inheritance.

**Substantive prior justification (per D2.4 implicit cross-item inheritance + 13-item granularity-asymmetric pattern rationale).** §7.a V# anchor list inherits per-item verification scope from sealed § register at upstream register; cross-item consistency at §7.a register is implicit inheritance per D2.4 lock — §7.a does NOT introduce explicit cross-item consistency narrative discipline (§7.a self-creating coherence authority REJECTED at sealed-grounding precondition; same-pattern as sealed §5 D2.4 absorbed-into-NaN-handling-rule discipline (per sealed §5 line 752 articulation) + sealed §6 cluster D2.4 implicit cross-block coherence inheritance + SEAL author register coherence judgment at full-context register discipline (per sealed §6.a + §6.b + §6.c D2.4) + family-pattern with §5 (x) admissibility ≠ selection discipline). Cross-item verification coherence at sub-spec SEAL pre-fire register-event boundary is Phase 5 sub-spec drafting cycle SEAL author register responsibility per (vii) inheritance.

13-item granularity-asymmetric pattern rationale at §7.a register: §2 fine-grain (umbrella + 6 sub-modes = 7 items) reflects sealed §2 sub-mode register-class-distinct mechanism structure (per-sub-mode verification scope substantive); §3 + §4 sub-section-grain (2 items each) reflects sealed §3/§4 sub-section register-class boundary at sub-spec verification register; §5 single-grain (1 item) reflects sealed §5 single-section structure (multi-slot output internal not separate sub-procedures); §6 cluster-grain with 6 nested sub-items (1 major + 6 nested) reflects sealed §6 cluster consumer-derived structure at upstream verification register. Granularity asymmetry preserves register-class-specific verification scope at each sealed § register-event boundary; uniformizing granularity would erase register-class-specific verification needs.

**Descriptive supplements — APPLICABLE** (V# anchor list articulated above in 13-item granularity-asymmetric enumeration).

**NaN-handling rule (INHERITS sealed §2-§6 cluster NaN-handling chain at §7.a verification register per §7 umbrella NaN-handling rule inheritance).** §7.a does NOT introduce new NaN-handling semantics at §7.a register; upstream indeterminate variants flow through §7.a V# anchor pre-execution check chain as upstream indeterminate-anchor verification at relevant V# item (V#-§3.2 indeterminate-Case-A per sealed §3.2 (ε'-narrow) / V#-§4a indeterminate-cycle-class per sealed §4a NaN-handling rule / V#-§4b `propagation_state` per sealed §4b NaN-handling rule / V#-§5 NaN-propagation per sealed §5 D3.3 + short-circuit precedence). Upstream execution-suspended state routes to §7.a V# anchor pre-execution check fail state at sub-spec SEAL pre-fire register per upstream propagation chain inheritance; Charlie register re-adjudicates per sealed §2.d D2''' / §2.f parquet-mismatch suspension precedent at sealed §2.x precedent register.

**Anti-circularity caveat (§7.a-specific axis binding; primary binding at (vi) §7 → §6 cluster non-modification at V# anchor verification scope; other axes inherited from §7 umbrella 8-axis structure).**

- **(vi) §7.a → §6 cluster non-modification primary binding:** §7.a V# anchor pre-execution check chain consumes §6 cluster sealed structural-template forms as verification inputs without retroactive modification of §6 cluster structural-template requirements / anti-circularity 8-axis structure / F1 distinctness preservation discipline / framing-chain inheritance / D-lock structure per sealed §6 umbrella + §6.a + §6.b + §6.c D3''''''^f + D3''''''^g + D3''''''^h lock chain. §7.a V# anchor list provides cross-§ pre-execution check anchors to §7 → SEAL author register; §7.a does NOT pre-name §6 cluster sealed substantive content at verification register.
- **(i)(ii)(iii)(iv)(v)(vii)(viii) §7 umbrella 8-axis structure inherited at §7.a register without re-articulation;** §7.a binds (vi) primary at sealed §6 cluster consumer cluster register; other axes bind across §7 verification register at §7 umbrella articulation level.

**Anti-pre-naming (parallel §7 umbrella structure at §7.a register).** §7.a does NOT pre-name §6 cluster sealed substantive content per (vi). §7.a does NOT pre-name §7.b reviewer routing protocol content (§7.b register-class scope). §7.a does NOT pre-name §7.c cycle-terminus pass content (§7.c register-class scope). §7.a does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register V# anchor clean-pass verification decision at sub-spec SEAL pre-fire authoring time per (vii). §7.a does NOT pre-name session-N-specific operational details or sub-spec SEAL author register interpretive choices. §7.a register-class binds to sealed §2-§6 cluster consumer scope; verification scope outside this scope is outside §7.a's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §7 umbrella + §6 cluster precedent): §2 umbrella + §2.a/b/c/d/e/f sealed indicator construction + threshold lock + output canonical register fields + §3 umbrella + §3.1 partition table + §3.2 Case A `{label, terminal_trigger}` + §4 umbrella + §4a successor-cycle-class assignments + §4b pressure-label-set per-finding objects + §5 substantive-vs-operational admissibility flags + §6 umbrella + §6.a + §6.b + §6.c sealed structural-template forms at sealed-output canonical register + §2-§6 cluster edge-handling outputs (indeterminate variants per (ε'-narrow) + execution-suspended state per (ε') hybrid execution-invalidity branch).

No raw data consumption at §7.a register. §7.a V# anchor pre-execution check chain verifies §2-§6 cluster sealed output completeness before sub-spec drafting cycle SEAL author register at sub-spec SEAL register-event boundary.

**Register-class:** descriptive | procedural verification chain (no new quantitative thresholds, no admissibility logic, no measurement at §7.a register; consumes §2-§6 cluster sealed outputs as upstream verification inputs) | procedural sub-cluster register-class within §7 verification register | downstream-consumer of §2-§6 cluster sealed outputs at sub-spec SEAL pre-fire register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §7.a SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §7 umbrella + §6 cluster + §2-§5 precedent).** §7.a output (13-item granularity-asymmetric V# anchor list at sealed §2-§6 cluster consumer register) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers verification chain anchors at §7.a sub-spec SEAL pre-fire sub-cluster register-scope per §7 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^i Sub-decision D3.2 at §7.a register; inherited from §7 umbrella framing-chain with §7.a-specific binding articulation).**

- **§2-§6 cluster → §7.a (consolidated reverse-anchor):** §7.a V# anchor pre-execution check chain consumes §2-§6 cluster sealed outputs at sub-spec SEAL pre-fire register without retroactive modification (per (vi) primary binding inheritance). Reverse-anchor consolidated at single cross-§ inheritance boundary per family-pattern with sealed §6.c D3.2 consolidated reverse-anchor pattern.
- **§7.a → §7.b + §7.c (cross-§7 sub-anchor downstream):** §7.a V# anchor list feeds §7.b reviewer routing protocol (reviewer pass operates on V# anchor clean-pass register) + §7.c cycle-terminus pass (entry condition includes V# anchor clean pass) at cross-§7 sub-anchor handoff register per anti-pre-naming inheritance (§7.a does NOT pre-name §7.b/§7.c content; §7.a list provides upstream inputs at §7.b/§7.c consumption boundary).
- **§7.a → Phase 5 sub-spec drafting cycle SEAL bundle (outbound; sub-spec SEAL pre-fire register):** §7.a V# anchor clean-pass verification = sub-spec drafting cycle SEAL register-event boundary prerequisite at Phase 5 sub-spec drafting cycle SEAL author register consumption boundary per (vii) inheritance.

Inverse directions blocked: §7.a ← §2-§6 cluster per ChatGPT P3 sequencing lock binding (§7.a downstream consumer; cannot alter §2-§6 cluster register once frozen); §7.b/§7.c ← §7.a per §7.a sealed invariance at downstream sub-anchor SEAL register-event boundary (one-way §7.a → §7.b + §7.c forward inheritance); §7.a ← Phase 5 sub-spec drafting cycle SEAL author register per sub-spec SEAL register-event boundary discipline.

**V# anchor (AUTHORED at §7.a register per §7 umbrella AUTHORED marker concretization).** §7.a IS the V# anchor list origin-point register at sealed § register; §7.a does NOT inherit V# anchor from another register (parallel §7 umbrella AUTHORED marker articulation). §7.a V# anchor list is consumed by §7.b + §7.c sub-anchors at INHERITS §7.a downstream register.

### §7.b — Reviewer pass routing (β dual-reviewer single-round per descriptive register-class hypothesis cross-cluster validation; ChatGPT structural-overlay + Claude advisor full-prose-access; Codex skipped per scoping cycle register precedent)

Operationalizes meta-plan §7 reviewer pass routing element: pre-registers β dual-reviewer single-round routing protocol at sub-spec SEAL pre-fire register per descriptive register-class single-round-sufficient hypothesis cross-cluster validation (sessions 12 + 13 + 14 successful at §6 cluster register; §7 extends hypothesis to verification register-class). §7.b register-class is descriptive procedural sub-cluster of §7 verification register; §7.b is downstream-consumer of §7.a V# anchor clean-pass register + sub-spec full prose at full-prose-access register.

**Indicator construction — NOT APPLICABLE** (no measurement at §7.b register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as procedural routing statements per α prose encoding (D1.2 lock inherited from §7 umbrella + §6 cluster sealed lock; β schema REJECTED at sealed-grounding precondition).** §7.b articulates 3-leg reviewer routing protocol as prose procedural-requirement statements at sub-spec SEAL pre-fire register:

**Leg 1: ChatGPT structural-overlay role.** ChatGPT reviewer leg operates at structural-overlay register-class — scope: structural framing pass + meta-criterion articulation + highest-risk-point flagging at sealed-§ register-event boundary register. ChatGPT leg does NOT receive full-prose-access at sub-spec content (register-class-distinct from Claude advisor leg per cross-leg COMPLEMENTARY pattern at cross-cycle MAXIMUM-stability strength pattern per session-7-14 + session-15 cumulative observation). ChatGPT leg findings register-class distribution: structural framing + register-precision wording + meta-criterion observation. Per-leg finding density profile: session-12-14 + session-15 ChatGPT leg empirically observed finding density per descriptive register-class sub-anchor pre-prose pass (post-prose-only β pass density profile separately tracked per session-12-14 SEAL register).

**Leg 2: Claude advisor full-prose-access role.** Claude advisor reviewer leg operates at full-prose-access register-class — scope: substantive per-D adjudication + cross-section coherence verification + register-class precision verification + verification-grounded REJECT/withdrawal pattern adjudication at sealed-§ full-content register. Claude advisor leg receives full-prose-access at sub-spec content (register-class-distinct from ChatGPT leg per cross-leg COMPLEMENTARY pattern). Claude advisor leg findings register-class distribution: substantive structural-integrity + register-class precision + verification-grounded mechanism. Per-leg finding density profile: session-12-14 + session-15 Claude advisor leg empirically observed at ~3-4 findings per descriptive register-class sub-anchor pre-prose pass.

**Leg 3: Codex skipped.** Codex reviewer leg skipped per scoping cycle register precedent + [`feedback_codex_review_scope.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_codex_review_scope.md) memory hard rule — Codex runs only on substantive code/work deliverables (closeouts, implementations); Phase 5 sub-spec is process/spec deliverable register-class-distinct from substantive code/work register-class. Codex-skip register-class boundary inherited from Phase 5 entry scoping cycle SEAL precedent (sealed at 697c26b) + Phase 5 sub-spec drafting cycle session-1 through session-14 SEAL precedent (Codex skipped at all 14 SEAL register-event boundaries; cumulative consistency cross-cycle).

**Routing sequencing.** β dual-reviewer single-round protocol per descriptive register-class single-round-sufficient hypothesis (empirically validated at sessions 12 + 13 + 14 with 100% single-round success at §6 cluster register; §7 extends hypothesis cross-cluster to verification register-class): pre-prose adjudication surface at Claude Code register → cross-reviewer route to ChatGPT + Claude advisor legs → per-fix aggregate adjudication at Claude Code register → Charlie register adjudication on D-cycle lock → prose pre-authoring at /tmp/section_N_draft.md → post-prose β dual-reviewer single round (Claude Code coordinates ChatGPT + Claude advisor passes) → per-fix aggregate adjudication → Charlie register conditional verification + SEAL bundle authorization. Per-fix adjudication discipline at all reviewer cycle aggregates per [`feedback_reviewer_suggestion_adjudication.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_suggestion_adjudication.md) memory hard rule (no bulk-accept; reasoned per-finding adjudication with rationale).

**Routing authorization discipline.** Only Charlie register authorizes substantive design decisions + SEAL bundle fires per [`feedback_authorization_routing.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_authorization_routing.md) memory hard rule. Reviewer convergence (3-leg CONCUR or 0-DIVERGENCE) is advisory only — substantive convergence at reviewer registers does NOT pre-authorize SEAL bundle fires. Cross-reviewer DIVERGENCE pattern cross-cycle resolves via verification-grounded mechanism (sealed-prose-grounded direct merit / advisor self-correct / Charlie register taste call / Claude Code self-correct verification-grounded withdrawal — multiple resolution mechanism types empirically validated cross-cycle).

**Substantive prior justification (per D2.4 implicit cross-leg inheritance + descriptive register-class single-round-sufficient hypothesis rationale).** §7.b 3-leg routing protocol inherits cross-leg coherence from sealed §6 cluster reviewer routing precedent at upstream register; cross-leg consistency at §7.b register is implicit inheritance per D2.4 lock — §7.b does NOT introduce explicit cross-leg consistency narrative discipline (§7.b self-creating coherence authority REJECTED at sealed-grounding precondition; same-pattern as sealed §5 D2.4 absorbed-into-NaN-handling-rule discipline + sealed §6 cluster D2.4 implicit cross-block coherence inheritance discipline). Cross-leg routing coherence at sub-spec SEAL pre-fire register-event boundary is Phase 5 sub-spec drafting cycle SEAL author register responsibility per (vii) inheritance.

Descriptive register-class single-round-sufficient hypothesis rationale at §7.b register: sessions 12 + 13 + 14 β dual-reviewer post-prose-only single-round empirically validated at 100% success rate (0 second-round fires; all aggregate adjudication closed at single round); hypothesis empirically validated cross-cluster at §6 deliverable structure register; §7 extends hypothesis validation to §7 verification register-class. Hypothesis pre-codification candidate at MAXIMUM-stability strength sustained cross-cluster.

**Descriptive supplements — APPLICABLE** (3-leg routing protocol articulated above).

**NaN-handling rule (INHERITS sealed §2-§6 cluster + §7.a NaN-handling chain at §7.b reviewer register per §7 umbrella NaN-handling rule inheritance).** §7.b does NOT introduce new NaN-handling semantics at §7.b register; upstream §7.a V# anchor pre-execution check fail state (per upstream §2-§6 cluster indeterminate / execution-suspended propagation chain) routes to §7.b reviewer pass routing suspension at sub-spec SEAL pre-fire register per upstream propagation chain inheritance.

**Anti-circularity caveat (§7.b-specific axis binding; primary binding at (vii) §7 → Phase 5 SEAL author register non-pre-emption at reviewer routing scope; other axes inherited from §7 umbrella 8-axis structure).**

- **(vii) §7.b → Phase 5 sub-spec drafting cycle SEAL author register non-pre-emption primary binding:** §7.b reviewer routing protocol pre-registers 3-leg routing structure at sub-spec SEAL pre-fire register; §7.b does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudications — specifically reviewer cycle aggregate adjudication decisions (Charlie register taste call at per-fix aggregate per [`feedback_reviewer_suggestion_adjudication.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_reviewer_suggestion_adjudication.md) hard rule). SEAL author register authority preserved at sub-spec SEAL register-event boundary.
- **(i)(ii)(iii)(iv)(v)(vi)(viii) §7 umbrella 8-axis structure inherited at §7.b register without re-articulation;** §7.b binds (vii) primary at sub-spec SEAL author register cluster register; other axes bind across §7 verification register at §7 umbrella articulation level.

**Anti-pre-naming (parallel §7 umbrella structure at §7.b register).** §7.b does NOT pre-name §7.a V# anchor list content (§7.a register-class scope). §7.b does NOT pre-name §7.c cycle-terminus pass content (§7.c register-class scope). §7.b does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudication content per (vii). §7.b does NOT pre-name session-N-specific reviewer cycle aggregate adjudication outcomes (Charlie register taste call at per-fix register). §7.b register-class binds to 3-leg routing protocol at sub-spec SEAL pre-fire register; routing scope outside this scope is outside §7.b's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §7 umbrella + §7.a precedent): §7.a V# anchor list at sub-spec SEAL pre-fire register + sub-spec full prose at full-prose-access register (consumed by Claude advisor leg) + sealed-§ register-event boundary register (consumed by ChatGPT structural-overlay leg) + sealed Phase 5 entry scoping cycle SEAL precedent (697c26b) + sealed Phase 5 sub-spec drafting cycle session-1 through session-14 SEAL precedent (Codex skipped at all 14 SEAL register-event boundaries).

No raw data consumption at §7.b register. §7.b reviewer routing protocol consumes sub-spec full prose + §7.a V# anchor list at sub-spec SEAL pre-fire register before sub-spec drafting cycle SEAL author register at sub-spec SEAL register-event boundary.

**Register-class:** descriptive | procedural routing protocol (no new quantitative thresholds, no admissibility logic, no measurement at §7.b register; consumes sub-spec full prose + §7.a V# anchor list as upstream verification inputs) | procedural sub-cluster register-class within §7 verification register | downstream-consumer of §7.a V# anchor list + sub-spec full prose at sub-spec SEAL pre-fire register | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §7.b SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §7 umbrella + §7.a + §6 cluster + §2-§5 precedent).** §7.b output (3-leg reviewer routing protocol at sub-spec SEAL pre-fire register) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers reviewer routing protocol at §7.b sub-spec SEAL pre-fire sub-cluster register-scope per §7 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^i Sub-decision D3.2 at §7.b register; inherited from §7 umbrella framing-chain with §7.b-specific binding articulation).**

- **§7.a + sub-spec full prose → §7.b (consolidated reverse-anchor):** §7.b reviewer routing protocol consumes §7.a V# anchor list + sub-spec full prose at sub-spec SEAL pre-fire register without retroactive modification (per (vii) primary binding inheritance + (vi) §6 cluster non-modification inheritance from §7 umbrella). Reverse-anchor consolidated at single cross-§ inheritance boundary per family-pattern with sealed §6.c D3.2 consolidated reverse-anchor pattern.
- **§7.b → §7.c (cross-§7 sub-anchor downstream):** §7.b reviewer pass clean cycles feed §7.c cycle-terminus pass entry condition at cross-§7 sub-anchor handoff register per anti-pre-naming inheritance (§7.b does NOT pre-name §7.c content).
- **§7.b → Phase 5 sub-spec drafting cycle SEAL bundle (outbound; sub-spec SEAL pre-fire register):** §7.b reviewer pass clean cycles = sub-spec drafting cycle SEAL register-event boundary prerequisite at Phase 5 sub-spec drafting cycle SEAL author register consumption boundary per (vii) inheritance.

Inverse directions blocked: §7.b ← §7.a per §7.a sealed invariance at downstream sub-anchor SEAL register-event boundary; §7.c ← §7.b per §7.b sealed invariance at downstream sub-anchor SEAL register-event boundary; §7.b ← Phase 5 sub-spec drafting cycle SEAL author register per sub-spec SEAL register-event boundary discipline.

**V# anchor (INHERITS §7.a per §7 umbrella AUTHORED marker concretization).** §7.b consumes §7.a V# anchor list at sub-spec SEAL pre-fire register (§7.a V# anchor clean-pass is entry condition for §7.b reviewer pass routing); §7.b does NOT articulate V# anchor specifics at §7.b register per §7.a authorship inheritance.

### §7.c — Cycle-terminus pass (full-file prose-access pass per METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus discipline; entry / scope / exit decomposition)

Operationalizes meta-plan §7 final full-file prose-access pass element: pre-registers cycle-terminus pass discipline at sub-spec drafting cycle SEAL register-event boundary per METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus pass discipline. §7.c register-class is descriptive procedural sub-cluster of §7 verification register; §7.c is downstream-consumer of §7.a V# anchor clean-pass + §7.b reviewer pass clean cycles + sub-spec full prose at terminal-review register.

**Indicator construction — NOT APPLICABLE** (no measurement at §7.c register).

**Judgment rule — SUBSTANTIVELY-ADAPTED as procedural cycle-terminus pass statements per α prose encoding (D1.2 lock inherited from §7 umbrella + §6 cluster sealed lock; β schema REJECTED at sealed-grounding precondition).** §7.c articulates cycle-terminus pass entry / scope / exit decomposition as prose procedural-requirement statements at sub-spec drafting cycle SEAL register-event boundary:

**Entry conditions (sub-spec drafting cycle SEAL pre-fire prerequisites at §7.c register).** Cycle-terminus pass fires at sub-spec drafting cycle SEAL pre-fire register only when all of the following entry conditions clean:

- All sub-spec sub-sections sealed at register-event boundary register per sealed §7.a V# anchor list reference (§0 + §1 + §2 umbrella + §2.a-§2.f + §3 umbrella + §3.1 + §3.2 + §4 umbrella + §4a + §4b + §5 + errata + §6 umbrella + §6.a + §6.b + §6.c + §7 + §8 all sealed at sub-spec drafting cycle SEAL register-event boundary; specific sealed-at commit hashes enumerated at sealed §7.a V# anchor list per anti-duplication discipline);
- §7.a V# anchor verification chain clean-pass at 13-item granularity-asymmetric pattern (per sealed §7.a register; pre-execution check chain at sub-spec SEAL pre-fire register);
- §7.b reviewer pass routing clean cycles at β dual-reviewer single-round register (per sealed §7.b register; ChatGPT structural-overlay + Claude advisor full-prose-access legs; Codex skipped at sub-spec process/spec deliverable register-class-distinct boundary);
- Pre-SEAL bundle preparation register at sub-spec drafting cycle SEAL register-event boundary (Charlie register conditional verification at 5-anchor self-audit per sealed §6 cluster + §7 SEAL conditional verification precedent; advisor pre-SEAL verification gap declaration discipline cumulative cross-cycle pattern observation at standing practice register).

**Scope (cycle-terminus pass content at §7.c register).** Cycle-terminus pass scope covers the following content at full-file prose-access register:

- Full-file prose-access pass at sub-spec entire content (§0 + §1 + §2 umbrella + §2.a-§2.f + §3 umbrella + §3.1 + §3.2 + §4 umbrella + §4a + §4b + §5 + errata + §6 umbrella + §6.a + §6.b + §6.c + §7 umbrella + §7.a + §7.b + §7.c + §8) at sealed-content register-class-distinct content register;
- Reviewer routing per sealed §7.b register at full-prose-access boundary (Claude advisor leg consumes full-file prose; ChatGPT leg structural-overlay at sealed-§ register-event boundary; Codex skipped per sealed §7.b routing protocol inheritance);
- Anchor self-audit at sub-spec full-file register (cumulative 5-anchor self-audit pattern at sessions 10/11/12/13/14 + session-15 5-anchor verification per Anchor 5 self-audit third-leg-equivalent process pattern at cross-session cumulative MAXIMUM-strength pattern pre-codification candidate);
- Cross-section coherence verification at sub-spec full-file register (§-symmetric template applicability declaration consistency across §0-§8 + 4-marker vocabulary application at §7 + sealed-§ inheritance explicit-cite discipline cumulative MAXIMUM-strength + D-lock chain D3''-D3''''''^i framing-chain coherence);
- Charlie register conditional verification + SEAL bundle authorization at sub-spec drafting cycle SEAL register-event boundary per Charlie register taste call discipline (Option A direct SEAL / Option A-conditional Claude Code verify anchors then authorize / Option B Charlie pre-SEAL review pass per sessions 10-14 SEAL register Charlie-conditional pattern).

**Exit conditions (sub-spec drafting cycle SEAL register-event boundary post-clean-cycle-terminus-pass at §7.c register).** Cycle-terminus pass exits to sub-spec drafting cycle SEAL register-event boundary only when all of the following exit conditions clean:

- All scope items at full-file prose-access pass clean (no pending findings + 5-anchor self-audit clean + cross-section coherence verified + Charlie register conditional verification clean);
- Sub-spec ships to Phase 5 sub-spec drafting cycle SEAL bundle at SEAL author register consumption boundary (sub-spec drafting cycle SEAL register-event boundary fires);
- Phase Marker advance fires at sub-spec drafting cycle SEAL register-event boundary (Phase Marker advance fires only at sub-spec drafting cycle SEAL at session containing final § sealed per PHASE2C_13/15 register precedent + session-1 through session-14 precedent + carry-forward through session-15+ until sub-spec drafting cycle SEAL register-event boundary);
- Cumulative pre-codification candidates accumulated across sub-spec drafting cycle sessions: advisor pre-SEAL verification gap declaration discipline + cross-reviewer DIVERGENCE adjudication with resolution-mechanism diversity + cross-reviewer COMPLEMENTARY pattern + verification-grounded REJECT/withdrawal pattern (with sub-claim-level adjudication mechanism diversity) + sealed-§ inheritance explicit-cite discipline + §-symmetric template 4-marker vocabulary extension AUTHORED + counting-precision discipline cross-session + descriptive register-class single-round-sufficient hypothesis cross-cluster validation + §6 cluster completion milestone + §7 verification register milestone + Symmetric substantive-adjudication capacity across reviewer legs + Sealed-§ family-pattern consolidation;
- Pre-codification candidate carry-forward discipline at sub-spec drafting cycle SEAL register: candidates above carry-forward to next methodology consolidation cycle at separate Charlie register-event boundary per anti-momentum-binding strict reading + [`feedback_authorization_routing.md`](.claude/projects/-Users-yutianyang-Documents-GitHub-btc-alpha-pipeline/memory/feedback_authorization_routing.md) hard rule inheritance at methodology consolidation cycle entry register (no bulk-codification at sub-spec drafting cycle SEAL register; codification register-class-distinct from sub-spec drafting cycle SEAL register; cumulative cross-session instance counts at non-sealed handoff brief register per session-N entry register-event boundary preserve audit-trail specificity outside sealed prose).

**Substantive prior justification (per D2.4 implicit cross-phase inheritance + cycle-terminus pass discipline rationale + METHODOLOGY_NOTES §17 sub-rule 4 anchor reference).** §7.c entry / scope / exit decomposition inherits cross-phase coherence from sealed METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus discipline + sealed §6 cluster cycle-terminus pass precedent at upstream register; cross-phase consistency at §7.c register is implicit inheritance per D2.4 lock — §7.c does NOT introduce explicit cross-phase consistency narrative discipline (§7.c self-creating coherence authority REJECTED at sealed-grounding precondition; same-pattern as sealed §5 D2.4 absorbed-into-NaN-handling-rule discipline + sealed §6 cluster D2.4 implicit cross-block coherence inheritance discipline).

Cycle-terminus pass discipline rationale at §7.c register: METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus pass discipline is the load-bearing cycle-end audit mechanism at multi-session cycle SEAL register; articulating entry / scope / exit decomposition at §7.c register-event boundary makes the discipline self-contained at sub-spec drafting cycle level vs reference-only at METHODOLOGY_NOTES upstream register; entry / scope / exit decomposition preserves load-bearing cycle-end audit mechanism integrity at sub-spec drafting cycle SEAL register-event boundary.

**Descriptive supplements — APPLICABLE** (entry / scope / exit decomposition articulated above).

**NaN-handling rule (INHERITS sealed §2-§6 cluster + §7.a + §7.b NaN-handling chain at §7.c terminal-review register per §7 umbrella NaN-handling rule inheritance).** §7.c does NOT introduce new NaN-handling semantics at §7.c register; upstream §7.a V# anchor pre-execution check fail state + §7.b reviewer pass routing suspension (per upstream §2-§6 cluster indeterminate / execution-suspended propagation chain) routes to §7.c cycle-terminus pass suspension at sub-spec drafting cycle SEAL register-event boundary per upstream propagation chain inheritance.

**Anti-circularity caveat (§7.c-specific axis binding; primary binding at (vii) §7 → Phase 5 SEAL author register non-pre-emption at cycle-terminus pass scope; other axes inherited from §7 umbrella 8-axis structure).**

- **(vii) §7.c → Phase 5 sub-spec drafting cycle SEAL author register non-pre-emption primary binding:** §7.c cycle-terminus pass discipline pre-registers entry / scope / exit decomposition at sub-spec drafting cycle SEAL register-event boundary; §7.c does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudications — specifically (a) cycle-terminus pass clean-pass adjudication (Charlie register taste call), (b) SEAL bundle authorization decision (Charlie register taste call per session-12-14 + session-15 Charlie-conditional pattern), (c) Phase Marker advance decision (Charlie register at sub-spec drafting cycle SEAL register-event boundary per PHASE2C_13/15 register precedent inheritance), (d) post-SEAL errata path engagement decision (Charlie register conditional per advisor pre-SEAL verification gap declaration discipline standing practice observation). SEAL author register authority preserved at sub-spec drafting cycle SEAL register-event boundary.
- **(i)(ii)(iii)(iv)(v)(vi)(viii) §7 umbrella 8-axis structure inherited at §7.c register without re-articulation;** §7.c binds (vii) primary at sub-spec drafting cycle SEAL author register cluster register; other axes bind across §7 verification register at §7 umbrella articulation level.

**Anti-pre-naming (parallel §7 umbrella structure at §7.c register).** §7.c does NOT pre-name §7.a V# anchor list content (§7.a register-class scope). §7.c does NOT pre-name §7.b reviewer routing protocol content (§7.b register-class scope). §7.c does NOT pre-name Phase 5 sub-spec drafting cycle SEAL author register adjudication content per (vii). §7.c does NOT pre-name session-N-specific cycle-terminus pass outcomes (Charlie register taste call at per-cycle register). §7.c register-class binds to cycle-terminus pass discipline at sub-spec drafting cycle SEAL register-event boundary; cycle-terminus scope outside this scope is outside §7.c's register-class.

**Input source per §1** (replaces "Data source" per §-symmetric applicability declaration per §7 umbrella + §7.a + §7.b precedent): §7.a V# anchor clean-pass at sub-spec SEAL pre-fire register + §7.b reviewer pass clean cycles at β dual-reviewer single-round register + sub-spec full prose at full-file prose-access register + sealed METHODOLOGY_NOTES §17 sub-rule 4 cycle-terminus pass discipline at upstream register + sealed §6 cluster cycle-terminus pass precedent at sessions 12-14 SEAL register.

No raw data consumption at §7.c register. §7.c cycle-terminus pass consumes sub-spec full prose + §7.a + §7.b clean-pass outputs at sub-spec drafting cycle SEAL register-event boundary before sub-spec ships to Phase 5 sub-spec drafting cycle SEAL author register consumption boundary.

**Register-class:** descriptive | procedural cycle-terminus pass discipline (no new quantitative thresholds, no admissibility logic, no measurement at §7.c register; consumes sub-spec full prose + §7.a + §7.b clean-pass outputs as upstream verification inputs) | procedural sub-cluster register-class within §7 verification register | downstream-consumer of §7.a + §7.b clean-pass outputs at sub-spec drafting cycle SEAL register-event boundary | inherits irreversible-interpretation constraints from §2.2 operationalization-freeze lock binding at §7.c SEAL register-event boundary.

**Phase 4 verdict invariance (sealed-invariance per §7 umbrella + §7.a + §7.b + §6 cluster + §2-§5 precedent).** §7.c output (cycle-terminus pass entry / scope / exit decomposition at sub-spec drafting cycle SEAL register-event boundary) does NOT modify the Phase 4 closeout verdict at 15bps; only pre-registers cycle-terminus pass discipline at §7.c sub-spec drafting cycle SEAL register-event boundary sub-cluster register-scope per §7 umbrella Phase 4 verdict invariance discipline inheritance.

**Framing-chain (3-direction per D3''''''^i Sub-decision D3.2 at §7.c register: 1 consolidated reverse-anchor inheritance from sealed §7.a + sealed §7.b + 2 outbound; §7.c is last sub-anchor per Option A 3-sub-anchor structure at session-15 entry — no §7.d outbound).**

- **§7.a + §7.b → §7.c (consolidated reverse-anchor):** §7.c cycle-terminus pass consumes §7.a V# anchor clean-pass + §7.b reviewer pass clean cycles + sub-spec full prose at sub-spec drafting cycle SEAL register-event boundary without retroactive modification (per (vii) primary binding inheritance + (vi) §6 cluster non-modification inheritance from §7 umbrella). Reverse-anchor consolidated at cross-§7 sub-anchor boundary per family-pattern with sealed §6.c D3.2 consolidated reverse-anchor pattern.
- **§7.c → §8 (outbound; cross-reference cross-section register):** §7.c cycle-terminus pass scope covers §8 at full-file prose-access register (per scope item 1); cycle-terminus pass discipline applies to §8 content at sub-spec drafting cycle SEAL register-event boundary per (vii) inheritance.
- **§7.c → Phase 5 sub-spec drafting cycle SEAL bundle (outbound; sub-spec SEAL register-event boundary):** §7.c cycle-terminus pass clean-pass = sub-spec drafting cycle SEAL register-event boundary fires at Phase 5 sub-spec drafting cycle SEAL author register consumption boundary per (vii) inheritance. Sub-spec ships to SEAL bundle at SEAL author register at sub-spec SEAL register-event boundary.

Inverse directions blocked: §7.c ← §7.a / §7.b per §7.a + §7.b sealed invariance at downstream sub-anchor SEAL register-event boundary (one-way §7.a + §7.b → §7.c forward inheritance); §7.c ← §8 per §8 register-class-distinct cross-reference register at sub-spec SEAL pre-fire register; §7.c ← Phase 5 sub-spec drafting cycle SEAL author register per sub-spec SEAL register-event boundary discipline.

**V# anchor (INHERITS §7.a per §7 umbrella AUTHORED marker concretization).** §7.c consumes §7.a V# anchor list at sub-spec drafting cycle SEAL register-event boundary (§7.a V# anchor clean-pass is entry condition for §7.c cycle-terminus pass); §7.c does NOT articulate V# anchor specifics at §7.c register per §7.a authorship inheritance.
