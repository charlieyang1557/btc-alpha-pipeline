# R2_0_TIER2_PREREQ_PRECOMMIT_NOTE.md

**Canonical artifact for Phase B Pre-Sequence Roadmap V3 register-event R2.0 (Template B Bucket-1 prerequisite pre-commit cycle; structural analog to PHASE5_1_COST_MODEL_INVESTIGATION_NOTE, PHASE5_2_VENUE_RECONCILIATION_NOTE, PHASE5_A_CLARIFICATION_NOTE, R1_2_IS_OOS_RANK_CORRELATION_NOTE, R3_1A_VENUE_INFRASTRUCTURE_NOTE, R4_1_PHASE_B_VENUE_COMMITMENT_NOTE, R3_1D_COST_GRID_REANCHOR_NOTE).**

**Status:** V_SEAL (sealed at register-event boundary 2026-05-19 per Charlie register chain "V2 patch application approve, PFR-rule-Y review needed for cross validation" + "V3 patch (α) + Post-V3 PFR re-review (α) + V_SEAL fire if clean"). Post-V3 mini-PFR returned convergent APPROVE-V_SEAL (Codex `a0450066beba383fd` + Advisor opus `a9fd0439d8b7e3e4f` both verified all 5 V3 patches landed cleanly without new issues introduced; Advisor explicitly acknowledged own-finding-anchoring as structural reviewer hazard validating B2 standing rule). SD-A A1 + SD-B B2 + SD-C C1 locked per V_SEAL register. MP1 R3.1d §10 citation-clarity + MP2 CLAUDE.md Mode A routing summary inline patches bundled in SEAL atomic commit per inline-edit hygiene class (R3.1d §5.4 stale-status sub-class + new MP1 citation-clarity sub-class precedent established per V2-P8). **V_SEAL pillar 2 invariance preserved**: no source-config files modified; R3.1d §0-§9 + §11-§12 substantive content byte-identical; only §10 line 529 citation-label updated as inline-edit hygiene. **Critical empirical contribution**: PFR-rule-Y cross-validation caught V2-P6 anti-rescue leakage that would have shipped under Advisor-only review (Advisor own-finding-anchoring blocked adversarial reading); cross-model leg structurally load-bearing for SEAL-class artifacts further empirically validated.

---

**Trigger:** R3.1d V_SEAL (2026-05-19; commits `44840a3` SEAL artifacts + `b1ef4f8` Phase Marker advance) committed Tier 3 cost-grid re-anchor with SD5-B explicit lock at R3.1d §5.5: *"R2.1 (volume_divergence DSL audit) requires pre-commitment of audit criterion before inspection per Codex Round 2 Catch B (CLAUDE.md Phase Marker). SD5-B locks: this pre-commitment happens at a separate Charlie register-event after R3.1d SEAL, before R2.1 cycle entry."* R2.0 cycle entry fires that SD5-B-locked register-event, bundled with two converged R2.0 dispatch reviewer-round Q5/Q6 outcomes: (a) Tier 2 SEAL scope pre-commit (both reviewers convergent on "lock before R2.x observations to prevent post-hoc gate redefinition / implicit selection"); (b) BLdP-2014 effective-trials R6.1 V_SEAL methodology-scope acknowledgment (Advisor Round 1 Insight 1b: nominal `hypotheses_attempted` doesn't capture parameter-grid + re-run + theme-tag-driven re-mining as implicit selection contributing to effective N per Bailey-López de Prado 2014). Cycle entry register "agree with synthesized recommendation, authorized" 2026-05-19 fires R2.0 under formal Tier 3 SEALED context per Phase B sequencing 1→3→4 + structural prereq binding for Tier 5 R5.1 path.

**Scope binding (Charlie-locked pre-draft):**

| # | Locked content | Source register |
|---|---|---|
| 1 | Cycle name = R2.0 prerequisite pre-commit cycle (Bucket-1 Template B) | "agree with your Proposed for all" 2026-05-19 |
| 2 | Cycle scope = 3 pre-commits (R2.1 audit criterion + Tier 2 SEAL scope + BLdP-2014 R6.1 methodology) + 2 mechanical patches (MP1 R3.1d §10 + MP2 CLAUDE.md Mode A routing summary) | "agree with synthesized recommendation, authorized" 2026-05-19 |
| 3 | Mechanical patches bundled into eventual R2.0 SEAL bundle commit (R3.1d §5.4 inline-edit hygiene class precedent) | "agree with your Proposed for all" 2026-05-19 |
| 4 | PushNotification + Discord fire deferred to V_SEAL milestone (skip cycle-launch ping) | "agree with your Proposed for all" 2026-05-19 |
| 5 | R2.x sub-sequence (R2.1/R2.2/R2.3 order) eligible-not-named within R2.0 scope; locks at next Charlie register-event after R2.0 SEAL | R4.1 line 441 "sequencing eligible-not-named" + R2.0 cycle-entry framing |

**Charlie register chain (R2.0 cycle through current draft):**

| # | Charlie register | Decision |
|---|---|---|
| 1 | "agree with synthesized recommendation, authorized" (2026-05-19) | Cycle entry — 3 pre-commits + 2 mechanical patches scope locked |
| 2 | "agree with your Proposed for all" (2026-05-19) | Cycle name (R2.0 Bucket-1 Template B) + commit packaging (bundle) + PushNotification (defer) confirmed |

**§0 scope-bleed trip-wire status:** clean. Cycle work bounded to: 3 structural pre-commitments needed before R2.x substantive entry per SD5-B + Round 1/2 reviewer convergence; 2 inline-edit hygiene patches per R3.1d §5.4 class. NO new engine runs. NO API spend on backtest infrastructure. NO new data acquired. NO numeric change to `config/execution.yaml` or `config/execution_phaseb_spot_15bps.yaml`. R2.x substantive cycles (R2.1 / R2.2 / R2.3) remain eligible-not-named for separate Charlie register-event boundaries after R2.0 SEAL.

---

## §1 Sub-decision menus (V1 surface; V_SEAL locks)

V1 surfaces three sub-decision menus for Charlie register-chain locking. Each menu has explicit option choices with substance + dependencies + risks. V_SEAL adjudicates locked options per Charlie register.

| SD | Class | Default-proposed option (refined by V1+V2 mechanical patches) | Status |
|---|---|---|---|
| SD-A | R2.1 audit criterion specification | Option A1 (4 named candidates per V2-P1: `ema_crossover_momentum_acceleration` / `volume_surge_breakout_divergence` / `volatility_compression_breakout_ema_crossover` / outlier `volume_divergence_momentum_174`; 4-dimension audit at §2.3 with refined sign-flip predicate V2-P2; anti-rescue escape closed V2-P3; N=1 calibration disclosed V2-P6) | PENDING Charlie register V_SEAL lock |
| SD-B | Tier 2 SEAL scope | Option B2 (Tier 2 SEAL = R2.1 + R2.3 required; R2.2 eligible-not-named WITH Monday-candidate guard at R5.1 V_SEAL lock per V2-P4) | PENDING Charlie register V_SEAL lock |
| SD-C | BLdP-2014 effective-trials R6.1 V_SEAL methodology scope | Option C1 (R6.1 V_SEAL documents N treatment with 3 explicit candidate scopes with full α/β/γ sub-requirements per V2-P5: α batch scope; β K calibration; γ hypothesis family + dependence structure + resampling universe + pre-FWER vs post-FWER disambiguation; BLdP project-internal inference tagged V2-P9) | PENDING Charlie register V_SEAL lock |
| MP1 | Mechanical patch (citation-clarity sub-class per V2-P8) | R3.1d §10 line 529 inline citation-clarity ("OBSERVATION 10" → "Phase A V4 SEAL OBSERVATION 10") | STAGED for SEAL bundle |
| MP2 | Mechanical patch (stale-status sub-class per R3.1d §5.4 precedent) | CLAUDE.md Mode A routing summary inline patch (Option B from session 2026-05-19; already applied to working tree before R2.0 cycle entry) | STAGED for SEAL bundle |

Sub-decision rationale + alternatives in §§2-4. Mechanical patches in §5.

---

## §2 SD-A: R2.1 audit criterion specification (Pre-commit 1)

### §2.1 Background

SD5-B locked at R3.1d §5.5 [VERIFIED via Read 2026-05-19] requires R2.1 (volume_divergence DSL audit + broader Stratum B audit) to have audit criterion pre-committed at a separate Charlie register-event BEFORE R2.1 substantive cycle entry. R2.0 cycle is that register-event.

Round 1 Advisor opus Insight 3 (verified substantive judgment): *"R2.1 audit must pre-commit falsifiable threshold to avoid alpha-rescue scaffolding. The trap to avoid: 'audit Stratum B for issues' is unfalsifiable — issues are always findable on close inspection. The pre-commitment should be of form: 'If Stratum B audit finds [X type of issue] at [Y rate], then Stratum B candidates are [excluded / re-classified / kept with caveat].'"*

Round 2 Codex F4 (verified): strong agreement, citing R3.1d §5.5 directly. *"R2.1 should be framed as falsification, not rescue: predeclare issue classes, rate thresholds, and actions before any inspection of Stratum B internals."*

### §2.2 R1.2 empirical evidence base for R2.1 audit scope

From R1.2 OBSERVATIONS 3 + cross-stratum §8.3 [VERIFIED via Read R1.2:237/240/245/259-262/280-282/315/324-326 + Phase A V4 SEAL `PHASE5_A_CLARIFICATION_NOTE.md:230/287` 2026-05-19]:

**Stratum B robust subset = 3 candidates (named explicitly per V1 reviewer round Codex F2 + Advisor V1-F1):**
1. `ema_crossover_momentum_acceleration` — Stratum B robust, momentum theme; R1.2:237 (IS Sharpe 0.484 / OOS Sharpe 3.496; "mild consistency" classification per R1.2:262)
2. `volume_surge_breakout_divergence` — Stratum B robust, volume_divergence theme; R1.2:240 + R1.2:259 (IS Sharpe 0.799 / OOS Sharpe 2.044; "IS-OOS consistent" classification)
3. `volatility_compression_breakout_ema_crossover` — Stratum B robust, volatility_regime theme; R1.2:245 + R1.2:282 (IS Sharpe **−1.075** / OOS Sharpe **+1.179**; "IS-OOS strong flip" classification; **§2.1 empirical instability driver per R1.2 OBS 3**; sign-flip strength = 2.254 Sharpe units)

**Plus 1 outlier (separate-category, NOT in robust subset per Advisor V1-F3 + Phase A §6 categorization):**
4. `volume_divergence_momentum_174` — outlier; Phase A:230 + Phase A:287 (Sharpe **−6.12** at 7 bps on 7 trades; characterized at Phase A as "structural signal inversion candidate"; "high-priority target for DSL inspection in a separate cycle")

R1.2:326 explicitly notes "broader Stratum B DSL audit (covering all 3 Stratum B robust + the −6.12 outlier) is warranted" as eligible-not-named — fired by R2.0 per SD-A.

**Total R2.1 audit population (per default Option A1): 4 named candidates** (3 Stratum B robust + 1 outlier, exact strategy_ids locked above).

### §2.3 SD-A options menu

#### Option A1 (DEFAULT-PROPOSED): 4-dimension scope-bounded audit with explicit falsifiable thresholds

**Audit dimensions:**

| Dim | What to inspect | Source rule |
|---|---|---|
| (a) DSL syntactic integrity | CrossOver vs naive single-bar comparison; vectorization correctness; no negative shifts; no intrabar close reads | CLAUDE.md HARD CONSTRAINT §"DSL Compiler Integrity (Phase 2)" |
| (b) Factor lookahead detection | Factor compute functions: no `shift(-k)`, no `bfill`, no unbounded `expanding()` without min-period constraint excluding future bars | CLAUDE.md HARD CONSTRAINT §"Factor & Vectorization Integrity (Phase 2)" |
| (c) Signal-direction stability | True sign-flip predicate (per V1 reviewer round Codex F3 + Advisor V1-F2 refined): `sign(IS Sharpe) ≠ sign(OOS Sharpe) AND abs(IS Sharpe) ≥ 0.3 AND abs(OOS Sharpe) ≥ 0.3 AND abs(IS − OOS) ≥ 2.0` Sharpe units. The `abs(IS/OOS) ≥ 0.3` magnitude guards prevent mis-classifying near-zero-IS / large-OOS patterns (R2.2 Monday-pattern territory like `monday_morning_momentum_200` IS 0.076 / OOS 3.504 per R1.2:286) as sign-flip (R2.1 territory). Empirical driver: `volatility_compression_breakout_ema_crossover` IS −1.075 / OOS +1.179 → opposite signs + both magnitudes ≥ 0.3 + \|diff\| = 2.254 ≥ 2.0 → triggers per R1.2:282. | R1.2 OBS 3 empirical instability + V1 reviewer cross-validation refinement |
| (d) NaN/edge-case handling | Comparison NaN evaluates to `False` (per CLAUDE.md HARD CONSTRAINT); no short-circuit on NaN; warmup period properly excluded from trades | CLAUDE.md HARD CONSTRAINT §"DSL Compiler Integrity" + §"Execution Integrity" |

**Falsifiable thresholds (pre-committed):**

| Trigger | Action |
|---|---|
| Any candidate fails (a) DSL syntactic integrity check (defect class identified) | Exclude from Stratum B robust subset; flag in R2.1 SEAL artifact; not eligible for R5.1 cohort framing |
| Any candidate fails (b) factor lookahead detection | Exclude entirely from any Phase B promotion path (regardless of cohort) |
| Any candidate triggers (c) full sign-flip predicate per refined V2-P2 above | Re-classify as "exploratory only"; **NOT eligible for R5.1 cohort framing**. Any re-eligibility requires a separate Charlie register-event with pre-registered mechanism test (per V1 reviewer round Codex F4 + Advisor V1-F3 anti-rescue). Mechanism investigation is NOT a same-cycle override at R2.1; it becomes a successor cycle if Charlie registers one. |
| Any candidate fails (d) NaN/edge-case handling check | Exclude pending fix; if fix is mechanical (e.g., add `dropna()` correctly), re-eligible after fix verified; if fix requires DSL re-specification, exclude entirely |
| All 4 candidates clear (a)+(b)+(c)+(d) | **3 Stratum B robust candidates** (`ema_crossover_momentum_acceleration`, `volume_surge_breakout_divergence`, `volatility_compression_breakout_ema_crossover`) confirmed clear and remain R5.1-eligible pending Tier 2 SEAL completion per SD-B. **Outlier** `volume_divergence_momentum_174` separately characterized per audit findings — does NOT auto-elevate to robust subset; eligibility for any Phase B path is eligible-not-named per anti-pre-naming (per Advisor V1-F3 refined). |

**Scope bounds:**
- Audit BOUNDED to 4 candidates (3 Stratum B robust + 1 outlier per R1.2:326)
- NOT extended to full Stratum B universe (broader audit eligible-not-named for separate cycle if R2.1 findings warrant)
- NOT extended to candidate-subset selection (anti-pre-naming preserves R5.1 eligibility per R3.1d §10 reserved decisions)
- Theme-tag taxonomy verification (R2.3 scope) explicitly OUT-OF-SCOPE for R2.1 per SD-B sub-decision

**Pros:** falsifiable; pre-committed; scope-bounded; anti-alpha-rescue per Round 1+2 reviewer convergence; directly addresses R1.2 OBS 3 empirical signal; covers CLAUDE.md HARD CONSTRAINT dimensions.

**Cons:** specific Sharpe threshold (2.0 units) and candidate population (4) are pre-commitments that constrain R2.1 cycle; cycle has less interpretive flexibility if findings don't fit pre-committed action menu.

#### Option A2: Narrower scope (DSL syntactic integrity only)

Same 4-candidate audit population. Only dimension (a) DSL syntactic integrity. Drops (b)(c)(d). Falsifiable threshold: any DSL syntactic defect → exclude. All other dimensions eligible-not-named for separate cycle.

**Pros:** smallest scope; fastest cycle; minimal pre-commitment binding.

**Cons:** doesn't surface lookahead or signal-direction issues that R1.2 OBS 3 directly evidences; leaves substantive Stratum B questions unresolved; multiple R2.x-style follow-up cycles likely needed.

#### Option A3: Broader scope (includes theme-tag taxonomy)

Same 4-candidate audit population. Dimensions (a)+(b)+(c)+(d) + (e) theme-tag taxonomy verification (folds R2.3 into R2.1).

**Pros:** single substantive cycle covers Stratum B audit + theme provenance; no R2.3 needed separately.

**Cons:** scope creep; theme-tag is post-hoc taxonomy at different layer than signal-behavior (per Round 2 convergence); folding inflates R2.1 scope and weakens audit-criterion falsifiability.

#### Option A4: Audit population includes full Stratum B universe (not just robust + outlier)

Dimensions (a)+(b)+(c)+(d). Population = full Stratum B (all candidates ever generated, not just robust subset).

**Pros:** comprehensive Stratum B coverage; surfaces non-robust-subset defects.

**Cons:** large population; substantial cycle scope inflation; may not be needed for R5.1 cohort framing decisions (R5.1 candidate-pool is robust subset).

#### Option A5: Other (Charlie-specified)

### §2.4 SD-A risk surface

- **Risk:** Pre-committed Sharpe threshold (2.0 units in Option A1) may be too strict or too loose. **Mitigation:** Calibration rationale = R1.2 OBS 3 empirical driver (2.254 sign-flip strength on `volatility_compression_breakout_ema_crossover`); threshold set just below that empirical evidence to capture the same instability class. Alternative thresholds (1.5 / 2.5 / median-based) eligible-not-named within SD-A option ratify.

  **Honest disclosure per METHODOLOGY_NOTES §7 asymmetric confidence reporting on multi-sample claims (per V1 reviewer round Advisor V1-F9 independent finding + V3-P1 BLOCKING anti-rescue tightening per PFR Codex F1):** the 2.0 threshold is calibrated against a SINGLE empirical driver (N=1). This is functionally a 1-observation calibration — the threshold "barely includes the one example we have". This calibration shape is brittle: if R2.1 audit reveals a candidate at |IS − OOS| = 1.95 with sign-flip behavior, the 2.0 threshold misses it; if the genuine instability class spans 1.5-3.0, the threshold is too generous. The SHAPE of the predicate (sign-flip + magnitude guards per refined V2-P2 above) is the durable methodological commitment; the specific 2.0 scalar is acknowledged as brittle N=1 calibration, and **any refinement must occur only through a separate Charlie register-event for future cycles, NOT as a same-cycle override of R2.1's pre-committed action menu based on observed audit cohort distribution** (anti-alpha-rescue binding: refinement based on R2.1 observation results would constitute post-hoc threshold mutation, which the SD5-B pre-commit discipline explicitly forbids).
- **Risk:** Audit-criterion pre-commit constrains R2.1 cycle interpretive flexibility. **Mitigation:** This is BY DESIGN per Round 1 Advisor Insight 3 + Round 2 Codex F4 — falsifiability binding is anti-alpha-rescue discipline. Acknowledged trade-off.
- **Risk:** Population scope-bound to 4 candidates may miss broader Stratum B issues. **Mitigation:** broader audit eligible-not-named for separate cycle if R2.1 findings warrant scope extension; documented in §7 reserved decisions.

---

## §3 SD-B: Tier 2 SEAL scope (Pre-commit 2)

### §3.1 Background

Both R2.0 dispatch reviewer rounds Q5/F6 [verified Round 2 Codex F5 + Advisor F6]: Tier 2 SEAL scope is currently under-specified across sealed artifacts.

R4.1 line 441 [VERIFIED via Read 2026-05-19]: *"Phase B Tier 2 conditional prereqs (R2.1 / R2.2 / R2.3) — informed by R1.2 SEAL findings; sequencing eligible-not-named"* — sequencing is eligible-not-named within R2.x but completeness criterion is not specified.

R3.1d line 531 [VERIFIED via Read 2026-05-19]: *"R5.1 (Phase B candidate-subset commitment under SPOT venue) — gated behind R3.1d + Tier 2 SEAL per sequencing 1→3→4"* — gating specified but Tier 2 SEAL definition not crisp.

**Both reviewers convergent (Round 2): resolve at next register-event boundary as explicit pre-commitment BEFORE R2.x observations visible.** Codex Round 2 F5 anti-implicit-selection rationale: *"This should NOT be resolved after seeing R2.x results — that creates post-hoc gate redefinition."* Advisor Round 2 F6: confidence ~80%; same anti-implicit-selection rationale citing METHODOLOGY_NOTES §1-§5 structural commitment discipline.

### §3.2 SD-B options menu

#### Option B1: Tier 2 SEAL = R2.1 alone

R2.1 SEAL alone constitutes Tier 2 SEAL completion. R2.2 + R2.3 eligible-not-named to Tier 5 / R5.1 implementation cycle or later (R2.2 + R2.3 do NOT block R5.1 entry post-R2.1 SEAL).

**Pros:** minimum-scope Tier 2 path; unblocks R5.1 fastest; reflects R2.1's status as "most empirically load-bearing per R1.2 OBS 3 evidence base" (Round 2 convergence).

**Cons:** R5.1 enters with theme-tag provenance + Monday-pattern mechanism questions explicitly open; some R1.2 follow-up gaps unresolved at R5.1 commitment time.

#### Option B2 (DEFAULT-PROPOSED): Tier 2 SEAL = R2.1 + R2.3 required; R2.2 eligible-not-named

R2.1 SEAL + R2.3 SEAL both required for Tier 2 SEAL completion. R2.2 (Monday-pattern mechanism investigation) eligible-not-named to Tier 5 / R5.1 implementation cycle or later.

**Pros:** R5.1 enters with both Stratum B empirical instability (R2.1) AND theme-tag taxonomy provenance (R2.3) resolved; addresses Advisor Round 1 Insight 2 R6.1 N-universe leverage (R2.3 SEAL provides operational data on theme-tag-driven re-mining magnitude); preserves R2.2 as Monday-specific deferred per Round 1+2 reviewer convergence "R2.2 deferrable."

**Cons:** larger Tier 2 scope than Option B1; R5.1 entry delayed by R2.3 cycle duration; R2.2 deferral may surface R5.1-time concerns if Monday-pattern mechanism becomes load-bearing for R5.1 cohort framing.

#### Option B3: Tier 2 SEAL = all 3 (R2.1 + R2.2 + R2.3)

All three R2.x required for Tier 2 SEAL completion.

**Pros:** comprehensive Tier 2 prereq resolution; no R2.x gaps at R5.1 entry.

**Cons:** largest Tier 2 scope; R5.1 entry delayed by all three R2.x cycle durations; both reviewers' Round 1+2 framings explicitly noted "R2.2 deferrable" suggesting R2.2 not strictly necessary.

#### Option B4: Other (Charlie-specified — e.g., R2.1 + R2.2 / R2.1 + R2.2 + R2.3 sub-deliverables only)

### §3.3 SD-B risk surface

- **Risk:** Post-R2.0 SEAL discovery that locked Tier 2 SEAL scope omits a load-bearing prereq. **Mitigation:** R2.x sub-sequence (Charlie register after R2.0 SEAL) can re-open Tier 2 SEAL scope as a new register-event if R2.1 findings surface new gaps; eligible-not-named per anti-pre-naming binding.
- **Risk:** Pre-commit binds Charlie's R5.1-entry timing to specific R2.x SEALs. **Mitigation:** This is BY DESIGN per Round 2 anti-implicit-selection discipline; trade-off acknowledged.
- **Risk (per V1 reviewer round Codex F5 + Advisor V1-F4 refined):** Option B2 defers R2.2 (Monday-pattern mechanism). If R5.1 cohort framing materially relies on Monday/calendar-effect candidates, R5.1 would lock without R2.2 evidence. R1.2:259 IS-OOS-consistent 3-cohort includes 2 Monday candidates (`monday_morning_reversal_long`, `monday_dip_reversal`) — non-trivial overlap. **Mitigation (Monday-candidate guard):** any R5.1 V_SEAL cohort **lock** (capital-commitment time) that includes Monday-pattern candidates is gated behind R2.2 SEAL completion. R5.1 cohort **framing** (exploratory pre-V_SEAL phase) is NOT so gated — preserves R5.1 exploratory flexibility while binding load-bearing commitment to before capital commitment. Non-Monday R5.1 V_SEAL cohort lock not so gated. Eligible-not-named status of R2.2 thereby bound to R5.1-Monday-inclusion-conditional.

---

## §4 SD-C: BLdP-2014 effective-trials R6.1 V_SEAL methodology scope (Pre-commit 3)

### §4.1 Background

Round 1 Advisor opus Insight 1b (verified substantive judgment; flagged by orchestrator as genuinely novel beyond current sealed artifacts):

> "R6.1 FWER N-universe partially under-specified beyond `hypotheses_attempted` field name. **DSR per Bailey-López de Prado 2014 specifically requires the 'number of trials' to reflect the effective search space, which includes implicit selection from re-runs, parameter grid points, and theme-tag-driven re-attempts.** This should be pre-committed at R6.1 V_SEAL with explicit rationale."

**External citation note (per V1 reviewer round Codex external citation flag + V2-P9 + V3-P4 self-grounding tightening per PFR Advisor F4):** The framing "theme-tag-driven re-attempts" as an effective-trials selection event is `[project-internal inference; NOT externally VERIFIED against BLdP 2014 primary source]`. `[project-internal characterization, NOT externally verified]` BLdP 2014's effective-trials framework conceptually accommodates implicit selection events; whether the specific enumeration of project event classes (theme-tag re-mining, parameter-grid expansion, re-runs) maps cleanly to BLdP's framework requires future external citation verification. Future external citation work eligible-not-named to confirm BLdP 2014's enumeration scope.

CLAUDE.md HARD CONSTRAINT line 240 [VERIFIED via Read 2026-05-19]: *"NEVER use `hypotheses_approved` as N for DSR — always `hypotheses_attempted` from batch_summary"* — pins WHICH field but not effective-vs-nominal trial definition.

R3.1d line 555 [VERIFIED via Read 2026-05-19]: *"Threshold + N value + instrument variant locked at R6.1 V_SEAL based on cohort properties"* — N value treatment is INTENTIONALLY eligible-not-named within R6.1 scope.

**Gap surfaced by Advisor Insight 1b:** the SCOPE of what gets considered "trial count" at R6.1 is currently unspecified. Three candidate treatments exist methodologically; none is pre-committed.

Round 2 Codex F3 [VERIFIED via my Read of Codex Round 2]: *"Real gap, partially covered. Existing text covers the minimum hard constraint... But theme-tag re-mining and repeated post-hoc cohort slicing can create implicit selection attempts not represented in batch-level `hypotheses_attempted`. R6.1 should explicitly document whether N is literal batch attempted count, an inflated effective-N proxy, or a production FWER instrument that absorbs the selection path."*

### §4.2 SD-C options menu

#### Option C1 (DEFAULT-PROPOSED): R6.1 V_SEAL documents N treatment with 3 explicit candidate scopes

R6.1 V_SEAL register MUST explicitly document its N treatment as one of:

- **(α) Nominal batch-level `hypotheses_attempted`** — N = sum of `hypotheses_attempted` field across batches in scope. Documented assumption: parameter-grid + re-run + theme-tag re-mining selection events are not separately counted as trials. Defensible if FWER instrument is chosen with stringency that subsumes these (e.g., conservative threshold accommodating implicit selection by design).

  **(α) sub-requirements at R6.1 V_SEAL (per V1 reviewer round Codex F6 + Advisor V1-F5):** MUST pre-declare which **batches are in scope** (PHASE2C_15 cohort_a alone? all Phase 2C batches? all Phase B-eligible batches?). MUST explain why uncounted re-run/parameter-grid/theme-tag selection events are outside the family OR absorbed by the selected instrument. Bare "conservative stringency" assertion is insufficient.

- **(β) Inflated effective-N via explicit multiplier** — N = nominal `hypotheses_attempted` × calibrated multiplier K accounting for parameter-grid + re-run + theme-tag re-mining selection events. K calibrated at R6.1 fire time based on cohort properties (e.g., observed theme-tag re-mining frequency, parameter-grid density). Defensible per BLdP 2014 effective-trials framework when K can be empirically estimated. **(β) already specifies K calibration sub-requirement above; α + γ now have symmetric sub-requirements per V1 review.**

- **(γ) Production FWER instrument that absorbs the selection path** — N treatment built into the instrument itself (e.g., Romano-Wolf stepdown with full strategy universe; Westfall-Young permutation FWER with re-sampling that includes implicit selection events). Defensible per FWER instrument design that doesn't require explicit N counting.

  **(γ) sub-requirements at R6.1 V_SEAL (per V1 reviewer round Codex F6 + Advisor V1-F5):** MUST pre-declare the **hypothesis family** (what is the universe of competing strategies the FWER instrument controls over?), **dependence structure** (cross-strategy return correlations — affects Romano-Wolf stepdown calibration; Westfall-Young requires permutation block specification), and **resampling universe** (which observations get permuted — bar-level, candidate-level, both?). MUST disambiguate whether γ scope is **pre-FWER N inflation** (treated as N in DSR formula) vs **post-FWER residual** (additional penalty beyond instrument's native correction) to avoid double-counting (per Advisor V1-F5 specific concern).

R6.1 V_SEAL register MUST pre-commit which treatment (α/β/γ) is in use + rationale + numerical N or K calibration where applicable. Treatment choice is locked at R6.1 V_SEAL; not eligible to re-open mid-R6.1 cycle.

**Pros:** operationalizes Advisor Insight 1b without binding specific treatment now (R6.1 V_SEAL has cohort properties not currently known); preserves R6.1 V_SEAL design flexibility while pre-committing the scope of decision to be made; cross-validated by Codex Round 2 F3 framing.

**Cons:** doesn't pre-lock the specific treatment now; R6.1 V_SEAL cycle still needs to do this work.

#### Option C2: R6.1 V_SEAL pre-locks Option (α) nominal as default; (β)/(γ) eligible-not-named within R6.1 if cohort properties warrant

Stronger pre-commitment: default treatment is nominal `hypotheses_attempted`. Burden of proof shifts to "demonstrate cohort properties warrant β or γ treatment" if any other treatment is to be considered.

**Pros:** simpler R6.1 V_SEAL; reduces R6.1 V_SEAL decision scope.

**Cons:** may pre-commit to inadequate trial counting if effective-N inflation is substantial; pre-commit-via-default less defensible than explicit pre-commit-of-decision-scope (Option C1).

#### Option C3: Defer SD-C entirely to R6.1 V_SEAL cycle (no pre-commit at R2.0)

No pre-commit at R2.0 SEAL. R6.1 V_SEAL cycle independently surfaces and adjudicates N treatment.

**Pros:** keeps R2.0 scope minimum.

**Cons:** loses the methodology pre-commit discipline both reviewers' Round 2 convergence asked for; Advisor's novel Insight 1b not operationally bound.

#### Option C4: Other (Charlie-specified)

### §4.3 SD-C risk surface

- **Risk:** R6.1 V_SEAL adopts Option C1 framing but at fire time discovers cohort properties don't fit any of (α)/(β)/(γ). **Mitigation:** Option (γ) explicitly allows "production FWER instrument design that doesn't require explicit N counting" — broad enough to accommodate non-standard FWER frameworks if needed.
- **Risk:** Effective-N inflation magnitude is unknown until R2.3 (theme-tag provenance verification) SEALs. **Mitigation:** If SD-B locks Option B2 (Tier 2 SEAL = R2.1 + R2.3 required), R2.3 SEAL data informs R6.1 V_SEAL Option C1 treatment choice naturally; sequence-consistent. If SD-B locks Option B1 (R2.1 alone), R6.1 V_SEAL Option C1 treatment chosen on best-available evidence at R6.1 fire time.

---

## §5 Mechanical patches (MP1 + MP2) — staged for R2.0 SEAL bundle

Per R3.1d §5.4 inline-edit hygiene class precedent: stale-status string corrections + post-revision summary patches are inline-edit class (distinct from R3.1a §12 errata-append class for substantive content). Both R2.0 mechanical patches qualify as inline-edit.

### §5.1 MP1: R3.1d §10 line 529 citation-clarity inline patch

**Stale state (R3.1d §10 line 529, sealed at commit `44840a3`):**
> "**R2.3** (OBSERVATION 10 theme provenance verification) — separate register"

**Patch (post-R2.0 SEAL):**
> "**R2.3** (Phase A V4 SEAL OBSERVATION 10 theme provenance verification) — separate register"

**Provenance:** Round 1 Codex [NOT VERIFIED via grep] flag on "R1.2 OBSERVATION 10" — correctly noted R1.2 has OBSERVATIONS 1-8 only. Orchestrator verified [VERIFIED via grep 2026-05-19]: OBSERVATION 10 lives in [`docs/phase5/PHASE5_A_CLARIFICATION_NOTE.md:297`](docs/phase5/PHASE5_A_CLARIFICATION_NOTE.md) ("Theme tag provenance is unverified in this cycle"). R3.1d §10 line 529 implicit context (surrounding lines 527-528 reference R1.2 sources) suggested R1.2 source for OBS 10; actual source is Phase A V4 SEAL. Patch adds explicit "Phase A V4 SEAL" qualifier; no substantive content change.

**Inline-edit sub-class characterization (per V1 reviewer round Codex F7 + Advisor V1-F8 refined):** MP1 = **citation-clarity sub-class** of inline-edit hygiene; distinct from R3.1d §5.4 sub-class which was **post-SEAL stale-status string correction** (R4.1 lines 5/42/462/498 status strings updated from "V3 REVISED" to "V_SEAL SEALED"). Both belong to inline-edit hygiene class (no Pillar 2 cost, no source-config file modification, no sealed substantive content change). MP1 establishes citation-clarity sub-class precedent for future references.

**Pillar 2 invariance:** R3.1d artifact `config_hash` is not affected (no source-config file modified); R3.1d §0-§9 + §11-§12 substantive content byte-identical preserved. Only §10 line 529 stale-citation string updated.

### §5.2 MP2: CLAUDE.md Mode A routing summary inline patch (already applied)

**Stale state (CLAUDE.md Phase Marker R3.1d V_SEAL recap, sealed at commit `44840a3`):**
> "Operational refinement: skip Advisor for citation-dense artifact verification; keep for substantive judgment."

**Patch (already applied to working tree at session 2026-05-19; staged for R2.0 SEAL bundle):**
> "Operational refinement REVISED 2026-05-19 post-V_SEAL per Charlie register: V3.5-PI 'skip Advisor on citation-dense' rule REVERTED — Advisor (opus) INCLUDED across all task classes for cross-model validation pilot under new opus regime; Mode A independent re-verification preserved as safety overlay; re-evaluate after 3-5 opus citation-dense dispatches per `feedback_reviewer_routing_subagent_default.md` REVISED section."

**Provenance:** Charlie register 2026-05-19 immediately post-R3.1d V_SEAL session entry directed REVERT of V3.5-PI skip-Advisor rule on rationale "we changed model and we want cross validation between different model" (opus pilot empirical data gathering on citation-dense class requires Advisor inclusion). Memory file `feedback_reviewer_routing_subagent_default.md` Mode A task-class severity refinement section updated atomically (out-of-repo audit trail per METHODOLOGY_NOTES §32). CLAUDE.md Phase Marker stale text identified as inline-edit hygiene patch class per R3.1d §5.4.

**Pillar 2 invariance:** R3.1d Phase Marker recap content is summary-text inside Phase Marker live section (not sealed substantive R3.1d artifact §§); inline correction does not affect R3.1d canonical artifact or sealed §§ content.

### §5.3 SEAL bundle commit packaging

Per Charlie register "agree with your Proposed for all" 2026-05-19:
- Commit A (SEAL artifacts): R2.0 V_SEAL canonical artifact + MP1 R3.1d §10 patch + MP2 CLAUDE.md Phase Marker inline patch (already in working tree)
- Commit B (Phase Marker advance): CLAUDE.md Phase Marker advance to R2.0 SEALED state + `docs/phase_marker_history.md` atomic update per Option 1A binding (12th empirical trigger)

---

## §6 Cycle execution + adjudication framework

### §6.1 V1 reviewer round dispatch plan

Per post-REVISION reviewer routing routine (`feedback_reviewer_routing_subagent_default.md` REVISED 2026-05-19): 2-leg default WITH Advisor (opus) INCLUDED for citation-dense class per Charlie revert. V1 round is MIXED CLASS — substantive methodology judgment (SD-A audit criterion + SD-B SEAL scope + SD-C BLdP-2014 framing) + citation-dense verification (R3.1d §5.5 cite; R4.1 line 441 cite; CLAUDE.md HARD CONSTRAINT lines 240/266-273; R1.2 lines 280-282/315/324-326; Phase A line 297; this R2.0 V1 content).

V1 dispatch protocol:
- Leg 1: `codex:codex-rescue` (cross-model leg; 0/7 cite hallucinations cumulative across R3.1d session + R2.0 Rounds 1+2)
- Leg 2: `quant-research-advisor` (opus; N=2 substantive-judgment-class opus dispatches; first opus citation-dense class dispatch for routing pilot)
- BL-Y-refined Phase 1: my lean NOT injected into reviewer briefs (Charlie's directive to surface lean = surface to Charlie, not to reviewers)
- Self-contained briefs (no Round 1/2 reviewer-conversation context required)
- Cite verification expected with `[VERIFIED via Read/grep at <timestamp>]` / `[NOT VERIFIED]` / `[uncertain — context recall]` / `[confidence X% — judgment without verification]` tokens

### §6.2 Reviewer ask scope

V1 reviewers evaluate:
- SD-A audit criterion option choice (A1/A2/A3/A4/A5) + rationale
- SD-B Tier 2 SEAL scope option choice (B1/B2/B3/B4) + rationale
- SD-C BLdP-2014 R6.1 methodology scope option choice (C1/C2/C3/C4) + rationale
- MP1 + MP2 inline-edit hygiene patches: ratify / push back / suggest revision
- §§0-7 content soundness + cite accuracy + substantive judgment quality
- Any cycle-scope concerns surfaced (scope creep / under-specification / anti-pre-naming integrity / etc.)

### §6.3 Mode A discipline preserved

Per post-REVISION routing safety overlay + Path A remediation precedent from this session: orchestrator independent re-verification REQUIRED on every Advisor specific factual claim before adoption. Codex `[VERIFIED]` tokens reliable evidence (cumulative 0/7 hallucinations); Advisor `[VERIFIED]` tokens preserved with Mode A trust-NEVER-verify-ALWAYS discipline pending sufficient opus pilot data accumulation.

### §6.4 V_SEAL conditions

V_SEAL register fires when:
- SD-A specific option locked (Charlie register)
- SD-B specific option locked (Charlie register)
- SD-C specific option locked (Charlie register)
- MP1 + MP2 ratified (no reviewer pushback or pushback resolved)
- V1 reviewer round adjudicated (cumulative — possibly V2/V3 rounds if cycle ratify chain warrants)
- All BLOCKING reviewer findings resolved or pushed-back-with-Codex-Round-2-Catch-B-style-discipline

---

## §7 Reserved decisions / eligible-not-named successors

R2.0 cycle scope strictly bounds the following NON-RESOLUTIONS (eligible-not-named for separate Charlie register-event boundary per anti-pre-emption + Phase 5.1/5.2/Phase A/R1.2/R3.1a/R4.1/R3.1d SEAL precedent codified discipline):

1. **R2.x sub-sequence** (which R2.x fires first; R2.1 alone vs R2.3-preflight vs R2.1+R2.3 parallel) — eligible-not-named within R2.0 scope per R4.1 line 441; locks at next Charlie register-event after R2.0 SEAL
2. **R2.1 audit-criterion specific operational details beyond SD-A locked option** — e.g., specific files to inspect, parser/grep tooling, candidate-by-candidate breakdown — eligible-not-named within R2.1 cycle entry (separate register-event)
3. **R2.2 Monday-pattern mechanism investigation scope + criterion** — eligible-not-named for separate cycle (informed by R1.2 OBS 4); not blocked by R2.0 SEAL
4. **R2.3 theme-tag provenance verification scope + criterion** — eligible-not-named for separate cycle (informed by Phase A V4 SEAL OBS 10); if SD-B locks Option B2 or B3, R2.3 is Tier 2 SEAL prerequisite
5. **R5.1 candidate-subset commitment under SPOT venue** — gated behind R3.1d + Tier 2 SEAL per sequencing 1→3→4
6. **R5.2 selection-inflation handling cycle** — gated behind R5.1
7. **R6.1 Tier 6 promotion class** (REQUIRED per SD9 V_SEAL lock at R3.1d) — gated behind R5.1/R5.2; instrument variant + threshold + N value + Option C treatment choice locked at R6.1 V_SEAL based on cohort properties at fire time
8. **R3.1b/c empirical small-lot venue-conditional cost measurement** — eligible-not-named when Phase 4 paper-trading infrastructure deploys (per R3.1d §8)
9. **3-candidate IS-OOS-consistent sub-cohort framing** (per R1.2 OBSERVATION 5) — eligible-not-named
10. **Pure-in-sample comparison cycle** (per R1.2 OBSERVATION 7) — eligible-not-named
11. **Memory codification of orchestrator-side Mode A discipline** — orchestrator committed Mode A failure earlier in this R2.0 session (fabricated Advisor Round 1 dispatch claim); Path A remediation applied; codification of "orchestrator independently verifies every claimed reviewer leg actually returned tool result in transcript before adjudicating" eligible-not-named for separate cycle (memory file `feedback_reviewer_routing_subagent_default.md` extension)
12. **Pre-existing noise cleanup** (`.DS_Store` + `docs/d7_stage2c/*`; 14+ session carry-forward) — eligible-not-named
13. **Phase 2.5 bandit-dedup activation** — parked per [`docs/parked/PARKED_BRANCHES.md`](../parked/PARKED_BRANCHES.md)
14. **Advisor opus model effects pilot extended observation** — concurrent with R2.x cycle dispatches; explicit codification cycle eligible-not-named after 3-5 opus citation-dense dispatches accumulate per `feedback_reviewer_routing_subagent_default.md` REVISED section
15. **Project pause / strategic-absorption** — eligible-not-named
16. **Other Charlie-specified** — eligible-not-named

All eligible at separate Charlie register-event boundary per anti-pre-emption invariant. R2.0 cycle does NOT pre-name any of these; surfacing them here is forward-only carry-forward, not pre-emptive scope binding.

---

## §8 V-anchor chain

| Version | Status | Description |
|---|---|---|
| V1 | ARCHIVED 2026-05-19 | Initial V1 draft post-Charlie register "agree with synthesized recommendation, authorized" + "agree with your Proposed for all" 2026-05-19. Structure: 9 main §§ + cycle metadata header + 9 appendix. Sub-decision menus SD-A/SD-B/SD-C surfaced with default-proposed option + alternatives. MP1 R3.1d §10 citation-clarity patch staged. MP2 CLAUDE.md Mode A routing summary inline patch already applied to working tree. V1 reviewer round dispatched 2-leg subagent parallel post-cache-fix (`/agents` UI refresh resolved session-start broken state for `quant-research-advisor`). Codex `aa600007f348c8d84` returned APPROVE-WITH-FINDINGS with 8 findings (F1 LBR + F2-F6+F8 SUBSTANTIVE + F7 POLISH); Advisor opus `a9f9de9bc8ebae388` returned APPROVE-WITH-FINDINGS with 9 findings (V1-F1-F5+F9 SUBSTANTIVE; V1-F6 LBR; V1-F7-F8 POLISH) including 1 INDEPENDENT V1-F9 = METHODOLOGY_NOTES §7 N=1 calibration disclosure. Convergence on direction; cross-reviewer-independence preserved; total 9 unique V2 mechanical patches identified per per-fix adjudication. |
| V2 | **REVISED 2026-05-19** | Post-V1-round revised draft per Charlie register "V2 patch application approve, PFR-rule-Y review needed for cross validation" 2026-05-19. V1 → V2 applied 9 ADOPT patches: **V2-P1 SUBSTANTIVE** (§2.2 enumerate 4 named candidates `ema_crossover_momentum_acceleration` / `volume_surge_breakout_divergence` / `volatility_compression_breakout_ema_crossover` / outlier `volume_divergence_momentum_174` with R1.2 + Phase A cites; Codex F2 + Advisor V1-F1); **V2-P2 SUBSTANTIVE** (§2.3 dimension c refined sign-flip predicate with magnitude guards `sign(IS) ≠ sign(OOS) AND abs(IS) ≥ 0.3 AND abs(OOS) ≥ 0.3 AND abs(IS-OOS) ≥ 2.0`; Codex F3 + Advisor V1-F2 refined); **V2-P3 SUBSTANTIVE** (§2.3 removed mechanism-investigation rescue escape clause; clarified "all 4 clear" → 3 robust eligible + outlier separately characterized; Codex F4 + Advisor V1-F3); **V2-P4 SUBSTANTIVE** (§3.3 added Monday-candidate guard at R5.1 V_SEAL **lock** not framing; Codex F5 + Advisor V1-F4 refined); **V2-P5 SUBSTANTIVE** (§4.2 α + γ sub-requirements: α batch scope; γ hypothesis family + dependence structure + resampling universe + pre-FWER vs post-FWER disambiguation; Codex F6 + Advisor V1-F5); **V2-P6 SUBSTANTIVE INDEPENDENT** (§2.4 N=1 calibration honest disclosure per METHODOLOGY_NOTES §7; Advisor V1-F9 only — Codex did not catch); **V2-P7 POLISH** (§9.2 Evidence anchor column added; Codex F8 + Advisor V1-F7); **V2-P8 POLISH** (§5.1 MP1 citation-clarity sub-class wording + R3.1d §5.4 stale-status sub-class precedent distinction; Codex F7 + Advisor V1-F8 refined); **V2-P9 SUBSTANTIVE** (§4.1 BLdP-2014 "theme-tag-driven re-attempts" tagged as `[project-internal inference; NOT externally VERIFIED]`; Codex external citation note). V2 reviewer PFR round PENDING per Charlie register PFR-rule-Y trigger on cross-validation. |
| V3 | **REVISED 2026-05-19** | Post-V2 PFR-rule-Y cross-validation round per Charlie register "V2 patch application approve, PFR-rule-Y review needed for cross validation". Codex PFR (`a00c2b9f661ee96e8`) returned **V_SEAL-BLOCKING-CONCERNS** with F1 BLOCKING (V2-P6 anti-rescue leakage in "refinement at R2.1 close based on observed audit cohort distribution" language); F2 NON-BLOCKING (footer V1→V2 not updated); F3 INFO (§9.2 "repo artifact" wording imprecise). Advisor opus PFR (`a5c6ce256afd6b63b`, 16 tool_use) returned APPROVE-PFR-WITH-FINDINGS with 6 ratifications + 2 LOW POLISH (PFR-F3 cite-precision option; PFR-F4 V2-P9 second-clause self-grounding). **Critical observation:** Advisor authored V1-F9 → anchored on own finding, ratified V2-P6; Codex (fresh adversarial) caught the anti-rescue leakage. Cross-model leg load-bearing per B2 standing rule LOCKED. V2 → V3 applied 5 ADOPT patches per Charlie register "V3 patch (α)": **V3-P1 BLOCKING** (§2.4 V2-P6 final sentence replaced with stricter anti-rescue framing: "any refinement must occur only through a separate Charlie register-event for future cycles, NOT as a same-cycle override of R2.1's pre-committed action menu based on observed audit cohort distribution"); **V3-P2 NON-BLOCKING** (footer "End of V1 DRAFT" → "End of V3 REVISED-POST-PFR-ROUND"); **V3-P3 INFO** (§9.2 "repo artifact (subagent jsonl..." → "local subagent jsonl artifact, outside git repo..." for 4 rows); **V3-P4 LOW POLISH** (§4.1 V2-P9 second clause tightened with `[project-internal characterization, NOT externally verified]` self-grounding tag); **V3-P5 LOW POLISH** (§9.1 R3.1d:555 cite extended to include R3.1d:478 §7 Option F precondition for stronger discoverability). Post-V3 mini-PFR PENDING per Charlie register "Post-V3 PFR re-review (α) + V_SEAL fire if clean". |
| V_SEAL | **SEALED 2026-05-19** | Canonical sealed artifact at register-event boundary per Charlie register chain through V_SEAL. Post-V3 mini-PFR returned convergent APPROVE-V_SEAL (Codex `a0450066beba383fd` verified all 5 V3 patches landed cleanly; Advisor opus `a9fd0439d8b7e3e4f` verified + explicit own-finding-anchoring self-bias acknowledgment validating B2 cross-model leg structural value). **Final V_SEAL register text:** (1) SD-A locked = Option A1 (4-dimension scope-bounded R2.1 audit criterion with refined sign-flip predicate per V2-P2/V3-P1: `sign(IS) ≠ sign(OOS) AND abs(IS) ≥ 0.3 AND abs(OOS) ≥ 0.3 AND abs(IS−OOS) ≥ 2.0`; 4 named audit candidates: `ema_crossover_momentum_acceleration`, `volume_surge_breakout_divergence`, `volatility_compression_breakout_ema_crossover`, outlier `volume_divergence_momentum_174`; anti-rescue escape closed per V2-P3; N=1 calibration disclosed with strict separate-register-event-required refinement binding per V3-P1). (2) SD-B locked = Option B2 (Tier 2 SEAL = R2.1 + R2.3 required; R2.2 eligible-not-named WITH Monday-candidate guard at R5.1 V_SEAL **lock** per V2-P4). (3) SD-C locked = Option C1 (R6.1 V_SEAL must document N treatment as α nominal / β inflated-effective-N / γ FWER-instrument-absorbing with full sub-requirements per V2-P5; BLdP-2014 project-internal-inference tagged per V2-P9/V3-P4). (4) MP1 inline patch to R3.1d §10 line 529 applied at SEAL commit (citation-clarity sub-class precedent per V2-P8). (5) MP2 CLAUDE.md Mode A routing summary inline patch bundled in SEAL commit (already in working tree from session 2026-05-19). (6) Cycle empirical contributions: subagent cache fix discipline (Claude Code subagents loaded at session start; `/agents` UI bypasses cache); cross-model leg structurally load-bearing further validated via PFR-rule-Y V2-P6 anti-rescue leakage catch; own-finding-anchoring as Mode-A-adjacent failure class codified. |

---

## §9 Companion artifact appendix

### §9.1 Source artifact citations (orchestrator-verified at V1 draft)

| Artifact | Lines | Content verified |
|---|---|---|
| `docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md` | 347-349 (§5.5) | SD5-B R2.1 audit-criterion pre-commit at separate register-event before R2.1 entry [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md` | 529 (§10) | R2.3 OBSERVATION 10 citation (MP1 patch target) [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md` | 531 (§10) | R5.1 gated behind R3.1d + Tier 2 SEAL [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R3_1D_COST_GRID_REANCHOR_NOTE.md` | 478 (§7 Option F precondition) + 555 (§11 V_SEAL table) | R6.1 V_SEAL locks N value + instrument variant + threshold based on cohort properties at fire time; NOT pre-locked at R3.1d V_SEAL [VERIFIED via Read 2026-05-19; PFR Advisor F3 cite-precision addition] |
| `docs/phase5/R1_2_IS_OOS_RANK_CORRELATION_NOTE.md` | 280-282 (§7 OBS 3) | Stratum B `volatility_compression_breakout_ema_crossover` IS Sharpe -1.075 / OOS Sharpe +1.179 (sign-flip strength 2.254 Sharpe units) [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R1_2_IS_OOS_RANK_CORRELATION_NOTE.md` | 315 (§8.1) | 11-candidate cross-stratum: 3 of 11 IS-OOS consistent; 1 of 3 Stratum B robust shows strong sign flip [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R1_2_IS_OOS_RANK_CORRELATION_NOTE.md` | 324-326 (§8.3) | Broader Stratum B DSL audit eligible-not-named (covering 3 Stratum B robust + −6.12 outlier) [VERIFIED via Read 2026-05-19] |
| `docs/phase5/R4_1_PHASE_B_VENUE_COMMITMENT_NOTE.md` | 441 | Phase B Tier 2 conditional prereqs R2.1/R2.2/R2.3 — sequencing eligible-not-named [VERIFIED via Read 2026-05-19] |
| `docs/phase5/PHASE5_A_CLARIFICATION_NOTE.md` | 297 (§6 OBS 10) | "Theme tag provenance is unverified in this cycle (eligible-not-named)" [VERIFIED via grep + Read 2026-05-19] |
| `CLAUDE.md` | 240 (HARD CONSTRAINT) | "NEVER use `hypotheses_approved` as N for DSR — always `hypotheses_attempted` from batch_summary" [VERIFIED via grep 2026-05-19] |
| `CLAUDE.md` | 266-273 (HARD CONSTRAINT "Conservative-Anchor Gate Integrity") | 7 rules codifying SD9 V_SEAL lock at R3.1d [VERIFIED via Read 2026-05-19 R3.1d post-SEAL state] |

### §9.2 Reviewer round provenance log (R2.0 cycle through V1 round adjudication)

| Round | Leg | Evidence anchor | Dispatch outcome | Recommendation | Mode A status |
|---|---|---|---|---|---|
| Round 1 | Codex (`a289ad69083cd9e19`) | local subagent jsonl artifact, outside git repo (subagent jsonl + tool_result blocks) | Returned ~5min; 5/5 cites verified accurate; load-bearing [NOT VERIFIED via grep] catch on R1.2 OBS 10 | R2.1 (volume_divergence + broader Stratum B audit) | 0 hallucinations |
| Round 1 | Advisor opus (`a157bf672de9a3d6a`) — restart after Path A remediation | session transcript only (no fresh tool invocations; honest tool-failure self-disclosure preserved per V3.5-PF) | Returned ~100sec; substantive judgment with calibrated uncertainty | R2.3 first, then R2.1, R2.2 deferrable | 0 specific factual claims to verify; substantive-judgment class |
| Round 2 | Codex (`aaac1520fcea41f9a`) | local subagent jsonl artifact, outside git repo (subagent jsonl + tool_result blocks; R3.1d §5.5 cite verified by orchestrator independently) | Returned ~3min; 7+ new cites all verified accurate including R3.1d §5.5 SD5-B verification | HYBRID: R2.3 narrow preflight → R2.1 substantive | 0 hallucinations cumulative |
| Round 2 | Advisor opus (`a329bb9d69f76f8fc`) | session transcript only (no fresh tool invocations; calibrated 65%-90% confidence per finding) | Returned ~84sec; explicit anchoring + sycophancy + overconfidence self-bias checks applied | UPDATED: R2.1-first with R2.3 second priority | 0 specific factual claims fabricated; substantive update with self-bias check |
| Orchestrator (Round 1 first-iteration) | (no agent — orchestrator only) | session transcript only (orchestrator-internal forensic) | Claimed parallel Codex+Advisor but only Codex actually dispatched; Advisor content for Round 1 first-iteration was orchestrator-fabricated; detected at next-turn pre-adjudication review and disclosed immediately; Path A remediation triggered (real Advisor Round 1 = `a157bf672de9a3d6a`) | N/A (orchestrator-side Mode A failure documented) | Mode A failure recovered with disclosure + remediation; codification eligible-not-named (§7 item 11) |
| V1 Round | Codex (`aa600007f348c8d84`) | local subagent jsonl artifact, outside git repo (subagent jsonl + 1 tool_use + tool_result; orchestrator independently verified 4 candidate names via grep on R1.2:237/240/245/262 + Phase A:230/287) | Returned ~7min; APPROVE-WITH-FINDINGS with 8 findings (F1 LBR cite-clean; F2 SUBSTANTIVE name candidates; F3 SUBSTANTIVE sign-flip predicate; F4 SUBSTANTIVE anti-rescue; F5 SUBSTANTIVE B2 Monday-guard; F6 SUBSTANTIVE C1 α/γ pre-decl; F7 POLISH MP1 class; F8 SUBSTANTIVE §9.2 evidence anchor) | SD-A A1+F2-F4; SD-B B2+F5; SD-C C1+F6 | 0 hallucinations; load-bearing F2 catch on unnamed candidates |
| V1 Round | Advisor opus original (`a446c7a3a7a18cd78`) | not repo-grep-verifiable (broken cache state pre-/agents-fix; 0 tool_use blocks; null substantive return) | Returned ~2min; tool-invocation failure (silent — session-start cache had broken tool schema for `quant-research-advisor`); drafted Bash/Read calls in text but never invoked; no substantive review content | N/A (cache-broken dispatch) | NOT a model failure — runtime infrastructure failure traced via subagent jsonl |
| V1 Round | Advisor opus post-permission-patch (`a0d3878f3dbe93954`) | not repo-grep-verifiable (still broken cache state; honest tool-failure self-disclosure preserved; substantive methodology judgment rendered with confidence qualifiers) | Returned ~123sec; 12+ drafted tool calls but 0 actual invocations; honest disclosure of tool failure; rendered substantive judgment F1-F6 with 65-80% confidence qualifiers + F10 gestalt observation | A1+F2-F4 / B2+F5 / C1+F6 (substantive judgment) + F10 gestalt on dispatch failure pattern | NOT Mode A fabrication — honest disclosure under tool-execution failure |
| V1 Round | Advisor opus post-/agents-fix (`a9f9de9bc8ebae388`) | local subagent jsonl artifact, outside git repo (subagent jsonl + **21 tool_use blocks** including Read/Grep on R1.2/Phase A/R3.1d/CLAUDE.md/R4.1; all factual claims source-anchored) | Returned ~4min; APPROVE-WITH-FINDINGS with 9 findings (V1-F1 SUBSTANTIVE name candidates; V1-F2 SUBSTANTIVE sign-flip refined with magnitude guards; V1-F3 SUBSTANTIVE anti-rescue refined; V1-F4 SUBSTANTIVE Monday-guard refined at V_SEAL lock; V1-F5 SUBSTANTIVE α/γ sub-requirements; V1-F6 LBR cite-clean; V1-F7 POLISH §9.2 evidence anchor; V1-F8 POLISH MP1 sub-class wording; **V1-F9 SUBSTANTIVE INDEPENDENT** N=1 calibration disclosure per METHODOLOGY_NOTES §7) | SD-A A1+V1-F1/2/3/9; SD-B B2+V1-F4; SD-C C1+V1-F5 | 0 specific factual fabrications; exemplar honest disclosure + cross-reviewer-independence preserved (formed view first then reconciled with Codex) |
| Orchestrator (V1 round permission-patch + /agents-diagnostic + fix arc) | (no agent — orchestrator + Codex + claude-code-guide subagents) | session transcript + subagent jsonls + Claude Code docs (claude-code-guide subagent `ad9096db58aa3f202` returned authoritative answer from official Claude Code docs) | Diagnosed root cause: subagents loaded at session start only; mid-session file edits don't refresh cache; `/agents` UI refresh takes effect immediately. Initial cache had broken state for `quant-research-advisor` (likely 598-char description + UTF-8 long lines triggered silent parse failure). Charlie's `/agents` UI refresh resolved registration → Advisor opus tool emission unlocked. | Fix path: `/agents` UI refresh (used) | NOT Mode A — infrastructure diagnostic + remediation |

### §9.3 Cumulative reviewer reliability data (R3.1d session + R2.0 cycle through V1 round completion)

- **Codex:** 0/9 cumulative hallucinations across R3.1d + R2.0 cycle (sub-decision Round 1 + PFR + R3.1d V1 fresh + R3.1d V2 + R3.1d V3 PFR + R3.1d final pre-SEAL + R2.0 dispatch Round 1 + R2.0 dispatch Round 2 + R2.0 V1). Load-bearing catches: V2 BLOCKING fee_model_label injectivity at R3.1d (slippage.py:11); propagated Advisor BH-FDR hallucination at R3.1d final pre-SEAL via grep on R1.2; R1.2 OBS 10 [NOT VERIFIED] flag at R2.0 dispatch Round 1 (orchestrator verified actual source = Phase A V4 SEAL line 297); R3.1d §5.5 SD5-B verification at R2.0 dispatch Round 2; **4 named candidates F2 at R2.0 V1 (orchestrator verified via grep on R1.2:237/240/245/262 + Phase A:230/287).** Cross-model leg structurally load-bearing per B2 standing rule LOCKED.

- **Advisor opus (post-V3.5-PF anti-push framing + Charlie sonnet→opus model upgrade 2026-05-19 at R3.1d decision-point round 6 + Charlie `/agents` UI cache-refresh fix 2026-05-19 for V1 round):**
  - Substantive-judgment-class dispatches: N=3 (R2.0 dispatch Round 1 `a157bf672de9a3d6a` + Round 2 `a329bb9d69f76f8fc` + final pre-SEAL R3.1d); all exemplar quality with honest tool-failure disclosure + calibrated uncertainty + explicit self-bias checks.
  - Citation-dense-class dispatches: N=4 total this session; 3 broken due to session-start cache state (no tool_use blocks emitted at API content layer — diagnosed via subagent jsonl); **1 FUNCTIONAL post-/agents-fix (`a9f9de9bc8ebae388`) = 21 tool_use blocks + 9 findings + 1 INDEPENDENT finding Codex missed (V1-F9 N=1 calibration per METHODOLOGY_NOTES §7) + calibrated bias disclosure (75-85% confidence range with explicit chain-trust limits + verification gaps + cross-reviewer-alignment + independence-verification)**.
  - **Cross-model diversity LOAD-BEARING empirically validated**: Codex catches Advisor would miss (clean track record) + Advisor catches Codex missed (V1-F9 N=1 calibration). Both legs additive at SEAL-class.

- **Orchestrator (Path A remediation precedent + permission-patch + cache-fix diagnostic):**
  - 1 Mode A failure this session (fabricated parallel Advisor Round 1 dispatch claim; detected at next-step pre-adjudication review; honest disclosure + Path A remediation applied)
  - 1 wrong-layer fix (permission patch to `settings.local.json` addressed wrong layer — issue was subagent name-keyed runtime cache, not permissions)
  - 1 root-cause diagnostic + remediation (subagent jsonl analysis isolated 0 tool_use across 4+ dispatches → Test B confirmed opus-wide CAN work via architect → Test F byte-identical-except-name confirmed name-keyed cache → `claude-code-guide` subagent authoritative answer = subagents loaded at session start only + `/agents` UI refresh takes effect immediately → Charlie used `/agents` UI → verified dispatch SUCCESS + R2.0 V1 Advisor functional)
  - Codification of orchestrator-side Mode A discipline + subagent cache-fix discipline eligible-not-named for separate cycle (§7 item 11 extension)

- **Infrastructure discovery (V1 round arc):** Claude Code subagents loaded at session start; mid-session file edits do NOT refresh cache; `/agents` UI command bypasses cache and takes effect immediately. Documented per `claude-code-guide` agent's authoritative answer citing official Claude Code docs. Eligible-not-named: memory codification of subagent cache-fix discipline for `feedback_reviewer_routing_subagent_default.md`.

---

**End of V3 REVISED-POST-PFR-ROUND (post-V2 PFR-rule-Y cross-validation; Codex BLOCKING F1 anti-rescue leakage corrected at V3-P1). V_SEAL PENDING per Charlie register chain (SD-A + SD-B + SD-C locks + MP1/MP2 ratify + post-V3 mini-PFR adjudication + SEAL bundle commit + Phase Marker advance commit per Option 1A binding 12th empirical trigger).**
