"""Phase 5 diagnostic execution package.

Operationalizes Phase 5 diagnostic-attribution procedure pre-registered at
sealed docs/phase5/PHASE5_DIAGNOSTIC_SUBSPEC.md (sub-spec drafting cycle SEAL
at commit 49ae7e3; §0-§8 sealed). This package fires the pre-registered
procedure against sealed Phase 4 forward-test artifacts + sealed candidate
cohort + sealed §1 inputs; produces per-mode indicator outputs (§2.a-§2.f) +
§3.1 multi-mode composite labels + §3.2 Case A {label, terminal_trigger}.

Sealed §4.1 hard scope binding: derived statistics + diagnostic computations
from existing sealed artifacts ARE allowed; acquisition of new empirical data
is NOT allowed.
"""
