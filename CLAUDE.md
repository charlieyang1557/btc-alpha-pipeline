# CLAUDE.md — BTC Alpha Pipeline

## What This Project Is

A single-person quantitative research system for BTC algorithmic trading strategies.
Built in phases: data infrastructure → backtesting → AI-assisted strategy mining → paper trading → live.

**Current status:** Phase 2A in progress — building AI-free infrastructure (factor
library, DSL compiler, hypothesis hash, regime holdout integration) in preparation
for AI-assisted strategy mining in Phase 2B.

**Completed phases:**
- Phase 0: Data infrastructure (validated BTC 1h data from 2020-01 onward)
- Phase 1A: Single-run Backtrader engine with 7bps effective cost model
- Phase 1B: Walk-forward orchestration + 4 baseline strategies + heuristic DSR

## Tech Stack

- **Python 3.11+** with dependencies pinned in `pyproject.toml`
- **Parquet** for all time-series market data (research layer)
- **SQLite** for experiment registry, paper trading state, and spend ledger (operational layer)
- **CCXT** for exchange API access (incremental data updates, future live trading)
- **Binance Vision** for bulk historical data download
- **Backtrader 1.9.78+** for event-driven backtesting
- **scipy** for statistical computations (Sharpe ratio, etc.)
- **matplotlib** for equity curve visualization (optional, debugging only)
- **pydantic v2** for DSL schema validation (Phase 2+)
- **Claude API via `anthropic` SDK** — Sonnet for both Proposer and Critic in Phase 2.
  Haiku is NOT used in Phase 2 (reserved for potential Phase 2.5 semantic dedup)

## Project Structure

```
btc-alpha-pipeline/
├── config/              # Execution conventions, date splits, schemas (IMMUTABLE by agents)
├── data/
│   ├── raw/             # Canonical current OHLCV parquet (archive/ holds snapshots)
│   ├── raw/archive/     # Pre-reconcile snapshots for reproducibility
│   ├── features/        # Precomputed factor parquet (Phase 2)
│   ├── quality/         # Auto-generated validation reports
│   ├── results/         # Per-run trade CSVs
│   ├── compiled_strategies/  # DSL compilation manifests (Phase 2)
│   └── batches/         # Batch leaderboards + auto-generated reports (Phase 2)
├── ingestion/           # Download, update, reconcile, validate scripts
├── backtest/            # Engine, metrics, experiment registry, trade audit
│   ├── bt_parquet_feed.py      # Parquet → Backtrader data adapter
│   ├── execution_model.py      # Reads execution.yaml, configures Cerebro
│   ├── slippage.py             # SlippageModel (effective 7bps for Phase 1-2)
│   ├── engine.py               # Single-run + walk-forward + regime holdout
│   ├── metrics.py              # Sharpe, drawdown, trade stats
│   ├── trade_audit.py          # Manual trade verification helper
│   ├── experiment_registry.py  # SQLite experiment tracking
│   ├── evaluate_dsr.py         # Heuristic multiple-testing screen + batch DSR
│   ├── batch_report.py         # Auto-generated batch reports (Phase 2)
│   └── experiments.db          # Auto-populated by engine
├── strategies/
│   ├── template.py             # Base strategy class
│   ├── baseline/               # Hand-written baselines (Phase 1)
│   ├── dsl.py                  # DSL pydantic schema (Phase 2)
│   ├── dsl_compiler.py         # DSL → Backtrader strategy class (Phase 2)
│   └── dsl_baselines/          # Baselines re-expressed in DSL (Phase 2A gate)
├── factors/             # Feature computation (Phase 2)
│   ├── registry.py             # FactorRegistry + feature_version governance
│   ├── build_features.py       # Full-dataset factor parquet builder
│   ├── returns.py
│   ├── moving_averages.py
│   ├── volatility.py
│   ├── momentum.py
│   ├── volume.py
│   └── structural.py
├── agents/              # AI hypothesis + strategy generation (Phase 2B)
│   ├── hypothesis_hash.py      # Canonical DSL hash + dedup (Phase 2A D3)
│   ├── proposer/               # Phase 2B D6 — Proposer agent (stub + Sonnet)
│   │   ├── interface.py        # ProposerBackend Protocol + I/O schemas
│   │   ├── stub_backend.py     # Deterministic stub backend (Stage 1)
│   │   ├── sonnet_backend.py   # Live Sonnet backend (Stage 2a+)
│   │   ├── prompt_builder.py   # Prompt construction + leakage audit helpers
│   │   └── stage2a_smoke.py    # Single-hypothesis smoke run script
│   ├── orchestrator/           # Phase 2B D8 — main batch loop
│   │   ├── ingest.py           # ProposerOutput → lifecycle state assignment
│   │   └── budget_ledger.py    # Crash-safe pre-charge SQLite ledger
│   ├── critic/                 # Phase 2B D7 — Critic module (rule gate + LLM)
│   │   ├── __init__.py         # Public API: CriticResult, run_critic, BatchContext
│   │   ├── result.py           # Frozen CriticResult dataclass + serialization
│   │   ├── batch_context.py    # BatchContext dataclass + theme constants
│   │   ├── d7a_feature_extraction.py  # DSL feature extraction primitives
│   │   ├── d7a_rules.py        # Deterministic D7a scoring rules (4 axes)
│   │   ├── d7b_backend.py      # Abstract D7bBackend protocol
│   │   ├── d7b_stub.py         # StubD7bBackend (all scores 0.5)
│   │   ├── d7b_live.py         # LiveSonnetD7bBackend (Stage 2a+, own Anthropic client)
│   │   ├── d7b_prompt.py       # D7b prompt template + leakage audit
│   │   ├── d7b_parser.py       # D7b response parser + forbidden-language scan
│   │   ├── replay.py           # Replay reconstruction from Stage 2d artifacts
│   │   └── orchestrator.py     # run_critic() orchestrator + reliability fuse
│   └── spend_ledger.db         # SQLite file owned by orchestrator/budget_ledger.py
├── risk/                # Position sizing and capital allocation (Phase 3+)
├── paper_trading/       # Simulated live execution (Phase 4)
├── tests/               # Automated test suite
└── live/                # Real money execution (Phase 5+)
```

## Document Conflict Priority

If any two documents in this project contradict each other, resolve by this hierarchy (highest priority first):

1. **`config/execution.yaml`** — execution semantics and fee assumptions
2. **`config/environments.yaml`** — date splits and research discipline (now at `v2` with regime holdout)
3. **`config/schemas.yaml`** — column definitions and validation rules
4. **`CLAUDE.md`** (this file) — hard constraints and prohibitions
5. **`data_dictionary.md`** — human-readable schema reference
6. **`PHASE2_BLUEPRINT.md`** (v2) — Phase 2 implementation plan (current phase)
7. **`PHASE1_BLUEPRINT.md`** — Phase 1 reference (completed)
8. **`PHASE0_BLUEPRINT.md`** — Phase 0 reference (completed)

Structured config files are the machine-readable source of truth. This file governs behavior and prohibitions but does not outrank the actual config values. If you encounter a conflict, follow the higher-priority document and flag the inconsistency in a code comment.

## Execution Convention (CRITICAL — READ BEFORE WRITING ANY STRATEGY CODE)

All backtests in this project follow these rules without exception:

1. **Signal timing:** Signals are computed using data available at bar N's close.
2. **Execution timing:** Orders execute at bar N+1's open price. NEVER at bar N's close.
3. **No same-bar execution:** A strategy CANNOT observe a bar's close and execute at that same bar's close. This is look-ahead bias.
4. **Cost model (Phase 1-2):** Effective cost = **7bps per side** (14bps round trip). This is a simplification of 4bps taker fee + 3bps slippage. Do NOT treat this as a realistic execution simulator — it is an effective cost model for baseline validation. The `fee_model` registry field must be `"effective_7bps_per_side"`. Upgrading to a volatility-scaled slippage model is deferred to Phase 3.
5. **Stop/limit orders within a bar:** If both stop-loss and take-profit would trigger within the same OHLCV bar, assume the adverse one triggers first (conservative).
6. **Zero-volume fill deferral:** If the designated fill bar has `volume == 0`, defer execution to the next bar with `volume > 0`, using that bar's open price. If deferral exceeds 24 bars (24 hours), cancel the order entirely.
7. **All times are UTC.** No exceptions. No implicit local timezone conversions.

These rules are defined in `config/execution.yaml` and enforced by `backtest/execution_model.py`.

## Backtrader-Specific Rules

### Cerebro Configuration (MUST be set for every backtest run)
- `cerebro.broker.set_coc(False)` — disable cheat-on-close
- `cerebro.broker.set_coo(False)` — disable cheat-on-open
- `cerebro.broker.setcommission(commission=0.0007)` — 7bps per side effective cost
- `cerebro.broker.setcash(10000)` — default starting capital

### Common Pitfalls (Claude Code MUST avoid these)
- `self.data.close[0]` is the CURRENT bar's close — NOT the next bar's
- `self.buy()` submits a market order that fills at the NEXT bar's open — this is correct behavior, do not try to "fix" it
- `self.data.datetime.datetime(0)` returns a **naive** datetime — always convert to UTC when comparing with our data
- Backtrader's `PandasData` feed expects the DataFrame index to be datetime — set index before passing
- If `fromdate`/`todate` are timezone-naive, Backtrader may misalign with our UTC data — always use timezone-aware datetime objects
- Backtrader indicators (SMA, etc.) are **bar-based, not time-based** — a 24-period SMA averages the last 24 rows regardless of time gaps between them. This is acceptable (31 gaps in 55K bars is negligible) but must be documented.

### Warmup Handling
- Each strategy declares `WARMUP_BARS` (number of bars before signals are valid)
- The engine loads data from `start_date` but only begins recording metrics after warmup
- **No trades are allowed during the warmup period**
- Strategies must only emit signals inside Backtrader's `next()` method, NEVER in `prenext()`
- `prenext()` runs while indicators are still warming up — it must remain empty or contain only logging
- The first eligible signal bar is the first bar where `next()` is called
- The first eligible fill is the bar after that signal
- Metrics (Sharpe, drawdown, etc.) are computed ONLY on the post-warmup period
- If a strategy attempts to trade during warmup, the engine must block it
- **Phase 2 DSL compiler**: `WARMUP_BARS` is auto-set to `registry.max_warmup(factors_used)` — see D2 in `PHASE2_BLUEPRINT.md`

## Timestamp & Timezone Rules

- **ALL timestamps in code, data, logs, and configs are UTC.**
- Parquet columns: use timezone-aware `datetime64[ms, UTC]`.
- SQLite columns: store as ISO 8601 strings with `Z` suffix (e.g., `2024-01-15T08:00:00Z`).
- Column naming convention: all time columns end in `_utc` (e.g., `open_time_utc`, `ingested_at_utc`, `created_at_utc`).
- Python code: always use `datetime.now(timezone.utc)`, NEVER `datetime.now()` or `datetime.utcnow()`.
- Pandas: when reading timestamps, always pass `utc=True` or explicitly localize with `.dt.tz_localize('UTC')`.
- **NEVER mix timezone-aware and timezone-naive datetimes.** This will silently corrupt data joins and backtest alignment.
- **Phase 2 budget**: "month" is strictly a **UTC calendar month** for monthly cap purposes. A new month begins at `YYYY-MM-01T00:00:00Z`. NEVER use a rolling 30-day window for budget accounting.

## Data Rules

- `data/raw/btcusdt_1h.parquet` is the **canonical current dataset**. It is overwritten by `reconcile.py` after each update.
- Before overwriting, `reconcile.py` MUST archive the previous version to `data/raw/archive/btcusdt_1h_YYYYMMDDTHHMMSSZ.parquet`.
- Raw data is NEVER modified by any process other than `reconcile.py`. Strategies, agents, and analysis scripts read only.
- `open_time_utc` is the primary key for all OHLCV data. It must be unique and sorted ascending.
- Zero-volume bars are flagged in validation reports but NOT auto-removed. They may indicate exchange downtime or data-quality issues.
- Missing bars (gaps) are flagged but NOT auto-interpolated. Forward-filling is PROHIBITED.
- All data files include `source` ("binance_vision", "ccxt_binance", or "ccxt_binanceus") and `ingested_at_utc` columns.

## Factor Parquet Rules (Phase 2)

- `data/features/btcusdt_1h_features.parquet` is the canonical factor dataset.
- `build_features.py` computes factors over the **full available OHLCV range**. Never build for a subset; subsetting happens at the consumption layer (engine `fromdate`/`todate`).
- The parquet's pyarrow metadata stores `feature_version` and `built_at_utc`.
- `feature_version` = SHA256 of canonical registry metadata, including per-factor: name, category, warmup_bars, inputs, output_dtype, and SHA256 of compute function source (via `inspect.getsource`).
- On any downstream read, if stored `feature_version` ≠ live `compute_feature_version(registry)`, the parquet MUST be force-rebuilt. No silent "use stale data" fallback.
- Registered factor compute functions MUST be top-level named callables. Lambdas, nested functions, and dynamically-generated callables are prohibited (breaks `inspect.getsource` stability).

## Experiment Tracking

- Every backtest run MUST be logged in `backtest/experiments.db` (SQLite).
- Required fields per run: `run_id`, `run_type`, `parent_run_id`, `strategy_name`, `git_commit`, `config_hash`, `split_version`, `data_snapshot_date`, `train_start`, `train_end`, `effective_start`, `warmup_bars`, `validation_start`, `validation_end`, `test_start`, `test_end`, `initial_capital`, `final_capital`, core metrics, `fee_model`.
- **Phase 2 additional fields**: `batch_id`, `hypothesis_hash`, `regime_holdout_passed`, `lifecycle_state`, `feature_version`.
- `run_type` must be one of: `"single_run"`, `"walk_forward_window"`, `"walk_forward_summary"`, `"regime_holdout"`, `"batch_summary"`
- `fee_model` for Phase 1-2: `"effective_7bps_per_side"` — do NOT use labels that imply separate fee/slippage modeling
- `split_version` for Phase 2: `"v2"` (train = 2020-2021 + 2023; holdout = 2022; validation = 2024; test = 2025)
- The experiment registry is the system of record for all research results.

## Date Split Rules

- Train/validation/test date boundaries are defined in `config/environments.yaml`.
- These boundaries are IMMUTABLE during a research phase.
- Strategies may ONLY be trained/optimized on data within the training window.
- Validation data is for hyperparameter selection and early stopping only.
- Test data is touched ONCE for final evaluation. If you peek and iterate, it becomes validation data.
- **Phase 2 regime holdout (2022)**: an additional in-train stress test. Agents never see its results. Only hypotheses that pass `regime_holdout_passed` advance to validation. See D4 in `PHASE2_BLUEPRINT.md` for the 4-condition passing criteria.

## Phase 2 DSL Rules

- All AI-generated strategies are expressed in a pydantic-validated DSL (`strategies/dsl.py`) and compiled to Backtrader via `strategies/dsl_compiler.py`. Raw Backtrader code from agents is not accepted in Phase 2.
- **DSL complexity budget (schema-enforced):** entry/exit groups ≤ 3, conditions per group ≤ 4, `max_hold_bars` ≤ 720, `name` ≤ 64 chars, `description` ≤ 300 chars.
- **Comparison operator semantics:** `crosses_above` / `crosses_below` MUST compile to `bt.indicators.CrossOver` or an explicit two-bar form `(a[0] > b[0]) AND (a[-1] <= b[-1])`. A naive single-bar comparison is a compiler bug.
- **NaN in comparisons:** NaN on either side of a comparison evaluates to `False`. Never `True`.
- **Factor-vs-scalar and factor-vs-factor are separate compiler code paths** with independent unit tests for each operator.
- **Compilation manifest:** each compiled strategy writes `data/compiled_strategies/<hypothesis_hash>.json` with canonical DSL, compiler git SHA, factor list snapshot, and feature_version. Drift in any of these fields raises `ManifestDriftError`.

## Phase 2 Agent & Budget Rules

- **Budget caps (hard-enforced in code):** $20 per batch, $100 per UTC calendar month. Enforcement happens PRE-call (before each API invocation), not post-call.
- **Spend ledger uses pre-flight charge pattern:** write `status="pending"` row with upper-bound cost estimate BEFORE the API call; update to `status="completed"` with actual cost after. Pending rows count as spent. Crashed batches are not resumed.
- **Hypothesis lifecycle states** (8 terminal, 1 transient):
  - Terminal: `proposer_invalid_dsl`, `duplicate`, `critic_rejected`, `train_failed`, `holdout_failed`, `dsr_failed`, `shortlisted`, `budget_exhausted`
  - Transient: `pending_dsr` (orchestrator-time; resolved by D9 at batch close)
- **Invariant:** `sum(terminal_lifecycle_counts) == hypotheses_attempted`. Checked at batch close ONLY, never mid-batch.
- **`hypotheses_attempted` counting rule:** increments immediately after each Proposer call returns, regardless of validity, duplication, or Critic outcome. Unissued slots (budget exhausted before proposing) are tracked separately in `batch_summary.unissued_slots`.
- **Theme rotation:** `theme = THEMES[(k - 1) % len(THEMES)]` where k is 1-indexed batch position and THEMES is the canonical 6-theme list defined in D6.
- **Theme rotation operational boundary (Stage 2c/2d):** Current Stage 2c/2d operational rotation uses the first 5 canonical themes (`THEME_CYCLE_LEN = 5` in `agents/proposer/stage2c_batch.py` and `stage2d_batch.py`). `multi_factor_combination` remains part of the canonical theme list but is not included in the current operational rotation until separately validated. **The exclusion is operational practice, not canonical specification; canonical anchors (`THEMES` tuple in `agents/themes.py`, `expectations.md`, `blueprint/PHASE2_BLUEPRINT.md`) retain the 6-theme list.** Resolves Issue 6 of D8.4 methodology refinement (sub-arc sealed at commit `767d0e5`) as documentation-completeness + methodology-acceptance per Issue 6 candidate-resolution-layer enumeration. Option to flip to 6 themes preserved for future decision when there's a specific Phase 2C reason to want multi-factor combination strategies; flip would require small 6th-theme-only smoke batch to verify candidate quality first.
- **Train-summary aggregation for disjoint train windows (v2):** `train_sharpe` = mean of per-window Sharpes; `train_return` = mean of per-window returns; `train_max_dd` = max of per-window drawdowns; `train_total_trades` = sum. NEVER stitch disjoint train-window equity curves into a continuous series.
- **Leaderboard ranking:** after filtering to `lifecycle_state == "shortlisted"`, rank by `min(train_sharpe, holdout_sharpe)` descending. Ties broken by `train_return` descending.
- **DSR N:** always `hypotheses_attempted` from the `batch_summary` row. NEVER use `hypotheses_approved` or survivor count.
- **D9 finalization authority:** `shortlisted` and `dsr_failed` terminal states are written ONLY by D9's `finalize_batch()`. The orchestrator writes `pending_dsr` and stops.

## Raw payload audit artifact retention (permanent)

`raw_payloads/` directories referenced by signed-off Stage 2 acceptance
notebooks are audit artifacts and must not be deleted or bulk-cleaned
without explicit human approval. Currently protected batches:

- Stage 2a signed-off:              raw_payloads/batch_03d62937-dbe8-46f2-a91b-50fa5696b14e/
- Stage 2a post-patch re-smoke:     raw_payloads/batch_74a52dae-7a2e-4555-b773-c95f2211ad9f/
- Stage 2b signed-off:              raw_payloads/batch_cd2f32ba-1984-4461-8216-1a9ac4ca2c17/
- Stage 2c signed-off:              raw_payloads/batch_e07f34a2-b532-4f35-a9f3-af97a5a96f1f/

New acceptance batches are added here as they sign off. Claude Code
must not include these paths in any cleanup operation.

---

## HARD CONSTRAINTS — THINGS CLAUDE CODE MUST NEVER DO

These are non-negotiable rules. Violating any of these invalidates research results.

### Data Integrity
- ❌ NEVER modify `config/environments.yaml` (date splits are immutable)
- ❌ NEVER modify `config/execution.yaml` without explicit human approval
- ❌ NEVER write to `data/raw/btcusdt_1h.parquet` from any script other than `reconcile.py`
- ❌ NEVER forward-fill missing bars or interpolate prices
- ❌ NEVER silently drop or filter rows from raw data
- ❌ NEVER auto-remove zero-volume bars (flag only)

### Execution Integrity
- ❌ NEVER assume same-bar execution (signal on close, execute on same close)
- ❌ NEVER hardcode transaction costs in strategy code — read from execution.yaml
- ❌ NEVER write strategies that access future data (no `shift(-1)` on price data for signals)
- ❌ NEVER use test-set performance to modify strategy parameters
- ❌ NEVER set `cheat_on_close=True` or `cheat_on_open=True` in Backtrader Cerebro
- ❌ NEVER fill orders on zero-volume bars — defer to next valid bar
- ❌ NEVER allow trades during the warmup period
- ❌ NEVER compute metrics (Sharpe, drawdown, etc.) including the warmup period
- ❌ NEVER use Backtrader's naive datetimes without converting to UTC for comparisons

### Factor & Vectorization Integrity (Phase 2)
- ❌ NEVER use global aggregations (`.mean()`, `.std()` on full series) in factor compute functions — use `.rolling(N)` or `.ewm(span=N, adjust=False)` only
- ❌ NEVER use future-touching operations in factors: `shift(-k)`, `bfill`, `fillna(method='bfill')`, unbounded `expanding()` without a minimum constraint that excludes future bars
- ❌ NEVER register a factor as a lambda, nested function, or dynamically-generated callable — top-level named functions only
- ❌ NEVER build factor parquet for a subset of dates — always full dataset; subsetting is a consumption-layer concern
- ❌ NEVER read a factor parquet whose stored `feature_version` mismatches the live registry hash — force rebuild instead
- ❌ NEVER modify a docstring and expect `feature_version` to change — docstrings are excluded from the hash

### DSL Compiler Integrity (Phase 2)
- ❌ NEVER translate `crosses_above` / `crosses_below` as a naive single-bar comparison; must use `bt.indicators.CrossOver` or explicit two-bar form
- ❌ NEVER merge factor-vs-scalar and factor-vs-factor into a single code path — they are separate with independent tests
- ❌ NEVER let NaN in a comparison evaluate to `True` or short-circuit; NaN is always `False`
- ❌ NEVER add a compiler special case for a specific baseline; if DSL cannot express a baseline, revise the DSL schema instead
- ❌ NEVER silently regenerate a compilation manifest on drift; drift raises and requires explicit human-acknowledged regeneration
- ❌ NEVER allow a DSL to compile to a strategy that uses negative shifts or intrabar reads of close

### AI Agent & Prompt Integrity (Phase 2)
- ❌ NEVER include validation (2024), test (2025), or regime-holdout (2022) metrics/data in any prompt context sent to an LLM, even after the fact
- ❌ NEVER include raw per-hypothesis numeric results (Sharpe, return, drawdown) in Proposer context — only aggregate stats and DSL-only examples
- ❌ NEVER use Haiku for Critic in Phase 2 — Critic requires Sonnet-level reasoning
- ❌ NEVER bypass the Critic — all approved hypotheses must pass through it
- ❌ NEVER let `overfitting_risk_score >= 4` produce an `approve` verdict — force reject in orchestrator code
- ❌ NEVER implement a Critic `refine` verdict in Phase 2 (v2 removed it; reconsider for Phase 2.5 if needed)

### Budget & Lifecycle Integrity (Phase 2)
- ❌ NEVER modify `agents/spend_ledger.db` from any script other than the orchestrator
- ❌ NEVER interpret "month" as a rolling 30-day window — strictly UTC calendar month
- ❌ NEVER perform a budget check AFTER an API call (must be pre-call)
- ❌ NEVER resume a batch that was marked `crashed` in the ledger
- ❌ NEVER use `hypotheses_approved` as N for DSR — always `hypotheses_attempted` from batch_summary
- ❌ NEVER assign `shortlisted` lifecycle state outside D9's `finalize_batch()`; orchestrator writes `pending_dsr` and stops
- ❌ NEVER check the lifecycle invariant mid-batch; it only holds at batch close
- ❌ NEVER stitch disjoint train-window equity curves into a single continuous series for metric computation

### Regime Holdout Integrity (Phase 2)
- ❌ NEVER expose a general-purpose CLI for regime holdout execution — orchestrator-internal only
- ❌ NEVER let the Proposer or Critic see regime holdout results in any form
- ❌ NEVER include 2022 bars in any walk-forward training window
- ❌ NEVER mark `regime_holdout_passed = True` unless ALL four criteria are met: `sharpe >= -0.5 AND max_dd <= 0.25 AND total_return >= -0.15 AND total_trades >= 5`

### Critic Integrity (Phase 2 D7)
- ❌ NEVER let `run_critic()` raise an exception — all failures are captured in `critic_status` codes
- ❌ NEVER modify D7a rule score formulas without updating the edge behavior table in `test_d7a_rules.py`
- ❌ NEVER let D7a rule scores fall outside `[0.0, 1.0]` or use more than 4 decimal places
- ❌ NEVER return `d7a_rule_scores = {k: 0.0}` when the score is unknown — use `None` for unknown, `0.0` for measured-as-bad
- ❌ NEVER enforce the reliability fuse in Stage 1 — `CRITIC_RELIABILITY_FUSE_ENFORCED` must remain `False` until Stage 2
- ❌ NEVER add critic_result to per-call records when `with_critic=False` — output must be byte-identical to pre-D7 behavior
- ❌ NEVER let the critic influence `approved_examples` window — critic annotates only, never filters
- ❌ NEVER enable prompt caching for D7b calls — this is a CONTRACT BOUNDARY (locked at Stage 2a)
- ❌ NEVER retry D7b content-level errors (malformed JSON, schema violation, refusal) — zero retries; these are forensic signals
- ❌ NEVER let D7b live backend share the D6 Proposer's `anthropic.Anthropic()` client — separate client is a CONTRACT BOUNDARY
- ❌ NEVER modify D7b prompt template wording without a new locked decision — frozen within a Stage 2 run
- ❌ NEVER omit `backend_kind` or `call_role` from `write_pending()` — both are required with no defaults
- ❌ NEVER co-mingle dry-run artifacts with production `raw_payloads/` — use `dryrun_payloads/` with physical isolation

### Code Quality
- ❌ NEVER generate a factor/indicator function without a docstring specifying: inputs, computation method, warmup period, output schema, and null policy
- ❌ NEVER skip validation steps when ingesting or updating data
- ❌ NEVER commit code that doesn't pass existing tests

### Library Policy
**Approved core libraries (Phase 0-1):** pandas, numpy, pyarrow, ccxt, requests, pyyaml, backtrader, scipy, matplotlib, and Python stdlib modules (sqlite3, pathlib, argparse, logging, hashlib, datetime, json, zipfile, io, uuid, typing, inspect).

**Approved Phase 2 additions:** `anthropic` (Claude API), `pydantic ~= 2.0` (DSL schema validation).

Any library not listed above requires explicit human approval before use. Standard typing/testing utilities (e.g., `dataclasses`, `typing_extensions`, `pytest`) are allowed without approval.

---

## Coding Standards

- All scripts have `if __name__ == "__main__"` with argparse
- All scripts support `--dry-run` where applicable
- All scripts log to stdout with ISO 8601 UTC timestamps
- All functions have type hints and docstrings
- All data-modifying operations are logged (what changed, row counts before/after)
- Non-zero exit code on any validation failure
- Use `pathlib.Path` for all file paths, never string concatenation
- Config loading: use a shared utility that reads YAML once and passes as dict

## Contract Markers

Three grep-discoverable comment markers document contract obligations in code:

- `CONTRACT GAP` — a test or mechanism that should exist but doesn't yet,
  with a trigger condition that will require adding it (e.g., "widening
  this Literal requires adding test_X in the same PR"). Use
  `rg "CONTRACT GAP"` to list all pending gaps.
- `CONTRACT BOUNDARY` — a deliberate separation between two mechanisms
  that look mergeable but must stay separate (e.g., D2 manifest
  canonicalization vs D3 dedup canonicalization). Mutual cross-references
  required.
- `DESIGN INVARIANT` — a non-obvious design decision that future readers
  might mistake for a bug (e.g., cross operators delay first-firable bar
  by 1). Explain the rationale at the site.

When introducing a contract obligation that can't be closed immediately,
tag it with one of these markers rather than a TODO or a checklist entry
in a separate document. Markers at the code site are self-maintaining;
external checklists are not.

Use these markers **sparingly**, for true contract obligations and
design boundaries — not for routine implementation notes. If the marker
points at something that will be fixed in the next PR, it's a regular
TODO, not a contract marker. Contract markers exist for obligations
whose trigger condition is **external to the current PR's scope**
(e.g., "when Literal X is widened", "when parallel execution is
added", "when a later phase begins"). Prefer placing the marker at the
exact code site where the invariant or future-trigger condition matters,
not at a distant wrapper or caller.

## Running the Pipeline

```bash
# Phase 0: Data management
python -m ingestion.bulk_download --pair BTCUSDT --interval 1h --start 2020-01
python -m ingestion.validators --file data/raw/btcusdt_1h.parquet --report data/quality/
python -m ingestion.incremental_update --pair BTCUSDT --interval 1h
python -m ingestion.reconcile --existing data/raw/btcusdt_1h.parquet --new data/raw/btcusdt_1h_update.parquet

# Phase 1A: Single-run backtesting
python -m backtest.engine --strategy sma_crossover --start 2024-01-01 --end 2024-12-31
python -m backtest.trade_audit --run-id <UUID> --trade-index 0 1 2
python -m backtest.experiment_registry --action list
python -m backtest.experiment_registry --action stats

# Phase 1B: Walk-forward
python -m backtest.engine --strategy sma_crossover --mode walk-forward
python -m backtest.evaluate_dsr --split-version v1

# Phase 2A: Factor library + DSL infrastructure
python -m factors.build_features --pair BTCUSDT --interval 1h
python -m factors.build_features --force-rebuild
# (DSL compiler + hypothesis hash + regime holdout have no user-facing CLI;
#  they are exercised via engine and test suite)

# Phase 2B: AI loop
python -m agents.orchestrator --batch-size 200 --max-usd 20
python -m agents.orchestrator --dry-run --batch-size 5         # mocked API end-to-end
python -m agents.orchestrator --status                          # monthly spend + recent batches
python -m backtest.evaluate_dsr --batch-id <UUID>               # finalize pending_dsr, emit leaderboard + report
```

## Known Data Characteristics

The canonical dataset (`data/raw/btcusdt_1h.parquet`) has these stable, verified properties:
- Dataset begins at **2020-01-01 00:00 UTC** and is extended via incremental CCXT updates
- **31 known missing hours** across 15 gap windows, all in 2020-2023 (historical exchange outages, verified stable across rebuilds)
- **3 known zero-volume bars** (2020-12-21, 2021-02-11, 2023-03-24) — all adjacent to gaps, all have O=H=L=C (frozen price)
- In currently validated snapshots, no gaps or zero-volume bars have been observed from 2024 onward
- All timestamps are UTC-aware and hour-aligned
- Exact row counts and source coverage boundaries change with each incremental update — check validation reports in `data/quality/` for current snapshot details

## Phase Marker (update as work progresses)

**Discipline rule:** this section must be updated in the same arc that ships any phase/stage sign-off, major closeout, or live batch fire. Stale Phase Marker misleads future work.

- **Current phase:** Phase 2C PHASE2C_11 statistical-significance machinery arc **SEALED** at Step 5 SEAL cycle (closeout MD at [`docs/closeout/PHASE2C_11_RESULTS.md`](docs/closeout/PHASE2C_11_RESULTS.md) sealed at commit `5dba0df` + this Phase Marker advance commit + tag `phase2c-11-statistical-significance-v1` at `5dba0df`). **Primary register disposition: artifact_evidence** at simplified DSR-style multiple-testing Sharpe deflation screen per closeout MD §1.1 §3.6 conservative AND-gate (Bonferroni fail at SR_max=0.9602178531387877 ≤ 3.25215836966607=sqrt(2·ln(198)) + DSR-style fail at argmax_p_value=0.999997737801711 ≥ 0.05; both pass criteria fail + both fail criteria meet at AND-gate routing yielding `population_disposition = artifact_evidence`). Per closeout MD §1.2 §6.7 canonical phrasing verbatim: "PHASE2C_11 primary register: artifact evidence at simplified DSR-style screen (Bonferroni fail + DSR-style p ≥ 0.5 at N=198, conservative N_eff). Existence-register resolves toward not-distinguishable-from-null at simplified-formulation register-class. Different candidate basis at successor cycle MAY produce different result. Successor cycle adjudication required for retrospective at deeper register OR breadth expansion." **Bounded scope per closeout MD §6.4 + §6.2 away-from-signal underclaiming guardrail:** in this batch (PHASE2C_8.1 audit_v1 mining batch_id `b6fcbf86-4d57-4d1f-ae41-1778296b1ae9`) / against canonical audit_v1 inputs (n_raw=198, n_eligible=154 post-§4.4 filter) / at simplified DSR-style register / at conservative N_eff = N_raw = 198 per §3.2 lockpoint; bounded at simplified-formulation register-class; pass/fail at canonical Bailey-López de Prado DSR register (deferred prerequisite per §4.7) is separate register-class downstream of PHASE2C_11. **§3 lockpoint compliance: 0 mutations across 8 categories** (formula / threshold / AND-gate / Sharpe estimation / null-deflation / pass-fail / canonical input / eligible-subset) preserved across implementation arc + closeout arc per closeout MD §9 lockpoint compliance audit. **§20 v2 Trigger 1 status: STILL CLOSED at `f82d040`** preserved across PHASE2C_11 SEAL (no §3 lockpoint mutation per closeout MD §11 reaffirmation). Numerical anchors verified byte-identical against `data/phase2c_evaluation_gate/audit_v1/_step3_result.json` (sha256=`64f733833e3cc47d32fc3a81c3f603c54c478c2064b455d161dbab6dff82ae03`; 83,347 bytes): bonferroni_threshold=3.25215836966607; expected_max_sharpe_null=2.01225257627714 (Gumbel approximation); SR_max=0.9602178531387877 at hash `18c2a5f742c20e31` (T_c=20); argmax z=−4.585713043255849, p_value=0.999997737801711; per_candidate disposition tally 0 signal / 154 artifact / 0 inconclusive at eligible subset n=154; sensitivity table N_eff ∈ {198, 80, 40} → artifact_evidence and N_eff=5 → inconclusive (descriptive only per closeout MD §4 + §5.4). In-arc commit anchors (chronological): scoping decision MD sealed at `3cfa357` + scoping cycle Phase Marker advance at `4c7d9d2`; sub-spec v3 sealed at `c5b740c` + Phase Marker advance to sub-spec drafting cycle SEALED at `151eb9e`; METHODOLOGY_NOTES §20 codified at `33731ce`; sub-spec v3 → v3.1 (Step 1 §19 Instance 5/6/7 patches per Charlie-authorized P2+precedent-doc path; admissible under §20) at `c021c60`; Step 1 deliverable v2 sealed at `61c17dc`; Step 2 input loader at `dfd9c72`; Step 2 deliverable v3 sealed at `6f116a8`; Step 3 schema sealed at `dbcf19d`; Step 3 TDD-RED 13 test classes / 45 tests at `73221c6` + synthetic-paths hotfix at `d7aa3ae` + fixture math hotfix-2 at `a91e71a`; Step 3 implementation `compute_simplified_dsr()` + RS-3 patches at `f82d040`; Codex first-fire Commit 1 at `f2e4087` (6/8 findings #1, #4, #5, #6, #7, #8 cleared) + Hotfix-3 at `1b8132e` (remaining 2/8 findings: #2 §4.4(1) enforcement + #3 unreachability docstring); Step 3 deliverable sealed at `6315ec0`; sub-spec v3.1 → v3.2 §5.4 §19 Instance patch at `8bc12de`; Step 3 deliverable amend (Codex deferred review closure on sha256 + p_value field reference at 4 sites) at `7e41058`; Step 4 closeout MD sealed at `5dba0df`. Sub-spec at [`docs/phase2c/PHASE2C_11_PLAN.md`](docs/phase2c/PHASE2C_11_PLAN.md) v3.2 sealed at `8bc12de` (724 lines / 9 sections); Step 3 deliverable at [`docs/phase2c/PHASE2C_11_STEP3_DELIVERABLE.md`](docs/phase2c/PHASE2C_11_STEP3_DELIVERABLE.md) (290 lines; amended at `7e41058`); closeout MD at [`docs/closeout/PHASE2C_11_RESULTS.md`](docs/closeout/PHASE2C_11_RESULTS.md) (408 lines / 15 sections); implementation at [`backtest/evaluate_dsr.py:956-1278`](backtest/evaluate_dsr.py) (`compute_simplified_dsr` canonical formula register + 3 frozen dataclasses + RS-3 attestation guard); 8/8 Codex first-fire findings cleared at register-precision per Step 3 substantive review cycle. Step 4 closeout cycle: triple-reviewer convergence (Codex substantive + ChatGPT structural + advisor prose-access) cross-validated 5/5 substantive findings at narrative register; per-finding adjudication discipline applied; Path 1 §0.4 §19 §5.4 routing per closeout MD §8; Path A.2 register-event boundary discipline (Step 3 amend at `7e41058` = closing deferred Codex review on Step 3 substance; Step 4 seal at `5dba0df` = closeout deliverable arc completion; symmetric to v3.2 §5.4 spec patch precedent at `8bc12de`); §6.2 forbidden-phrase scan literal-substring binding at compliance audit (6 toward-signal overclaiming + 5 away-from-signal underclaiming phrases verified ABSENT outside §7 audit table per closeout MD §7.1 + §7.2 + §7.3 audit methodology). Successor cycle path register: deferred per anti-pre-naming option (ii) precedent at PHASE2C_10_SCOPING_DECISION §4.4 + closeout MD §12; specific direction NOT pre-committed at this register; successor scoping cycle is the next arc after PHASE2C_11 SEAL at fresh session post-Step-5 per pacing discipline. Register-class-eligible paths per closeout MD §6.4: (a) mechanism deeper investigation at depth greater than PHASE2C_9 light-touch / (c) calibration variation at mining-batch calibration probe / (g) breadth expansion at new candidate basis at different mining configuration / (canonical-DSR scope expansion per §4.7 Path A or Path B prerequisite, register-class-eligible but unlikely to overturn artifact-evidence per §6.4); (f) Phase 3 trajectory FORECLOSED for current survivor set unless later cycle identifies different validated candidate basis per §6.4 + scoping-doc §4.3 path (f) wording. Carry-forwards to methodology consolidation cycle (deferred to separate fresh session post-Step-5): (1) cumulative §19 spec-vs-empirical-reality finding pattern instances at PHASE2C_11 cycle = **4 instances** at sub-spec drafting cycle per v3.2 §0.5 verbatim (Instance 1 v1 canonical-artifact register; Instance 2 v2 canonical-project-convention register; Instance 3 advisor β-Refine recommendation at structural-precision register; Instance 4 v2 canonical-artifact-mapping register LOAD-BEARING) + 3 instances at Step 1 implementation arc per v3.1 `c021c60` (§3.6 cited approximation 3.07 → 3.2522; §4.4(5) cross-validation framing; §7.1 multiplicity profile correction) + 3 instances at Step 4 closeout arc Codex deferred review at `8bc12de` + `7e41058` + `5dba0df` (§5.4 cited approximations 3.07/2.79 → 3.2522/2.9604 at v3.2 patch; sha256 trailing-newline at 5 sites; argmax_p_value vs p_value field-name typo at 2 sites) = **10 cumulative within-arc**; (2) advisor §5 prediction-error discipline = 6 cumulative instances at PHASE2C_11 cycle per Step 4 handoff §19 Instance #6; (3) ChatGPT S8 bounded-scope-repetition rejection codification candidate (REJECTED with pushback documented at closeout cycle; bounded-scope wording is intentional discipline at deliverable register per §6.2 + §6.4); (4) §6.2 forbidden-phrase scan literal-substring binding + §7 audit-table exemption codification candidate (codified explicit at closeout MD §7.3); (5) Path A.2 register-event boundary discipline codification candidate (deferred-Codex-review closure events deserve separate commit boundaries from downstream deliverable seals to preserve audit-trail clarity; symmetric precedent at v3.2 §5.4 patch at `8bc12de`); (6) triple-reviewer convergence at substantive register codification candidate (Codex substantive + ChatGPT structural + advisor prose-access cross-validated 5/5 substantive findings at Step 4 closeout cycle — higher-confidence signal than dual-reviewer alone for narrative-register deliverables). Active next action: successor scoping cycle at fresh session post-Step-5 adjudicates specific path from §6.4 register-class-eligible register (anti-pre-naming option (ii) binding); specific direction NOT pre-committed at this register. Push timing at this commit: 1 commit at this Step 5 SEAL bundle (this Phase Marker advance commit pushed together with annotated tag `phase2c-11-statistical-significance-v1` at `5dba0df` per (P1) bundled push triple-reviewer convergence).

- **Prior phase (PHASE2C_10 SEALED):** Phase 2C PHASE2C_10 methodology consolidation arc SEALED at Step 2 closeout cycle (sign-off MD at [`docs/closeout/PHASE2C_10_METHODOLOGY_CONSOLIDATION_SIGNOFF.md`](docs/closeout/PHASE2C_10_METHODOLOGY_CONSOLIDATION_SIGNOFF.md) committed at `ff3e4ca` + Phase Marker advance commit at `4c7d9d2` + tag `phase2c-10-methodology-consolidation-v1` at `4c7d9d2`); §3.5/§3.6 weak-tier candidates folded into §16 ### Failure-mode signal at commit `8ed1b34` (METHODOLOGY_NOTES.md +16 lines; 2 short watch-for paragraphs at §16 host slot citing §11 (§3.5 pre-fire audit pattern) + §14 (§3.6 self-first-then-reviewer) as parent disciplines via cross-reference register; (A) fold-both-into-§16 disposition over (B) brief §20 + (C) hybrid per ChatGPT execution plan + Claude Code adjudication; placement-vs-cross-reference register distinction operationalized cleanly per §5.4 guardrail 4 §16+ append-only constraint; α-Refine applied at register-class consistency per advisor light pass catch — parenthetical register anchoring at subject + "as parent discipline" framing for §11/§14 unambiguous attribution; ChatGPT skipped + Codex skipped per plan §5.3 lighter-touch on weak-tier; direct in-place append (no temp working draft) per ChatGPT execution plan; §16 instance count candidate #14 — advisor prose-pass register caught grammatical-ambiguity register overextension parallel to §18 C1 + §19 C2/C3 catch class but at advisor register since Codex skipped); §19 sealed at commit `0c6831f` (METHODOLOGY_NOTES.md §19 spec-vs-empirical-reality finding pattern at Weak tier observation-only + cross-cycle-pending; +46 lines including separator block; single-commit seal per §0.4 discipline correction; option (b) single-section structural register at compressed 43 lines per Weak-tier brevity register signaling per ChatGPT guardrail 2; 5 cumulative patches across 3 reviewer registers + advisor calibration: 1 ChatGPT 4-paragraph compression direction with Adv-1 α drop + 3 Codex C1/C2/C3 cross-reference precision + 1 advisor α C4-ripple at fresh-register full-file pass); §18 sealed at commit `2ac95ab` (METHODOLOGY_NOTES.md §18 §7 carry-forward density at interpretive arc closeouts; +161 lines; Medium tier; 8 patches cumulative: 3 ChatGPT P1/P2/P3 + 4 Codex C1/C2/C3/C4 + 1 advisor α-Refine); §17 sealed at commit `3eff56a` (METHODOLOGY_NOTES.md §17 procedural-confirmation defect class; +213 lines; 11 patches cumulative); §16 sealed at commit `7959140` (METHODOLOGY_NOTES.md §16 anchor-prose-access discipline; +188 lines). Step 1 sub-spec drafting cycle sealed at commit `d2a53fa` after 5 iteration commits (`d0222c7` working draft → `d2b6166` instance #5 dual-reviewer patches → `1f9a015` post-seal full-file prose-pass patches → `d2a53fa` Activity A pre-fire audit patch); plan at [`docs/phase2c/PHASE2C_10_PLAN.md`](docs/phase2c/PHASE2C_10_PLAN.md) (467 lines) is canonical input for Step 2 authoring per §3.1-§3.6 codification scopes. Active next action: successor scoping cycle adjudicates Phase 2 continuation/conclusion paths from PHASE2C_10 scoping decision §2 path register: (a) Structured re-examination at depth greater than light-touch / (b) Statistical-significance machinery (Q-9.A) / (c) Calibration-variation (Q-9.C) / (f) Phase 3 trajectory [Phase 2 conclusion; triggers PHASE2_CLOSEOUT_SIGNOFF authorship] / (g) Breadth expansion. Per PHASE2C_9 §8.2 cycle-boundary preservation: forward-pointer register does NOT pre-name successor scoping cycle's substantive direction; specific direction selection not pre-committed at PHASE2C_10 closeout. Methodology consolidation arc concludes; project returns to substantive empirical work at successor cycle. Per advisor refinement at prior session closes: §17/§18/§19 structural gates fire at per-section scope-lock boundaries when each section's authoring approaches; §16 + §17 + §18 + §19 fired independently and are now sealed. Per advisor's substantive meta-critique: process recursion at sub-spec sealing register (5 iteration commits before any substantive METHODOLOGY_NOTES.md content authored) flagged as anti-pattern; §16 + §17 + §18 + §19 authoring proceeded with discipline operating during authoring (dual-reviewer pass + Codex + full-file pass), not as preconditions to authoring — empirically validated at §16 + §17 + §18 + §19 seal cycles. Hard scope per scoping doc §1.4 + plan §0.1: METHODOLOGY_NOTES update only; no new generation/evaluation/strategy redesign/DSR-PBO-CPCV/Phase 3 progression/API spend. Codex routing per plan §5.3: Codex review fires on §16/§17/§18/§19 substantive content at sealed register before each section's seal commit (fired at §16 with C1-C4 patches + at §17 with C1-C3 patches + at §18 with C1-C4 patches + at §19 with bounded scope C1-C3 patches per plan §5.3 lighter-touch on weak-tier; ChatGPT-directed Codex-after-compression sequencing operationally validated at §19 cycle). §17 iterative-pattern operating-rule register adoption resolved at §17 codification: Application checklist sub-rule item 4 (within §17) per plan §3.2 disposition; §17 sub-rule 4 operationalized at §18 + §19 own seal cycles (fresh-register full-file pass surfaced α-Refine + α C4-ripple residuals; patches applied + re-verified CLEAN before seal authorization — recursive empirical validation continuing across §16/§17/§18/§19 seal cycles, fourth consecutive cycle). §16 cumulative instance count empirical anchor at §3.5/§3.6 cycle: 9 candidate instances cumulative across §17 + §18 + §19 + §3.5/§3.6 cycles (§17 cycle: #6 σ-scope catch / #7 ChatGPT P2 register-precision / #8 φ-A cross-section consistency; §18 cycle: #9 Codex C1 §11 cross-reference precision catch / #10 Codex C3 §10 cross-reference precision catch; §19 cycle: #11 Codex C2 §15 hierarchy precision catch / #12 Codex C3 §15 operating-rule overclaim catch / #13 advisor α C4-ripple §17 sub-rule 4 catch at fresh register; §3.5/§3.6 cycle: #14 advisor α §11/§14 grammatical-ambiguity register overextension catch at advisor light pass — empirical observation that catch class isn't reviewer-register-locked but pass-focus-locked; advisor + ChatGPT can operate canonical-scope-verification when explicitly directed at pass focus); §16 instance count update deferred to next §16-relevant register or successor cycle. Activity K disposition confirmed at Step 2 closeout: K-defer-to-successor per plan §4.3 + §1.4 hard scope (CLAUDE.md project-discipline notes line 459 staleness fix OUT OF SCOPE for PHASE2C_10; carries forward as tracked observation per §3.5/§3.6 fold-in registered at §16 ### Failure-mode signal). §16 cumulative instance count update register confirmed at Step 2 closeout: defer to successor cycle (audit-trail anchor preserved at seal commit messages across §17 + §18 + §19 + §3.5/§3.6 + closeout sign-off cycles; cumulative §16 candidate instances now 10 across §17 + §18 + §19 + §3.5/§3.6 + closeout sign-off cycles — added #15 advisor β factual attribution catch via canonical-artifact verification at Step 2 closeout sign-off MD register, same defect class as §18 C1 + §19 C2/C3 Codex catches but at advisor register since Codex skipped per closeout cycle routing). Push timing at this commit: ~13 commits unpushed cumulative pushed at Step 2 closeout-cycle bundle (5 from Step 1 + §16 seal + Phase Marker §16 advance + §17 seal + Phase Marker §17 advance + §18 seal + Phase Marker §18 advance + §19 seal + Phase Marker §19 advance + §3.5/§3.6 fold-in seal + Phase Marker §3.5/§3.6 advance + sign-off MD seal + this Phase Marker SEALED advance with tag).
- **Completed:**
  - Phase 0, Phase 1A, Phase 1B (with corrected v2 WF baseline supplement at [`docs/closeout/PHASE1B_WF_CORRECTED_BASELINE_SUPPLEMENT.md`](docs/closeout/PHASE1B_WF_CORRECTED_BASELINE_SUPPLEMENT.md))
  - Phase 2A (D1-D5 all signed off, [`docs/closeout/PHASE2A_SIGNOFF.md`](docs/closeout/PHASE2A_SIGNOFF.md))
  - Phase 2B D6 Stage 1 (stub plumbing); D6 Stage 2 lessons learned ([`docs/closeout/PHASE2B_D6_STAGE2_LESSONS_LEARNED.md`](docs/closeout/PHASE2B_D6_STAGE2_LESSONS_LEARNED.md))
  - Phase 2B D7 Stage 1 / 2a / 2b / 2c / 2d (D7 Stage 2d signed off 2026-04-23 at [`docs/closeout/PHASE2B_D7_STAGE2D_SIGNOFF.md`](docs/closeout/PHASE2B_D7_STAGE2D_SIGNOFF.md))
  - Phase 2C Smoke α (Stage 2c dry-run, [`docs/closeout/PHASE2C_1_SMOKE_SIGNOFF.md`](docs/closeout/PHASE2C_1_SMOKE_SIGNOFF.md)) + β (live Sonnet, [`docs/closeout/PHASE2C_2_SMOKE_SIGNOFF.md`](docs/closeout/PHASE2C_2_SMOKE_SIGNOFF.md))
  - Phase 2C Batch 1 fire ([`docs/closeout/PHASE2C_3_BATCH1.md`](docs/closeout/PHASE2C_3_BATCH1.md))
  - Phase 2C Phase 1 walk-forward closeout ([`docs/closeout/PHASE2C_5_PHASE1_RESULTS.md`](docs/closeout/PHASE2C_5_PHASE1_RESULTS.md)) + corrected-engine erratum ([`docs/closeout/PHASE2C_5_PHASE1_RESULTS_ERRATUM.md`](docs/closeout/PHASE2C_5_PHASE1_RESULTS_ERRATUM.md))
  - Corrected WF Engine Project Arc (engine fix at `eb1c87f`, lineage discipline at [`backtest/wf_lineage.py`](backtest/wf_lineage.py), tag `wf-corrected-v1`, sign-off at [`docs/closeout/CORRECTED_WF_ENGINE_SIGNOFF.md`](docs/closeout/CORRECTED_WF_ENGINE_SIGNOFF.md))
  - PHASE2C_6 Evaluation Gate Arc (single-regime 2022 holdout evaluation across 198 corrected-engine candidates; closeout at [`docs/closeout/PHASE2C_6_EVALUATION_GATE_RESULTS.md`](docs/closeout/PHASE2C_6_EVALUATION_GATE_RESULTS.md); plan at [`docs/phase2c/PHASE2C_6_EVALUATION_GATE_PLAN.md`](docs/phase2c/PHASE2C_6_EVALUATION_GATE_PLAN.md); Codex review-response at PHASE2C_6.7 commit; new consumer guard `check_evaluation_semantics_or_raise()` for single-run holdout attestation domain in [`backtest/wf_lineage.py`](backtest/wf_lineage.py))
  - PHASE2C_7.1 Multi-Regime Evaluation Gate Arc (Path B per PHASE2C_7.0 scoping; same 198 candidates evaluated against `validation_2024` and compared candidate-aligned to PHASE2C_6's 2022 evaluation; closeout at [`docs/closeout/PHASE2C_7_1_RESULTS.md`](docs/closeout/PHASE2C_7_1_RESULTS.md); artifacts at `data/phase2c_evaluation_gate/{audit_2024_v1, audit_2024_v1_filtered, comparison_2022_vs_2024_v1}/`; Codex review-response at PHASE2C_7.1.7 commit; closeout schema discriminator `artifact_schema_version="phase2c_7_1"`; tag `phase2c-7-1-multi-regime-v1`)
  - PHASE2C_8.0 Scoping Decision (Q-B1 selection over Q-B2/Q-B3.a/Q-B3.b/Q-B4; scoping document at [`docs/phase2c/PHASE2C_8_SCOPING_DECISION.md`](docs/phase2c/PHASE2C_8_SCOPING_DECISION.md) committed at `f223316`; established the n=4 baseline composition + Option A engine-version invariance + in-sample caveat (Concern A) framework that PHASE2C_8.1 implementation operates within)
  - PHASE2C_8.1 Multi-Regime Evaluation Gate Arc (extended; n=4 evaluation against bear_2022 + validation_2024 + eval_2020_v1 + eval_2021_v1; spec at [`docs/phase2c/PHASE2C_8_1_PLAN.md`](docs/phase2c/PHASE2C_8_1_PLAN.md) committed at `1e85d1d`; implementation arc commits `1e85d1d..8086adf` (10 commits); closeout at [`docs/closeout/PHASE2C_8_1_RESULTS.md`](docs/closeout/PHASE2C_8_1_RESULTS.md); novel artifact paths `data/phase2c_evaluation_gate/{eval_2020_v1, eval_2020_v1_filtered, eval_2021_v1, eval_2021_v1_filtered, audit_v1_filtered, comparison_2022_2024_2020_2021_v1}/`; novel schema discriminator `artifact_schema_version="phase2c_8_1"` for train-overlap regimes; verification chain at three independent layers including permanent in-repo recompute gate at `tests/test_phase2c_8_1_independent_recompute.py`; tag `phase2c-8-1-multi-regime-extended-v1`)
  - PHASE2C_9.0 Scoping Decision (Q-9.B selection over Q-9.A/Q-9.C/Q-9.D after dual-reviewer convergence + sequential-hybrid framing collapse to Q-9.B light-touch alone; scoping document at [`docs/phase2c/PHASE2C_9_SCOPING_DECISION.md`](docs/phase2c/PHASE2C_9_SCOPING_DECISION.md) committed at `3e0c99d`; established the cycle-boundary-semantics framework that PHASE2C_9 implementation operates within — PHASE2C_9 = Q-9.B alone, post-Q-9.B successor scoping cycle deferred to post-findings deliberation)
  - PHASE2C_9 Q-9.B Spec (mining-process retrospective at light-touch depth; spec at [`docs/phase2c/PHASE2C_9_PLAN.md`](docs/phase2c/PHASE2C_9_PLAN.md) committed at `8aa1c66`; D1-D6 pre-pinned decisions; 6-step sequential gating; pre-registered exit conditions Case A.1-A.4 / B.1-B.3 / C.1-C.3 with operational evidence criteria; cycle-boundary preservation language audit per Claude advisor's three carry-forward considerations; ChatGPT-caught PHASE2C_10 pre-naming blocker resolved at second-pass dual-reviewer with 23 substantive replacements + 1 forbidden-language illustration retained at §7.2)
  - PHASE2C_9 Step 1 — Mining-process source mechanism reconstruction (closeout §3 at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) committed at `13be4ff`; 1139 lines / 212 file:line citations across 9 in-scope source files; three substantive observations surfaced for §7 mechanism-vs-observation comparison register: (1) `_theme_for_slot` len(THEMES)=6 modulus vs `_theme_for_position` THEME_CYCLE_LEN=5 modulus mismatch; (2) Critic annotates only / lifecycle-state assignment at orchestrator ingest layer; (3) Stage 2c THEME_HINTS vs Critic-side `batch_context.theme_hints` dual surface; dual-reviewer pass clean — ChatGPT conditional approval + Claude advisor second-pass register-precision review)
  - PHASE2C_9 Step 2 — Artifact-distribution audit + Stage 2d attribution patch (closeout §4 at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) committed at `777ea3c`; 1437 lines total / 242 file:line citations after +251 line §4 working draft + §3 register-precision correction; 198 canonical anchor verified at three independent measurement points; per-theme valid_count distribution 40/40/40/39/39; total_actual_cost_usd=$2.30; three additional §7 carry-forward observations: rejected_complexity at description-length boundary; rsi_14_dominance first-50-block anomaly at 92%; factor-set repetition rate ~28.79%; §3 register-precision correction patching §3.1.4 / §3.3.2 / §3.4 from Stage 2c attribution to Stage 2d canonical batch attribution per ChatGPT first-pass dual-reviewer adjudication; Step 2 dual-reviewer pass clean — ChatGPT first-pass + Claude advisor second-pass register-precision review)
  - PHASE2C_9 Step 3 — Theme × pass-count cross-tab (closeout §5 at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) committed at `c3a2f20`; 1660 lines total / 246 file:line citations after +223 line §5 working draft; 5×4 theme × regime cross-tab reproducing PHASE2C_8.1 §7.1 byte-for-byte; theme totals 40/40/40/39/39 + per-regime totals 13/87/74/38; 4-axis canonical-number cross-checks all pass; 9 mechanical asymmetry observations at cardinality-report register; two additional §7 carry-forward observations: train-overlap regime divergence at per-theme level (eval_2020_v1 ≈ 2× eval_2021_v1); per-regime pass-rate range asymmetry across themes; cumulative §7 carry-forward register now at 8 observations across Steps 1-3; Step 3 dual-reviewer pass clean)
  - PHASE2C_9 Step 4 — Lone-survivor walkthrough (closeout §6 at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) committed at `1d069ba`; ~2080 lines total after +125 line §6 working draft including §6.0 generation-side trace + §6.1 compilation manifest + §6.2 Critic gate trace + §6.3 evaluation-side trace cross-reference + §6.4 deliverable summary; end-to-end mining-process trace for hash `0845d1d7898412f2` (`volume_surge_momentum_entry`, theme `volume_divergence`, batch position 39, audit-only partition origin); single-trade-margin filter exclusion in bear_2022 mechanically verified by `audit_v1_filtered/` directory absence (19 trades vs ≥20 threshold); permissive AND-gate accepts -10.2% return in eval_2021_v1; theme_coherence at D7a whole-DSL register = 1/6 ≈ 0.1667 (correction patch from initial 1/4 entry-only register reading per dual-reviewer second-pass adjudication); audit-only partition origin verified at `wf_test_period_sharpe = -0.072 < 0.5`; engine lineage `eb1c87f` / `wf-corrected-v1` cross-checked across all 4 regimes; three additional §7 carry-forward observations: single-trade-margin filter exclusion mechanism; permissive AND-gate at -10.2% return; theme alignment at D7a whole-DSL register; cumulative §7 carry-forward register now at 11 observations across Steps 1-4; Step 4 dual-reviewer pass clean — ChatGPT first-pass + Claude advisor second-pass register-precision review including theme_coherence formula correction)
  - PHASE2C_9 Step 5 — Mechanism-vs-observation comparison (§7 evidence maps for Case A.1-A.4 / B.1-B.3 / C.1-C.3 sub-registers; closeout §7 at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) sealed at `d548ea2` after Step 5 working draft `e11e806` + follow-up register-precision patches per advisor substantive prose-access pass; ~3153 lines total after +1148 lines for §7 working draft + Concerns A/C/D residual patches; cumulative-11 carry-forward observations all explicitly cited at §7.0.0 inventory table + §7.0-§7.2 prose; A.4 sub-register direct qualifying + direct disqualifying counter-evidence at descriptive register; A.1/A.2/A.3 qualifying weaker than disqualifying; Case B partial qualification at descriptive register; Case C C.1/C.2/C.3 each show direct qualifying evidence at descriptive register matching spec §4.3 evidence-pattern text; Step 5 dual-reviewer cycle: round-1 ChatGPT + Claude advisor structural-summary pass + round-2 advisor substantive prose-access pass surfacing Concerns A residual / C internal-consistency / D "hampered" interpretive register / verified B framing scope + F Path B operationalization clean; standing instruction codified at Step 5 close — anchor-prose-access discipline at multi-hundred-line interpretive deliverables; Step 5 sealed at advisor + ChatGPT register per round-2 sign-offs)
  - PHASE2C_9 Step 6 sub-spec — §4.4 application framing pre-registered (sub-spec at [`docs/phase2c/PHASE2C_9_STEP6_SUBSPEC.md`](docs/phase2c/PHASE2C_9_STEP6_SUBSPEC.md) committed at `d1657bd`; 823 lines locking §4.4 application framing as mechanical-procedure with bounded comparison rule grounded in PHASE2C_9_PLAN.md §4.4 verbatim text reading; §1.1-§1.6 framing block including §1.5 evidence-comparison rule three-rule structure with Reading (i) lock at §4.1 "stronger than" hard case + §1.6 chain-falsifiability per-link-point structure (3 link-points per Case); §1.2 verbatim-quote scope (≤2 sentences + cite-only + paraphrase-forbidden) + §3.4 Q-9-N tracked-fix numbering convention + §3.5 four mandatory methodology-codification candidates with status-register precision per candidate + §5.2 14-step activity sequence with authoring-against-§7-fresh discipline at Activity 2 + Phase Marker Event A / Event B distinguished; three dual-reviewer rounds completed — Round 1 structural + framing decision + Concerns G/H/I/J; Round 2 substantive prose-access + Flags 1/2/3 verbatim-quote/chain-falsifiability/authoring-fresh; Round 3 ChatGPT §3.5 disambiguation + Concerns K/L slot-vs-content/status-register-precision; Concerns M/N carried forward as Step 6 fire-time §8-author-discipline checks; verification grep clean; pre-fire Concern N audit CLEAN at session-close 5 sequencing checks; sub-spec sealed at advisor + ChatGPT register per round-3 conditional sign-offs; anchor-prose-access discipline operating at instance #2 throughout sub-spec drafting cycle)
  - PHASE2C_9 Step 6 — Case C determination + closeout assembly final (closeout final at [`docs/closeout/PHASE2C_9_RESULTS.md`](docs/closeout/PHASE2C_9_RESULTS.md) sealed at `2c96889`; 4152 lines total after +935 lines for §8 + §1 + §2 + §9 authoring + 3 patches; case determination **Case C with sub-registers C.1 + C.2 + C.3 documented** per §4.4 mechanical-procedure application + sub-spec §1.5 evidence-comparison rule; Activity 11 dual-reviewer pass per anchor-prose-access discipline standing instruction instance #3 — ChatGPT first-pass conditional approve with 2 required checks + Claude advisor substantive prose-access pass surfacing 8 Concerns O-V; 3 patches applied (ChatGPT check 1 paraphrase-creep at §8.3 Q-9-02 line 3819-3821; Concern P Case B B.1-alone-sufficient framing at §8.0; Concern R §8.4 entry #5 status "observation-only + cross-cycle-pending" alignment with mandatory entry #2 register precedent); Concerns O / Q / S / T / U / V verified clean on prose access; five §8-author-discipline checks operationalized correctly at fire-time per Concerns M/N/Flag-3/K/L fire-time discipline; Activity 12 commit AUTHORIZED at both reviewer registers per round-2 sign-offs post-patches; tracked-fix register Q-9-01 ingest-layer mechanism reconstruction + Q-9-02 evidence asymmetry that produced Case C determination; methodology-codification candidates 5 entries (4 mandatory cumulative + 1 emergent pre-fire-audit-pattern) with status-register precision per Concern L disposition; PHASE2C_9 closeout final at this commit cycle)
  - PHASE2C_10.0 Scoping Decision (Path (e) methodology consolidation cycle selected over Paths (a) structured-re-examination / (b) statistical-significance machinery / (c) calibration-variation / (d) other-paths / (f) Phase 3 trajectory / (g) breadth expansion per §3 path-by-path evaluation against §1.2 Goal B alignment + §1.3 short-cycle budget + §1.4 8-constraint compliance; arc designation per anti-pre-naming option (ii) yields PHASE2C_10 = METHODOLOGY_NOTES update arc consolidating PHASE2C_9 + PHASE2C_10 scoping carry-forward methodology candidates; scoping document at [`docs/phase2c/PHASE2C_10_SCOPING_DECISION.md`](docs/phase2c/PHASE2C_10_SCOPING_DECISION.md) committed at `1053c73`; established the §16+ append convention + tiered codification provisional + Option A tier-precision-deferred at §6.1 + 4 ChatGPT guardrails locked at §5.4 + six methodology candidates carry-forward register at §6 framework that PHASE2C_10 implementation operates within; post-PHASE2C_10 successor scoping cycle deferred per anti-pre-naming option (ii); standing instruction set operational — anchor-prose-access discipline at instance #4 fired at scoping cycle, anti-momentum-binding discipline carrying provisional tier labels as inputs not constraints, empirical verification discipline (METHODOLOGY_NOTES §1) on file-structure citations)
  - PHASE2C_10 Step 1 — Sub-spec drafting cycle sealed (plan at [`docs/phase2c/PHASE2C_10_PLAN.md`](docs/phase2c/PHASE2C_10_PLAN.md) sealed at `d2a53fa` after 5 iteration commits: `d0222c7` working draft → `d2b6166` instance #5 dual-reviewer patches → `1f9a015` post-seal full-file prose-pass patches → `d2a53fa` Activity A pre-fire audit patch; 467 lines; six methodology candidates adjudicated at §3 with final tier dispositions per §3.7 — (1) anchor-prose-access discipline Strong → §16 dedicated section; (2) procedural-confirmation defect class Strong → §17 dedicated section (instance count 5 cumulative per iteration-chain operational validation: PHASE2C_9 Step 5 + Step 6 + PHASE2C_10 plan d0222c7→d2b6166 + d2b6166→1f9a015 + 1f9a015→d2a53fa); (3) spec-vs-empirical-reality finding pattern Weak with medium-pending status note → brief §19 dedicated section (cumulative count 6 across 3 cycles: PHASE2C_9 = 3; PHASE2C_10 scoping = 2; PHASE2C_10 plan drafting = 1); (4) §7 carry-forward density at interpretive arc closeouts Medium with operating-rule-pending status note → §18 dedicated section (initial Strong-tier sub-spec promotion reverted per dual-reviewer pushback at operating-rule-articulation grounds; 4/4 PHASE2C interpretive closeout instance saturation supports observation-strengthening but Application checklist as authored is observation-framing not operating-rule-codification per §13/§14/§15 precedent); (5) pre-fire audit pattern Weak (1 instance) → folded into §16 ### Failure-mode signal OR brief §20; (6) self-first-then-reviewer Weak (1 instance) → folded into §16 ### Failure-mode signal OR brief §20. §4 emergent-vs-folded adjudication: advisor-register sub-pattern folds into candidate (3) per §4.3 disposition; CLAUDE.md project-discipline notes staleness fix disposition (Activity K inclusion vs successor cycle carry-forward) deferred to Step 2 dual-reviewer pass. Standing instructions operational: anchor-prose-access discipline cumulative instance count = 4 at sub-spec sealing (instances #1-#4); iterative-pattern operating-rule candidate "full-file prose-access pass at sealed-commit register; section-targeted patches do not preclude need for full-file final pass" empirically validated at 5 candidate (2) instances; carries to §17 codification at Step 2 implementation arc as Application checklist sub-rule candidate. Process-recursion meta-observation per advisor session-close framing: 5 iteration commits at Step 1 sealing register before any substantive METHODOLOGY_NOTES.md content authored; flagged as anti-pattern; Step 2 fires authoring with discipline operating during authoring, not as preconditions to authoring)
  - PHASE2C_10 Step 2 §16 sealed (anchor-prose-access discipline at multi-hundred-line interpretive deliverables; sealed at commit `7959140`; +188 lines appended to [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md) §16 after §15 + temp file PHASE2C_10_STEP2_S16_WORKING_DRAFT.md rm'd in single atomic commit per §0.4 discipline correction; full 4-subsection codification per §3.1 — Principle / Trigger context / Application checklist / Failure-mode signal; ~178 lines §16 prose body within ~150-175 envelope per §13/§14/§15 precedent. Activity D adjudication across four reviewer registers cumulative: 15 §16-prose-body patches (7 advisor/ChatGPT prior session + 4 Codex C1-C4 + 4 full-file-pass α/β/γ/δ) + 4 scaffolding-housekeeping fixes (ε/ζ/η/θ) at temp-file scaffolding across two scaffolding passes. Iterative-pattern empirical record at §16 seal cycle (informs §17 codification): 3 patch-verify cycles surfaced residuals at scaffolding register (α/β/γ/δ → ε/ζ/η → θ); §16 prose body clean-on-pass after Cycle 1 (no §16 prose body patches in cycles 2-3); fresh-register full-file pass at next-session entry confirmed clean before Charlie-register seal authorization. §17 codification observation logged at §16 seal cycle: scaffolding-vs-shipping-register distinction — full-file pass at sealed-commit register applies to shipping content; scaffolding cleanness gates next-session entry, not seal commit content. Anchor-prose-access discipline cumulative instance count = 5 (instances #1-#4 + this seal cycle's fresh-register full-file pass on patched temp file at advisor register before seal authorization). Procedural-confirmation defect class instance count remains at 5 cumulative from Step 1 (no new instance generated this seal cycle — discipline operating during authoring, not pre-authoring procedural recursion))
  - PHASE2C_10 Step 2 §17 sealed (procedural-confirmation defect class at first-commit-before-prose-access; sealed at commit `3eff56a`; +213 lines appended to [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md) §17 after §16 + temp file PHASE2C_10_STEP2_S17_WORKING_DRAFT.md rm'd in single atomic commit per §0.4 discipline correction; full 4-subsection codification per §3.2 — Principle / Trigger context / Application checklist / Failure-mode signal; ~210 lines §17 prose body (above §13-§16 ~145-178 precedent envelope; expansion is substantive content per σ-C scope discipline — 5 instances + iterative-pattern sub-rule with empirical anchors + chained §16 dependency + orthogonal §6 cross-reference, not bloat). Activity F adjudication across 3 reviewer registers + 1 fresh-register full-file pass cumulative: 11 patches (4 σ-C advisor scope refinement + 3 ChatGPT P1/P2/P3 structural-overlay + 3 Codex C1/C2/C3 adversarial + 1 advisor φ-A fresh-register cross-section consistency catch). σ-C scope adjudication at advisor register (load-bearing structural concern surfaced; Charlie-register adjudicated σ-C: §17 single-discipline section — procedural-confirmation primary; iterative-pattern at Application sub-rule item 4 only; scaffolding-vs-shipping at Failure-mode signal entry only); 4 σ-C refinement patches applied (Principle ¶5/¶6 removed; Trigger context restructured to distinguish primary 1-3 from sub-rule support 4-5; Application item 4 sub-rule register clarification + item 5 removed). Recursive empirical validation: §17's own Application sub-rule 4 (full-file prose-access pass at sealed-commit register; section-targeted patches do not preclude need for full-file final pass) operationalized at §17's own seal cycle — fresh-register full-file pass surfaced Concern φ (Trigger context Sentence 4 redundancy post-C1 patch); φ-A patch resolved residual; 5-step rerun returned CLEAN; seal authorized post-rerun-clean. §16 cumulative instance count empirical anchor at §17 cycle: 3 candidate instances logged in §17 seal commit (#6 σ-scope catch on §17 by advisor prose-pass; #7 ChatGPT P2 register-precision catch at canonical-vs-plan-internal reference; #8 φ-A cross-section consistency catch by §17 sub-rule 4 itself); §16 instance count update at successor §16-relevant register or carry-forward. Procedural-confirmation defect class established at strong-tier basis: 3 primary instances (PHASE2C_9 Step 5 §7 working-draft + PHASE2C_9 Step 6 closeout assembly + PHASE2C_10 plan d0222c7); iterative-pattern sub-rule supported at 2 additional instances (d2b6166→1f9a015 post-seal full-file pass + 1f9a015→d2a53fa pre-fire audit). §17 iterative-pattern operating-rule register adoption resolved at Application checklist sub-rule item 4 within §17 per plan §3.2)
  - PHASE2C_10 Step 2 §18 sealed (§7 carry-forward density at interpretive arc closeouts; sealed at commit `2ac95ab`; +161 lines appended to [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md) §18 after §17 + temp file PHASE2C_10_STEP2_S18_WORKING_DRAFT.md rm'd in single atomic commit per §0.4 discipline correction (temp file lived in working tree only per framing β; no commit history); full 4-subsection codification per §3.4 — Principle / Trigger context / Application checklist / Failure-mode signal; 158 lines §18 prose body within Medium-tier envelope. Tier disposition: Medium with operating-rule-pending status note per §3.4 + dual-reviewer convergence + Charlie-register adjudication; 4/4 PHASE2C interpretive arc closeout saturation gives observation pattern broad empirical basis at observation-framing register (not operating-rule-codification per §13/§14/§15 precedent); Strong-tier promotion contingent on operating-rule articulation crystallizing into concrete prescriptive rule. Activity H adjudication across 3 reviewer registers + 1 fresh-register full-file pass cumulative: 8 patches (3 ChatGPT P1/P2/P3 structural-overlay: ¶6 tier-authority revision absorbing Adv-1 + operating-rule candidate softening + Application checklist Medium-tier caveat at intro; 4 Codex C1/C2/C3/C4 adversarial: §11 cross-reference precision against actual METHODOLOGY_NOTES §11 scope lines 942-960 + "broad empirical basis" tier-register hygiene + §10 by-analogy framing against actual §10 scope lines 777-793 + 851-895 + heterogeneous register-load summary fix at line-145 narrowly escalating Adv-2; 1 advisor α-Refine: Principle ¶5 closing-clause restatement removed for canonical-artifact prose-precision alignment with §13-§17 precedent). O1 (Application checklist imperative headings) + O2 (Failure-mode strong-register phrasing) closed as observation-only no-patch per dual-reviewer convergence (P3 caveat substantively mitigates O1 tier-creep; "Watch for" diagnostic framing structurally accommodates O2 strong-register phrasing). Recursive empirical validation: §17's Application sub-rule 4 (full-file prose-access pass at sealed-commit register; section-targeted patches do not preclude need for full-file final pass) operationalized at §18's own seal cycle — fresh-register full-file pass surfaced α-Refine residual; patch applied + re-verified CLEAN before Charlie-register seal authorization (recursive empirical validation continuing across §16/§17/§18 seal cycles). §16 cumulative instance count empirical anchor at §18 cycle: 2 candidate instances logged in §18 seal commit (#9 Codex C1 §11 cross-reference precision catch by independent file-citation against canonical METHODOLOGY_NOTES §11 wording — defect that prose-overlay + structural-overlay registers couldn't catch by construction; #10 Codex C3 §10 cross-reference precision catch by independent file-citation against canonical METHODOLOGY_NOTES §10 wording — same defect class as #9); §16 instance count update deferred to next §16-relevant register or successor cycle; cumulative §16 candidate instances now 5 across §17 + §18 cycles (#6/#7/#8 from §17 + #9/#10 from §18). Three-reviewer Activity H structure (advisor prose-pass + ChatGPT structural-overlay + Codex adversarial) demonstrated distinct catch-class coverage at §18 cycle: each reviewer register surfaced defects the others missed by construction; Codex's cross-reference precision catches (C1 §11 + C3 §10) by independent file-citation against canonical METHODOLOGY_NOTES wording empirically validated three-reviewer coverage at adversarial register; calibration shift advisor α-Hold → α-Refine across turns honestly logged at advisor register; pattern-coherence with §16/§17 cycle precedents preserved)
  - PHASE2C_10 SEALED at Step 2 closeout cycle (sign-off MD at [`docs/closeout/PHASE2C_10_METHODOLOGY_CONSOLIDATION_SIGNOFF.md`](docs/closeout/PHASE2C_10_METHODOLOGY_CONSOLIDATION_SIGNOFF.md) sealed at commit `ff3e4ca`; Phase Marker advance to PHASE2C_10 SEALED at this commit with tag `phase2c-10-methodology-consolidation-v1`. Sign-off MD at 113 lines, 12 sections covering anchors + outcomes + deferred items + next path per ChatGPT scope direction; factual sign-off artifact at summary-of-cycle-record register. Activity M final-assembly verification per §11 closeout-assembly checklist CLEAN: METHODOLOGY_NOTES.md §16-§19 + §16 ### Failure-mode signal §3.5/§3.6 fold-ins coherent at canonical artifact register; CLAUDE.md Phase Marker progression coherent across §16→§17→§18→§19→§3.5/§3.6 advances; 5 seal commits + 5 Phase Marker advance commits landed correctly. Activity N reviewer cycle: 6 patches applied to sign-off MD across light pass + verification register cumulative — 4 advisor + ChatGPT direct patches (Status SEALED → Pending PHASE2C_10 SEALED marker + tag; Date sealed → Closeout date; Tag → Tag pending fire; Anchor commit framing → Closeout-cycle entry anchor framing) + 1 advisor δ-Refine push timing wording cumulative-count arithmetic clarification + 1 advisor β-Refine factually corrected Forward-pointer section (β verification at canonical artifact register surfaced factual attribution error: paths (e)/(f)/(g) were incorrectly attributed to PHASE2C_9 §8.2 register when canonical artifact contains only (a)/(b)/(c)/(d); β-Refine corrected attribution to PHASE2C_10 scoping decision §2 path register; same defect class as §18 C1 + §19 C2/C3 Codex catches but at advisor register since Codex skipped per closeout cycle routing). γ verification at §16 seal commit message (7959140) confirmed §16 patch count claim clean. Activity K disposition K-defer-to-successor per plan §4.3 + §1.4 hard scope (CLAUDE.md project-discipline notes line 459 staleness fix OUT OF SCOPE for PHASE2C_10; carries forward as tracked observation per §3.5/§3.6 fold-in). §16 cumulative instance count update register defer to successor cycle. §16 cumulative instance count empirical anchor at Step 2 closeout cycle: candidate #15 advisor β factual attribution catch via canonical-artifact verification at sign-off MD register; cumulative §16 candidate instances now 10 across §17 + §18 + §19 + §3.5/§3.6 + closeout sign-off cycles. Methodology consolidation arc concludes at PHASE2C_10 SEALED; successor scoping cycle adjudicates Phase 2 continuation/conclusion paths from PHASE2C_10 scoping decision §2 path register; PHASE2_CLOSEOUT_SIGNOFF.md fires at Phase 2 conclusion register (path (f) Phase 3 trajectory authorization specifically; paths (a)/(b)/(c)/(g) are Phase 2 continuation paths and do NOT trigger this sign-off).)
  - PHASE2C_10 Step 2 §3.5/§3.6 weak-tier candidates folded into §16 ### Failure-mode signal (sealed at commit `8ed1b34`; +16 lines appended to [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md) §16 ### Failure-mode signal as 2 short watch-for paragraphs; (A) fold-both-into-§16 disposition over (B) brief §20 + (C) hybrid per ChatGPT execution plan + Claude Code adjudication; placement-vs-cross-reference register distinction operationalized cleanly per §5.4 guardrail 4 §16+ append-only constraint — entries land at §16 host slot citing §11 (§3.5 pre-fire audit pattern at session-close of prior session for sub-spec/scoping deliverables; 1 PHASE2C_9 instance) + §14 (§3.6 self-first-then-reviewer adjudication discipline at reviewer-engagement register; 1 PHASE2C_10 scoping cycle instance) as parent disciplines via cross-reference register. Both candidates Weak tier observation-only at this register (no operating-rule codification beyond existing parent-discipline coverage; no cross-cycle accumulation tracking — that's §19's distinct register slot). Direct in-place append (no temp working draft per ChatGPT execution plan for Weak-tier 1-instance lighter operational footprint); ChatGPT structural light pass cleared; advisor light pass surfaced α-Refine concern at "at the [register]" attachment ambiguity (natural English parse modifies preceding noun phrase = §11/§14 scope claim); Codex skipped per plan §5.3 lighter-touch on weak-tier; Charlie-register adjudicated divergence with Claude Code's reasoning aligning with advisor; α-Refine applied at register-class consistency — parenthetical register anchoring at subject + "as parent discipline" framing for §11/§14 unambiguous attribution; ChatGPT 4-step verification checklist passed (register attaches to pattern not §11/§14; "as parent discipline" present; observation-only status preserved). 5 cumulative patches across 3 reviewer registers + advisor calibration cycle (1 ChatGPT execution plan compression direction + 2 advisor wording refinements §3.5 + §3.6 + 2 ChatGPT verification checklist). §16 instance count empirical anchor candidate from §3.5/§3.6 cycle: #14 advisor prose-pass register caught grammatical-ambiguity register overextension that ChatGPT structural-overlay register cleared by construction (parallel to §18 C1 + §19 C2/C3 cross-reference precision catch class but at advisor register since Codex skipped); empirical observation at advisor register: catch class isn't reviewer-register-locked but pass-focus-locked — advisor + ChatGPT can operate canonical-scope-verification at advisor register when explicitly directed at pass focus per §11/§14 cross-reference framing focus discipline; cumulative §16 candidate instances now 9 across §17 + §18 + §19 + §3.5/§3.6 cycles (#6/#7/#8 + #9/#10 + #11/#12/#13 + #14). Single-commit seal per §0.4 (METHODOLOGY_NOTES.md edit only; no temp file rm needed since direct-in-place-append pattern); §17 sub-rule 4 register-class adapted to direct-in-place pattern (no fresh-register full-file pass needed since no temp working draft + plan-recommended starting baseline + bounded surgical refinement scope). Step 2 weak-tier observation entries §3.5 + §3.6 complete; Step 2 closeout cycle next per §5.2 sealed sequence.)
  - PHASE2C_10 Step 2 §19 sealed (Spec-vs-empirical-reality finding pattern at Weak tier observation-only + cross-cycle-pending; sealed at commit `0c6831f`; +46 lines appended to [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md) §19 after §18 + temp file PHASE2C_10_STEP2_S19_WORKING_DRAFT.md rm'd in single atomic commit per §0.4 discipline correction (temp file lived in working tree only per framing β; no commit history); option (b) single-section structural register at compressed 43 lines within Weak-tier brevity envelope per ChatGPT guardrail 2 ("weak candidates visibly weak"); brevity register itself signals tier-class at canonical artifact distinct from §13-§18 4-subsection register. Tier disposition: Weak with observation-only + cross-cycle-pending status note per §3.3 + §3.7. Empirical anchor: 6 cumulative instances across 3 PHASE2C cycles (PHASE2C_9 = 3 within-arc instances per §8.4 mandatory-tracked-fix register entry #2; PHASE2C_10 scoping = 2 within-cycle instances at entry 4 §1-§7 stale anchor + entry 5 §16-as-single-section framing; PHASE2C_10 plan drafting = 1 within-cycle instance at entry 6 CLAUDE.md project-discipline notes staleness). Activity I (working draft authoring at option (a) 4-subsection ~100 lines) + Activity J adjudication across 3 reviewer registers + 1 fresh-register full-file pass cumulative: 5 patches (1 ChatGPT 4-paragraph compression direction Observation+scope/Evidence/Relationship to §1/§15/Status with Adv-1 α drop "at this Step 2 fire" time-bound reference removed during compression + structural option (b) single-section locked over option (a) 4-subsection per dual-reviewer convergence on weak-tier brevity register signaling + length compressed ~100 → 43 lines; 3 Codex C1/C2/C3 cross-reference precision: C1 subject-object register-precision at §1 framing "per-cycle defect mode under §1" replacing "operating mode of §1" + C2 §15 hierarchy over-nesting restructured §19 hierarchy from "sub-class (§19) of §15 of §1" to "cumulative observation layer under §1 with §15 covering advisor-anchor subset" + C3 §15 operating-rule overclaim distinguished §15 specific scope from §1 broader scope; 1 advisor α C4-ripple ¶4 sentence 3 "discipline distinct from §15" → "discipline distinct from existing §1 / §15 coverage" parallel to C3 framing). β concern (¶2 time-bound "actual file state was §1-§15") closed as observation-only no-patch per advisor recommendation (past-tense framing carries time-binding adequately). Codex routing per ChatGPT post-compression sequencing direction: bounded scope on §1/§15 cross-reference precision + weak-tier register discipline (parallel to §18 C1/C3 catch-class but at narrower targeted scope per plan §5.3 lighter-touch on weak-tier); ChatGPT-directed Codex-after-compression sequencing operationally validated. Recursive empirical validation: §17's Application sub-rule 4 operationalized at §19's own seal cycle (fourth consecutive cycle §16/§17/§18/§19 — fresh-register full-file pass surfaced α C4-ripple residual at ¶4 sentence 3; patch applied + re-verified CLEAN before Charlie-register seal authorization). Codex C2 resolved plan §3.3 line 176 operating-rule articulation question at register-precise level: §19 NOT fully sub-class of §15 — §19 stands at cumulative observation layer under §1's general empirical-verification surface, with §15 covering advisor-anchor subset at receiving-cycle catch; §19's pattern (authors/reviewers citing remembered structure broadly) is broader than §15's specific scope (advisor-supplied numerical/structural anchors at receiving-cycle pre-drafting). §16 cumulative instance count empirical anchor at §19 cycle: 3 candidate instances logged in §19 seal commit (#11 Codex C2 §15 hierarchy precision catch by independent file-citation against canonical METHODOLOGY_NOTES §1 + §15 actual scopes — defect that prose-overlay + structural-overlay registers couldn't catch by construction; #12 Codex C3 §15 operating-rule overclaim catch — same defect class as #11 follows from C2; #13 advisor α C4-ripple §17 sub-rule 4 catch at fresh register — section-targeted C2/C3 patches cleared targeted sites; ¶4 sentence 3 parallel framing residual surfaced at fresh-register prose-pass parallel to §17 cycle's φ-A pattern + §18 cycle's α-Refine pattern); cumulative §16 candidate instances now 8 across §17 + §18 + §19 cycles (#6/#7/#8 from §17 + #9/#10 from §18 + #11/#12/#13 from §19). Three-reviewer Activity J structure (advisor + ChatGPT + Codex) demonstrated distinct catch-class coverage at §19 cycle: ChatGPT structural-overlay caught compression direction + Codex-after-compression routing call at structural-overlay register; Codex adversarial caught 3 cross-reference precision defects (C1+C2+C3) by independent canonical-artifact file-citation; advisor fresh-register caught §17 sub-rule 4 ripple (α C4-ripple) at cross-section consistency register; third consecutive cycle (§16/§18/§19) demonstrating Codex's adversarial-register strength at cross-reference precision)
  - PHASE2C_11.0 Scoping Decision (Path (b) — statistical-significance machinery (Q-9.A register) selected over Paths (a) structured-re-examination / (c) calibration-variation / (f) Phase 3 trajectory / (g) breadth expansion per §3 path-by-path evaluation against §1.2 dominant uncertainty finding (signal-vs-artifact existence at observed candidates); arc designation per anti-pre-naming option (ii) committed at §4.4: PHASE2C_11 = statistical-significance machinery arc (DSR machinery as MVD; PBO at sub-spec drafting cycle if scope permits; CPCV deferred to post-PHASE2C_11); scoping document at [`docs/phase2c/PHASE2C_11_SCOPING_DECISION.md`](docs/phase2c/PHASE2C_11_SCOPING_DECISION.md) committed at `3cfa357` (637 lines / 8 sections); 8 patches + φ-Refine residual applied across reviewer-cycle adjudication (P1-P5 + γ-Refine + φ-Refine); 3 reviewer registers operated — ChatGPT first-pass + Claude advisor fresh-register pass + Codex skipped per scoping-doc register precedent + feedback-codex-review-scope memory; established the sub-spec pre-registration paragraph framework (P3 patch at §4.2 — anti-p-hacking guardrail at selection register: sub-spec must define candidate population + trial count + return metric + Sharpe estimation method + null/deflation assumptions before any result is computed) that PHASE2C_11 sub-spec drafting cycle operated within; Phase Marker advance to PHASE2C_11 scoping cycle SEALED at `4c7d9d2`)
  - PHASE2C_11 Sub-spec drafting cycle (simplified DSR-style multiple-testing Sharpe deflation screen MVD scope; v1 → v2 → v3 substantive-rewrite chain across single sub-spec authoring cycle (no v1+v2 commits; in-session-only working drafts) per four canonical-artifact-register verification-gap discoveries documented at v3 §0.5 — Instance 1 v1 canonical-artifact register (per-trade returns availability mis-spec) / Instance 2 v2 canonical-project-convention register (Section RS consumer-side helper requirement absent → RS-1/RS-2/RS-3 patches) / Instance 3 advisor β-Refine recommendation at structural-precision register (β probe surfaced Instance 4) / Instance 4 v2 canonical-artifact-mapping register LOAD-BEARING (audit_v1 hash scheme = 16-char DSL canonical hash ≠ experiments.db 64-char SHA256 + audit_v1 produces no per-trade CSVs → v3 simplified MVD rewrite); plan at [`docs/phase2c/PHASE2C_11_PLAN.md`](docs/phase2c/PHASE2C_11_PLAN.md) v3 sealed at `c5b740c` + Phase Marker advance to sub-spec drafting cycle SEALED at `151eb9e` + v3 → v3.1 patches at `c021c60` (Step 1 §19 Instance 5/6/7 patches per Charlie-authorized P2+precedent-doc path; admissible under METHODOLOGY_NOTES §20) + v3.1 → v3.2 §5.4 §19 Instance patch at `8bc12de` (724 lines / 9 sections final); RS-1/RS-2/RS-3 patches operationalize Section RS corrected-engine consumer-side discipline per [TECHNIQUE_BACKLOG.md §2.2.3](strategies/TECHNIQUE_BACKLOG.md) + CLAUDE.md "Hard rule for any future WF-consuming work"; canonical Bailey-López de Prado DSR explicitly deferred to post-PHASE2C_11 prerequisite work register per §4.1 + §4.7; 3 reviewer registers operated — Claude Code authoring + ChatGPT first-pass + Claude advisor full-prose-access pass; Codex skipped at sub-spec drafting register per scoping-doc precedent + feedback-codex-review-scope memory; §17 sub-rule 4 recursive empirical validation operating at 7th consecutive cycle)
  - PHASE2C_11 Step 1 — Artifact inventory + RS guard verification + α-Refine deliverable extension (deliverable v2 sealed at `61c17dc` — artifact inventory CLEAN at 5 pillars; §20 v2 codification + sub-spec v3.1 patches recorded at register-precision audit trail; METHODOLOGY_NOTES §20 codified at `33731ce` documenting pre-result lockpoint mis-specification exception path under arc-level anti-p-hacking guardrails; surfaced 3 §19 instances at implementation arc per v3.1 `c021c60` (§3.6 cited approximation 3.07 → 3.2522; §4.4(5) cross-validation framing; §7.1 multiplicity profile correction) → routed via P2+precedent-doc path; §20 v2 Trigger 1 status CLOSED at `f82d040` preserved across remainder of arc per closeout MD §11 reaffirmation)
  - PHASE2C_11 Step 2 — Input loader + deliverable (Step 2 input loader + tests with post-Codex patches at `dfd9c72` (v3.1 lockpoints preserved) + Step 2 deliverable v3 sealed at `6f116a8` — input statistics artifact (schema v2); cross-validation per §3.4 + §4.4(5); eligible subset N=154 documented; excluded candidate list complete; RS-2 guard calls operational; reviewer authorization at sealed register per §7.2 Step 2 gating)
  - PHASE2C_11 Step 3 — Simplified DSR-style screen calculation (Step 3 schema sealed at `dbcf19d` — SimplifiedDSRResult + PerCandidateDisposition + SensitivityRow frozen dataclasses; TDD-RED state at `73221c6` — 13 test classes / 45 tests authored; synthetic-paths hotfix at `d7aa3ae` + fixture math hotfix-2 at `a91e71a` (F1 var=1.0 exact + F2 ramp scale to make h042 argmax); Step 3 implementation at `f82d040` — `compute_simplified_dsr()` canonical formula register at [`backtest/evaluate_dsr.py:956-1278`](backtest/evaluate_dsr.py) + 3 frozen dataclasses + RS-3 attestation guard + Class #1 regression; Codex first-fire Commit 1 at `f2e4087` (6/8 findings #1, #4, #5, #6, #7, #8 cleared at implementation + test-infrastructure register) + Hotfix-3 at `1b8132e` (remaining 2/8 findings: #2 §4.4(1) enforcement at `compute_simplified_dsr()` API entry + #3 unreachability docstring formalized); Step 3 deliverable v3 sealed at `6315ec0` — simplified DSR-style screen numerical/tables register at (α) strict canonical scope; Step 3 deliverable amend at `7e41058` — Codex first-fire deferred review closure on sha256 + p_value field reference at 4 sites (closes §9 reviewer-trail anchor); result artifact at `data/phase2c_evaluation_gate/audit_v1/_step3_result.json` sha256=`64f733833e3cc47d32fc3a81c3f603c54c478c2064b455d161dbab6dff82ae03` (83,347 bytes); 8/8 Codex first-fire findings cleared at register-precision; deliverable at [`docs/phase2c/PHASE2C_11_STEP3_DELIVERABLE.md`](docs/phase2c/PHASE2C_11_STEP3_DELIVERABLE.md) 290 lines)
  - PHASE2C_11 Step 4 — Closeout MD seal (closeout deliverable at [`docs/closeout/PHASE2C_11_RESULTS.md`](docs/closeout/PHASE2C_11_RESULTS.md) sealed at `5dba0df` — 408 lines / 15 sections; artifact-evidence narrative + §6.7 canonical phrasing verbatim + §0.4 §19 §5.4 routing adjudication record + §6.4 successor-cycle paths register-class-eligible + §7.1 + §7.2 + §7.3 forbidden-phrase compliance audit (6 toward-signal overclaiming + 5 away-from-signal underclaiming phrases verified ABSENT outside §7 audit table) + §9 lockpoint compliance audit (0 mutations across 8 categories) + §11 §20 v2 Trigger 1 reaffirmation (still CLOSED at `f82d040`) + §14 carry-forwards to Step 5 SEAL; triple-reviewer convergence (Codex substantive + ChatGPT structural + advisor prose-access) cross-validated 5/5 substantive findings at narrative register; per-finding adjudication discipline applied; primary register disposition = artifact_evidence at simplified-formulation register-class per §3.6 conservative AND-gate; bounded scope per §6.4 + §6.2 away-from-signal underclaiming guardrail)
  - PHASE2C_11 SEALED at Step 5 SEAL cycle (Phase Marker advance commit at this commit + tag `phase2c-11-statistical-significance-v1` at `5dba0df` — closeout seal commit per §7.4 + Path A.2 register-event boundary discipline (tag points to deliverable seal, NOT Phase Marker housekeeping commit). PHASE2C_11 statistical-significance machinery arc concludes at primary register disposition = artifact_evidence at simplified-formulation register-class; canonical Bailey-López de Prado DSR confirmation deferred to post-PHASE2C_11 prerequisite work per §4.7. Successor cycle path NOT pre-committed at this register per anti-pre-naming option (ii); successor scoping cycle is the next arc after PHASE2C_11 SEAL at fresh session post-Step-5 per pacing discipline. Register-class-eligible successor paths per closeout MD §6.4: (a) mechanism deeper investigation / (c) calibration variation / (g) breadth expansion / (canonical-DSR scope expansion); (f) Phase 3 trajectory foreclosed for current survivor set unless later cycle identifies different validated candidate basis. Carry-forwards to methodology consolidation cycle (deferred to separate fresh session post-Step-5): cumulative §19 instance count = 10 within-arc (4 sub-spec drafting + 3 Step 1 implementation + 3 Step 4 closeout) + advisor §5 prediction-error discipline = 6 cumulative + 4 codification candidates (S8 bounded-scope-repetition rejection / §6.2 forbidden-phrase literal-substring + §7 audit-table exemption / Path A.2 register-event boundary / triple-reviewer convergence at substantive register). Triple-reviewer convergence (Charlie + ChatGPT + advisor) at Step 5 SEAL register: (A) consolidation per PHASE2C_10 precedent + commit + annotated tag at `5dba0df` + (P1) bundled push + §19 empirical verify (§0.5 verbatim confirmed 4 instances))
- **Active blueprint:** `blueprint/PHASE2_BLUEPRINT.md` (v2; **freshness note** added at top per Option B at commit `3a554fb` documenting D9 + post-D9 evaluation framework supersession by PHASE2C_3-9 arc series; CLAUDE.md Phase Marker remains operational source of truth)
- **Current batch_id:** `b6fcbf86-4d57-4d1f-ae41-1778296b1ae9` (Phase 2C Phase 1 walk-forward; corrected-engine re-run in `_corrected/` directory is canonical; same batch consumed by PHASE2C_6 single-regime evaluation runs at `data/phase2c_evaluation_gate/{smoke,primary,audit}_v1/`, by PHASE2C_7.1 multi-regime evaluation runs at `data/phase2c_evaluation_gate/{audit_2024_v1, audit_2024_v1_filtered, comparison_2022_vs_2024_v1}/`, and by PHASE2C_8.1 extended multi-regime evaluation runs at `data/phase2c_evaluation_gate/{eval_2020_v1, eval_2020_v1_filtered, eval_2021_v1, eval_2021_v1_filtered, audit_v1_filtered, comparison_2022_2024_2020_2021_v1}/`)
- **Current UTC-month spend (April 2026):** ~$8.65 (D7 Stage 2d $5.89 + Phase 2C Batch-1 $2.30 + smoke and dry-run batches; per `agents/spend_ledger.db` `ledger` table, queried 2026-04-26; PHASE2C_6 + PHASE2C_7.1 + PHASE2C_8.1 evaluation work incurred no API spend — local backtest evaluation only)
- **Hard rule for any future WF-consuming work:** must consume corrected artifacts only and call `backtest.wf_lineage.check_wf_semantics_or_raise()` before computing derived metrics from walk-forward summaries. For single-run holdout artifacts (PHASE2C_6 attestation domain `single_run_holdout_v1`), use the companion guard `backtest.wf_lineage.check_evaluation_semantics_or_raise()`. See [`docs/decisions/WF_TEST_BOUNDARY_SEMANTICS.md`](docs/decisions/WF_TEST_BOUNDARY_SEMANTICS.md) Section RS for the corrected-engine consumption discipline that governs both attestation domains.

## Project-discipline notes

Standing project-discipline principles (apply across all work cycles, not bound to a phase) are codified at [`docs/discipline/METHODOLOGY_NOTES.md`](docs/discipline/METHODOLOGY_NOTES.md). Seven principles currently in force: §1 empirical verification for factual claims, §2 meta-claim verification discipline, §3 regime-aware calibration bands, §4 scale-step discipline for empirical evaluations, §5 precondition verification for structural and organizational principles, §6 commit messages are not canonical result layers, §7 asymmetric confidence reporting on multi-sample claims. §8 is the synthesis "How to apply these principles" section. The §4-§7 additions were codified during the PHASE2C_6 evaluation gate arc (commit `536f737`). Future cycles append new lessons as additional sections.
