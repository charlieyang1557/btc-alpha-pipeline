# execution_phase4_15bps.yaml — Phase 4 realistic base cost
# Per PHASE4_PLAN §1.4 (sealed at 432b2bd): 10bps Binance VIP 0 spot
# taker + 5bps slippage = 15bps per side. Round-trip: 30bps.
#
# *** THIS CONFIG IS THE BASIS FOR PHASE 4 §1.5 SUCCESS CRITERION. ***
#
# Sensitivity bounds at 13bps (-2 slip) and 17bps (+2 slip) are
# auxiliary descriptive; success/failure of Phase 4's persistence test
# is evaluated at this 15bps basis only.
# Sealed at: Phase 4 implementation arc Task 3 (commit pending)
# Last reviewed: 2026-05-09

execution:
  signal_timing: "bar_close"
  fill_timing: "next_bar_open"
  stop_limit_intrabar: "adverse_first"

cost_model:
  name: "phase4_realistic_base_15bps"
  maker_fee_bps: 2.0
  taker_fee_bps: 10.0
  default_fee_bps: 10.0
  slippage_bps: 5.0

zero_volume:
  treatment: "flag_only"

position:
  max_position_pct: 1.0
  default_position_pct: 1.0
  max_leverage: 1.0

timezone:
  canonical: "UTC"

timeframe:
  primary: "1h"
